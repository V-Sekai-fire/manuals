<?xml version="1.0" encoding="UTF-8"?>
<rss  xmlns:atom="http://www.w3.org/2005/Atom" 
      xmlns:media="http://search.yahoo.com/mrss/" 
      xmlns:content="http://purl.org/rss/1.0/modules/content/" 
      xmlns:dc="http://purl.org/dc/elements/1.1/" 
      version="2.0">
<channel>
<title>V-Sekai Fire — Manuals</title>
<link>https://v-sekai-multiplayer-fabric.github.io/manuals/decisions.html</link>
<atom:link href="https://v-sekai-multiplayer-fabric.github.io/manuals/decisions.xml" rel="self" type="application/rss+xml"/>
<description></description>
<generator>quarto-1.10.18</generator>
<lastBuildDate>Thu, 06 Aug 2026 00:00:00 GMT</lastBuildDate>
<item>
  <title>Actor-lite worker pool architecture for zone-server-h2o</title>
  <link>https://v-sekai-multiplayer-fabric.github.io/manuals/decisions/20260806-actor-lite-worker-pool.html</link>
  <description><![CDATA[ 





<p><em><a href="../index.html">Manuals</a> › <a href="../decisions.html">Decisions</a> › Actor-lite worker pool architecture for zone-server-h2o</em></p>
<p><strong>Tier:</strong> proof of concept · <strong>Status:</strong> accepted</p>
<blockquote class="blockquote">
<p>Ported from <a href="https://github.com/weftspun/h2o-bench-tpcc"><code>weftspun/h2o-bench-tpcc</code></a>’s <code>rfd/0005-actor-lite-architecture.md</code> as part of the <a href="https://github.com/v-sekai-multiplayer-fabric/zone-server-h2o">zone-server-h2o</a> consolidation — see that repo’s README for current status. Content below is the original RFD, unmodified except for this header and stripping the old State line.</p>
</blockquote>
<p><strong>Target:</strong> top 10 TechEmpower R23 data update test</p>
<section id="r23-data-update-test-top-10" class="level2">
<h2 class="anchored" data-anchor-id="r23-data-update-test-top-10">R23 data update test — top 10</h2>
<table class="caption-top table">
<thead>
<tr class="header">
<th>Rank</th>
<th>Framework</th>
<th>RPS</th>
<th>Lang</th>
<th>ORM</th>
</tr>
</thead>
<tbody>
<tr class="odd">
<td>1</td>
<td>may-minihttp</td>
<td>1,327,378</td>
<td>Rust</td>
<td>Raw</td>
</tr>
<tr class="even">
<td><strong>2</strong></td>
<td><strong>h2o</strong></td>
<td><strong>1,226,814</strong></td>
<td><strong>C</strong></td>
<td><strong>Raw</strong></td>
</tr>
<tr class="odd">
<td>3</td>
<td>ntex [sailfish]</td>
<td>1,210,348</td>
<td>Rust</td>
<td>Raw</td>
</tr>
<tr class="even">
<td>4</td>
<td>ntex [async-std,db]</td>
<td>1,197,351</td>
<td>Rust</td>
<td>Raw</td>
</tr>
<tr class="odd">
<td>5</td>
<td>xitca-web</td>
<td>1,146,712</td>
<td>Rust</td>
<td>Raw</td>
</tr>
<tr class="even">
<td>6</td>
<td>xitca-web [orm]</td>
<td>1,115,124</td>
<td>Rust</td>
<td>Full</td>
</tr>
<tr class="odd">
<td>7</td>
<td>axum [postgresql]</td>
<td>1,114,265</td>
<td>Rust</td>
<td>Raw</td>
</tr>
<tr class="even">
<td>8</td>
<td>lithium-postgres</td>
<td>1,073,846</td>
<td>C++</td>
<td>Full</td>
</tr>
<tr class="odd">
<td>9</td>
<td>lithium-postgres-beta</td>
<td>1,068,560</td>
<td>C++</td>
<td>Full</td>
</tr>
<tr class="even">
<td>10</td>
<td>hyper-db</td>
<td>1,066,644</td>
<td>Rust</td>
<td>Raw</td>
</tr>
</tbody>
</table>
<p>h2o is already rank #2 with 1,226,814 RPS. All top 10 use Postgres with raw SQL (no ORM). The h2o-bench-tpcc target is to match this performance with TPC-C transactions (heavier per request) on CockroachDB (Postgres wire protocol).</p>
</section>
<section id="architecture" class="level2">
<h2 class="anchored" data-anchor-id="architecture">Architecture</h2>
<pre><code>HTTP client (wrk)
    │
    ▼
H2O network thread (event loop, HTTP/3)
    │  spsc_ring_push (lock-free, no mutex)
    ├──► Worker 0 (SPSC ring → libpq pipeline → h2o_multithread_send)
    ├──► Worker 1
    └──► Worker N
    │
    │  h2o_multithread_send (return path)
    ▼
H2O network thread (send HTTP response)
    │
    ▼
FoundationDB (libfdb_c, pure C, no JVM)</code></pre>
</section>
<section id="three-components" class="level2">
<h2 class="anchored" data-anchor-id="three-components">Three components</h2>
<section id="spsc-lock-free-ring-buffer-spsc_ring.c" class="level3">
<h3 class="anchored" data-anchor-id="spsc-lock-free-ring-buffer-spsc_ring.c">1. SPSC lock-free ring buffer (<code>spsc_ring.c</code>)</h3>
<p>Single-producer/single-consumer ring with atomic load/store. Power-of-two capacity (mask-based indexing). No CAS, no mutexes.</p>
<ul>
<li>Producer (H2O thread): <code>atomic_store_release(head)</code></li>
<li>Consumer (worker thread): <code>atomic_store_release(tail)</code></li>
<li>Invariant: <code>tail &lt;= head &lt;= tail + capacity</code></li>
</ul>
<p>Verified by CBMC (<code>test/cbmc/spsc_harness.c</code>) and Lean 4 (<code>test/verification/TpccVerification/Spsc.lean</code>).</p>
</section>
<section id="worker-threads-worker_pool.c" class="level3">
<h3 class="anchored" data-anchor-id="worker-threads-worker_pool.c">2. Worker threads (<code>worker_pool.c</code>)</h3>
<p>Each worker:</p>
<ul>
<li>Owns one SPSC ring (1024 slots)</li>
<li>Owns one libpq connection (pipeline mode)</li>
<li>Runs a tight loop: pop -&gt; execute -&gt; return</li>
<li>Returns results via <code>h2o_multithread_send</code></li>
</ul>
<p>Dispatch: round-robin across workers (atomic fetch_add).</p>
</section>
<section id="return-path" class="level3">
<h3 class="anchored" data-anchor-id="return-path">3. Return path</h3>
<p><code>h2o_multithread_send()</code> wakes the H2O event loop to send the HTTP response. No shared state on the return path.</p>
</section>
</section>
<section id="verification-layers" class="level2">
<h2 class="anchored" data-anchor-id="verification-layers">Verification layers</h2>
<table class="caption-top table">
<colgroup>
<col style="width: 16%">
<col style="width: 21%">
<col style="width: 61%">
</colgroup>
<thead>
<tr class="header">
<th>Layer</th>
<th>Tool</th>
<th>What it verifies</th>
</tr>
</thead>
<tbody>
<tr class="odd">
<td>C invariants</td>
<td>CBMC</td>
<td>SPSC ring FIFO, bounds, head-tail invariant</td>
</tr>
<tr class="even">
<td>Specification</td>
<td>Lean 4</td>
<td>SPSC linearizability, push/pop preserve bounds</td>
</tr>
<tr class="odd">
<td>TPC-C invariants</td>
<td>plausible-witness-dag</td>
<td>NewOrder atomicity, Delivery correctness, Stock non-negative</td>
</tr>
</tbody>
</table>
</section>
<section id="why-this-beats-a-generalized-actor-framework" class="level2">
<h2 class="anchored" data-anchor-id="why-this-beats-a-generalized-actor-framework">Why this beats a generalized actor framework</h2>
<ul>
<li>No scheduler overhead — each worker is a bare pthread</li>
<li>No lock contention — SPSC rings have zero cross-thread writes on the fast path</li>
<li>FDB async callbacks chain transaction steps without blocking</li>
<li>FDB transactions are native ACID (no SQL BEGIN/COMMIT overhead)</li>
<li>libfdb_c is pure C — no JVM, no JNI, no SQL parser</li>
<li>h2o’s event loop handles HTTP/3 natively (no separate QUIC stack)</li>
<li>Top 10 is exclusively Rust and C/C++ — no managed runtime in the top 10</li>
</ul>


</section>

 ]]></description>
  <guid>https://v-sekai-multiplayer-fabric.github.io/manuals/decisions/20260806-actor-lite-worker-pool.html</guid>
  <pubDate>Thu, 06 Aug 2026 00:00:00 GMT</pubDate>
</item>
<item>
  <title>Async FDB callback chain on the libh2o event loop</title>
  <link>https://v-sekai-multiplayer-fabric.github.io/manuals/decisions/20260806-async-fdb-callback-chain.html</link>
  <description><![CDATA[ 





<p><em><a href="../index.html">Manuals</a> › <a href="../decisions.html">Decisions</a> › Async FDB callback chain on the libh2o event loop</em></p>
<p><strong>Tier:</strong> proof of concept · <strong>Status:</strong> accepted</p>
<blockquote class="blockquote">
<p>Ported from <a href="https://github.com/weftspun/h2o-bench-tpcc"><code>weftspun/h2o-bench-tpcc</code></a>’s <code>rfd/0011-async-fdb-callback-chain.md</code> as part of the <a href="https://github.com/v-sekai-multiplayer-fabric/zone-server-h2o">zone-server-h2o</a> consolidation — see that repo’s README for current status. Content below is the original RFD, unmodified except for this header and stripping the old State line.</p>
</blockquote>
<section id="decision" class="level2">
<h2 class="anchored" data-anchor-id="decision">Decision</h2>
<p>Implement TPC-C transactions as async callback chains over FDB futures. Each transaction step is a function that submits an FDB future and registers a callback for when it resolves. The callback performs the next step.</p>
</section>
<section id="pattern" class="level2">
<h2 class="anchored" data-anchor-id="pattern">Pattern</h2>
<pre><code>create_transaction
  -&gt; get(district)        [async future]
    -&gt; on_district_read   [callback: extract next_o_id]
      -&gt; get(customer)    [async future]
        -&gt; on_customer_read [callback: extract discount]
          -&gt; get(stock[0]) [async future]
            -&gt; on_stock_read [callback: update stock, next item]
              -&gt; ... (loop for 5-15 items)
              -&gt; set(oorder, new_order, order_line[], stock[])
              -&gt; commit [async future]
                -&gt; on_commit [callback: send HTTP response]</code></pre>
</section>
<section id="why-async-not-blocking" class="level2">
<h2 class="anchored" data-anchor-id="why-async-not-blocking">Why async, not blocking</h2>
<p>FDB’s C API is callback-based by design. <code>fdb_future_set_callback()</code> fires when the future resolves. Blocking with <code>fdb_future_block_until_ready()</code> would stall the event loop thread.</p>
<p>In the actor-lite architecture (RFD 0005), the H2O network thread dispatches work to worker threads via SPSC rings. If a worker blocks on FDB, it can’t process the next request from its ring.</p>
</section>
<section id="error-handling-and-retry" class="level2">
<h2 class="anchored" data-anchor-id="error-handling-and-retry">Error handling and retry</h2>
<p>On any FDB error (conflict, timeout, etc.), call <code>fdb_transaction_on_error(tr, err)</code>. This returns a future. When it resolves, the transaction has been reset. The callback re-reads the first key and restarts the chain.</p>
<p>The retry is transparent: the same <code>new_order_ctx_t</code> struct flows through the chain, carrying the transaction parameters. On reset, the read state is cleared and the chain restarts from step 1.</p>
</section>
<section id="memory-management" class="level2">
<h2 class="anchored" data-anchor-id="memory-management">Memory management</h2>
<p>Each transaction allocates a context struct (<code>new_order_ctx_t</code>, <code>payment_ctx_t</code>, etc.) on the heap. The context is freed in the final callback (commit success or unrecoverable error). The FDB transaction handle is destroyed in the same callback.</p>
<p>No reference counting needed: the callback chain is linear, each step has exactly one outstanding future, and the context outlives all callbacks.</p>
</section>
<section id="limitations" class="level2">
<h2 class="anchored" data-anchor-id="limitations">Limitations</h2>
<ul>
<li>No batching across transactions: each HTTP request creates its own FDB transaction. Batching multiple requests into one transaction would improve throughput but violate TPC-C spec (each terminal executes one transaction at a time).</li>
<li>Stack depth is bounded: the callback chain is finite (max 15 stock reads + writes + commit), so no stack overflow risk.</li>
</ul>


</section>

 ]]></description>
  <guid>https://v-sekai-multiplayer-fabric.github.io/manuals/decisions/20260806-async-fdb-callback-chain.html</guid>
  <pubDate>Thu, 06 Aug 2026 00:00:00 GMT</pubDate>
</item>
<item>
  <title>Binary value encoding for FDB values</title>
  <link>https://v-sekai-multiplayer-fabric.github.io/manuals/decisions/20260806-binary-value-encoding-for-fdb.html</link>
  <description><![CDATA[ 





<p><em><a href="../index.html">Manuals</a> › <a href="../decisions.html">Decisions</a> › Binary value encoding for FDB values</em></p>
<p><strong>Tier:</strong> proof of concept · <strong>Status:</strong> accepted</p>
<blockquote class="blockquote">
<p>Ported from <a href="https://github.com/weftspun/h2o-bench-tpcc"><code>weftspun/h2o-bench-tpcc</code></a>’s <code>rfd/0010-binary-value-encoding.md</code> as part of the <a href="https://github.com/v-sekai-multiplayer-fabric/zone-server-h2o">zone-server-h2o</a> consolidation — see that repo’s README for current status. Content below is the original RFD, unmodified except for this header and stripping the old State line.</p>
</blockquote>
<section id="decision" class="level2">
<h2 class="anchored" data-anchor-id="decision">Decision</h2>
<p>Encode TPC-C row values as packed C structs with network byte order (big-endian) integers. No protobuf, no JSON, no FDB tuple layer for values.</p>
</section>
<section id="rationale" class="level2">
<h2 class="anchored" data-anchor-id="rationale">Rationale</h2>
<ol type="1">
<li><p>Zero-copy deserialization: a packed struct can be cast directly from FDB’s <code>FDBKeyValue.value</code> pointer. No parsing step. The callback handler does <code>(stock_val_t *)kv-&gt;value</code> and reads fields.</p></li>
<li><p>Minimal size: no type tags, no field names, no separators. A warehouse row is 109 bytes packed vs ~350 bytes in JSON.</p></li>
<li><p>Deterministic layout: <code>#pragma pack(push, 1)</code> ensures no padding. The same struct compiles identically on the loader, the server, and the verification harness. Cross-platform safe as long as all parties use the same header file.</p></li>
<li><p>Endianness is fixed: network byte order for all integers. x86_64 and ARM both support unaligned access, so the byte-swap cost is negligible.</p></li>
</ol>
</section>
<section id="tradeoffs" class="level2">
<h2 class="anchored" data-anchor-id="tradeoffs">Tradeoffs</h2>
<ul>
<li>Schema evolution: adding a field breaks compatibility. Solution: version the struct (include a <code>uint8_t version</code> field) or use FDB key prefixes to distinguish row formats.</li>
<li>Debugging: binary values are not human-readable in fdbcli. Solution: the loader and verification harness have debug print functions that decode the struct.</li>
<li>Portability: requires matching <code>#pragma pack</code> behavior across compilers. GCC and Clang both support this correctly.</li>
</ul>
</section>
<section id="what-we-did-not-use" class="level2">
<h2 class="anchored" data-anchor-id="what-we-did-not-use">What we did not use</h2>
<ul>
<li>FDB tuple layer for values: the tuple layer is for keys (where lexicographic ordering matters). Values don’t need ordering, so the tuple layer’s overhead is unnecessary.</li>
<li>Protocol Buffers / Cap’n Proto: adds a dependency and codegen step. For fixed-schema TPC-C, a C struct is simpler and faster.</li>
<li>JSON: 3-5x larger, requires a parser, and yajl allocation per row would dominate the hot path.</li>
</ul>


</section>

 ]]></description>
  <guid>https://v-sekai-multiplayer-fabric.github.io/manuals/decisions/20260806-binary-value-encoding-for-fdb.html</guid>
  <pubDate>Thu, 06 Aug 2026 00:00:00 GMT</pubDate>
</item>
<item>
  <title>FoundationDB selection over CockroachDB for zone-server-h2o state</title>
  <link>https://v-sekai-multiplayer-fabric.github.io/manuals/decisions/20260806-fdb-over-cockroachdb-for-zone-state.html</link>
  <description><![CDATA[ 





<p><em><a href="../index.html">Manuals</a> › <a href="../decisions.html">Decisions</a> › FoundationDB selection over CockroachDB for zone-server-h2o state</em></p>
<p><strong>Tier:</strong> proof of concept · <strong>Status:</strong> accepted</p>
<blockquote class="blockquote">
<p>Ported from <a href="https://github.com/weftspun/h2o-bench-tpcc"><code>weftspun/h2o-bench-tpcc</code></a>’s <code>rfd/0006-fdb-selection.md</code> as part of the <a href="https://github.com/v-sekai-multiplayer-fabric/zone-server-h2o">zone-server-h2o</a> consolidation — see that repo’s README for current status. Content below is the original RFD, unmodified except for this header and stripping the old State line.</p>
</blockquote>
<section id="decision" class="level2">
<h2 class="anchored" data-anchor-id="decision">Decision</h2>
<p>Use raw FoundationDB C API (<code>libfdb_c</code>) as the database for h2o-bench-tpcc, not CockroachDB.</p>
</section>
<section id="rationale" class="level2">
<h2 class="anchored" data-anchor-id="rationale">Rationale</h2>
<p><strong>1. Write throughput.</strong> TPC-C is 88% writes (NewOrder + Payment). FDB’s log-structured MVCC separates transaction processing from storage, giving fundamentally lower write latency than CockroachDB’s Raft consensus path. For a write-heavy benchmark, the database is the bottleneck.</p>
<p><strong>2. Active Apple development.</strong> FDB 7.3.79 (July 2026), actively maintained by Apple. CockroachDB’s v-sekai fork is a dead engine with no upstream activity.</p>
<p><strong>3. Pure C client.</strong> <code>libfdb_c</code> is a native C shared library — no JVM, no JNI, no gRPC bridge. The C API is callback-based (<code>fdb_future_set_callback</code>), integrating naturally with h2o’s event loop.</p>
<p><strong>4. Native ACID transactions.</strong> FDB transactions are built in: <code>fdb_database_create_transaction</code> / <code>fdb_transaction_set</code> / <code>fdb_transaction_commit</code>. No <code>BEGIN</code>/<code>COMMIT</code> SQL parsing, no pipeline mode complexity. The transaction object IS the transaction.</p>
<p><strong>5. No SQL layer overhead.</strong> Raw key-value operations eliminate SQL parsing, planning, and optimization. The framework’s throughput is measured against the raw storage engine, not an SQL interpreter.</p>
</section>
<section id="api-surface-fdb-c-api-7.3" class="level2">
<h2 class="anchored" data-anchor-id="api-surface-fdb-c-api-7.3">API surface (FDB C API 7.3)</h2>
<pre><code>fdb_select_api_version_impl(730, 730)
fdb_setup_network()           // start FDB network thread
fdb_create_database(cluster)  // get database handle
fdb_database_create_transaction(db, &amp;tr)  // create transaction
fdb_transaction_get(tr, key, len, snapshot, &amp;future)  // read
fdb_transaction_set(tr, key, klen, val, vlen)  // write
fdb_transaction_commit(tr, &amp;future)  // commit
fdb_transaction_on_error(tr, err, &amp;future)  // retry
fdb_future_set_callback(future, cb, ctx)  // async callback
fdb_future_get_value(future, &amp;out, &amp;len)  // extract result</code></pre>
</section>
<section id="what-we-lose" class="level2">
<h2 class="anchored" data-anchor-id="what-we-lose">What we lose</h2>
<ul>
<li>SQL comparability with other TFB entries (they all use Postgres)</li>
<li>TPC-C’s SQL schema must be re-mapped to FDB key-value subspace design</li>
<li>StockLevel’s COUNT requires a range scan instead of SQL aggregate</li>
</ul>
</section>
<section id="when-to-revisit" class="level2">
<h2 class="anchored" data-anchor-id="when-to-revisit">When to revisit</h2>
<p>If TFB-comparable numbers become the goal, add CockroachDB as a second adapter behind the same h2o handler interface. The FDB adapter stays primary.</p>


</section>

 ]]></description>
  <guid>https://v-sekai-multiplayer-fabric.github.io/manuals/decisions/20260806-fdb-over-cockroachdb-for-zone-state.html</guid>
  <pubDate>Thu, 06 Aug 2026 00:00:00 GMT</pubDate>
</item>
<item>
  <title>Macaroon + eBPF/XDP security fabric</title>
  <link>https://v-sekai-multiplayer-fabric.github.io/manuals/decisions/20260806-macaroon-xdp-security.html</link>
  <description><![CDATA[ 





<p><em><a href="../index.html">Manuals</a> › <a href="../decisions.html">Decisions</a> › Macaroon + eBPF/XDP security fabric</em></p>
<p><strong>Tier:</strong> proof of concept · <strong>Status:</strong> accepted</p>
<blockquote class="blockquote">
<p>Ported from <a href="https://github.com/weftspun/h2o-bench-tpcc"><code>weftspun/h2o-bench-tpcc</code></a>’s <code>rfd/0018-macaroon-xdp-security.md</code> as part of the <a href="https://github.com/v-sekai-multiplayer-fabric/zone-server-h2o">zone-server-h2o</a> consolidation — see that repo’s README for current status. Content below is the original RFD, unmodified except for this header and stripping the old State line.</p>
</blockquote>
<section id="decision" class="level2">
<h2 class="anchored" data-anchor-id="decision">Decision</h2>
<p>Use a two-tier security architecture: Macaroon-based authentication in user space, eBPF/XDP session-key enforcement in kernel space. Validate the cryptographic token once at connection time, then push a 64-bit session key into an eBPF whitelist map for line-rate enforcement on every subsequent packet.</p>
</section>
<section id="rationale" class="level2">
<h2 class="anchored" data-anchor-id="rationale">Rationale</h2>
<p>Directly validating a chained-HMAC Macaroon inside an XDP packet filter is impossible and undesirable:</p>
<ol type="1">
<li><p>The eBPF verifier rejects it. The BPF verifier imposes strict limits on instruction count, loops, and stack size. HMAC-SHA256 with caveat chaining requires hundreds of instructions and variable-length parsing, so the verifier would reject the program.</p></li>
<li><p>Parsing complex tokens at the network interface destroys the sub-10µs latency target from RFD 0002. A single Macaroon validation costs ~50-100µs (HMAC + caveat walk); at 60Hz × 200 entities per zone, that’s 15,360 validations/sec per zone, or 1.5 seconds of CPU per second on crypto alone.</p></li>
<li><p>It is the wrong layer. Macaroons are authorization tokens (who can do what). XDP is a packet filter (which packets are allowed). Mixing authorization logic into packet filtering violates separation of concerns.</p></li>
</ol>
</section>
<section id="architecture" class="level2">
<h2 class="anchored" data-anchor-id="architecture">Architecture</h2>
<pre><code>                    CONTROL PLANE (once per connection)
                    ─────────────────────────────────────

  Player ──connect──► Matchmaker
                        │ authenticate (OAuth, password, etc.)
                        │ issue Macaroon with caveats:
                        │   "zone_id = 45"
                        │   "server_ip = 192.168.1.50"
                        │   "expires &lt; 5min"
                        │ generate 64-bit session_key
                        │
                        ▼
  Player ──hello(Macaroon, session_key)──► User-space orchestrator
                                            (weft-warp-loop zone server)
                        │
                        │ validate Macaroon HMAC signature
                        │ verify caveats (zone_id, server_ip, expiry)
                        │ if valid:
                        │   bpf_map_update_elem(WHITELIST_MAP,
                        │     key=player_ip,
                        │     value={session_key, zone_id, core_id})
                        │
                        ▼
                     WHITELIST_MAP (eBPF)
                    ┌─────────────────────────────┐
                    │ player_ip → session_key      │
                    │             zone_id          │
                    │             core_id          │
                    │             expiry_timestamp │
                    └─────────────────────────────┘

                    DATA PLANE (every packet, 60Hz)
                    ──────────────────────────────

  Player ──UDP(session_key, payload)──► NIC
                                        │
                                        ▼
                                     XDP program
                                        │ lookup src_ip in WHITELIST_MAP
                                        │
                                        ├── valid: route to core_id
                                        │         (zero crypto, ~10ns)
                                        │
                                        └── invalid: DROP
                                                   (DDoS, spoof, expired)</code></pre>
</section>
<section id="three-components" class="level2">
<h2 class="anchored" data-anchor-id="three-components">Three components</h2>
<section id="matchmaker-control-plane-infrequent" class="level3">
<h3 class="anchored" data-anchor-id="matchmaker-control-plane-infrequent">1. Matchmaker (control plane, infrequent)</h3>
<p>The Matchmaker is a central service (or distributed with consensus) that:</p>
<ul>
<li>Authenticates the player (OAuth, password, device attestation)</li>
<li>Determines target zone via load balancing + spatial locality</li>
<li>Issues a Macaroon attenuated with caveats:
<ul>
<li><code>zone_id = N</code> — player is assigned to zone N</li>
<li><code>server_ip = A.B.C.D</code> — player must connect to this server</li>
<li><code>expires &lt; T</code> — token valid until timestamp T</li>
<li><code>rate_limit = R</code> — max packets/sec from this player</li>
</ul></li>
<li>Generates a cryptographically random 64-bit session key</li>
<li>Returns Macaroon + session_key + server address to player</li>
</ul>
<p>The Matchmaker runs in user space with full access to crypto libraries. It is not in the hot path — it runs once per connection (every ~5 minutes for session renewal).</p>
</section>
<section id="user-space-orchestrator-control-plane-once-per-session" class="level3">
<h3 class="anchored" data-anchor-id="user-space-orchestrator-control-plane-once-per-session">2. User-space orchestrator (control plane, once per session)</h3>
<p>The zone server (weft-warp-loop) receives the player’s initial Hello packet containing the Macaroon. In user space:</p>
<ul>
<li>Validate the Macaroon’s HMAC signature against the root key</li>
<li>Walk the caveat chain: verify zone_id matches a local zone, server_ip matches this server, expiry is in the future</li>
<li>If valid: call <code>bpf_map_update_elem()</code> to insert the player’s IP + session_key + zone_id + core_id into the eBPF WHITELIST_MAP</li>
<li>If invalid: reject the connection</li>
</ul>
<p>This is the bridge between cryptographic security (user space) and kernel-bypass enforcement (eBPF). It runs once per session, not per packet.</p>
</section>
<section id="xdpebpf-data-plane-every-packet" class="level3">
<h3 class="anchored" data-anchor-id="xdpebpf-data-plane-every-packet">3. XDP/eBPF (data plane, every packet)</h3>
<p>The XDP program intercepts every UDP packet at the NIC, before the Linux kernel does any allocation or processing:</p>
<div class="code-copy-outer-scaffold"><div class="sourceCode" id="cb2" style="background: #f1f3f5;"><pre class="sourceCode c code-with-copy"><code class="sourceCode c"><span id="cb2-1"><span class="co" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">// XDP program (runs at line rate, ~10ns per packet)</span></span>
<span id="cb2-2">SEC<span class="op" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">(</span><span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"xdp"</span><span class="op" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">)</span></span>
<span id="cb2-3"><span class="dt" style="color: #AD0000;
background-color: null;
font-style: inherit;">int</span> zone_router<span class="op" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">(</span><span class="kw" style="color: #003B4F;
background-color: null;
font-weight: bold;
font-style: inherit;">struct</span> xdp_md <span class="op" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">*</span>ctx<span class="op" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">)</span> <span class="op" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">{</span></span>
<span id="cb2-4">    <span class="co" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">// Parse packet headers (IP + UDP + custom session header)</span></span>
<span id="cb2-5">    <span class="dt" style="color: #AD0000;
background-color: null;
font-style: inherit;">uint32_t</span> src_ip <span class="op" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">=</span> parse_src_ip<span class="op" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">(</span>ctx<span class="op" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">);</span></span>
<span id="cb2-6">    <span class="dt" style="color: #AD0000;
background-color: null;
font-style: inherit;">uint64_t</span> session_key <span class="op" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">=</span> parse_session_key<span class="op" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">(</span>ctx<span class="op" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">);</span></span>
<span id="cb2-7"></span>
<span id="cb2-8">    <span class="co" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">// Lookup in whitelist map (BPF hash map, O(1))</span></span>
<span id="cb2-9">    <span class="kw" style="color: #003B4F;
background-color: null;
font-weight: bold;
font-style: inherit;">struct</span> session_info <span class="op" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">*</span>info<span class="op" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">;</span></span>
<span id="cb2-10">    info <span class="op" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">=</span> bpf_map_lookup_elem<span class="op" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">(&amp;</span>whitelist_map<span class="op" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">,</span> <span class="op" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">&amp;</span>src_ip<span class="op" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">);</span></span>
<span id="cb2-11">    <span class="cf" style="color: #003B4F;
background-color: null;
font-weight: bold;
font-style: inherit;">if</span> <span class="op" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">(!</span>info<span class="op" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">)</span></span>
<span id="cb2-12">        <span class="cf" style="color: #003B4F;
background-color: null;
font-weight: bold;
font-style: inherit;">return</span> XDP_DROP<span class="op" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">;</span>  <span class="co" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">// unauthenticated — drop instantly</span></span>
<span id="cb2-13"></span>
<span id="cb2-14">    <span class="co" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">// Validate session key</span></span>
<span id="cb2-15">    <span class="cf" style="color: #003B4F;
background-color: null;
font-weight: bold;
font-style: inherit;">if</span> <span class="op" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">(</span>info<span class="op" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">-&gt;</span>session_key <span class="op" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">!=</span> session_key<span class="op" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">)</span></span>
<span id="cb2-16">        <span class="cf" style="color: #003B4F;
background-color: null;
font-weight: bold;
font-style: inherit;">return</span> XDP_DROP<span class="op" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">;</span>  <span class="co" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">// wrong key — spoofed or stale</span></span>
<span id="cb2-17"></span>
<span id="cb2-18">    <span class="co" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">// Check expiry</span></span>
<span id="cb2-19">    <span class="cf" style="color: #003B4F;
background-color: null;
font-weight: bold;
font-style: inherit;">if</span> <span class="op" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">(</span>bpf_ktime_get_ns<span class="op" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">()</span> <span class="op" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">&gt;</span> info<span class="op" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">-&gt;</span>expiry_ns<span class="op" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">)</span></span>
<span id="cb2-20">        <span class="cf" style="color: #003B4F;
background-color: null;
font-weight: bold;
font-style: inherit;">return</span> XDP_DROP<span class="op" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">;</span>  <span class="co" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">// session expired</span></span>
<span id="cb2-21"></span>
<span id="cb2-22">    <span class="co" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">// Route to the CPU core handling this zone</span></span>
<span id="cb2-23">    <span class="co" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">// (via CPU steering or redirect to the right ring buffer)</span></span>
<span id="cb2-24">    <span class="cf" style="color: #003B4F;
background-color: null;
font-weight: bold;
font-style: inherit;">return</span> route_to_core<span class="op" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">(</span>info<span class="op" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">-&gt;</span>core_id<span class="op" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">,</span> ctx<span class="op" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">);</span></span>
<span id="cb2-25"><span class="op" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">}</span></span></code></pre></div></div>
<p><strong>No cryptographic operations in the XDP path.</strong> Only a hash map lookup and integer comparison. This executes in ~10-50 nanoseconds per packet.</p>
</section>
</section>
<section id="why-this-works-for-25m-ccu" class="level2">
<h2 class="anchored" data-anchor-id="why-this-works-for-25m-ccu">Why this works for 25M CCU</h2>
<section id="packet-rate-analysis" class="level3">
<h3 class="anchored" data-anchor-id="packet-rate-analysis">Packet rate analysis</h3>
<p>At 25M CCU, 60Hz tick rate, 200 entities/zone:</p>
<table class="caption-top table">
<thead>
<tr class="header">
<th>Metric</th>
<th>Value</th>
</tr>
</thead>
<tbody>
<tr class="odd">
<td>Total players</td>
<td>25,000,000</td>
</tr>
<tr class="even">
<td>Packets/player/sec</td>
<td>60</td>
</tr>
<tr class="odd">
<td>Total packets/sec</td>
<td>1,500,000,000 (1.5 Gpps)</td>
</tr>
<tr class="even">
<td>Zones (200 entities each)</td>
<td>125,000</td>
</tr>
<tr class="odd">
<td>Packets/zone/sec</td>
<td>12,000 (200 entities × 60Hz)</td>
</tr>
<tr class="even">
<td>XDP cost/packet</td>
<td>~50ns</td>
</tr>
<tr class="odd">
<td>XDP CPU/zone/sec</td>
<td>0.6ms (12,000 × 50ns)</td>
</tr>
<tr class="even">
<td>XDP CPU/core (16 zones/core)</td>
<td>9.6ms/sec (1% of one core)</td>
</tr>
</tbody>
</table>
<p>The XDP filter consumes &lt;1% of a single core’s CPU. The remaining 99% is available for ZoneTick computation and FDB batch writes.</p>
</section>
<section id="ddos-resistance" class="level3">
<h3 class="anchored" data-anchor-id="ddos-resistance">DDoS resistance</h3>
<p>A DDoS attack sends millions of spoofed packets. Without XDP, each packet traverses the full kernel network stack (allocation, socket lookup, conntrack, userspace wakeup) — ~5µs per packet, saturating all cores.</p>
<p>With XDP, spoofed packets are dropped at the NIC in ~50ns. A 100 Gbps NIC can process 150M packets/sec in XDP. At 50ns/drop, that’s 7.5ms of CPU/sec — one core handles the entire DDoS while the other 31 cores run the game simulation uninterrupted.</p>
</section>
<section id="session-key-entropy" class="level3">
<h3 class="anchored" data-anchor-id="session-key-entropy">Session key entropy</h3>
<p>64-bit session key: 2⁶⁴ = 1.8 × 10¹⁹ possible keys. At 1.5 Gpps brute-force rate, expected time to forge one key: ~385 years. With session expiry at 5 minutes, the attacker has 300 seconds × 1.5 Gpps = 4.5 × 10¹¹ attempts, giving a forgery probability of 2.5 × 10⁻⁸ — negligible.</p>
</section>
<section id="whitelist_map-memory" class="level3">
<h3 class="anchored" data-anchor-id="whitelist_map-memory">WHITELIST_MAP memory</h3>
<p>Each entry: 4 bytes (IP) + 8 bytes (session_key) + 4 bytes (zone_id)</p>
<ul>
<li>4 bytes (core_id) + 8 bytes (expiry) = 28 bytes. For 25M entries: 700MB. BPF maps support up to 2GB on modern kernels. Alternatively, use a per-CPU map (per-core whitelist) to reduce each map to ~44MB.</li>
</ul>
</section>
</section>
<section id="macaroon-caveat-design" class="level2">
<h2 class="anchored" data-anchor-id="macaroon-caveat-design">Macaroon caveat design</h2>
<pre><code>location = "zf://zone-server.example.com"
identifier = "player-12345-session"
caveats = [
    "zone_id = 45",
    "server_ip = 192.168.1.50",
    "expires &lt; 1718984700",
    "rate_limit = 120",        // max 120 packets/sec (2x 60Hz)
    "entity_id = 12345",       // player's entity slot in zone
]
discharge = HMAC-SHA256(root_key, location || identifier || caveats)</code></pre>
<p>The user-space orchestrator verifies:</p>
<ol type="1">
<li>HMAC signature matches (token not forged)</li>
<li><code>zone_id</code> is a zone hosted on this server</li>
<li><code>server_ip</code> matches this server’s IP</li>
<li><code>expires</code> is in the future</li>
<li><code>entity_id</code> is a valid slot in the zone’s slotmap (RFD 0017)</li>
</ol>
<p>The <code>entity_id</code> caveat ties the Macaroon to a specific slotmap entry. If the player’s entity is destroyed (slot freed, generation bumped), the session is invalidated — the orchestrator removes the entry from WHITELIST_MAP on the next tick.</p>
</section>
<section id="session-lifecycle" class="level2">
<h2 class="anchored" data-anchor-id="session-lifecycle">Session lifecycle</h2>
<pre><code>1. Player authenticates → Matchmaker issues Macaroon (5min TTL)
2. Player sends Hello + Macaroon → orchestrator validates → WHITELIST_MAP update
3. Player sends UDP packets at 60Hz → XDP validates + routes (every packet)
4. At 4min30sec: orchestrator sends renewal prompt to player
5. Player re-authenticates → new Macaroon → WHITELIST_MAP update (refresh)
6. If player disconnects: orchestrator removes from WHITELIST_MAP on next tick
7. If Macaroon expires without renewal: XDP drops packets (expiry check in XDP)</code></pre>
</section>
<section id="relationship-to-other-rfds" class="level2">
<h2 class="anchored" data-anchor-id="relationship-to-other-rfds">Relationship to other RFDs</h2>
<ul>
<li><p><strong>RFD 0002</strong> (zonefabric): XDP routes packets to the core handling each zone. The zone’s slotmap (RFD 0017) stores the entity state. XDP does not touch the slotmap — it only routes packets.</p></li>
<li><p><strong>RFD 0017</strong> (slotmap): The <code>entity_id</code> caveat in the Macaroon maps to a slotmap handle. When the entity is destroyed, the slotmap generation bump invalidates the session.</p></li>
<li><p><strong>RFD 0005</strong> (actor-lite): The user-space orchestrator runs on the H2O event loop. The <code>bpf_map_update_elem()</code> call is made from the worker thread that handles the Hello packet.</p></li>
<li><p><strong>RFD 0016</strong> (zstd): Orthogonal — zstd compresses FDB values, XDP handles packet routing. They operate on different layers.</p></li>
<li><p>mas-bandwidth/fps: Glenn Fiedler’s architecture mentions XDP for packet processing and relays but does not detail authentication. This RFD fills that gap: Macaroons provide the authentication, XDP provides the enforcement.</p></li>
</ul>
</section>
<section id="what-this-rfd-does-not-cover" class="level2">
<h2 class="anchored" data-anchor-id="what-this-rfd-does-not-cover">What this RFD does NOT cover</h2>
<ul>
<li><p>Macaroon implementation: use libmacaroons (C library) or py-macaroons. The Matchmaker implementation is out of scope; this RFD defines the interface (issue, validate, attenuate) and the eBPF handoff.</p></li>
<li><p>eBPF program compilation: the XDP program is compiled with clang/llvm to BPF bytecode and loaded with libbpf. This is a build- time concern, documented in RFD 0014 (CI pipeline).</p></li>
<li><p>TLS/mTLS: the control-plane connection (Hello + Macaroon) uses TLS. The data-plane (UDP game packets) does not; XDP enforces authenticity via the session key, and game state is not sensitive (positions, velocities). Encryption of game packets is a future concern if anti-cheat requires it.</p></li>
<li><p>Player-to-player security: this RFD secures the server from unauthenticated clients. It does not prevent an authenticated player from sending malicious game actions (aimbots, speed hacks). Server-side validation of game actions is a game-logic concern, not a network-security concern.</p></li>
</ul>


</section>

 ]]></description>
  <guid>https://v-sekai-multiplayer-fabric.github.io/manuals/decisions/20260806-macaroon-xdp-security.html</guid>
  <pubDate>Thu, 06 Aug 2026 00:00:00 GMT</pubDate>
</item>
<item>
  <title>PERT critical path for zonefabric implementation</title>
  <link>https://v-sekai-multiplayer-fabric.github.io/manuals/decisions/20260806-pert-critical-path-zonefabric.html</link>
  <description><![CDATA[ 





<p><em><a href="../index.html">Manuals</a> › <a href="../decisions.html">Decisions</a> › PERT critical path for zonefabric implementation</em></p>
<p><strong>Tier:</strong> proof of concept · <strong>Status:</strong> accepted</p>
<blockquote class="blockquote">
<p>Ported from <a href="https://github.com/weftspun/h2o-bench-tpcc"><code>weftspun/h2o-bench-tpcc</code></a>’s <code>rfd/0020-pert-critical-path.md</code> as part of the <a href="https://github.com/v-sekai-multiplayer-fabric/zone-server-h2o">zone-server-h2o</a> consolidation — see that repo’s README for current status. Content below is the original RFD, unmodified except for this header and stripping the old State line.</p>
</blockquote>
<section id="decision" class="level2">
<h2 class="anchored" data-anchor-id="decision">Decision</h2>
<p>Model the zonefabric implementation as a PERT (Program Evaluation and Review Technique) network. Each RFD’s implementation is a task with optimistic (O), most-likely (M), and pessimistic (P) duration estimates. The critical path — the longest dependency chain — determines the minimum time to a working zonefabric benchmark.</p>
</section>
<section id="method" class="level2">
<h2 class="anchored" data-anchor-id="method">Method</h2>
<p>Expected duration (TE) for each task uses the PERT formula:</p>
<pre><code>TE = (O + 4M + P) / 6</code></pre>
<p>Variance (σ²) measures uncertainty:</p>
<pre><code>σ² = ((P - O) / 6)²</code></pre>
<p>The critical path is the chain of dependent tasks with the longest total TE. Any delay on the critical path delays the entire project. Tasks off the critical path have slack — they can slip without affecting the end date.</p>
</section>
<section id="task-list" class="level2">
<h2 class="anchored" data-anchor-id="task-list">Task list</h2>
<p>All durations in engineering days. One engineer, full-time.</p>
<table class="caption-top table">
<colgroup>
<col style="width: 3%">
<col style="width: 61%">
<col style="width: 13%">
<col style="width: 3%">
<col style="width: 3%">
<col style="width: 3%">
<col style="width: 3%">
<col style="width: 5%">
</colgroup>
<thead>
<tr class="header">
<th>ID</th>
<th>Task</th>
<th>Depends on</th>
<th>O</th>
<th>M</th>
<th>P</th>
<th>TE</th>
<th>σ²</th>
</tr>
</thead>
<tbody>
<tr class="odd">
<td>A</td>
<td>Binary value encoding (RFD 0010)</td>
<td>—</td>
<td>1</td>
<td>2</td>
<td>4</td>
<td>2.2</td>
<td>0.25</td>
</tr>
<tr class="even">
<td>B</td>
<td>FDB keyspace + async callbacks (RFD 0009, 0011)</td>
<td>A</td>
<td>2</td>
<td>3</td>
<td>6</td>
<td>3.3</td>
<td>0.44</td>
</tr>
<tr class="odd">
<td>C</td>
<td>Actor-lite worker pool (RFD 0005)</td>
<td>B</td>
<td>3</td>
<td>5</td>
<td>9</td>
<td>5.3</td>
<td>1.00</td>
</tr>
<tr class="even">
<td>D</td>
<td>Slotmap entity storage (RFD 0017)</td>
<td>A, B</td>
<td>2</td>
<td>3</td>
<td>5</td>
<td>3.2</td>
<td>0.25</td>
</tr>
<tr class="odd">
<td>E</td>
<td>Zstd compression (RFD 0016)</td>
<td>A</td>
<td>1</td>
<td>2</td>
<td>3</td>
<td>2.0</td>
<td>0.11</td>
</tr>
<tr class="even">
<td>F</td>
<td>ZoneTick (read/write loop)</td>
<td>C, D</td>
<td>3</td>
<td>4</td>
<td>8</td>
<td>4.5</td>
<td>0.69</td>
</tr>
<tr class="odd">
<td>G</td>
<td>Zone-state blob persistence</td>
<td>D, E, F</td>
<td>2</td>
<td>3</td>
<td>5</td>
<td>3.2</td>
<td>0.25</td>
</tr>
<tr class="even">
<td>H</td>
<td>GhostRelevance (AOI query)</td>
<td>D, F</td>
<td>2</td>
<td>3</td>
<td>6</td>
<td>3.3</td>
<td>0.44</td>
</tr>
<tr class="odd">
<td>I</td>
<td>CastSpell (effect + fanout)</td>
<td>D, F, H</td>
<td>3</td>
<td>5</td>
<td>10</td>
<td>5.5</td>
<td>1.36</td>
</tr>
<tr class="even">
<td>J</td>
<td>EntityMigration (zone handoff)</td>
<td>D, F</td>
<td>2</td>
<td>4</td>
<td>7</td>
<td>4.2</td>
<td>0.69</td>
</tr>
<tr class="odd">
<td>K</td>
<td>ZoneSplit (cost threshold)</td>
<td>F, G</td>
<td>2</td>
<td>3</td>
<td>5</td>
<td>3.2</td>
<td>0.25</td>
</tr>
<tr class="even">
<td>L</td>
<td>Macaroon + XDP security (RFD 0018)</td>
<td>C, D</td>
<td>4</td>
<td>7</td>
<td>14</td>
<td>7.7</td>
<td>2.78</td>
</tr>
<tr class="odd">
<td>M</td>
<td>Feature ablation (RFD 0019)</td>
<td>F, H, I</td>
<td>3</td>
<td>5</td>
<td>8</td>
<td>5.2</td>
<td>0.69</td>
</tr>
<tr class="even">
<td>N</td>
<td>Benchmark harness (wrk scripts)</td>
<td>F, G</td>
<td>1</td>
<td>2</td>
<td>3</td>
<td>2.0</td>
<td>0.11</td>
</tr>
<tr class="odd">
<td>O</td>
<td>Core-scaling measurement</td>
<td>N</td>
<td>2</td>
<td>3</td>
<td>5</td>
<td>3.2</td>
<td>0.25</td>
</tr>
</tbody>
</table>
</section>
<section id="dependency-graph" class="level2">
<h2 class="anchored" data-anchor-id="dependency-graph">Dependency graph</h2>
<pre><code>A (binary encoding)
├── B (FDB keyspace + async)
│   ├── C (actor-lite pool)
│   │   ├── F (ZoneTick) ◄═══ CRITICAL PATH
│   │   │   ├── G (zone-state blob)
│   │   │   │   └── K (ZoneSplit)
│   │   │   ├── H (GhostRelevance)
│   │   │   │   └── I (CastSpell)
│   │   │   │       └── M (ablation)
│   │   │   ├── J (EntityMigration)
│   │   │   └── N (benchmark harness)
│   │   │       └── O (scaling measurement)
│   │   └── L (Macaroon + XDP)
│   └── D (slotmap) ──► (feeds into F, H, I, J, L)
├── E (zstd) ──► (feeds into G)
└── (A also feeds D, E directly)</code></pre>
</section>
<section id="critical-path" class="level2">
<h2 class="anchored" data-anchor-id="critical-path">Critical path</h2>
<p>The critical path is the longest chain through the dependency graph:</p>
<pre><code>A → B → C → F → I → M</code></pre>
<table class="caption-top table">
<thead>
<tr class="header">
<th>Step</th>
<th>Task</th>
<th>TE (days)</th>
<th>Cumulative</th>
</tr>
</thead>
<tbody>
<tr class="odd">
<td>1</td>
<td>A: Binary value encoding</td>
<td>2.2</td>
<td>2.2</td>
</tr>
<tr class="even">
<td>2</td>
<td>B: FDB keyspace + async callbacks</td>
<td>3.3</td>
<td>5.5</td>
</tr>
<tr class="odd">
<td>3</td>
<td>C: Actor-lite worker pool</td>
<td>5.3</td>
<td>10.8</td>
</tr>
<tr class="even">
<td>4</td>
<td>F: ZoneTick</td>
<td>4.5</td>
<td>15.3</td>
</tr>
<tr class="odd">
<td>5</td>
<td>I: CastSpell</td>
<td>5.5</td>
<td>20.8</td>
</tr>
<tr class="even">
<td>6</td>
<td>M: Feature ablation</td>
<td>5.2</td>
<td>26.0</td>
</tr>
</tbody>
</table>
<p><strong>Critical path total: 26.0 engineering days (~5.2 weeks)</strong></p>
<p>This is the minimum time to a verified, ablation-tested zonefabric benchmark with CastSpell. The critical path passes through CastSpell because it depends on three upstream tasks (slotmap, tick, ghost) and has the highest variance (σ² = 1.36) — it’s both the longest and the riskiest task.</p>
</section>
<section id="slack-analysis" class="level2">
<h2 class="anchored" data-anchor-id="slack-analysis">Slack analysis</h2>
<p>Tasks NOT on the critical path have slack. Slack = (latest finish) − (earliest start + duration).</p>
<table class="caption-top table">
<colgroup>
<col style="width: 36%">
<col style="width: 5%">
<col style="width: 25%">
<col style="width: 25%">
<col style="width: 8%">
</colgroup>
<thead>
<tr class="header">
<th>Task</th>
<th>TE</th>
<th>Earliest start</th>
<th>Latest finish</th>
<th>Slack</th>
</tr>
</thead>
<tbody>
<tr class="odd">
<td>D: Slotmap</td>
<td>3.2</td>
<td>5.5 (after A+B)</td>
<td>10.8 (before F)</td>
<td>2.1</td>
</tr>
<tr class="even">
<td>E: Zstd</td>
<td>2.0</td>
<td>2.2 (after A)</td>
<td>15.3 (before G)</td>
<td>11.1</td>
</tr>
<tr class="odd">
<td>G: Zone-state blob</td>
<td>3.2</td>
<td>15.3 (after F)</td>
<td>19.8 (before K)</td>
<td>1.3</td>
</tr>
<tr class="even">
<td>H: GhostRelevance</td>
<td>3.3</td>
<td>15.3 (after F)</td>
<td>20.8 (before I)</td>
<td>2.2</td>
</tr>
<tr class="odd">
<td>J: EntityMigration</td>
<td>4.2</td>
<td>15.3 (after F)</td>
<td>26.0 (end)</td>
<td>6.5</td>
</tr>
<tr class="even">
<td>K: ZoneSplit</td>
<td>3.2</td>
<td>18.5 (after G)</td>
<td>26.0 (end)</td>
<td>4.3</td>
</tr>
<tr class="odd">
<td>L: Macaroon + XDP</td>
<td>7.7</td>
<td>10.8 (after C)</td>
<td>26.0 (end)</td>
<td>7.5</td>
</tr>
<tr class="even">
<td>N: Benchmark harness</td>
<td>2.0</td>
<td>18.5 (after G)</td>
<td>26.0 (end)</td>
<td>5.5</td>
</tr>
<tr class="odd">
<td>O: Scaling measurement</td>
<td>3.2</td>
<td>20.5 (after N)</td>
<td>26.0 (end)</td>
<td>2.3</td>
</tr>
</tbody>
</table>
<section id="slack-interpretation" class="level3">
<h3 class="anchored" data-anchor-id="slack-interpretation">Slack interpretation</h3>
<ul>
<li><p>E (Zstd) has 11.1 days of slack. Compression can be added any time before zone-state blob persistence. This confirms RFD 0019’s ablation result: zstd is deferrable. Do NOT build it first.</p></li>
<li><p>L (Macaroon + XDP) has 7.5 days of slack. Security can be built in parallel with game logic. This confirms RFD 0019: security features are independent of game-logic invariants.</p></li>
<li><p>J (EntityMigration) has 6.5 days of slack. Migration is needed for correctness at scale but can be stubbed initially (entities stay in their birth zone). Build it after the core loop works.</p></li>
<li><p>D (Slotmap) has only 2.1 days of slack and is near-critical. This confirms RFD 0019: slotmap is foundational and must be built early. Any delay on slotmap pushes the critical path.</p></li>
</ul>
</section>
</section>
<section id="milestones" class="level2">
<h2 class="anchored" data-anchor-id="milestones">Milestones</h2>
<table class="caption-top table">
<colgroup>
<col style="width: 16%">
<col style="width: 11%">
<col style="width: 71%">
</colgroup>
<thead>
<tr class="header">
<th>Milestone</th>
<th>Cumulative days</th>
<th>What’s working</th>
</tr>
</thead>
<tbody>
<tr class="odd">
<td>M1: FDB reads/writes</td>
<td>5.5</td>
<td>Binary encoding + FDB keyspace. Can read/write entity rows.</td>
</tr>
<tr class="even">
<td>M2: Worker pool</td>
<td>10.8</td>
<td>H2O event loop dispatches to worker threads via SPSC ring.</td>
</tr>
<tr class="odd">
<td>M3: Zone tick</td>
<td>15.3</td>
<td>ZoneTick reads entities, updates positions, writes back. <strong>First benchmarkable milestone.</strong></td>
</tr>
<tr class="even">
<td>M4: Ghost queries</td>
<td>18.6</td>
<td>GhostRelevance returns entities within GHOST_RANGE.</td>
</tr>
<tr class="odd">
<td>M5: CastSpell</td>
<td>20.8</td>
<td>Effects + fanout work. <strong>Full game loop operational.</strong></td>
</tr>
<tr class="even">
<td>M6: Ablation verified</td>
<td>26.0</td>
<td>Feature ablation confirms no speculator debt. <strong>Ship-ready.</strong></td>
</tr>
</tbody>
</table>
</section>
<section id="parallelization-with-two-engineers" class="level2">
<h2 class="anchored" data-anchor-id="parallelization-with-two-engineers">Parallelization with two engineers</h2>
<p>If a second engineer is available, the slack tasks can be parallelized:</p>
<table class="caption-top table">
<colgroup>
<col style="width: 35%">
<col style="width: 64%">
</colgroup>
<thead>
<tr class="header">
<th>Engineer 1 (critical path)</th>
<th>Engineer 2 (slack tasks)</th>
</tr>
</thead>
<tbody>
<tr class="odd">
<td>A: Binary encoding (2.2d)</td>
<td>(pair on A)</td>
</tr>
<tr class="even">
<td>B: FDB keyspace (3.3d)</td>
<td>E: Zstd (2.0d) → slack</td>
</tr>
<tr class="odd">
<td>C: Actor-lite pool (5.3d)</td>
<td>D: Slotmap (3.2d) → L: XDP (7.7d, partial)</td>
</tr>
<tr class="even">
<td>F: ZoneTick (4.5d)</td>
<td>H: GhostRelevance (3.3d) → J: Migration (4.2d)</td>
</tr>
<tr class="odd">
<td>I: CastSpell (5.5d)</td>
<td>G: Zone-state blob (3.2d) → K: ZoneSplit (3.2d)</td>
</tr>
<tr class="even">
<td>M: Ablation (5.2d)</td>
<td>N: Benchmark harness (2.0d) → O: Scaling (3.2d)</td>
</tr>
</tbody>
</table>
<p>With two engineers, the critical path remains A→B→C→F→I→M at 26 days (Engineer 1), but all slack tasks are completed in parallel. Total project duration: ~26 days instead of ~38 days (serial sum).</p>
<p>The critical path cannot be shortened by adding engineers — it’s sequential by dependency. Only reducing task scope (e.g., stubbing CastSpell’s fanout as a fixed-radius range scan without effect entities) can shorten it.</p>
</section>
<section id="risk-register" class="level2">
<h2 class="anchored" data-anchor-id="risk-register">Risk register</h2>
<table class="caption-top table">
<colgroup>
<col style="width: 32%">
<col style="width: 7%">
<col style="width: 16%">
<col style="width: 42%">
</colgroup>
<thead>
<tr class="header">
<th>Risk</th>
<th>Probability</th>
<th>Impact</th>
<th>Mitigation</th>
</tr>
</thead>
<tbody>
<tr class="odd">
<td>CastSpell fanout complexity exceeds estimate</td>
<td>Medium</td>
<td>+3 days on critical path</td>
<td>Stub with full-radius scan first, optimize later</td>
</tr>
<tr class="even">
<td>FDB async callback chain has memory bugs</td>
<td>Low</td>
<td>+5 days (debugging)</td>
<td>CBMC verification (RFD 0008) before integration</td>
</tr>
<tr class="odd">
<td>Slotmap serialization breaks generational IDs</td>
<td>Low</td>
<td>+2 days on D</td>
<td>Test round-trip serialize/deserialize before F integration</td>
</tr>
<tr class="even">
<td>XDP/eBPF program rejected by verifier</td>
<td>Medium</td>
<td>+4 days on L</td>
<td>Start L early (has 7.5d slack), use libbpf skeletons</td>
</tr>
<tr class="odd">
<td>Zstd dictionary training needed for good ratio</td>
<td>Low</td>
<td>+1 day on E</td>
<td>Use level 3 default, skip dictionary training initially</td>
</tr>
<tr class="even">
<td>Actor-lite SPSC ring has race condition</td>
<td>Low</td>
<td>+3 days on C</td>
<td>Lean 4 proof of linearizability (RFD 0008) before C completes</td>
</tr>
</tbody>
</table>
</section>
<section id="build-order-recommended" class="level2">
<h2 class="anchored" data-anchor-id="build-order-recommended">Build order (recommended)</h2>
<p>Based on the PERT analysis and RFD 0019 ablation matrix:</p>
<pre><code>Week 1: A (binary encoding) → B (FDB keyspace) → D (slotmap)
Week 2: C (actor-lite pool) → F (ZoneTick) → E (zstd, parallel)
Week 3: H (ghost) → G (zone-state blob) → N (benchmark harness)
Week 4: I (CastSpell) → J (migration) → K (zone split)
Week 5: L (XDP security, parallel) → M (ablation) → O (scaling)</code></pre>
<p>This order respects all dependencies, keeps the critical path moving, and defers high-slack tasks (zstd, XDP, zone split) to later weeks.</p>
</section>
<section id="relationship-to-other-rfds" class="level2">
<h2 class="anchored" data-anchor-id="relationship-to-other-rfds">Relationship to other RFDs</h2>
<ul>
<li><p><strong>RFD 0019</strong> (feature ablation): The dependency graph in this PERT chart is derived from the ablation matrix. The ablation confirms which tasks are load-bearing (on or near the critical path) and which are deferrable (high slack).</p></li>
<li><p><strong>RFD 0008</strong> (verification): CBMC and Lean 4 verification happen during tasks C (SPSC ring) and D (slotmap), not as a separate phase. Verification is built into the task estimates.</p></li>
<li><p><strong>RFD 0014</strong> (CI): The CI pipeline is assumed to exist from the start (building on every commit). It is infrastructure that supports all tasks, not a separate task.</p></li>
<li><p><strong>RFD 0013</strong> (benchmark harness): Task N is the wrk script implementation, which depends on the zone-state blob (G) being writable to FDB.</p></li>
</ul>


</section>

 ]]></description>
  <guid>https://v-sekai-multiplayer-fabric.github.io/manuals/decisions/20260806-pert-critical-path-zonefabric.html</guid>
  <pubDate>Thu, 06 Aug 2026 00:00:00 GMT</pubDate>
</item>
<item>
  <title>Replace FabricZone (Godot) with zone-server-h2o (libh2o + FDB + Fil-C)</title>
  <link>https://v-sekai-multiplayer-fabric.github.io/manuals/decisions/20260806-zone-server-h2o-replaces-godot-fabriczone.html</link>
  <description><![CDATA[ 





<p><em><a href="../index.html">Manuals</a> › <a href="../decisions.html">Decisions</a> › Replace FabricZone (Godot) with zone-server-h2o (libh2o + FDB + Fil-C)</em></p>
<p><strong>Tier:</strong> proof of concept · <strong>Status:</strong> accepted</p>
<section id="context" class="level2">
<h2 class="anchored" data-anchor-id="context">Context</h2>
<p>The production zone server (<code>zone-server</code>, deployed as <code>multiplayer-fabric-zone</code> on Fly.io) is a boot scaffold today — OpenTelemetry init only, no WebTransport listener, no game logic (<code>project/main.gd</code>’s own <code>TODO(cycle-5)</code> comment). The real entity/simulation engine, <code>FabricZone</code>/<code>FabricZoneJournal</code>/<code>FabricMMOGZone</code>, exists as a working Godot C++ module in <code>V-Sekai-fire/multiplayer-fabric-build</code> (<code>godot/modules/multiplayer_fabric/</code>) but has never been wired into a deployed zone server. Separately, <code>weftspun/h2o-bench-tpcc</code> designed (RFD 0002, <code>discussion</code> state, never implemented) a <code>libh2o</code> + FoundationDB “zonefabric” scenario modeling the same hub/instanced-zone shape — entity authority via a Hilbert-curve partition, ghost/AOI interest management — independently of this org’s actual zone-server work.</p>
</section>
<section id="decision" class="level2">
<h2 class="anchored" data-anchor-id="decision">Decision</h2>
<p>Build <a href="https://github.com/v-sekai-multiplayer-fabric/zone-server-h2o"><code>zone-server-h2o</code></a>: a native <code>libh2o</code> + FoundationDB zone server, built with <a href="https://github.com/pizlonator/fil-c">Fil-C</a> for memory safety against untrusted client input, that fully replaces the Godot <code>FabricZone</code> engine on the <strong>server</strong> side. The client stays Godot engine, unchanged.</p>
<p>Scope carried over from <code>FabricZone</code>/<code>FabricZoneJournal</code> (not a blank-slate design): entity slots, zone-to-zone entity migration, ghost/AOI state, and a durable journal (spawn/despawn/payload-update/snapshot/replay) — reimplemented against FDB instead of local SQLite, so it is shared across zone-server processes instead of single-node.</p>
<p>Physics/IK is ported from <a href="https://github.com/sinew-mocap/solve"><code>sinew-mocap/solve</code></a> (FK + LBS skinning), constrained by the <code>lean-humanoid-rom</code> and <code>swing-twist-kusudama</code> proofs — no working IK implementation existed inside <code>FabricZone</code> itself to port from directly.</p>
<p>Entity and ReBAC types are generated from <code>lean-entity-packet</code> and <code>lean-rebac-core</code> respectively, rather than hand-duplicated across the C++ engine being retired, the new FDB/Fil-C code, and any client-facing schema.</p>
<p>First working milestone: a WebTransport datagram round-trip plus a bare <code>ZoneTick</code> (<code>position += velocity * dt</code>, no physics) — everything else (migration, ghosts, physics/IK, Fil-C hardening) is built after that is proven, not deferred in scope.</p>
<p><code>weftspun/h2o-bench-tpcc</code> is archived once its reusable infrastructure and zonefabric design are ported. Its accepted TPC-C benchmark work (<code>RFD 0001</code>) is unaffected by this decision and stays there, read-only.</p>
</section>
<section id="consequences" class="level2">
<h2 class="anchored" data-anchor-id="consequences">Consequences</h2>
<ul>
<li>The Godot zone-server deployment (<code>zone-server</code> repo, Fly app <code>multiplayer-fabric-zone</code>) is retired once <code>zone-server-h2o</code> passes equivalent verification, in favor of the new implementation.</li>
<li><code>zone-backend</code> (Uro) is unaffected: it still only does identity, zone directory, asset storage, and ReBAC, and still never sees per-entity data in the live session.</li>
<li>CockroachDB’s scope stays account-level (identity, zones directory, avatars). The one exception under discussion — inventory/profile commits, previously headed toward CockroachDB via the progression hexagon’s SQLite “commit valve” — targets FDB instead, as the single write path for everything the zone server owns.</li>
<li>The related, previously-unimplemented <code>weftspun/h2o-bench-tpcc</code> RFDs this decision carries forward are filed alongside this entry, dated the same day: zonefabric scaling, actor-lite worker pool, FDB selection, verification strategy, binary value encoding, async FDB callback chain, zstd compression, slotmap entity storage, macaroon/XDP security, feature ablation, and the PERT critical path.</li>
</ul>


</section>

 ]]></description>
  <guid>https://v-sekai-multiplayer-fabric.github.io/manuals/decisions/20260806-zone-server-h2o-replaces-godot-fabriczone.html</guid>
  <pubDate>Thu, 06 Aug 2026 00:00:00 GMT</pubDate>
</item>
<item>
  <title>Sandboxed Godot game logic in zone-server-h2o, via raw libriscv</title>
  <link>https://v-sekai-multiplayer-fabric.github.io/manuals/decisions/20260806-sandboxed-godot-in-zone-server-h2o-via-raw-libriscv.html</link>
  <description><![CDATA[ 





<p><em><a href="../index.html">Manuals</a> › <a href="../decisions.html">Decisions</a> › Sandboxed Godot game logic in zone-server-h2o, via raw libriscv</em></p>
<p><strong>Tier:</strong> proof of concept · <strong>Status:</strong> accepted</p>
<section id="context" class="level2">
<h2 class="anchored" data-anchor-id="context">Context</h2>
<p><code>godot-riscv-spike</code> (RFD 0001 item 6) proved a real, full Godot engine instance boots and ticks inside <code>libriscv</code>’s own sandbox (<code>rvlinux</code>). This holds for the real sandbox, not just <code>qemu-riscv64</code>. Five real <code>libriscv</code> fixes made this work. Host-level <code>strace -f</code> confirmed the result. <code>syscalls.jsonld</code>’s own audit already named the target use case. The <code>socket</code> syscall’s own note reads: “Relevant to CastSpell effects that talk to zone-server-h2o.”</p>
<p>This session raised and closed two designs before this one.</p>
<ol type="1">
<li><code>godot-sandbox</code>’s own <code>Sandbox</code> Node and <code>vmcall</code> API needs its own full native Godot host process, a separate engine instance from the one <code>godot-riscv-spike</code> already proved working. <code>taskweft/taskweft</code>’s own RFD 0004 independently rejects this option for the identical reason. Its own words: “bound to a live Godot process.” This confirms the concern is real, not unique to this project.</li>
<li><strong>Linking the sandboxed guest directly into <code>zone-server-h2o</code>’s own binary.</strong> <code>zone-server-h2o</code> is a stock-clang CMake build. The guest needs <code>fabric-godot-core</code>’s own SCons, musl, and <code>rv64</code> cross-compile pipeline. Combining them ties two unrelated build systems together for no real benefit.</li>
</ol>
</section>
<section id="decision" class="level2">
<h2 class="anchored" data-anchor-id="decision">Decision</h2>
<p>Use the spike’s proven whole-engine-in-<code>rvlinux</code> approach directly. Drive it via raw <code>libriscv</code> (<code>Machine&lt;W&gt;</code>’s own <code>copy_to_guest</code>/<code>copy_from_guest</code>/<code>vmcall</code>). Orchestrate it as a separate process, not combined into one binary.</p>
<ul>
<li><strong><code>zone-server-h2o</code></strong> (unchanged build) spawns a new <strong><code>sandbox-orchestrator</code></strong> binary. This is new, and separate, and it links raw <code>libriscv</code>. It connects over a <code>socketpair()</code>, registered with <code>h2o</code>’s own event loop the same way <code>udp_fd</code>/<code>timer_fd</code> already are. It is not a blocking thread bolted on the side.</li>
<li>The <strong>guest ELF</strong> builds from <code>godot-riscv-spike</code>’s own proven <code>godot_instance_harness.cpp</code>. It exposes two functions callable by name via <code>vmcall()</code>. <code>godot_boot(cbor_config)</code> runs once: <code>initialize()</code>/<code>start()</code>, the exact sequence <code>FINDINGS.md</code> already verified. <code>godot_tick(input_addr, output_addr)</code> runs once per real tick, at 64 Hz (<code>ZONE_TICK_HZ</code>).</li>
<li>The guest runs <strong>offline</strong>. No socket syscalls run inside the sandbox. <code>libriscv</code> already has them implemented for a different, later need, confirmed real, not stubs, per <code>syscalls.jsonld</code>. This integration does not use them. All real networking stays in <code>zone-server-h2o</code>’s own process.</li>
<li>The data format is CBOR, with JSON-LD framing for anything self-describing. This covers <code>godot_boot</code>’s one-time config and any other control message. One real hot path stays bitpacked instead: the per-tick entity buffer that crosses the boundary on every <code>GodotInstance::iteration()</code> call. It reuses <code>xr_grid_entity_packet_t</code>’s existing 100-byte wire format verbatim. It addresses entities by RFD 0002’s own slotmap index, not a flat scan. This shares one addressing scheme with RFD 0002’s own, still-deferred, <code>zone_state</code> batch, instead of inventing a second one.</li>
</ul>
</section>
<section id="related-prior-art" class="level2">
<h2 class="anchored" data-anchor-id="related-prior-art">Related prior art</h2>
<p><code>taskweft/taskweft</code> RFD 0004 covers “KHR_interactivity Tier 2,” which embeds <code>libriscv</code> and compiles behavior graphs to riscv64. It independently reached the same conclusion: prefer <code>libriscv</code> over <code>godot-sandbox</code>. It set two precedents this decision reuses directly. First, values cross the ABI as JSON (<code>TwValue</code> (de)serialization), the same reasoning behind the CBOR choice here. Second, <code>libriscv</code>’s own machine-snapshot/resume capability is a real alternative to repeated <code>vmcall()</code>s for state persistence between ticks. This is worth a direct comparison during implementation.</p>
</section>
<section id="consequences" class="level2">
<h2 class="anchored" data-anchor-id="consequences">Consequences</h2>
<p>Good: this reuses <code>godot-riscv-spike</code>’s own proven, <code>strace</code>-confirmed boot sequence and five real fixes as they stand. <code>zone-server-h2o</code>’s own build, transport, and FDB layers stay untouched. One process boundary needs reasoning about, not a merged build.</p>
<p>Bad: this is a genuinely new subsystem. Guest-side exported functions, a new host orchestrator binary, and <code>h2o</code> evloop wiring are all real, unstarted implementation work, not just documentation. The exact <code>GodotInstance</code>/scene API <code>godot_tick</code> uses to apply entity input and read state back out is not yet nailed down.</p>
</section>
<section id="related" class="level2">
<h2 class="anchored" data-anchor-id="related">Related</h2>
<ul>
<li><code>v-sekai-multiplayer-fabric/godot-riscv-spike</code>, <code>FINDINGS.md</code> and <code>syscalls.jsonld</code></li>
<li><code>v-sekai-multiplayer-fabric/zone-server-h2o</code></li>
<li><code>taskweft/taskweft</code>, RFD 0003 and RFD 0004</li>
<li>This repo’s own PR #126, the zonefabric decision docs ported here from <code>zone-server-h2o</code>’s own consolidation</li>
<li>RFD 0002’s <code>zone_state</code> slotmap design (this repo), referenced by <code>zone-server-h2o</code>’s own <code>src/zf_kv.c</code> comments</li>
</ul>


</section>

 ]]></description>
  <guid>https://v-sekai-multiplayer-fabric.github.io/manuals/decisions/20260806-sandboxed-godot-in-zone-server-h2o-via-raw-libriscv.html</guid>
  <pubDate>Thu, 06 Aug 2026 00:00:00 GMT</pubDate>
</item>
<item>
  <title>Budgeter hexagon — core, ports, and adapters</title>
  <link>https://v-sekai-multiplayer-fabric.github.io/manuals/decisions/20260611-hexagon-budgeter-core.html</link>
  <description><![CDATA[ 





<p><em><a href="../index.html">Manuals</a> › <a href="../decisions.html">Decisions</a> › Budgeter hexagon — core, ports, and adapters</em></p>
<p><strong>Tier:</strong> proof of concept · <strong>Status:</strong> accepted</p>
<blockquote class="blockquote">
<p>Moved to <a href="../rfd/0039-hexagon-budgeter-core/README.md">rfd/0039</a>.</p>
</blockquote>
<section id="context-and-problem-statement" class="level2">
<h2 class="anchored" data-anchor-id="context-and-problem-statement">Context and Problem Statement</h2>
<p>The slice needs graceful degradation under load on the mobile floor, dialing quality knobs per frame so the player stays inside the experience during a burst.</p>
</section>
<section id="decision-outcome" class="level2">
<h2 class="anchored" data-anchor-id="decision-outcome">Decision Outcome</h2>
<p>Chosen option: structure the budgeter as a hexagon. The core is a constraint solver mapping on-device measurements to knob settings — avatar level-of-detail, interpolation against extrapolation, voice radius, and audio sample quality — as a pure reducer; it dials the knobs every frame, never overrules itself, and never dictates how the artists work.</p>
<p>Driving port: <code>measurement_source</code> (frame time, thermals, and entity counts). Driven port: <code>knob_sink</code> (the settings the runtime applies). Adapters: <code>feat/module-open-telemetry</code> feeds <code>measurement_source</code>; the engine runtime applies <code>knob_sink</code>; a fixture adapter replays a load spike for CI.</p>
</section>
<section id="consequences" class="level2">
<h2 class="anchored" data-anchor-id="consequences">Consequences</h2>
<ul>
<li>As a hundred extra players land in the hub, the budgeter degrades gracefully, so the player stays inside the experience.</li>
<li>The solve runs against a recorded spike in CI, so the knob response is testable with no device.</li>
<li>The budgeter stays advisory, so it never overrules the artists.</li>
</ul>
</section>
<section id="confirmation" class="level2">
<h2 class="anchored" data-anchor-id="confirmation">Confirmation</h2>
<p>The core replays a recorded load spike and asserts the knob response, with no device under it.</p>


</section>
<section id="related" class="level2">
<h2 class="anchored" data-anchor-id="related">Related</h2>
<ul>
<li><p><a href="../changelog/2026/20260612-deck-log.html">V-Sekai Multiplayer Fabric Deck Log — 2026-06-12 (Friday)</a></p></li>
<li><p><a href="20260611-loot-action-core-loop-mvp-vertical-slice.html">Loot-action core-loop MVP vertical slice</a></p></li>
<li><p><a href="20260629-defer-loot-slice-hardening-until-needed.html">Defer the loot-slice hardening scope until its need arrives</a></p></li>
<li><p><a href="../rfd/0039-hexagon-budgeter-core/README.html">RFD 0039: Budgeter hexagon — core, ports, and adapters</a></p></li>
</ul>
</section>

 ]]></description>
  <guid>https://v-sekai-multiplayer-fabric.github.io/manuals/decisions/20260611-hexagon-budgeter-core.html</guid>
  <pubDate>Thu, 11 Jun 2026 00:00:00 GMT</pubDate>
</item>
<item>
  <title>CASSIE desktop curvenet authoring for content, no Blender</title>
  <link>https://v-sekai-multiplayer-fabric.github.io/manuals/decisions/20260611-cassie-desktop-curvenet-authoring.html</link>
  <description><![CDATA[ 





<p><em><a href="../index.html">Manuals</a> › <a href="../decisions.html">Decisions</a> › CASSIE desktop curvenet authoring for content, no Blender</em></p>
<p><strong>Status:</strong> accepted</p>
<blockquote class="blockquote">
<p>Moved to <a href="../rfd/0029-cassie-desktop-curvenet-authoring/README.md">rfd/0029</a>.</p>
</blockquote>
<section id="context-and-problem-statement" class="level2">
<h2 class="anchored" data-anchor-id="context-and-problem-statement">Context and Problem Statement</h2>
<p>The content pipeline needs budget-bounded geometry for the slice without standing up Blender. The <code>feat/module-cassie</code> curve-and-surface sketcher produces curve networks that triangulate to a controllable poly count.</p>
</section>
<section id="decision-outcome" class="level2">
<h2 class="anchored" data-anchor-id="decision-outcome">Decision Outcome</h2>
<p>Chosen option: author the Hub, the Field, and the enemy as CASSIE curvenets through the desktop path for the deadline, with no Blender, because a curvenet is a bounded, inspectable graph the <code>zone-baker</code> can cost, and the desktop path skips the in-headset ergonomics for the one-week build.</p>
</section>
<section id="consequences" class="level2">
<h2 class="anchored" data-anchor-id="consequences">Consequences</h2>
<ul>
<li>The geometry arrives near budget, so it passes the baker with little rework.</li>
<li>The in-headset sketching ergonomics land after the gate.</li>
<li>The pipeline carries no Blender dependency.</li>
</ul>
</section>
<section id="confirmation" class="level2">
<h2 class="anchored" data-anchor-id="confirmation">Confirmation</h2>
<p>The authored Hub and Field pass the <code>zone-baker</code> at budget.</p>


</section>
<section id="related" class="level2">
<h2 class="anchored" data-anchor-id="related">Related</h2>
<ul>
<li><p><a href="../changelog/2026/20260612-deck-log.html">V-Sekai Multiplayer Fabric Deck Log — 2026-06-12 (Friday)</a></p></li>
<li><p><a href="20260611-content-build-merged-double-precision-mcp.html">Content creation in a single merged double-precision build via the editor MCP</a></p></li>
<li><p><a href="../rfd/0029-cassie-desktop-curvenet-authoring/README.html">RFD 0029: CASSIE desktop curvenet authoring for content, no Blender</a></p></li>
</ul>
</section>

 ]]></description>
  <guid>https://v-sekai-multiplayer-fabric.github.io/manuals/decisions/20260611-cassie-desktop-curvenet-authoring.html</guid>
  <pubDate>Thu, 11 Jun 2026 00:00:00 GMT</pubDate>
</item>
<item>
  <title>CI/CD runners operate as a finite queueing system</title>
  <link>https://v-sekai-multiplayer-fabric.github.io/manuals/decisions/20260611-cicd-runners-as-queueing-system.html</link>
  <description><![CDATA[ 





<p><em><a href="../index.html">Manuals</a> › <a href="../decisions.html">Decisions</a> › CI/CD runners operate as a finite queueing system</em></p>
<p><strong>Tier:</strong> baseline · <strong>Status:</strong> accepted</p>
<blockquote class="blockquote">
<p>Moved to <a href="../rfd/0030-cicd-runners-as-queueing-system/README.md">rfd/0030</a>.</p>
</blockquote>
<section id="context-and-problem-statement" class="level2">
<h2 class="anchored" data-anchor-id="context-and-problem-statement">Context and Problem Statement</h2>
<p>The organization shares one GitHub Actions runner pool across every repository, observed at roughly 14–20 concurrent jobs. A single push to the Godot fork triggers a matrix of about 20 jobs, and the slowest of them (sanitizer and Windows builds) hold a runner for one to two hours. Pushes arrive from fix branches, pull requests, merge commits, reruns, and even archive repositories that carry the same workflow files. When arrivals outpace the pool, every queued job waits behind work that may carry no information at all. How does CI work get admitted to the runner pool so that signal arrives quickly?</p>
</section>
<section id="decision-drivers" class="level2">
<h2 class="anchored" data-anchor-id="decision-drivers">Decision Drivers</h2>
<ul>
<li>The pool is a queueing system with a small fixed number of servers: as utilization approaches saturation, waiting time grows without bound (M/M/c behaviour), and the delay hits short high-signal jobs as hard as long ones.</li>
<li>One logical change can spawn several full matrices: a branch push and its pull request double-trigger, the merge push triggers again, and a rerun repeats whatever already ran.</li>
<li>Some queued work is known in advance to produce nothing: a job whose fix is not on the branch yet fails with certainty, and a run on a merged or deleted ref validates nothing.</li>
<li>Diagnostics need CI hardware (a crash that only reproduces on GCC Linux runners) but not the whole matrix.</li>
</ul>
</section>
<section id="considered-options" class="level2">
<h2 class="anchored" data-anchor-id="considered-options">Considered Options</h2>
<ul>
<li>Unbounded admission: push and rerun freely, let GitHub queue everything.</li>
<li>Hard concurrency caps in workflow configuration (per-repo <code>concurrency</code> groups beyond the existing per-branch cancellation).</li>
<li>An operating discipline that bounds arrivals at the source: batch, sequence, and cancel zero-information work.</li>
</ul>
</section>
<section id="decision-outcome" class="level2">
<h2 class="anchored" data-anchor-id="decision-outcome">Decision Outcome</h2>
<p>Chosen option: “an operating discipline that bounds arrivals at the source”, because the waste comes from how work is submitted, not from how GitHub schedules it — and a discipline adapts per situation where a hard cap blocks urgent work behind stale work.</p>
<p>The discipline:</p>
<ul>
<li>Two or three full matrices run at a time; further work waits for a slot. A merge push waits until the in-flight run it would concurrency-cancel finishes, so partially-completed long jobs are not thrown away mid-service.</li>
<li>Arrivals are batched: fixes land as one commit per push where possible, and a late-discovered fix joins an already-open PR branch rather than opening a new one. One merge then validates the whole batch with a single matrix.</li>
<li>Zero-information runs are cancelled immediately: the push-event run that duplicates a PR’s pull-request run, runs still queued on merged or deleted refs, runs whose remaining jobs have a known outcome, and workflow runs triggered by pushes to archive repositories (Actions stays disabled on <code>godot-archived</code>).</li>
<li>Diagnostics are trimmed to one job: a throwaway branch edits the workflow down to the single job that reproduces the problem, and reuses the warm build cache by keeping the matrix entry’s <code>cache-name</code>. The branch is deleted once the answer is in hand.</li>
<li>A failed job whose fix is not yet on the branch never reruns; until the fix lands it would occupy a server for its full service time and return a result that is already known.</li>
<li>Backlog is admitted selectively: stale branches needing fresh signal enter the queue worst-first, one or two at a time, when utilization is low rather than as a bulk rerun.</li>
</ul>
<section id="consequences" class="level3">
<h3 class="anchored" data-anchor-id="consequences">Consequences</h3>
<ul>
<li>Good: high-signal jobs spend their time in service, not in queue; verdicts on key jobs arrive in roughly the service time of the job itself.</li>
<li>Good: the merge policy (merge on a passing key job) composes cleanly — batched PRs mean each merge buys validation for several fixes with one matrix.</li>
<li>Bad: sequencing adds wall-clock latency to individual merges; a merge-ready PR waits for an unrelated in-flight run to finish.</li>
<li>Bad: the discipline lives in operator habit and this record, not in enforced configuration; a contributor who pushes freely still floods the pool.</li>
</ul>
</section>
<section id="confirmation" class="level3">
<h3 class="anchored" data-anchor-id="confirmation">Confirmation</h3>
<p>The discipline is in practice on the CHI-364 CI campaign: six redundant queued runs cancelled in one sweep (two merged PRs’ leftovers, a deleted tag’s run, a push/PR duplicate), Actions disabled on <code>godot-archived</code> after an archive push was found competing with the main repo’s queue, a GCC-only segfault diagnosed through a single-job trimmed workflow over four iterations on a warm cache, and fixes batched so that two branches reached green with one validation matrix each.</p>
</section>
</section>
<section id="more-information" class="level2">
<h2 class="anchored" data-anchor-id="more-information">More Information</h2>
<p>The queue is observable with <code>gh run list --repo &lt;repo&gt; --json databaseId,status,headBranch</code> filtered to <code>queued</code>/<code>in_progress</code>, and per-run job counts with <code>gh run view &lt;id&gt; --json jobs</code>. Little’s law gives the sanity check: jobs in system ≈ arrival rate × time in system, so when queued jobs exceed the server count several times over, every new matrix admits about an hour of added latency to its own verdict.</p>


</section>
<section id="related" class="level2">
<h2 class="anchored" data-anchor-id="related">Related</h2>
<ul>
<li><p><a href="../rfd/0030-cicd-runners-as-queueing-system/README.html">RFD 0030: CI/CD runners operate as a finite queueing system</a></p></li>
</ul>
</section>

 ]]></description>
  <guid>https://v-sekai-multiplayer-fabric.github.io/manuals/decisions/20260611-cicd-runners-as-queueing-system.html</guid>
  <pubDate>Thu, 11 Jun 2026 00:00:00 GMT</pubDate>
</item>
<item>
  <title>Combat hexagon — core, ports, and adapters</title>
  <link>https://v-sekai-multiplayer-fabric.github.io/manuals/decisions/20260611-hexagon-combat-core.html</link>
  <description><![CDATA[ 





<p><em><a href="../index.html">Manuals</a> › <a href="../decisions.html">Decisions</a> › Combat hexagon — core, ports, and adapters</em></p>
<p><strong>Tier:</strong> proof of concept · <strong>Status:</strong> accepted</p>
<blockquote class="blockquote">
<p>Moved to <a href="../rfd/0040-hexagon-combat-core/README.md">rfd/0040</a>.</p>
</blockquote>
<section id="context-and-problem-statement" class="level2">
<h2 class="anchored" data-anchor-id="context-and-problem-statement">Context and Problem Statement</h2>
<p>The combat concern — combo timing, hit validation, the enemy spawn-invulnerability window, and damage — needs to be deterministic and testable with no server and no headset. Per <a href="../decisions/20260610-hexagonal-core-ports-adapters.html">the hexagonal decision</a> it becomes a core behind narrow ports.</p>
</section>
<section id="decision-outcome" class="level2">
<h2 class="anchored" data-anchor-id="decision-outcome">Decision Outcome</h2>
<p>Chosen option: structure combat as a hexagon. The core resolves the combo timing, validates each hit against the authoritative positions, holds the enemy invulnerable for its spawn window, and deducts health, as a pure reducer (<a href="../decisions/20260611-core-contract-pure-reducer-byte-state.html">the core contract</a>) over deterministic state (<a href="../decisions/20260611-deterministic-cores-integer-seeded-rng.html">the determinism decision</a>).</p>
<p>Driving ports: <code>input_source</code> (player commands with timestamps and targets), <code>tick_source</code> (the constant-step clock), <code>behavior_source</code> (enemy intents). Driven ports: <code>state_sink</code> (authoritative entity state), <code>event_sink</code> (hits, deaths, door unlocks). Adapters: <code>feat/module-http3</code> feeds <code>input_source</code>; the <code>zone-server</code> hosts the core and drives <code>tick_source</code> under <a href="../decisions/20260611-server-authoritative-simulation-deferred-rollback.html">server authority</a>; <a href="../decisions/20260611-generated-behavior-sandboxed-riscv.html">sandboxed behavior</a> implements <code>behavior_source</code>; a fixture adapter replays recorded inputs for CI.</p>
</section>
<section id="consequences" class="level2">
<h2 class="anchored" data-anchor-id="consequences">Consequences</h2>
<ul>
<li>The server-authoritative adapter binds <code>input_source</code> and <code>state_sink</code> with interpolation and no prediction for the deadline; the rollback adapter lands after the gate behind the same ports.</li>
<li>The combat core runs headless against fixtures, so a flaky adapter fails in isolation.</li>
<li>The melee archetype ships first; the ranged and caster archetypes land after the gate.</li>
</ul>
</section>
<section id="confirmation" class="level2">
<h2 class="anchored" data-anchor-id="confirmation">Confirmation</h2>
<p>The combat core passes its fixtures, and a melee combo lands a hit under server authority on the OpenXR client.</p>


</section>
<section id="related" class="level2">
<h2 class="anchored" data-anchor-id="related">Related</h2>
<ul>
<li><p><a href="../changelog/2026/20260612-deck-log.html">V-Sekai Multiplayer Fabric Deck Log — 2026-06-12 (Friday)</a></p></li>
<li><p><a href="20260610-hexagonal-core-ports-adapters.html">Hexagonal core/ports/adapters as the component convention</a></p></li>
<li><p><a href="20260611-core-contract-pure-reducer-byte-state.html">Cores as pure reducers over byte-serialized state</a></p></li>
<li><p><a href="20260611-deterministic-cores-integer-seeded-rng.html">Deterministic cores via r128 fixed-point and seeded RNG</a></p></li>
<li><p><a href="20260611-generated-behavior-sandboxed-riscv.html">Generated behavior runs as sandboxed RISC-V</a></p></li>
<li><p><a href="20260611-loot-action-core-loop-mvp-vertical-slice.html">Loot-action core-loop MVP vertical slice</a></p></li>
<li><p><a href="20260611-server-authoritative-simulation-deferred-rollback.html">Server-authoritative simulation with deferred rollback</a></p></li>
<li><p><a href="../rfd/0040-hexagon-combat-core/README.html">RFD 0040: Combat hexagon — core, ports, and adapters</a></p></li>
</ul>
</section>

 ]]></description>
  <guid>https://v-sekai-multiplayer-fabric.github.io/manuals/decisions/20260611-hexagon-combat-core.html</guid>
  <pubDate>Thu, 11 Jun 2026 00:00:00 GMT</pubDate>
</item>
<item>
  <title>Content creation in a single merged double-precision build via the editor MCP</title>
  <link>https://v-sekai-multiplayer-fabric.github.io/manuals/decisions/20260611-content-build-merged-double-precision-mcp.html</link>
  <description><![CDATA[ 





<p><em><a href="../index.html">Manuals</a> › <a href="../decisions.html">Decisions</a> › Content creation in a single merged double-precision build via the editor MCP</em></p>
<p><strong>Status:</strong> accepted</p>
<blockquote class="blockquote">
<p>Moved to <a href="../rfd/0031-content-build-merged-double-precision-mcp/README.md">rfd/0031</a>.</p>
</blockquote>
<section id="context-and-problem-statement" class="level2">
<h2 class="anchored" data-anchor-id="context-and-problem-statement">Context and Problem Statement</h2>
<p>Content creation needs the full engine, every <code>feat/*</code> capability at double precision, without Blender, and the team budgets a single such build for the slice.</p>
</section>
<section id="decision-outcome" class="level2">
<h2 class="anchored" data-anchor-id="decision-outcome">Decision Outcome</h2>
<p>Chosen option: produce one Godot build by merging all <code>feat/*</code> branches with the <code>merge</code> tool at <code>precision=double</code> (<a href="../decisions/20260501-godot-double-precision-template-release-for-zone.html">the double-precision decision</a>, <a href="../decisions/20260606-compiling-godot-engine.html">the compiling decision</a>), and drive content creation through the <code>vsekai-godot-mcp</code> editor server, where the Godot editor is the MCP server over streamable-HTTP, because one merged build covers authoring and the MCP lets a content tool drive the editor directly. The pipeline uses no Blender (<a href="../decisions/20260611-cassie-desktop-curvenet-authoring.html">the CASSIE authoring decision</a>).</p>
</section>
<section id="consequences" class="level2">
<h2 class="anchored" data-anchor-id="consequences">Consequences</h2>
<ul>
<li>One merged double-precision build covers content authoring for the slice.</li>
<li>A content tool drives the editor through the MCP over streamable-HTTP.</li>
<li>The pipeline carries no Blender dependency.</li>
</ul>
</section>
<section id="confirmation" class="level2">
<h2 class="anchored" data-anchor-id="confirmation">Confirmation</h2>
<p>The merged double-precision build opens and the MCP drives an authoring round trip.</p>


</section>
<section id="related" class="level2">
<h2 class="anchored" data-anchor-id="related">Related</h2>
<ul>
<li><p><a href="../changelog/2026/20260612-deck-log.html">V-Sekai Multiplayer Fabric Deck Log — 2026-06-12 (Friday)</a></p></li>
<li><p><a href="20260501-godot-double-precision-template-release-for-zone.html">Godot double precision template_release for zone servers</a></p></li>
<li><p><a href="20260606-compiling-godot-engine.html">Compiling the Godot engine</a></p></li>
<li><p><a href="20260611-cassie-desktop-curvenet-authoring.html">CASSIE desktop curvenet authoring for content, no Blender</a></p></li>
<li><p><a href="../rfd/0031-content-build-merged-double-precision-mcp/README.html">RFD 0031: Content creation in a single merged double-precision build via the editor MCP</a></p></li>
</ul>
</section>

 ]]></description>
  <guid>https://v-sekai-multiplayer-fabric.github.io/manuals/decisions/20260611-content-build-merged-double-precision-mcp.html</guid>
  <pubDate>Thu, 11 Jun 2026 00:00:00 GMT</pubDate>
</item>
<item>
  <title>Core kernel codegen via lean-slang (Lean to Slang to SPIR-V)</title>
  <link>https://v-sekai-multiplayer-fabric.github.io/manuals/decisions/20260611-core-codegen-lean-slang.html</link>
  <description><![CDATA[ 





<p><em><a href="../index.html">Manuals</a> › <a href="../decisions.html">Decisions</a> › Core kernel codegen via lean-slang (Lean to Slang to SPIR-V)</em></p>
<p><strong>Status:</strong> accepted</p>
<blockquote class="blockquote">
<p>Moved to <a href="../rfd/0032-core-codegen-lean-slang/README.md">rfd/0032</a>.</p>
</blockquote>
<section id="context-and-problem-statement" class="level2">
<h2 class="anchored" data-anchor-id="context-and-problem-statement">Context and Problem Statement</h2>
<p>The cores carry compute kernels — hit raycasts, the budgeter solve, geometry costing — that need to be verified once and run as GPU compute, with the dispatch wrapped behind the flat C ABI port (<a href="../decisions/20260611-lean4-kernel-cores-flat-c-host-adapters.html">the Lean cores decision</a>). Hand-porting a kernel from a separate spec to a shader drifts.</p>
</section>
<section id="decision-drivers" class="level2">
<h2 class="anchored" data-anchor-id="decision-drivers">Decision Drivers</h2>
<ul>
<li>One verified Lean source per kernel, with no separate shader to drift.</li>
<li>The kernel runs on the GPU and integrates with GPU convergence; the CPU Slang target lacks that integration.</li>
<li>Integer ops keep the kernel deterministic across conformant devices.</li>
</ul>
</section>
<section id="considered-options" class="level2">
<h2 class="anchored" data-anchor-id="considered-options">Considered Options</h2>
<ul>
<li>Hand-write each kernel as a shader from a separate spec.</li>
<li>Lower a CPU host target through Slang.</li>
<li>Author the kernel in Lean and lower it to SPIR-V.</li>
</ul>
</section>
<section id="decision-outcome" class="level2">
<h2 class="anchored" data-anchor-id="decision-outcome">Decision Outcome</h2>
<p>Chosen option: author the compute kernels in Lean and lower them to Slang through <a href="https://github.com/V-Sekai-fire/lean-slang">lean-slang</a>, then run <code>slangc -target spirv</code> to SPIR-V (<code>.spv</code>), and dispatch the <code>.spv</code> behind the flat C ABI, because SPIR-V runs the kernel on the GPU with the convergence integration the CPU Slang target lacks, and one verified Lean source produces the shipped kernel. The <code>idtx-flow</code> repository already depends on <code>LeanSlang</code> and byte-pins the emitted Slang against committed source with <code>native_decide</code>.</p>
</section>
<section id="consequences" class="level2">
<h2 class="anchored" data-anchor-id="consequences">Consequences</h2>
<ul>
<li>One Lean kernel source produces the <code>.spv</code>, so the spec and the shipped kernel stay one artifact.</li>
<li>The lowered Slang and the <code>.spv</code> are committed and byte-pinned, so a regen that drifts fails the pin.</li>
<li>The kernels use integer ops, so the SPIR-V stays deterministic across conformant devices (<a href="../decisions/20260611-deterministic-cores-integer-seeded-rng.html">the determinism decision</a>).</li>
</ul>
</section>
<section id="confirmation" class="level2">
<h2 class="anchored" data-anchor-id="confirmation">Confirmation</h2>
<p>The emitted Slang matches its committed byte-pin, and the dispatched <code>.spv</code> kernel passes the core fixtures.</p>


</section>
<section id="related" class="level2">
<h2 class="anchored" data-anchor-id="related">Related</h2>
<ul>
<li><p><a href="../changelog/2026/20260612-deck-log.html">V-Sekai Multiplayer Fabric Deck Log — 2026-06-12 (Friday)</a></p></li>
<li><p><a href="20260611-core-contract-pure-reducer-byte-state.html">Cores as pure reducers over byte-serialized state</a></p></li>
<li><p><a href="20260611-deterministic-cores-integer-seeded-rng.html">Deterministic cores via r128 fixed-point and seeded RNG</a></p></li>
<li><p><a href="20260611-lean4-kernel-cores-flat-c-host-adapters.html">Lean 4 build-time kernel cores with flat-C host adapters</a></p></li>
<li><p><a href="20260612-r128-fixed-point-as-lean-library.html">r128 Q64.64 fixed-point as a Lean library for the cores</a></p></li>
<li><p><a href="../rfd/0032-core-codegen-lean-slang/README.html">RFD 0032: Core kernel codegen via lean-slang (Lean to Slang to SPIR-V)</a></p></li>
</ul>
</section>

 ]]></description>
  <guid>https://v-sekai-multiplayer-fabric.github.io/manuals/decisions/20260611-core-codegen-lean-slang.html</guid>
  <pubDate>Thu, 11 Jun 2026 00:00:00 GMT</pubDate>
</item>
<item>
  <title>Cores as pure reducers over byte-serialized state</title>
  <link>https://v-sekai-multiplayer-fabric.github.io/manuals/decisions/20260611-core-contract-pure-reducer-byte-state.html</link>
  <description><![CDATA[ 





<p><em><a href="../index.html">Manuals</a> › <a href="../decisions.html">Decisions</a> › Cores as pure reducers over byte-serialized state</em></p>
<p><strong>Status:</strong> accepted</p>
<blockquote class="blockquote">
<p>Moved to <a href="../rfd/0033-core-contract-pure-reducer-byte-state/README.md">rfd/0033</a>.</p>
</blockquote>
<section id="context-and-problem-statement" class="level2">
<h2 class="anchored" data-anchor-id="context-and-problem-statement">Context and Problem Statement</h2>
<p>Every hexagonal core (<a href="../decisions/20260610-hexagonal-core-ports-adapters.html">the hexagonal decision</a>) needs to be replayable, snapshot-able, fixture-testable, and transport-agnostic. A core that hides mutable state behind methods defeats all four.</p>
</section>
<section id="considered-options" class="level2">
<h2 class="anchored" data-anchor-id="considered-options">Considered Options</h2>
<ul>
<li>Stateful objects whose internal state lives behind methods.</li>
<li>A pure reducer over an explicit, serializable state value.</li>
</ul>
</section>
<section id="decision-outcome" class="level2">
<h2 class="anchored" data-anchor-id="decision-outcome">Decision Outcome</h2>
<p>Chosen option: model each core as a pure reducer, <code>step : State -&gt; Event -&gt; State x Effects</code>, with <code>State</code>, <code>Event</code>, and <code>Effects</code> serialized to bytes deterministically, so the core is bytes to bytes. The flat C ABI exposes <code>step</code> plus <code>snapshot</code> and <code>restore</code> over the byte state, and the compute kernels lower through <a href="../decisions/20260611-core-codegen-lean-slang.html">lean-slang</a> to SPIR-V, dispatched on the GPU. Hidden mutable state defeats snapshots and exact fixtures, so the first option loses replay and rollback.</p>
<p>With <a href="../decisions/20260611-deterministic-cores-integer-seeded-rng.html">the determinism decision</a>, this makes replay byte-exact and snapshots a value copy.</p>
</section>
<section id="consequences" class="level2">
<h2 class="anchored" data-anchor-id="consequences">Consequences</h2>
<ul>
<li>Snapshots and rollback are copies of the state bytes.</li>
<li>Fixtures pin exact output bytes, so a divergence shows up as a failing fixture.</li>
<li>The same <code>step</code> backs every adapter, so <code>webtransportd</code> and the engine carry the same bytes.</li>
</ul>
</section>
<section id="confirmation" class="level2">
<h2 class="anchored" data-anchor-id="confirmation">Confirmation</h2>
<p>Replaying an event log reproduces the state bytes on every target.</p>


</section>
<section id="related" class="level2">
<h2 class="anchored" data-anchor-id="related">Related</h2>
<ul>
<li><p><a href="../changelog/2026/20260612-deck-log.html">V-Sekai Multiplayer Fabric Deck Log — 2026-06-12 (Friday)</a></p></li>
<li><p><a href="20260610-hexagonal-core-ports-adapters.html">Hexagonal core/ports/adapters as the component convention</a></p></li>
<li><p><a href="20260611-core-codegen-lean-slang.html">Core kernel codegen via lean-slang (Lean to Slang to SPIR-V)</a></p></li>
<li><p><a href="20260611-deterministic-cores-integer-seeded-rng.html">Deterministic cores via r128 fixed-point and seeded RNG</a></p></li>
<li><p><a href="20260611-hexagon-combat-core.html">Combat hexagon — core, ports, and adapters</a></p></li>
<li><p><a href="20260611-loot-action-core-loop-mvp-vertical-slice.html">Loot-action core-loop MVP vertical slice</a></p></li>
<li><p><a href="20260611-server-authoritative-simulation-deferred-rollback.html">Server-authoritative simulation with deferred rollback</a></p></li>
<li><p><a href="../rfd/0033-core-contract-pure-reducer-byte-state/README.html">RFD 0033: Cores as pure reducers over byte-serialized state</a></p></li>
</ul>
</section>

 ]]></description>
  <guid>https://v-sekai-multiplayer-fabric.github.io/manuals/decisions/20260611-core-contract-pure-reducer-byte-state.html</guid>
  <pubDate>Thu, 11 Jun 2026 00:00:00 GMT</pubDate>
</item>
<item>
  <title>Deterministic cores via r128 fixed-point and seeded RNG</title>
  <link>https://v-sekai-multiplayer-fabric.github.io/manuals/decisions/20260611-deterministic-cores-integer-seeded-rng.html</link>
  <description><![CDATA[ 





<p><em><a href="../index.html">Manuals</a> › <a href="../decisions.html">Decisions</a> › Deterministic cores via r128 fixed-point and seeded RNG</em></p>
<p><strong>Status:</strong> accepted</p>
<blockquote class="blockquote">
<p>Moved to <a href="../rfd/0034-deterministic-cores-integer-seeded-rng/README.md">rfd/0034</a>.</p>
</blockquote>
<section id="context-and-problem-statement" class="level2">
<h2 class="anchored" data-anchor-id="context-and-problem-statement">Context and Problem Statement</h2>
<p>Replay, recorded fixtures, and the future rollback adapter need the authoritative cores to be bit-exact across the RTX 4090 workstation and the Steam Deck. IEEE-754 floating point diverges across platforms through fused multiply-add, instruction reordering, and transcendental functions, so a float in the authoritative path breaks replay silently.</p>
</section>
<section id="decision-outcome" class="level2">
<h2 class="anchored" data-anchor-id="decision-outcome">Decision Outcome</h2>
<p>Chosen option: the authoritative cores use r128 Q64.64 fixed-point and a seeded RNG threaded through state, with no floating point, because fixed-point over 64-bit integers reproduces bit-exact across machines while an explicit seed in the state makes the loot roll replayable. The cores import r128 as a Lean library (<a href="../decisions/20260612-r128-fixed-point-as-lean-library.html">the r128 Lean library decision</a>) that lowers to SPIR-V, and the engine’s vendored <code>thirdparty/misc/r128</code> stays the host reference.</p>
</section>
<section id="consequences" class="level2">
<h2 class="anchored" data-anchor-id="consequences">Consequences</h2>
<ul>
<li>Replay and snapshots stay byte-exact, so the rollback adapter becomes cheap.</li>
<li>The fixtures pin exact outputs, so a divergence shows up as a failing fixture rather than a silent drift.</li>
<li>The loot roll is reproducible from the seed in the state.</li>
</ul>
</section>
<section id="confirmation" class="level2">
<h2 class="anchored" data-anchor-id="confirmation">Confirmation</h2>
<p>Replaying an input log reproduces the state hash on every target.</p>


</section>
<section id="related" class="level2">
<h2 class="anchored" data-anchor-id="related">Related</h2>
<ul>
<li><p><a href="../changelog/2026/20260612-deck-log.html">V-Sekai Multiplayer Fabric Deck Log — 2026-06-12 (Friday)</a></p></li>
<li><p><a href="20260611-core-codegen-lean-slang.html">Core kernel codegen via lean-slang (Lean to Slang to SPIR-V)</a></p></li>
<li><p><a href="20260611-core-contract-pure-reducer-byte-state.html">Cores as pure reducers over byte-serialized state</a></p></li>
<li><p><a href="20260611-hexagon-combat-core.html">Combat hexagon — core, ports, and adapters</a></p></li>
<li><p><a href="20260612-r128-fixed-point-as-lean-library.html">r128 Q64.64 fixed-point as a Lean library for the cores</a></p></li>
<li><p><a href="../rfd/0034-deterministic-cores-integer-seeded-rng/README.html">RFD 0034: Deterministic cores via r128 fixed-point and seeded RNG</a></p></li>
</ul>
</section>

 ]]></description>
  <guid>https://v-sekai-multiplayer-fabric.github.io/manuals/decisions/20260611-deterministic-cores-integer-seeded-rng.html</guid>
  <pubDate>Thu, 11 Jun 2026 00:00:00 GMT</pubDate>
</item>
<item>
  <title>First-party curated content with zone-baker budgets</title>
  <link>https://v-sekai-multiplayer-fabric.github.io/manuals/decisions/20260611-first-party-curated-content-zone-baker-budgets.html</link>
  <description><![CDATA[ 





<p><em><a href="../index.html">Manuals</a> › <a href="../decisions.html">Decisions</a> › First-party curated content with zone-baker budgets</em></p>
<p><strong>Tier:</strong> baseline · <strong>Status:</strong> accepted</p>
<blockquote class="blockquote">
<p>Moved to <a href="../rfd/0035-first-party-curated-content-zone-baker-budgets/README.md">rfd/0035</a>.</p>
</blockquote>
<section id="context-and-problem-statement" class="level2">
<h2 class="anchored" data-anchor-id="context-and-problem-statement">Context and Problem Statement</h2>
<p>A user-generated-content runtime carries arbitrary, unpredictable per-frame cost, which is fatal on a mobile GPU holding a stereo VR frame. The MVP needs predictable cost and the freedom to co-optimize content against the engine.</p>
</section>
<section id="decision-outcome" class="level2">
<h2 class="anchored" data-anchor-id="decision-outcome">Decision Outcome</h2>
<p>Chosen option: ship first-party curated content only, with no user-generated-content runtime, and let the <code>zone-baker</code> enforce hard budgets at bake time, because curated content lets the engine co-optimize with the art and lets the baker reject anything over budget before it reaches a device.</p>
</section>
<section id="consequences" class="level2">
<h2 class="anchored" data-anchor-id="consequences">Consequences</h2>
<ul>
<li>A four-player Field room holds its geometry, four avatars, and props under roughly 500,000 visible triangles and 200 draw calls per eye on the standalone VR build.</li>
<li>The baker rejects any asset that exceeds the budget, so cost stays predictable.</li>
<li>The content surface stays small, which keeps the determinism and the budgeter tractable.</li>
</ul>
</section>
<section id="confirmation" class="level2">
<h2 class="anchored" data-anchor-id="confirmation">Confirmation</h2>
<p>Every shipped asset passes the <code>zone-baker</code> at budget, and a four-player room holds the per-eye limits on the standalone VR build.</p>


</section>
<section id="related" class="level2">
<h2 class="anchored" data-anchor-id="related">Related</h2>
<ul>
<li><p><a href="../changelog/2026/20260612-deck-log.html">V-Sekai Multiplayer Fabric Deck Log — 2026-06-12 (Friday)</a></p></li>
<li><p><a href="20260611-loot-action-core-loop-mvp-vertical-slice.html">Loot-action core-loop MVP vertical slice</a></p></li>
<li><p><a href="20260629-defer-loot-slice-hardening-until-needed.html">Defer the loot-slice hardening scope until its need arrives</a></p></li>
<li><p><a href="../rfd/0035-first-party-curated-content-zone-baker-budgets/README.html">RFD 0035: First-party curated content with zone-baker budgets</a></p></li>
</ul>
</section>

 ]]></description>
  <guid>https://v-sekai-multiplayer-fabric.github.io/manuals/decisions/20260611-first-party-curated-content-zone-baker-budgets.html</guid>
  <pubDate>Thu, 11 Jun 2026 00:00:00 GMT</pubDate>
</item>
<item>
  <title>Forward renderer with baked light for the mobile floor</title>
  <link>https://v-sekai-multiplayer-fabric.github.io/manuals/decisions/20260611-forward-renderer-baked-light.html</link>
  <description><![CDATA[ 





<p><em><a href="../index.html">Manuals</a> › <a href="../decisions.html">Decisions</a> › Forward renderer with baked light for the mobile floor</em></p>
<p><strong>Status:</strong> accepted</p>
<blockquote class="blockquote">
<p>Moved to <a href="../rfd/0036-forward-renderer-baked-light/README.md">rfd/0036</a>.</p>
</blockquote>
<section id="context-and-problem-statement" class="level2">
<h2 class="anchored" data-anchor-id="context-and-problem-statement">Context and Problem Statement</h2>
<p>The mobile tile renderer rations bandwidth across file, memory, and network IO, and deferred rendering competes for it. The slice needs predictable frame cost regardless of light count.</p>
</section>
<section id="considered-options" class="level2">
<h2 class="anchored" data-anchor-id="considered-options">Considered Options</h2>
<ul>
<li>Deferred rendering.</li>
<li>A simple forward renderer with baked global illumination.</li>
</ul>
</section>
<section id="decision-outcome" class="level2">
<h2 class="anchored" data-anchor-id="decision-outcome">Decision Outcome</h2>
<p>Chosen option: a simple forward renderer with baked global illumination and light probes for static geometry, a dedicated shadow pass for avatars, and probe lighting for dynamic entities, because the frame cost stays predictable regardless of light count. Deferred rendering pressures the bandwidth the mobile tile renderer rations.</p>
</section>
<section id="consequences" class="level2">
<h2 class="anchored" data-anchor-id="consequences">Consequences</h2>
<ul>
<li>Frame cost stays predictable, so artists place many lights without watching the budget.</li>
<li>Dynamic entities take lower-fidelity probe lighting.</li>
<li>The renderer removes one axis the small team otherwise tunes by hand.</li>
</ul>
</section>
<section id="confirmation" class="level2">
<h2 class="anchored" data-anchor-id="confirmation">Confirmation</h2>
<p>The Field room holds the frame floor on the standalone VR build with many lights placed.</p>


</section>
<section id="related" class="level2">
<h2 class="anchored" data-anchor-id="related">Related</h2>
<ul>
<li><p><a href="../changelog/2026/20260612-deck-log.html">V-Sekai Multiplayer Fabric Deck Log — 2026-06-12 (Friday)</a></p></li>
<li><p><a href="../rfd/0036-forward-renderer-baked-light/README.html">RFD 0036: Forward renderer with baked light for the mobile floor</a></p></li>
</ul>
</section>

 ]]></description>
  <guid>https://v-sekai-multiplayer-fabric.github.io/manuals/decisions/20260611-forward-renderer-baked-light.html</guid>
  <pubDate>Thu, 11 Jun 2026 00:00:00 GMT</pubDate>
</item>
<item>
  <title>Generated behavior runs as sandboxed RISC-V</title>
  <link>https://v-sekai-multiplayer-fabric.github.io/manuals/decisions/20260611-generated-behavior-sandboxed-riscv.html</link>
  <description><![CDATA[ 





<p><em><a href="../index.html">Manuals</a> › <a href="../decisions.html">Decisions</a> › Generated behavior runs as sandboxed RISC-V</em></p>
<p><strong>Status:</strong> accepted</p>
<blockquote class="blockquote">
<p>Moved to <a href="../rfd/0037-generated-behavior-sandboxed-riscv/README.md">rfd/0037</a>.</p>
</blockquote>
<section id="context-and-problem-statement" class="level2">
<h2 class="anchored" data-anchor-id="context-and-problem-statement">Context and Problem Statement</h2>
<p>Enemy and ability behavior may be generated, and running generated logic in-process risks the whole instance. The combat hexagon’s <code>behavior_source</code> port needs a safe implementation.</p>
</section>
<section id="decision-outcome" class="level2">
<h2 class="anchored" data-anchor-id="decision-outcome">Decision Outcome</h2>
<p>Chosen option: run generated enemy and art behavior as sandboxed RISC-V programs through <code>feat/sandbox</code>, implementing the combat <code>behavior_source</code> port, because the sandbox contains a misbehaving program rather than letting it take down the instance. This is the runtime guarantee under the bounded, declarative vocabulary the AI emits.</p>
</section>
<section id="consequences" class="level2">
<h2 class="anchored" data-anchor-id="consequences">Consequences</h2>
<ul>
<li>Generated logic is isolated, so a bad program degrades one entity rather than the instance.</li>
<li>Behavior is an adapter behind a port, so a fixture adapter replays scripted intents for CI.</li>
<li>The same port accepts hand-written and generated behavior.</li>
</ul>
</section>
<section id="confirmation" class="level2">
<h2 class="anchored" data-anchor-id="confirmation">Confirmation</h2>
<p>A misbehaving generated program is contained without taking down the instance, and the fixture adapter reproduces scripted intents with no sandbox.</p>


</section>
<section id="related" class="level2">
<h2 class="anchored" data-anchor-id="related">Related</h2>
<ul>
<li><p><a href="../changelog/2026/20260612-deck-log.html">V-Sekai Multiplayer Fabric Deck Log — 2026-06-12 (Friday)</a></p></li>
<li><p><a href="20260611-hexagon-combat-core.html">Combat hexagon — core, ports, and adapters</a></p></li>
<li><p><a href="../rfd/0037-generated-behavior-sandboxed-riscv/README.html">RFD 0037: Generated behavior runs as sandboxed RISC-V</a></p></li>
</ul>
</section>

 ]]></description>
  <guid>https://v-sekai-multiplayer-fabric.github.io/manuals/decisions/20260611-generated-behavior-sandboxed-riscv.html</guid>
  <pubDate>Thu, 11 Jun 2026 00:00:00 GMT</pubDate>
</item>
<item>
  <title>Genre-generic naming, no trademarked proper nouns</title>
  <link>https://v-sekai-multiplayer-fabric.github.io/manuals/decisions/20260611-genre-generic-naming-no-trademarks.html</link>
  <description><![CDATA[ 





<p><em><a href="../index.html">Manuals</a> › <a href="../decisions.html">Decisions</a> › Genre-generic naming, no trademarked proper nouns</em></p>
<p><strong>Status:</strong> accepted</p>
<blockquote class="blockquote">
<p>Moved to <a href="../rfd/0038-genre-generic-naming-no-trademarks/README.md">rfd/0038</a>.</p>
</blockquote>
<section id="context-and-problem-statement" class="level2">
<h2 class="anchored" data-anchor-id="context-and-problem-statement">Context and Problem Statement</h2>
<p>The core loop draws on a trademarked action RPG. The project reuses none of that game’s trademarks, so the docs and the game need a neutral vocabulary that carries the mechanics without the marks.</p>
</section>
<section id="decision-outcome" class="level2">
<h2 class="anchored" data-anchor-id="decision-outcome">Decision Outcome</h2>
<p>Chosen option: use genre-generic vocabulary throughout the docs, the filenames, and the game, because generic terms describe the mechanics while carrying no trademark.</p>
<p>The vocabulary: Hub for the social staging area, Field for a combat instance, melee, ranged, and caster for the archetypes, art for a learnable ability, affinity for the gating stat, and energy for the ability resource.</p>
</section>
<section id="consequences" class="level2">
<h2 class="anchored" data-anchor-id="consequences">Consequences</h2>
<ul>
<li>No trademarked proper nouns appear in filenames, docs, or content.</li>
<li>The docs read as their own product.</li>
<li>New writing reuses the same neutral terms, so the vocabulary stays consistent.</li>
</ul>
</section>
<section id="confirmation" class="level2">
<h2 class="anchored" data-anchor-id="confirmation">Confirmation</h2>
<p>A trademark scan over the project docs returns clean.</p>


</section>
<section id="related" class="level2">
<h2 class="anchored" data-anchor-id="related">Related</h2>
<ul>
<li><p><a href="../changelog/2026/20260612-deck-log.html">V-Sekai Multiplayer Fabric Deck Log — 2026-06-12 (Friday)</a></p></li>
<li><p><a href="../rfd/0038-genre-generic-naming-no-trademarks/README.html">RFD 0038: Genre-generic naming, no trademarked proper nouns</a></p></li>
</ul>
</section>

 ]]></description>
  <guid>https://v-sekai-multiplayer-fabric.github.io/manuals/decisions/20260611-genre-generic-naming-no-trademarks.html</guid>
  <pubDate>Thu, 11 Jun 2026 00:00:00 GMT</pubDate>
</item>
</channel>
</rss>
