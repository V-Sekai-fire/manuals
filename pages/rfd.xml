<?xml version="1.0" encoding="UTF-8"?>
<rss  xmlns:atom="http://www.w3.org/2005/Atom" 
      xmlns:media="http://search.yahoo.com/mrss/" 
      xmlns:content="http://purl.org/rss/1.0/modules/content/" 
      xmlns:dc="http://purl.org/dc/elements/1.1/" 
      version="2.0">
<channel>
<title>V-Sekai Fire — Manuals</title>
<link>https://v-sekai-multiplayer-fabric.github.io/multiplayer-fabric-manuals/pages/rfd.html</link>
<atom:link href="https://v-sekai-multiplayer-fabric.github.io/multiplayer-fabric-manuals/pages/rfd.xml" rel="self" type="application/rss+xml"/>
<description></description>
<generator>quarto-1.10.18</generator>
<lastBuildDate>Tue, 18 Aug 2026 01:10:59 GMT</lastBuildDate>
<item>
  <title>RFD 0123: A second WebTransport implementation, and the names that make room for it</title>
  <link>https://v-sekai-multiplayer-fabric.github.io/multiplayer-fabric-manuals/rfd/0123-a-second-webtransport-implementation/</link>
  <description><![CDATA[ 





<p><em><a href="../../index.html">Manuals</a> › <a href="../../pages/rfd.html">RFDs</a> › RFD 0123: A second WebTransport implementation, and the names that make room for it</em></p>
<p><strong>State:</strong> discussion · <strong>Scope:</strong> the WebTransport contract, who implements it, and what the transport repositories are called</p>
<section id="problem" class="level2">
<h2 class="anchored" data-anchor-id="problem">Problem</h2>
<p>The fabric states its wire in <code>contract-wt</code>, <code>contract-entity-packet</code>, <code>contract-connection-fsm</code> and <code>contract-http3-queue</code>, and implements it once. RFD 0088 chose that: “Client and server share one proven QUIC stack instead of two different, independently verified ones.” <code>transport-gateway</code>, <code>transport-ingest</code> and the engine’s <code>modules/http3</code> all vendor picoquic and picotls, so both ends of every session run the same code.</p>
<p>That buys interoperability by construction and gives up the check. A wire implemented once describes the program that implements it, and nothing establishes that the specification is implementable from the specification. Every assumption both ends share passes every test the pair can run. <code>contract-wt</code> already makes this argument for the engine’s H3 stack and acts on it in aioquic; nothing makes it for the transport layer.</p>
</section>
<section id="decision" class="level2">
<h2 class="anchored" data-anchor-id="decision">Decision</h2>
<p><strong>A second implementation, on a stack that shares no code with the first.</strong> <code>transport-gateway-python</code> and <code>transport-ingest-python</code> terminate the same contract on <code>aioquic</code>, which implements QUIC and TLS 1.3 in Python from the RFCs, and hand off over the iceoryx2 ring. Khronos ratifies against two independent implementations for this reason.</p>
<p>Three stacks were considered and the choice turns on what each shares with the first. Binding picoquic from Python was rejected because both ends would then run the same QUIC and the same TLS, so the second implementation would test the binding rather than the contract. h2o was rejected because its WebTransport is an unmerged pull request stacked on another unmerged pull request, with tests unchecked, against an HTTP/3 stack h2o calls experimental; its QUIC is quicly and would have qualified otherwise. <code>pywebtransport</code> shipped first here and was replaced: it refuses an EC private key, and <code>contract-wt</code> records the Godot demo server generating a fresh P-256 certificate on every run. <code>DETAILS.md</code> has the measurements.</p>
<p><strong>It carries no player traffic.</strong> <code>transport-gateway-c</code>’s <code>PACKET_PATH.md</code> measures a scripting runtime at 5.70 M/s against a 15 M/s bar, and 117.8 ns per runtime crossing against a 66.7 ns per-packet budget. Those numbers stand and they are why policy lives in an interactor rather than in the packet path. The Python pair produces agreement or disagreement, and no throughput claim goes in a record without a measurement in <code>data/measurements/</code>.</p>
<p><strong>The language is what tells the four repositories apart.</strong> <code>transport-gateway</code> becomes <code>transport-gateway-c</code> and <code>transport-ingest</code> becomes <code>transport-ingest-c</code>. Two implementations of one contract are distinguished only by how they are built, so RFD 0111’s preference for names that say what a thing does cannot separate them, and <code>transport-picoquic</code> is the precedent for naming a transport repository after its stack. Naming the language rather than the library also survives a change of library, which this record has already exercised: the Python pair moved from <code>pywebtransport</code> to <code>aioquic</code> before it merged and neither name moved. Neither pair keeps the unqualified name, because the unqualified name implies the other is a variant.</p>
<p><strong>The codec is emitted, never retyped.</strong> <code>contract-entity-packet</code> gains a Python emitter beside <code>EntityPacket/EmitC.lean</code>, so <code>lake exe packet_emit</code> writes the C header, the Python module and the 64 golden vectors from one run. A second implementation that reads the layout off the first tests whether two people can copy a table.</p>
<p><strong>The ring is reached through the <code>iceoryx2</code> wheel.</strong> The C repositories dlopen iceoryx2 through a dispatch table generated from <code>iceoryx2.sigs</code>, so that “the harness builds on a machine that has never seen iceoryx2” and because “iceoryx2 is Rust, and weft writes no Rust.” A prebuilt wheel is a runtime artifact rather than a build-graph edge and adds no second <code>.sigs</code> file. Bus access stays behind one module in each repository so the choice is reversible.</p>
<p><strong><code>contract-wt</code> stays where it is.</strong> It runs the same <code>aioquic</code> this pair now runs, and it checks a different subject: the engine’s <code>modules/http3</code> as a client, where these check the transport layer as a server. One Python stack in two places is less to keep current than two, and its roster client is what caught <code>WebTransportPeer</code> tracking clients in one bool. What it does not give is a third opinion, so a fault inside <code>aioquic</code> itself would go unseen by both.</p>
</section>
<section id="consequences" class="level2">
<h2 class="anchored" data-anchor-id="consequences">Consequences</h2>
<p>The implementation contradicted one claim the fabric makes about its own wire before it carried a byte of real traffic, which is the argument for it stated as evidence rather than as principle. <code>transport-fanout</code> caps a slice at 64 records and calls 6400 bytes “comfortably inside one message”; two stacks sharing no code put the limit at 11 records, and <code>datasource-queen</code>’s own <code>WT_MTU_MAX</code> agrees with them rather than with the comment.</p>
<p>That is one finding, not a pile of them, and the count is worth keeping honest. Building the pair also surfaced three defects in the libraries it uses — an EC key a stack would not load, a subscriber buffer that drops the oldest of three sends, a datagram queue that wedges on an oversized send. Those are real and cost real time, and a third implementation on a fourth stack would not have found any of them. <code>DETAILS.md</code> keeps them apart for that reason.</p>
<p>The cost is a fourth transport repository and a second wire reader to keep in step. RFD 0122 named that cost exactly — “every wire change is now two changes that must agree, and nothing checks that they do” — and rejected a second implementation carrying player traffic for it. Checking that they agree is what these two produce, so the cost lands where the benefit is.</p>
</section>
<section id="references" class="level2">
<h2 class="anchored" data-anchor-id="references">References</h2>
<ul>
<li>RFD 0088 chose one QUIC stack on both ends; RFD 0122 rejected a second implementation on the player path and is not contradicted; RFD 0047 abandoned <code>webtransportd</code> and still governs the production path; RFD 0111 sets the name shape this amends for four repositories</li>
<li>RFD 0049 for the reliability classes the two repositories split on, RFD 0096 for the measurement discipline, RFD 0108 for the rule that a lagging reader never stalls the writer</li>
<li><code>contract-entity-packet</code> for the layout, the emitter and the golden vectors; <code>contract-wt</code> for the same argument one stack earlier</li>
<li>The measurements, the method, and the rename list: <code>DETAILS.md</code></li>
</ul>
</section>
<section id="detail" class="level2">
<h2 class="anchored" data-anchor-id="detail">Detail</h2>
</section>
<section id="what-the-second-implementation-found" class="level2">
<h2 class="anchored" data-anchor-id="what-the-second-implementation-found">What the second implementation found</h2>
<p>One disagreement about the contract, and three integration defects found while building. The distinction matters, because only the first is the thing a second implementation exists to produce: it is a claim the fabric makes about its own wire, contradicted by two stacks that share no code. The other three are properties of the libraries this pair happens to use, and a third implementation on a fourth stack would not have found them.</p>
<p>All measured on 2026-08-17, macOS arm64, over a loopback session. Each is recorded in the <code>OPEN_GAPS.md</code> of the repository that found it.</p>
<section id="a-64-entity-slice-does-not-fit-in-one-datagram" class="level3">
<h3 class="anchored" data-anchor-id="a-64-entity-slice-does-not-fit-in-one-datagram">A 64-entity slice does not fit in one datagram</h3>
<p><code>transport-fanout/src/fanout.h:39</code> caps a subscriber’s slice and states the reason: “Cap one subscriber’s slice so a single write stays inside a datagram-sized batch. 64 entities * 100 bytes = 6400 bytes, comfortably inside one message.”</p>
<p>Two implementations sharing no code disagree with that comment and agree with each other, one fresh connection per size:</p>
<table class="caption-top table">
<colgroup>
<col style="width: 29%">
<col style="width: 20%">
<col style="width: 16%">
<col style="width: 32%">
</colgroup>
<thead>
<tr class="header">
<th>stack</th>
<th>largest datagram</th>
<th>whole records</th>
<th>failure above the limit</th>
</tr>
</thead>
<tbody>
<tr class="odd">
<td><code>aioquic</code> 1.3.0</td>
<td>1169 bytes</td>
<td>11</td>
<td>queues the frame and jams</td>
</tr>
<tr class="even">
<td><code>pywebtransport</code> 0.20.1</td>
<td>1161 bytes</td>
<td>11</td>
<td>refuses at the API</td>
</tr>
</tbody>
</table>
<p>The eight-byte difference is header overhead and both land on 11 records. The cause is structural rather than a library limit: a DATAGRAM frame has to fit inside one QUIC packet, and <code>aioquic</code> reports <code>_max_datagram_size = 1200</code> against a negotiated <code>_remote_max_datagram_frame_size = 65536</code>, so the packet size binds and the frame limit never does. 6400 bytes is five QUIC packets.</p>
<p><code>datasource-queen/src/wt.c:32</code> sets <code>WT_MTU_MAX</code> to 1300, which is 13 records, so the C side’s configuration contradicts the C side’s comment before either Python implementation is considered. <code>transport-fanout</code> has never run and <code>transport-ingest-c</code> has no <code>main</code>, so nothing had exercised the claim.</p>
<p><code>transport-ingest-python</code> splits a slice at 11 records so it runs. What stays open is whether <code>MAX_SLICE_ENTITIES</code> is wrong, whether slices belong on streams, or whether <code>fanout_one</code>’s silent truncation at 64 was always the real cap.</p>
</section>
<section id="a-library-defect-one-oversized-datagram-jams-every-datagram-after-it" class="level3">
<h3 class="anchored" data-anchor-id="a-library-defect-one-oversized-datagram-jams-every-datagram-after-it">A library defect: one oversized datagram jams every datagram after it</h3>
<p><code>aioquic</code>’s <code>_write_datagram_frame</code> asks the packet builder for room, and when the frame cannot fit, the caller breaks out of the send loop without popping the queue, so the oversized datagram stays at the head forever.</p>
<p>Measured: after one 6400-byte send, three subsequent 100-byte datagrams never arrived and the pending queue grew from one entry to three. One bad send does not lose one message, it ends that session’s datagram path.</p>
<p><code>transport-ingest-python</code> fragments against the connection and refuses anything over the cap, so it cannot trigger this. Reporting it upstream was considered and dropped: the trigger is an application sending a datagram larger than a packet, which RFC 9221 already forbids, so it is misuse handling rather than a protocol defect and it is not reachable from peer input. quiche exposes <code>dgram_max_writable_len</code> and quic-go returns a too-large error carrying the size, while aioquic documents no size contract and exposes no way to query one, which is why <code>datagram_capacity</code> reads private attributes.</p>
<p>This also contaminated a measurement. A binary search over delivery first returned 1050 bytes, because one oversized probe jammed the connection and every later size read as lost. Datagrams are unreliable, so a threshold cannot be probed on one connection at all, which is why the table above uses a fresh connection per size.</p>
</section>
<section id="a-library-limit-pywebtransport-rejects-ec-server-keys-and-that-decided-the-stack" class="level3">
<h3 class="anchored" data-anchor-id="a-library-limit-pywebtransport-rejects-ec-server-keys-and-that-decided-the-stack">A library limit: pywebtransport rejects EC server keys, and that decided the stack</h3>
<p><code>contract-wt/README.md</code> records the Godot demo server building “a fresh self-signed P-256 certificate on every run”. <code>pywebtransport</code> refuses to open a listener with one, failing with “failed to parse private key as RSA, ECDSA, or EdDSA”. PKCS#8 keys from LibreSSL 3.3.6:</p>
<table class="caption-top table">
<thead>
<tr class="header">
<th>key</th>
<th><code>pywebtransport</code> 0.20.1</th>
<th><code>aioquic</code> 1.3.0</th>
</tr>
</thead>
<tbody>
<tr class="odd">
<td>EC prime256v1 (P-256)</td>
<td>rejected</td>
<td>accepted</td>
</tr>
<tr class="even">
<td>EC secp384r1 (P-384)</td>
<td>rejected</td>
<td>accepted</td>
</tr>
<tr class="odd">
<td>RSA 2048</td>
<td>accepted</td>
<td>accepted</td>
</tr>
<tr class="even">
<td>Ed25519</td>
<td>untested</td>
<td>untested</td>
</tr>
</tbody>
</table>
<p>Ed25519 is untested on both because LibreSSL 3.3.6 answers <code>Unknown algorithm ed25519</code> and no key was produced to try.</p>
<p>Each end’s key is its own, so this never stopped the two talking. It stopped the Python pair serving a role the Godot side serves today, which is why the pair moved to <code>aioquic</code>. A live session with a P-256 server key answers CONNECT with <code>:status 200</code>.</p>
</section>
<section id="a-configuration-defect-a-default-iceoryx2-subscriber-drops-the-oldest-of-three-sends" class="level3">
<h3 class="anchored" data-anchor-id="a-configuration-defect-a-default-iceoryx2-subscriber-drops-the-oldest-of-three-sends">A configuration defect: a default iceoryx2 subscriber drops the oldest of three sends</h3>
<p><code>iceoryx2</code> defaults a subscriber’s buffer to <strong>2</strong> samples with safe overflow on. Three records published in a burst arrived as two, and the missing one was the oldest, before any reader was slow. Both repositories now set the buffer explicitly and keep safe overflow, because RFD 0108 requires that a lagging reader never stall the writer.</p>
<p>The drop stays undetectable. <code>HeaderPublishSubscribe</code> carries a node id, a publisher id and an element count, and no sequence number, so nothing distinguishes a dropped sample from one never sent. RFD 0108 says a subscriber whose cursor falls out of the ring “receives a resync signal rather than a gap”; no such signal exists, and defining one is a wire decision.</p>
</section>
</section>
<section id="the-renames" class="level2">
<h2 class="anchored" data-anchor-id="the-renames">The renames</h2>
<p>Two repositories renamed, two created. GitHub redirects the old names.</p>
<table class="caption-top table">
<colgroup>
<col style="width: 26%">
<col style="width: 35%">
<col style="width: 38%">
</colgroup>
<thead>
<tr class="header">
<th>before</th>
<th>after</th>
<th>path</th>
</tr>
</thead>
<tbody>
<tr class="odd">
<td><code>transport-gateway</code></td>
<td><code>transport-gateway-c</code></td>
<td><code>1-transport/gateway-c</code></td>
</tr>
<tr class="even">
<td><code>transport-ingest</code></td>
<td><code>transport-ingest-c</code></td>
<td><code>1-transport/ingest-c</code></td>
</tr>
<tr class="odd">
<td>—</td>
<td><code>transport-gateway-python</code></td>
<td><code>1-transport/gateway-python</code></td>
</tr>
<tr class="even">
<td>—</td>
<td><code>transport-ingest-python</code></td>
<td><code>1-transport/ingest-python</code></td>
</tr>
</tbody>
</table>
<p><code>check_path_recomposes</code> in <code>check_docs.py</code> requires the directory and its child to rebuild the repository name, and all four do. The manifest count moves from 45 to 47.</p>
<p>Both C repositories carried a description using a word RFD 0111 retired — “hands the result to a <strong>plane</strong> over iceoryx2” — because the READMEs were converted and the GitHub descriptions were missed. Both now say interactor.</p>
<p><code>check_docs.py</code>’s moved-repository check found eight stale references in six files across five repositories, each fixed in its own pull request: <code>transport-fanout</code>, <code>transport-gateway-c</code>, <code>transport-ingest-c</code>, <code>interactor-ward</code>, <code>entities-gyre</code> and <code>datasource-queen</code>.</p>
</section>
<section id="why-the-language-and-not-the-library" class="level2">
<h2 class="anchored" data-anchor-id="why-the-language-and-not-the-library">Why the language and not the library</h2>
<p>RFD 0111 retires names that say how a thing was built, and <code>-c</code> and <code>-python</code> are exactly that. The rule targets directories that collect whatever nobody classified: <code>service/</code>, <code>lean/</code>, <code>engine/</code>, <code>vendor/</code>. Two repositories implementing one contract are a different case, because the build is the only thing that separates them and the role word is already taken by both.</p>
<p><code>transport-picoquic</code>, which RFD 0111 renamed from <code>fabric-edge</code>, is the precedent for naming a transport repository after its stack, taken for the same reason: “Bare <code>transport</code> would read as a name that went missing.”</p>
<p>The language outlives the library. Swapping picoquic for another C QUIC stack, or <code>pywebtransport</code> for aioquic, leaves both names true, where <code>-picoquic</code> and <code>-pywebtransport</code> would strand.</p>
</section>
<section id="what-is-built" class="level2">
<h2 class="anchored" data-anchor-id="what-is-built">What is built</h2>
<p>Both repositories carry the conformance gate and the terminator, on <code>aioquic</code> 1.3.0 and <code>iceoryx2</code> 0.9.3, and neither carries a cross-test.</p>
<p><code>conformance.py</code> decodes all 64 golden vectors, checks the fields the CSV names, and re-encodes to compare byte for byte. <code>--self-test</code> corrupts one vector and the gate is wrong if that passes. CI runs both, on every pull request and in the merge group.</p>
<p><code>transport-ingest-python</code> was driven end to end on a live session with an EC P-256 server key: CONNECT answered <code>:status 200</code>, three records in one datagram reached the ring byte-identical to the wire, and a 250-byte datagram was dropped whole rather than trimmed to two records.</p>
<p>The live cross-test against the C pair is not written, and there is nothing yet to write it against. <code>transport-gateway-c</code> and <code>transport-ingest-c</code> have no <code>main</code> and no <code>CMakeLists.txt</code>, and both READMEs say “State: not started”. <code>transport-ingest-c</code>’s copy of the transport code is a stale fork of <code>transport-gateway-c</code>’s: it passes an <code>h3zero_callback_ctx_t</code> where <code>h3zero_callback</code> casts to <code>picohttp_server_parameters_t</code>, and it lacks the <code>h3zero_declare_stream_prefix</code> call. That divergence is the reason both Python repositories consume one emitted codec rather than two copies.</p>


</section>

 ]]></description>
  <guid>https://v-sekai-multiplayer-fabric.github.io/multiplayer-fabric-manuals/rfd/0123-a-second-webtransport-implementation/</guid>
  <pubDate>Tue, 18 Aug 2026 01:10:59 GMT</pubDate>
</item>
<item>
  <title>RFD 0122: The client is native, from entities-godot’s gyre branch</title>
  <link>https://v-sekai-multiplayer-fabric.github.io/multiplayer-fabric-manuals/rfd/0122-the-client-is-native/</link>
  <description><![CDATA[ 





<p><strong>State:</strong> published · <strong>Scope:</strong> the Queen of the Gyre, and what a player runs to reach her</p>
<section id="problem" class="level2">
<h2 class="anchored" data-anchor-id="problem">Problem</h2>
<p>RFD 0112 put the Queen’s client in a browser and chose Lexical for the slash command field, which was the hard half and was built. The field was the wrong thing to optimise for.</p>
<p>A browser client is a second implementation of everything below that field. <code>client/src/cbor.js</code> decodes what the ward publishes in a language sharing nothing with the decoder the rest of the fabric uses; it reaches the ward over a WebTransport stack merely compatible with the ward’s; it carries npm, esbuild, and Playwright, which nothing else here needs; and it cannot enter XR, the point of the setting.</p>
</section>
<section id="decision" class="level2">
<h2 class="anchored" data-anchor-id="decision">Decision</h2>
<p>The client is a Godot build from <code>entities-godot</code>, branch <code>gyre</code> — one of five entries in the <code>fabric</code> manifest’s <code>default.xml</code> that track something other than <code>main</code>.</p>
<p>The branch carries the parts already: <code>modules/http3</code> gives <code>WebTransportPeer</code> over the same picoquic backend <code>queen</code>’s <code>src/wt.c</code> terminates and <code>transport-gateway</code> vendors, <code>modules/xr_grid</code> gives the 100-byte <code>XRGridEntityPacket</code> that <code>lean-entity-packet</code> specifies, and <code>modules/multiplayer_fabric</code> gives <code>FabricMultiplayerPeer</code>. This adds no engine code; it decides which client is the client. <code>DETAILS.md</code> has the module mapping.</p>
<p>The slash command field becomes a <code>Control</code>, so RFD 0112’s hard half stops being hard: a caret in a <code>contenteditable</code> is a browser problem, and there is no browser. The ward does not change — <code>queen serve</code> already speaks WebTransport. What changes is who connects.</p>
</section>
<section id="references" class="level2">
<h2 class="anchored" data-anchor-id="references">References</h2>
<ul>
<li>RFD 0112 chose the browser client and is abandoned; RFD 0085 holds the setting; RFD 0111 sets the words transport layer and service; RFD 0047 is why the Queen terminates QUIC herself</li>
<li><code>lean-entity-packet</code> specifies the 100-byte packet both ends now share</li>
<li>The module mapping, the transport path, and what happens to the web client: <code>DETAILS.md</code></li>
</ul>
</section>
<section id="detail" class="level2">
<h2 class="anchored" data-anchor-id="detail">Detail</h2>
</section>
<section id="context-and-problem-statement" class="level2">
<h2 class="anchored" data-anchor-id="context-and-problem-statement">Context and problem statement</h2>
<p>RFD 0112 read the problem as a text field. <code>service-store/src/queen.c</code> had no client at all when it was written, and the visible gap was that a player had nowhere to type. So the document spent its length on the field: a decorator node is one block to the caret, an atom node in ProseMirror can hold the caret and needs a plugin to correct it, and Meta tests the mobile and IME paths at scale. Lexical was the right answer to that question.</p>
<p>The question aged badly. <code>queen</code> now has <code>serve</code>, <code>src/wt.c</code>, and <code>src/transport_tcp.c</code>; the ward is entities with places, and a subscriber’s slice is decided by an interest filter. The field is the smallest part of a client for that. What matters is who decodes the packet, which QUIC implementation carries it, and whether a player can stand in the zone rather than look at it.</p>
</section>
<section id="what-the-browser-client-costs" class="level2">
<h2 class="anchored" data-anchor-id="what-the-browser-client-costs">What the browser client costs</h2>
<p>Three costs, none of which is the field.</p>
<p>A second decoder. <code>client/src/cbor.js</code> is a JavaScript implementation of what the ward publishes. Every wire change is now two changes that must agree, and nothing checks that they do. <code>lean-entity-packet</code> models the packet in Lean with a <code>packet_golden.csv</code> of canonical bytes, and the JavaScript is not held to those vectors.</p>
<p>A second transport. The browser’s WebTransport is whatever the browser ships. <code>queen</code> terminates QUIC with picoquic and picotls, and <code>transport-gateway</code> vendors both precisely so that one implementation and one TLS library run on both ends. A browser client gives that up and replaces it with compatibility, which is a thing you find out about in the field.</p>
<p>No XR. RFD 0085’s setting is a ring station a player is inside. A browser field can show a slash command and cannot put anybody anywhere.</p>
</section>
<section id="the-parts-already-on-the-branch" class="level2">
<h2 class="anchored" data-anchor-id="the-parts-already-on-the-branch">The parts already on the branch</h2>
<p><code>entities-godot</code> at <code>gyre</code> carries the client’s whole stack as engine modules. This decision adds no engine code; it selects.</p>
<table class="caption-top table">
<colgroup>
<col style="width: 27%">
<col style="width: 47%">
<col style="width: 25%">
</colgroup>
<thead>
<tr class="header">
<th>Module</th>
<th>What it provides</th>
<th>What it replaces</th>
</tr>
</thead>
<tbody>
<tr class="odd">
<td><code>modules/http3</code></td>
<td><code>WebTransportPeer</code>, <code>quic_picoquic_backend.cpp</code></td>
<td>the browser’s WebTransport</td>
</tr>
<tr class="even">
<td><code>modules/xr_grid</code></td>
<td><code>XRGridEntityPacket</code></td>
<td><code>client/src/cbor.js</code></td>
</tr>
<tr class="odd">
<td><code>modules/multiplayer_fabric</code></td>
<td><code>FabricMultiplayerPeer</code>, zone, snapshot, journal</td>
<td><code>client/src/main.js</code> state</td>
</tr>
<tr class="even">
<td><code>modules/sqlite</code></td>
<td>a local store for what the client caches</td>
<td>nothing; new</td>
</tr>
</tbody>
</table>
<p><code>FabricMultiplayerPeer</code> documents WebTransport as its example backend and notes that a peer must open independent streams per packet, which WebTransport does. That is the same property <code>service-store/CLAUDE.md</code> states from the server side: a datagram is one message and a stream FIN is the boundary, so no framing layer exists on either end.</p>
</section>
<section id="the-transport-path" class="level2">
<h2 class="anchored" data-anchor-id="the-transport-path">The transport path</h2>
<p>A player’s client opens <code>WebTransportPeer.create_client(host, port, "/wt")</code> against the port <code>queen serve</code> binds. <code>src/wt.c</code> terminates the session in the Queen’s own process — no <code>webtransportd</code>, per RFD 0047 and the rule in <code>CLAUDE.md</code>. Entity state arrives as <code>XRGridEntityPacket</code>, 100 bytes, delta coded, filtered by interest before it is sent.</p>
<p>The client is therefore a subscriber and nothing more privileged. <code>Weft.Authority</code> decides which controller drives an avatar, the ward checks a command’s rebac relations on receipt, and a native client has no more standing than a browser one did.</p>
</section>
<section id="what-happens-to-the-web-client" class="level2">
<h2 class="anchored" data-anchor-id="what-happens-to-the-web-client">What happens to the web client</h2>
<p><code>service-store/client</code> is not deleted by this document. It is four source files, two Playwright specs, and three npm dependencies, and none of it is on the CI path — <code>ci/inside.sh</code> runs <code>queen</code> and never runs npm. It can be removed in a change of its own, which is where the argument about whether to keep a browser build as a fallback belongs.</p>
<p>What this document settles is that it is not the client. Nothing new is written against it, and the slash command surface is built as a Godot <code>Control</code> against the modules above.</p>
</section>
<section id="hazards" class="level2">
<h2 class="anchored" data-anchor-id="hazards">Hazards</h2>
<p>The build is the cost. A native client is compiled per platform from an engine fork, and a browser client is served. <code>fabric-godot-assembly</code> and <code>godot-images</code> exist for exactly this, so the cost is known rather than new, but it is real and it is paid on every platform.</p>
<p>The branch is a fork of a fork. <code>gyre</code> sits on top of <code>multiplayer-fabric</code>, which merges fourteen feature branches on top of Godot’s <code>master</code>. A client pinned there inherits that merge surface. The manifest states the branch, which is the minimum; nothing here pins a commit.</p>
<p>The field regresses before it improves. RFD 0112’s field works today. A <code>Control</code> that shows a parameter as an inline block has to be written, and the first version of it will be worse than the Lexical one. That is accepted for the same reason as the build cost: the field was never the part that determined whether this client was right.</p>
</section>
<section id="consequences" class="level2">
<h2 class="anchored" data-anchor-id="consequences">Consequences</h2>
<p>RFD 0112 is abandoned. <code>service-store/client</code> — Lexical, esbuild, the Playwright specs and the JavaScript decoder — stops being the client. Removing it is its own change in its own repository and is not assumed by this document.</p>
<p>The cost is a build. A browser client is served; a native client is compiled per platform from an engine fork, which is slower to produce and heavier to distribute. That is accepted, because the alternative was paying for a second decoder and a second transport forever to avoid paying for a build once.</p>


</section>

 ]]></description>
  <guid>https://v-sekai-multiplayer-fabric.github.io/multiplayer-fabric-manuals/rfd/0122-the-client-is-native/</guid>
  <pubDate>Tue, 18 Aug 2026 01:10:59 GMT</pubDate>
</item>
<item>
  <title>RFD 0121: Amend RFD 0111 against the tree</title>
  <link>https://v-sekai-multiplayer-fabric.github.io/multiplayer-fabric-manuals/rfd/0121-amend-rfd-0111-against-the-tree/</link>
  <description><![CDATA[ 





<p><em><a href="../../index.html">Manuals</a> › <a href="../../pages/rfd.html">RFDs</a> › RFD 0121: Amend RFD 0111 against the tree</em></p>
<p><strong>State:</strong> discussion · <strong>Scope:</strong> architecture vocabulary across all repositories</p>
<section id="problem" class="level2">
<h2 class="anchored" data-anchor-id="problem">Problem</h2>
<p>RFD 0111 is executed: the thirty renames landed, and GitHub redirects each old name. Four of its statements disagree with the organisation and the checkout today, and each reads as a survey, so a wrong one names a thing that is not there.</p>
</section>
<section id="decision" class="level2">
<h2 class="anchored" data-anchor-id="decision">Decision</h2>
<p>Amend RFD 0111 in four places. <code>DETAILS.md</code> holds the evidence for each, with the command that produces it and the date it was run.</p>
<table class="caption-top table">
<colgroup>
<col style="width: 24%">
<col style="width: 46%">
<col style="width: 28%">
</colgroup>
<thead>
<tr class="header">
<th>the statement</th>
<th>the tree on 2026-08-16</th>
<th>the amendment</th>
</tr>
</thead>
<tbody>
<tr class="odd">
<td>the word <code>fabric</code> is in one name</td>
<td>fifteen names carry the prefix, six are live and owned, and 0111 names one</td>
<td>claim the five that remain, and list them</td>
</tr>
<tr class="even">
<td><code>entities-godot</code> holds entities</td>
<td>the Godot engine: <code>SConstruct</code>, <code>editor/</code>, <code>drivers/</code>, <code>servers/</code></td>
<td>move it to the names the shape does not decide</td>
</tr>
<tr class="odd">
<td><code>ports/</code> becomes <code>repositories/</code></td>
<td><code>repository</code> is the one collision RFD 0111 creates</td>
<td>rename to <code>repository/</code>, singular</td>
</tr>
<tr class="even">
<td>five READMEs describe absent directories</td>
<td>two of the five have a README, and rebac’s triad is real one namespace down</td>
<td>correct shared’s, qualify rebac’s paths</td>
</tr>
</tbody>
</table>
<p>A fifth claim was checked and withdrawn: repositories named <code>loot</code>, <code>combat</code>, and <code>progression</code> do hold the RFD 0028 triad, so that statement stands.</p>
<p>The conversion table, the retired deployment words, the type-first name shape, the thirty renames, and the case for “interactor” all hold, unchanged by this RFD.</p>
</section>
<section id="references" class="level2">
<h2 class="anchored" data-anchor-id="references">References</h2>
<ul>
<li>RFD 0111, amended by this RFD. RFD 0028, for the triad this RFD keeps naming</li>
<li>RFD 0000, for the rule that one fact has one source, and that it is not prose</li>
<li>The survey, the commands that produced it, and the counts: <code>DETAILS.md</code></li>
</ul>
</section>
<section id="detail" class="level2">
<h2 class="anchored" data-anchor-id="detail">Detail</h2>
</section>
<section id="how-this-was-checked" class="level2">
<h2 class="anchored" data-anchor-id="how-this-was-checked">How this was checked</h2>
<p>Every count below comes from the GitHub API or from a checkout on disk, on 2026-08-16, against <code>repo</code> manifest revision <code>main</code>. Each claim states the command that produces it, so a later reader can re-run it rather than trust the number.</p>
<p>The renames of RFD 0111 have landed. GitHub answers the old names with the new ones:</p>
<div class="code-copy-outer-scaffold"><div class="sourceCode" id="cb1" style="background: #f1f3f5;"><pre class="sourceCode sh code-with-copy"><code class="sourceCode bash"><span id="cb1-1"><span class="ex" style="color: null;
background-color: null;
font-style: inherit;">gh</span> api repos/v-sekai-multiplayer-fabric/fabric-authority-plane <span class="at" style="color: #657422;
background-color: null;
font-style: inherit;">--jq</span> .full_name</span>
<span id="cb1-2"><span class="co" style="color: #5E5E5E;
background-color: null;
font-style: inherit;"># v-sekai-multiplayer-fabric/interactor-authority</span></span>
<span id="cb1-3"><span class="ex" style="color: null;
background-color: null;
font-style: inherit;">gh</span> api repos/v-sekai-multiplayer-fabric/fabric-interactor <span class="at" style="color: #657422;
background-color: null;
font-style: inherit;">--jq</span> .full_name</span>
<span id="cb1-4"><span class="co" style="color: #5E5E5E;
background-color: null;
font-style: inherit;"># v-sekai-multiplayer-fabric/contract-command</span></span>
<span id="cb1-5"><span class="ex" style="color: null;
background-color: null;
font-style: inherit;">gh</span> api repos/v-sekai-multiplayer-fabric/fabric-service-meta <span class="at" style="color: #657422;
background-color: null;
font-style: inherit;">--jq</span> .full_name</span>
<span id="cb1-6"><span class="co" style="color: #5E5E5E;
background-color: null;
font-style: inherit;"># v-sekai-multiplayer-fabric/fabric</span></span></code></pre></div></div>
</section>
<section id="the-prefix-survives-in-five-live-names" class="level2">
<h2 class="anchored" data-anchor-id="the-prefix-survives-in-five-live-names">The prefix survives in five live names</h2>
<div class="code-copy-outer-scaffold"><div class="sourceCode" id="cb2" style="background: #f1f3f5;"><pre class="sourceCode sh code-with-copy"><code class="sourceCode bash"><span id="cb2-1"><span class="ex" style="color: null;
background-color: null;
font-style: inherit;">gh</span> api <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"orgs/v-sekai-multiplayer-fabric/repos?per_page=100"</span> <span class="at" style="color: #657422;
background-color: null;
font-style: inherit;">--paginate</span> <span class="at" style="color: #657422;
background-color: null;
font-style: inherit;">--jq</span> <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">'.[].name'</span> <span class="kw" style="color: #003B4F;
background-color: null;
font-weight: bold;
font-style: inherit;">|</span> <span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">grep</span> <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">'^fabric-'</span></span></code></pre></div></div>
<p>Fifteen names carry the prefix. The bare <code>fabric</code> is the sixteenth match for <code>^fabric</code> and is the name RFD 0111 frees, so it is excluded here.</p>
<table class="caption-top table">
<colgroup>
<col style="width: 46%">
<col style="width: 12%">
<col style="width: 6%">
<col style="width: 34%">
</colgroup>
<thead>
<tr class="header">
<th>name</th>
<th>archived</th>
<th>fork</th>
<th>reaches the convention</th>
</tr>
</thead>
<tbody>
<tr class="odd">
<td><code>fabric-ardy</code></td>
<td>no</td>
<td>yes</td>
<td>no, upstream name</td>
</tr>
<tr class="even">
<td><code>fabric-casync-central</code></td>
<td>yes</td>
<td>no</td>
<td>no, archived</td>
</tr>
<tr class="odd">
<td><code>fabric-container-verify</code></td>
<td>yes</td>
<td>no</td>
<td>no, archived</td>
</tr>
<tr class="even">
<td><code>fabric-game-observability</code></td>
<td>no</td>
<td>no</td>
<td>yes</td>
</tr>
<tr class="odd">
<td><code>fabric-godot-assembly</code></td>
<td>no</td>
<td>no</td>
<td>yes</td>
</tr>
<tr class="even">
<td><code>fabric-godot-packaging</code></td>
<td>yes</td>
<td>no</td>
<td>no, archived</td>
</tr>
<tr class="odd">
<td><code>fabric-harness</code></td>
<td>no</td>
<td>no</td>
<td>yes, deferred by 0111</td>
</tr>
<tr class="even">
<td><code>fabric-locomotion-generator</code></td>
<td>no</td>
<td>yes</td>
<td>no, upstream name</td>
</tr>
<tr class="odd">
<td><code>fabric-motion-diffusion</code></td>
<td>no</td>
<td>yes</td>
<td>no, upstream name</td>
</tr>
<tr class="even">
<td><code>fabric-platform-central</code></td>
<td>yes</td>
<td>no</td>
<td>no, archived</td>
</tr>
<tr class="odd">
<td><code>fabric-quickstart</code></td>
<td>no</td>
<td>no</td>
<td>yes</td>
</tr>
<tr class="even">
<td><code>fabric-scoop-central</code></td>
<td>yes</td>
<td>no</td>
<td>no, archived</td>
</tr>
<tr class="odd">
<td><code>fabric-stage-runtime</code></td>
<td>no</td>
<td>no</td>
<td>yes</td>
</tr>
<tr class="even">
<td><code>fabric-wholebody-control</code></td>
<td>no</td>
<td>yes</td>
<td>no, upstream name</td>
</tr>
<tr class="odd">
<td><code>fabric-wt-harness</code></td>
<td>no</td>
<td>no</td>
<td>yes</td>
</tr>
</tbody>
</table>
<p>Five archived and four forks leave six live and owned. RFD 0111 names <code>fabric-harness</code> and says it becomes <code>contract-bus</code> when that pass is taken, which leaves five the RFD does not mention at all.</p>
<p>Two of the five sit in the <code>repo</code> manifest and check out on every <code>repo sync</code>: <code>fabric-godot-assembly</code> and <code>fabric-wt-harness</code>. A reader of the workspace root sees the retired prefix on the first listing.</p>
</section>
<section id="entities-godot-is-the-engine" class="level2">
<h2 class="anchored" data-anchor-id="entities-godot-is-the-engine"><code>entities-godot</code> is the engine</h2>
<div class="code-copy-outer-scaffold"><div class="sourceCode" id="cb3" style="background: #f1f3f5;"><pre class="sourceCode sh code-with-copy"><code class="sourceCode bash"><span id="cb3-1"><span class="ex" style="color: null;
background-color: null;
font-style: inherit;">gh</span> api repos/v-sekai-multiplayer-fabric/entities-godot/contents <span class="at" style="color: #657422;
background-color: null;
font-style: inherit;">--jq</span> <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">'[.[].name]|join(" ")'</span></span></code></pre></div></div>
<pre><code>.clang-format .clang-tidy .clangd .editorconfig .git-blame-ignore-revs
.gitattributes .github .gitignore .mailmap .pre-commit-config.yaml AUTHORS.md
CHANGELOG.md CONTRIBUTING.md COPYRIGHT.txt DONORS.md LICENSE.txt README.md
SConstruct core doc drivers editor gles3_builders.py glsl_builders.py main
methods.py misc modules platform platform_methods.py pyproject.toml scene
scu_builders.py servers tests thirdparty version.py</code></pre>
<p><code>SConstruct</code>, <code>editor/</code>, <code>drivers/</code>, <code>platform/</code>, <code>servers/</code>, <code>modules/</code>, and <code>thirdparty/</code> are the Godot engine tree, 1 708 369 KB of C++ by the API’s own size field. The Netflix definition of an entity is a domain object that has no knowledge of where it is stored. An engine that renders, builds, and ships an editor holds many things, and a set of such domain objects is a small part of none of the directories above.</p>
<p>RFD 0111 defends this name in one paragraph, and the paragraph argues a different point: that the <code>core/</code> directory inside the repository comes from upstream and must keep its name. That reasoning is sound and this RFD keeps it. It says nothing about whether <code>entities-</code> describes the repository.</p>
<p>The test RFD 0111 applies elsewhere is whether a name asserts something false. <code>fabric-service-meta</code> was rejected as <code>service-meta</code> because “<code>service-meta</code> would assert the thing that is false”. <code>contract-command</code> was chosen over <code>interactor-anything</code> because the latter “picks the side the git repository exists to avoid picking”. <code>entities-godot</code> fails the same test and is the seventh name the shape does not decide.</p>
</section>
<section id="repositories-puts-the-created-collision-in-the-tree" class="level2">
<h2 class="anchored" data-anchor-id="repositories-puts-the-created-collision-in-the-tree"><code>repositories/</code> puts the created collision in the tree</h2>
<p>RFD 0111 renames the RFD 0028 triad in three git repositories. Those three exist and hold the triad, which the API confirms:</p>
<div class="code-copy-outer-scaffold"><div class="sourceCode" id="cb5" style="background: #f1f3f5;"><pre class="sourceCode sh code-with-copy"><code class="sourceCode bash"><span id="cb5-1"><span class="cf" style="color: #003B4F;
background-color: null;
font-weight: bold;
font-style: inherit;">for</span> r <span class="kw" style="color: #003B4F;
background-color: null;
font-weight: bold;
font-style: inherit;">in</span> loot combat progression<span class="kw" style="color: #003B4F;
background-color: null;
font-weight: bold;
font-style: inherit;">;</span> <span class="cf" style="color: #003B4F;
background-color: null;
font-weight: bold;
font-style: inherit;">do</span></span>
<span id="cb5-2">  <span class="ex" style="color: null;
background-color: null;
font-style: inherit;">gh</span> api <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"repos/v-sekai-multiplayer-fabric/</span><span class="va" style="color: #111111;
background-color: null;
font-style: inherit;">$r</span><span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">/contents"</span> <span class="at" style="color: #657422;
background-color: null;
font-style: inherit;">--jq</span> <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">'[.[]|select(.type=="dir").name]|join(" ")'</span></span>
<span id="cb5-3"><span class="cf" style="color: #003B4F;
background-color: null;
font-weight: bold;
font-style: inherit;">done</span></span>
<span id="cb5-4"><span class="co" style="color: #5E5E5E;
background-color: null;
font-style: inherit;"># loot:        .github adapters core ports</span></span>
<span id="cb5-5"><span class="co" style="color: #5E5E5E;
background-color: null;
font-style: inherit;"># combat:      adapters core ports</span></span>
<span id="cb5-6"><span class="co" style="color: #5E5E5E;
background-color: null;
font-style: inherit;"># progression: adapters core ports</span></span></code></pre></div></div>
<p>RFD 0111 states the resolution for the word it takes from Netflix: write “git repository” in full, and keep “repository” alone for the interface. It chooses that over keeping Cockburn’s “port” on the ground that “a collision in prose costs less than a collision in code”.</p>
<p>A directory named <code>repositories/</code> sits in the tree of a git repository, which is the place the RFD ranks as the more expensive of the two. The plural reads as a collection of checkouts, which is the meaning the RFD spends the section pushing away. The singular <code>repository/</code> reads as the interface, matches <code>core/</code> and <code>ports/</code> in number, and costs nothing to adopt because the rename has not run yet.</p>
<p>The other two renames in that triad stand. <code>core/</code> to <code>entities/</code> and <code>adapters/</code> to <code>datasources/</code> carry no collision.</p>
<p>Three git repositories named <code>loot</code>, <code>combat</code>, and <code>progression</code> are also absent from the rename list and from the <code>repo</code> manifest, and their names state no type. Whether the type-first convention reaches them is a question this RFD raises and does not answer, because the answer decides three more names.</p>
</section>
<section id="the-gap-is-two-readmes-rather-than-five" class="level2">
<h2 class="anchored" data-anchor-id="the-gap-is-two-readmes-rather-than-five">The gap is two READMEs rather than five</h2>
<p>RFD 0111 says the <code>lean-*-core</code> READMEs state a hexagon layout of <code>core/</code>, <code>ports/</code>, and <code>adapters/</code>, that those directories do not exist, and that each of those git repositories holds one Lean namespace directory. The checkout disagrees in two of the five.</p>
<div class="code-copy-outer-scaffold"><div class="sourceCode" id="cb6" style="background: #f1f3f5;"><pre class="sourceCode sh code-with-copy"><code class="sourceCode bash"><span id="cb6-1"><span class="cf" style="color: #003B4F;
background-color: null;
font-weight: bold;
font-style: inherit;">for</span> d <span class="kw" style="color: #003B4F;
background-color: null;
font-weight: bold;
font-style: inherit;">in</span> entities-lean-<span class="pp" style="color: #AD0000;
background-color: null;
font-style: inherit;">*</span><span class="kw" style="color: #003B4F;
background-color: null;
font-weight: bold;
font-style: inherit;">;</span> <span class="cf" style="color: #003B4F;
background-color: null;
font-weight: bold;
font-style: inherit;">do</span></span>
<span id="cb6-2">  <span class="bu" style="color: null;
background-color: null;
font-style: inherit;">printf</span> <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">'%-28s '</span> <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"</span><span class="va" style="color: #111111;
background-color: null;
font-style: inherit;">$d</span><span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"</span></span>
<span id="cb6-3">  <span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">find</span> <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"</span><span class="va" style="color: #111111;
background-color: null;
font-style: inherit;">$d</span><span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"</span> <span class="at" style="color: #657422;
background-color: null;
font-style: inherit;">-type</span> d <span class="at" style="color: #657422;
background-color: null;
font-style: inherit;">-not</span> <span class="at" style="color: #657422;
background-color: null;
font-style: inherit;">-path</span> <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">'*/.git/*'</span> <span class="at" style="color: #657422;
background-color: null;
font-style: inherit;">-not</span> <span class="at" style="color: #657422;
background-color: null;
font-style: inherit;">-name</span> .git <span class="kw" style="color: #003B4F;
background-color: null;
font-weight: bold;
font-style: inherit;">|</span> <span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">sed</span> <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"s|^</span><span class="va" style="color: #111111;
background-color: null;
font-style: inherit;">$d</span><span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">/||"</span> <span class="kw" style="color: #003B4F;
background-color: null;
font-weight: bold;
font-style: inherit;">|</span> <span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">tr</span> <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">'\n'</span> <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">' '</span></span>
<span id="cb6-4">  <span class="bu" style="color: null;
background-color: null;
font-style: inherit;">echo</span></span>
<span id="cb6-5"><span class="cf" style="color: #003B4F;
background-color: null;
font-weight: bold;
font-style: inherit;">done</span></span></code></pre></div></div>
<table class="caption-top table">
<colgroup>
<col style="width: 35%">
<col style="width: 64%">
</colgroup>
<thead>
<tr class="header">
<th>git repository</th>
<th>the directories it holds</th>
</tr>
</thead>
<tbody>
<tr class="odd">
<td><code>entities-lean-combat</code></td>
<td><code>CombatCore</code></td>
</tr>
<tr class="even">
<td><code>entities-lean-loot</code></td>
<td><code>r128gpu</code> <code>r128test</code> <code>parity</code> <code>LootCore</code> <code>.github</code></td>
</tr>
<tr class="odd">
<td><code>entities-lean-progression</code></td>
<td><code>ProgressionCore</code></td>
</tr>
<tr class="even">
<td><code>entities-lean-rebac</code></td>
<td><code>Rebac</code> <code>Rebac/core</code> <code>Rebac/ports</code> <code>.github</code></td>
</tr>
<tr class="odd">
<td><code>entities-lean-shared</code></td>
<td><code>Shared</code> <code>.github</code></td>
</tr>
</tbody>
</table>
<p><code>entities-lean-rebac</code> holds <code>core/</code> and <code>ports/</code>, one namespace directory down, and both carry Lean sources: <code>Rebac/core/NoGod.lean</code>, <code>Rebac/core/ReBAC.lean</code>, and <code>Rebac/ports/AuthQuery.lean</code>. Its README says</p>
<blockquote class="blockquote">
<ul>
<li><code>core/</code> — dependency-free domain logic + proofs</li>
<li><code>ports/</code> — narrow driving (source) / driven (sink) contracts</li>
</ul>
</blockquote>
<p>which describes what is there. The correction it needs is the prefix <code>Rebac/</code> on each path, and the removal of <code>adapters/</code>, which the repository does not have.</p>
<p><code>entities-lean-loot</code> holds three directories beside <code>LootCore</code>, so “one Lean namespace directory” describes three of the five rather than all of them.</p>
<p>Three of the five carry no README at all, which is the larger half of this gap:</p>
<div class="code-copy-outer-scaffold"><div class="sourceCode" id="cb7" style="background: #f1f3f5;"><pre class="sourceCode sh code-with-copy"><code class="sourceCode bash"><span id="cb7-1"><span class="cf" style="color: #003B4F;
background-color: null;
font-weight: bold;
font-style: inherit;">for</span> d <span class="kw" style="color: #003B4F;
background-color: null;
font-weight: bold;
font-style: inherit;">in</span> entities-lean-<span class="pp" style="color: #AD0000;
background-color: null;
font-style: inherit;">*</span><span class="kw" style="color: #003B4F;
background-color: null;
font-weight: bold;
font-style: inherit;">;</span> <span class="cf" style="color: #003B4F;
background-color: null;
font-weight: bold;
font-style: inherit;">do</span> <span class="bu" style="color: null;
background-color: null;
font-style: inherit;">printf</span> <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">'%-28s %s\n'</span> <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"</span><span class="va" style="color: #111111;
background-color: null;
font-style: inherit;">$d</span><span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"</span> <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"</span><span class="va" style="color: #111111;
background-color: null;
font-style: inherit;">$(</span><span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">git</span> <span class="at" style="color: #657422;
background-color: null;
font-style: inherit;">-C</span> <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"</span><span class="va" style="color: #111111;
background-color: null;
font-style: inherit;">$d</span><span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"</span> ls-files <span class="kw" style="color: #003B4F;
background-color: null;
font-weight: bold;
font-style: inherit;">|</span> <span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">grep</span> <span class="at" style="color: #657422;
background-color: null;
font-style: inherit;">-ci</span> readme<span class="va" style="color: #111111;
background-color: null;
font-style: inherit;">)</span><span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"</span><span class="kw" style="color: #003B4F;
background-color: null;
font-weight: bold;
font-style: inherit;">;</span> <span class="cf" style="color: #003B4F;
background-color: null;
font-weight: bold;
font-style: inherit;">done</span></span>
<span id="cb7-2"><span class="co" style="color: #5E5E5E;
background-color: null;
font-style: inherit;"># entities-lean-combat       0</span></span>
<span id="cb7-3"><span class="co" style="color: #5E5E5E;
background-color: null;
font-style: inherit;"># entities-lean-loot         0</span></span>
<span id="cb7-4"><span class="co" style="color: #5E5E5E;
background-color: null;
font-style: inherit;"># entities-lean-progression  0</span></span>
<span id="cb7-5"><span class="co" style="color: #5E5E5E;
background-color: null;
font-style: inherit;"># entities-lean-rebac        2</span></span>
<span id="cb7-6"><span class="co" style="color: #5E5E5E;
background-color: null;
font-style: inherit;"># entities-lean-shared       1</span></span></code></pre></div></div>
<p>So RFD 0111 describes five READMEs where two exist. <code>entities-lean-shared</code> is the one whose layout section is wholly false: it names the triad and the repository holds <code>Shared/Types.lean</code> and nothing else. <code>entities-lean-rebac</code> is the one that needs its paths qualified, in both its top-level README and the second one at <code>Rebac/README.md</code>, which names an <code>adapters/</code> that is absent.</p>
<p>One README to correct and one to qualify. The count in that section, and the sentence “Those directories do not exist”, are the two claims to amend. Whether <code>combat</code>, <code>loot</code>, and <code>progression</code> should gain a README is a separate question this RFD does not answer.</p>
</section>
<section id="what-this-rfd-leaves-alone" class="level2">
<h2 class="anchored" data-anchor-id="what-this-rfd-leaves-alone">What this RFD leaves alone</h2>
<p>The conversion table, the retirement of plane, edge plane, domain, and store plane, the type-first name shape, the thirty renames, and the reasoning for “interactor” over the alternatives all hold. Every one of them was checked against the organisation and matches.</p>
<p>The <code>domain</code> field on the wire in RFD 0091 is still unrenamed, and still needs its own RFD, as RFD 0111 says.</p>


</section>

 ]]></description>
  <guid>https://v-sekai-multiplayer-fabric.github.io/multiplayer-fabric-manuals/rfd/0121-amend-rfd-0111-against-the-tree/</guid>
  <pubDate>Tue, 18 Aug 2026 01:10:59 GMT</pubDate>
</item>
<item>
  <title>RFD 0120: The Gyre became a settlement game</title>
  <link>https://v-sekai-multiplayer-fabric.github.io/multiplayer-fabric-manuals/rfd/0120-the-gyre-became-a-settlement-game/</link>
  <description><![CDATA[ 





<p><em><a href="../../index.html">Manuals</a> › <a href="../../pages/rfd.html">RFDs</a> › RFD 0120: The Gyre became a settlement game</em></p>
<p><strong>State:</strong> discussion · <strong>Scope:</strong> the Gyre’s shape, the player’s role, and what the ward simulates</p>
<section id="problem" class="level2">
<h2 class="anchored" data-anchor-id="problem">Problem</h2>
<p>RFD 0085 records the Gyre as a MUD on the loot and action shell: a Hub in the Under-Market and the Commons, bounded contracts out in the field, six zones, and a player who is a Spark taking work to pay down a debt. <code>service-store</code> built something else — the player is the Queen, the Sparks act on their own, and the loop is commissioning rather than travelling. The code has run this way since the first ward played while the record still describes the earlier design, so anyone reading 0085 to understand what <code>queen</code> does will be wrong about the player, the loop, and the map. This RFD records what was built, so the two stop disagreeing.</p>
</section>
<section id="decision" class="level2">
<h2 class="anchored" data-anchor-id="decision">Decision</h2>
<p><strong>The player is the Queen, and the Queen never takes a contract.</strong> The shape is <code>My Life as a King</code>: commission, then wait. Sparks choose their own work, and the whole of the player’s game is deciding what to build and in what order. The comment in <code>src/queen.c</code> states the reasoning, which is that a loop like this is a state machine over days with nothing in the critical path to draw, and that is the one game shape genuinely better as a database than as an engine.</p>
<p><strong>Six venues replace six zones.</strong> The Under-Market and the Commons survive as the two decks a venue stands on, thirty metres apart. What they hold is a build list rather than a map to travel: Cycle’s End Tavern, Splicer’s Den, Transit Rails, Exchange Plaza, Chapel of the Backup, Broadcast Row. Each changes how Sparks behave rather than granting a number, so the Tavern makes them bolder because they rest and the Rails put more work on the board.</p>
<p><strong>A contract resolves against risk, on the board, in a cycle.</strong> Nine kinds post to a board of six, or nine once the Rails stand. A Spark takes one if its nerve carries it, and a draw against the contract’s risk decides the outcome. Seven of the nine still resolve without a fight, which is the part of 0085 that carried over unchanged.</p>
<p><strong>The Debt Clock is the antagonist, and it compounds.</strong> Debt grows one percent per cycle and the treasury pays it down. Income from contracts is the only thing that makes scrip, which is what lets one sum decide whether the ward is honest.</p>
<p><strong>The currency is scrip.</strong> 0085 calls it chits. The implementation calls it scrip throughout, and the invariant is written in those terms.</p>
<p><strong>A cycle is game time and never wall-clock time.</strong> The ward publishes at 20 Hz because the rest of the fabric does, and that clock says nothing except how often the ward publishes. Tying the two would tie the rate debt compounds at to the rate the network publishes at, and it would end the replay check.</p>
</section>
<section id="references" class="level2">
<h2 class="anchored" data-anchor-id="references">References</h2>
<ul>
<li>RFD 0085: the setting, which this amends rather than replaces. RFD 0119: the interaction model that depends on which loop is real</li>
<li><code>service-store</code>: <code>src/queen.c</code>, the venues, the board, and the clock</li>
</ul>
</section>
<section id="detail" class="level2">
<h2 class="anchored" data-anchor-id="detail">Detail</h2>
</section>
<section id="what-changed-line-by-line" class="level2">
<h2 class="anchored" data-anchor-id="what-changed-line-by-line">What changed, line by line</h2>
<table class="caption-top table">
<colgroup>
<col style="width: 11%">
<col style="width: 36%">
<col style="width: 52%">
</colgroup>
<thead>
<tr class="header">
<th></th>
<th>RFD 0085</th>
<th><code>service-store</code></th>
</tr>
</thead>
<tbody>
<tr class="odd">
<td>The player</td>
<td>A Spark taking contracts</td>
<td>The Queen, commissioning venues</td>
</tr>
<tr class="even">
<td>The loop</td>
<td>Hub, field, return</td>
<td>Commission, then wait a cycle</td>
</tr>
<tr class="odd">
<td>The map</td>
<td>Six zones to travel</td>
<td>Six venues on two decks</td>
</tr>
<tr class="even">
<td>Contracts</td>
<td>Taken by the player</td>
<td>Chosen by Sparks, resolved against risk</td>
</tr>
<tr class="odd">
<td>Currency</td>
<td>Chits</td>
<td>Scrip</td>
</tr>
<tr class="even">
<td>Antagonist</td>
<td>Overseer Q-11</td>
<td>The Debt Clock, compounding one percent a cycle</td>
</tr>
<tr class="odd">
<td>Combat</td>
<td>One of nine, avoidable</td>
<td>One of nine, avoidable</td>
</tr>
<tr class="even">
<td>Failure</td>
<td>Death, and a dropped Memory Drive</td>
<td>Wear, capped, and a Frame that stops earning</td>
</tr>
</tbody>
</table>
<p>The last two rows are what survived intact.</p>
</section>
<section id="the-venues-and-what-each-one-does" class="level2">
<h2 class="anchored" data-anchor-id="the-venues-and-what-each-one-does">The venues, and what each one does</h2>
<table class="caption-top table">
<colgroup>
<col style="width: 28%">
<col style="width: 5%">
<col style="width: 65%">
</colgroup>
<thead>
<tr class="header">
<th>Venue</th>
<th>Cost</th>
<th>Effect</th>
</tr>
</thead>
<tbody>
<tr class="odd">
<td>Cycle’s End Tavern</td>
<td>120</td>
<td>Rest, so a Spark takes longer odds</td>
</tr>
<tr class="even">
<td>Splicer’s Den</td>
<td>200</td>
<td>Frames repaired, so wear stops ending careers</td>
</tr>
<tr class="odd">
<td>Transit Rails</td>
<td>260</td>
<td>More contracts reach the board</td>
</tr>
<tr class="even">
<td>Exchange Plaza</td>
<td>340</td>
<td>Salvage sells for what it is worth</td>
</tr>
<tr class="odd">
<td>Chapel of the Backup</td>
<td>420</td>
<td>A failure costs a cycle rather than a Spark</td>
</tr>
<tr class="even">
<td>Broadcast Row</td>
<td>500</td>
<td>Word gets out, and Sparks arrive</td>
</tr>
</tbody>
</table>
<p>A venue changes behaviour rather than adding a number. The Tavern and the Chapel raise the nerve a Spark brings to a contract, the Den takes wear back faster than rest does, the Rails widen the board, and the Row is the only source of new Sparks. Each is a decision about what kind of ward this becomes.</p>
</section>
<section id="the-numbers-the-ward-holds" class="level2">
<h2 class="anchored" data-anchor-id="the-numbers-the-ward-holds">The numbers the ward holds</h2>
<p>A ward starts with a treasury of 300 against a debt of 4000. Debt compounds at one percent a cycle, and what the treasury can spare pays it down and counts as retired. A contract that succeeds pays its posted amount, and that is the only event that issues scrip.</p>
<p>Wear rises by 2 on a success and 5 on a failure, falls by 1 for the cycle’s rest, falls a further 4 where the Den stands, and is held between 0 and 90. Nerve is 100 less wear, plus 25 with the Tavern and 20 with the Chapel. A Spark with no nerve for anything on the board stands idle.</p>
<p>Somebody hears the Broadcast Row every 12 cycles. The arrival counts cycles rather than drawing, so a replay puts the newcomer in the same place.</p>
<p>A ward holds at most 48 Sparks, because a subscriber’s slice holds 64 entities and the venues, the Queen and a full board come out of that budget first. A larger ward is refused, and a game past that size is more than one ward, to a limit of eight.</p>
</section>
<section id="why-the-shape-changed" class="level2">
<h2 class="anchored" data-anchor-id="why-the-shape-changed">Why the shape changed</h2>
<p>The comment at the top of <code>src/queen.c</code> gives the reason directly. A settlement game is a state machine over days with nothing in the critical path to draw, which makes it the one game shape genuinely better as a database than as an engine. The Queen commissions and waits, so a cycle is a transaction and the whole of the visible state is a query.</p>
<p>The MUD shape needed a room, a description, and a player standing in it, none of which a store plane exercises. The settlement shape gave <code>service-store</code> a workload that is exactly the thing the store plane claims to be good at: N Sparks is N databases committing every cycle, and paying one is a transfer between two of them under the parallel commit protocol.</p>
</section>
<section id="what-this-leaves-open" class="level2">
<h2 class="anchored" data-anchor-id="what-this-leaves-open">What this leaves open</h2>
<p>Whether the Spark-facing game returns. Nothing here forecloses it, and RFD 0119 assumes it does, with hands and salvage in a headset. The ward would be the economy underneath it rather than the whole of it.</p>
<p>Whether Memory Drives arrive with that, since they are the one adversarial interaction the setting describes and the one object whose duplication is unsurvivable in the fiction.</p>
<p>Whether the absent zones come back as places, given that venues, Sparks and contracts already carry coordinates in micrometres for the interest filter.</p>
</section>
<section id="what-has-not-been-built" class="level2">
<h2 class="anchored" data-anchor-id="what-has-not-been-built">What has not been built</h2>
<p>The Reclamation Wards, the Tangle, the Sub-Net and the Underhull have no representation in the ward. Splicer Jax, Overseer Q-11 and Rook are absent, though the Splicer’s Den carries Jax’s role as a venue. Memory Drives, and the looting and ransoming of them, are unimplemented, which matters because they are the setting’s one adversarial interaction and RFD 0119 turns on them.</p>
<p>These stay in 0085 as setting. This RFD narrows what <code>service-store</code> claims to simulate rather than retiring the fiction around it.</p>
</section>
<section id="consequences" class="level2">
<h2 class="anchored" data-anchor-id="consequences">Consequences</h2>
<p>0085 remains the setting record and stops being the design record for the ward. A reader who wants to know what the simulation does reads this one.</p>
<p>The salvage and interaction model in RFD 0119 sits on the cycle boundary rather than on a Hub and Field loop, because the loop it was written against is not the loop that exists.</p>


</section>

 ]]></description>
  <guid>https://v-sekai-multiplayer-fabric.github.io/multiplayer-fabric-manuals/rfd/0120-the-gyre-became-a-settlement-game/</guid>
  <pubDate>Tue, 18 Aug 2026 01:10:59 GMT</pubDate>
</item>
<item>
  <title>RFD 0119: Salvage you can hold</title>
  <link>https://v-sekai-multiplayer-fabric.github.io/multiplayer-fabric-manuals/rfd/0119-salvage-you-can-hold/</link>
  <description><![CDATA[ 





<p><em><a href="../../index.html">Manuals</a> › <a href="../../pages/rfd.html">RFDs</a> › RFD 0119: Salvage you can hold</em></p>
<p><strong>State:</strong> discussion · <strong>Scope:</strong> client interaction model, authority, the boundary between physics and the ledger</p>
<section id="problem" class="level2">
<h2 class="anchored" data-anchor-id="problem">Problem</h2>
<p><code>service-store</code> holds an invariant: salvage is unique, and no item is in two Sparks’ hands. A transaction across two databases enforces it, and <code>honest()</code> checks it every cycle. Glenn Fiedler’s VR cubes hold the same invariant under another name — ownership stops two players holding one cube, and a sequence number per cube enforces it while a host arbitrates. His rule is exact: “Ownership sequence increments each time a player grabs a cube. Ownership is stronger than authority, such that an increase in ownership sequence wins over an increase in authority sequence number.”</p>
<p>One invariant, two mechanisms, three orders of magnitude apart in cost: the transaction is exact and takes a round trip, the sequence number converges and takes nothing. RFD 0085 makes the question urgent by naming the object that carries it. Memory Drives house a Spark’s consciousness, drop on death, and can be looted or ransomed. A drive is holdable, contested, and the one item where duplication is unsurvivable in the fiction as well as in the ledger: two people holding one drive is two people holding one person.</p>
</section>
<section id="decision" class="level2">
<h2 class="anchored" data-anchor-id="decision">Decision</h2>
<p><strong>Two authorities, split by what a mistake costs.</strong> Physics authority is distributed and taken by whoever last touched an object, and it is allowed to be briefly wrong. Economic authority stays with <code>service-store</code>, single-writer and transactional, and is never allowed to be wrong. A dropped crate costs a frame of jitter; a duplicated drive costs the invariant every other check rests on.</p>
<p><strong>The cycle is the line, because a cycle is already one transaction.</strong> <code>service-store</code> makes each cycle a single group commit across the ward and every Spark it touches. Salvage loose in the world between cycles is physics the ledger has never heard of, and the cycle that ends is the commit that takes it. The boundary the interaction model needs is the one the store already draws, so nothing new carries it.</p>
<p><strong>A claim is one transaction that deletes and inserts together.</strong> Converting loose salvage into a row removes the physical object and writes the <code>held</code> entry in the same commit, and drawing an item back into the world runs it backwards. Uniqueness holds because the ledger counts only what it has committed, and the conversion is the only place the two representations meet.</p>
<p><strong>Memory Drives never enter the distributed regime.</strong> Fiedler states his model suits “cooperative experiences only, as it does not provide the security of a server-authoritative network model”. Seven of the Gyre’s nine contracts resolve without a fight, so that limitation costs almost nothing. Drive looting and ransom is the exception and is adversarial by design, so custody stays transactional even while the physical shell is thrown around.</p>
<p><strong>Physics never reaches the seeded simulation.</strong> <code>CLAUDE.md</code> requires that a cycle not advance on wall-clock time and that the RNG draw order never move. PhysX is non-deterministic, which is why Fiedler rejected lockstep. A thrown crate may not decide a contract, feed a draw, or advance a cycle: the physics layer reads the ward and the ward declines to read it back.</p>
<p><strong>The board is the second layer, played at the same time.</strong> PRAGMATA runs Hugh’s movement and Diana’s hacking together, one player coordinating both. The Gyre has both halves already — hands in the world at frame rate, and the Queen’s board of contracts and cycles deciding what the object in your hand is worth. Neither pauses for the other.</p>
</section>
<section id="references" class="level2">
<h2 class="anchored" data-anchor-id="references">References</h2>
<ul>
<li><a href="https://gafferongames.com/post/networked_physics_in_virtual_reality/">Networked Physics in Virtual Reality</a>, Glenn Fiedler: the authority model, the priority accumulator, and the delta encoding; <a href="https://store.steampowered.com/app/3357650/PRAGMATA/">PRAGMATA</a>, CAPCOM: two characters played at once</li>
<li>RFD 0085: the setting, the Hub and Field loop, and Memory Drives</li>
<li><code>service-store</code> (the ward, the invariants, the parallel commit), <code>transport-fanout</code> (the interest filter and entity packet), <code>interactor-authority</code> (the single writer the ledger keeps)</li>
</ul>
</section>
<section id="detail" class="level2">
<h2 class="anchored" data-anchor-id="detail">Detail</h2>
</section>
<section id="one-invariant-at-two-consistency-levels" class="level2">
<h2 class="anchored" data-anchor-id="one-invariant-at-two-consistency-levels">One invariant at two consistency levels</h2>
<table class="caption-top table">
<colgroup>
<col style="width: 18%">
<col style="width: 43%">
<col style="width: 38%">
</colgroup>
<thead>
<tr class="header">
<th></th>
<th>Loose, between cycles</th>
<th>Claimed, at the cycle</th>
</tr>
</thead>
<tbody>
<tr class="odd">
<td>Enforced by</td>
<td>Ownership sequence number, host arbitrates</td>
<td>Transaction across two databases</td>
</tr>
<tr class="even">
<td>Cost</td>
<td>Nothing. Rides in the state update</td>
<td>About one round trip</td>
</tr>
<tr class="odd">
<td>Wrong for</td>
<td>A few frames, then converges</td>
<td>Never</td>
</tr>
<tr class="even">
<td>Checked by</td>
<td>Convergence</td>
<td><code>honest()</code>, every cycle</td>
</tr>
<tr class="odd">
<td>Failure looks like</td>
<td>A crate snaps to another pose</td>
<td>Scrip or salvage that does not add up</td>
</tr>
</tbody>
</table>
<p>The two rows describe the same rule. What differs is the price paid for it and how long it may be violated.</p>
</section>
<section id="what-fiedlers-model-supplies" class="level2">
<h2 class="anchored" data-anchor-id="what-fiedlers-model-supplies">What Fiedler’s model supplies</h2>
<p>Authority passes to whoever last interacted with an object, and ownership stops a second player taking it while it is held. Both ride as sequence numbers in every state update, and the host accepts an update when its sequence is newer. Conflicts are reported as rare in practice even under significant latency.</p>
<p>State is quantized identically on both sides before each physics step, at 1/1000th of a centimetre, and synchronized at 10 Hz against PhysX’s non-determinism. A priority accumulator decides which objects fit in each packet, boosting whatever collided or was recently thrown, which gives direct control over bandwidth against a fixed packet size. Delta compression against per-object baselines encodes an unchanged object in one bit and a ballistic-predicted object in one more. Four players fit under 256 kbps each.</p>
<p>A held object leaves the physics stream entirely and rides in the holder’s avatar packet as a relative transform, which stops the hand and the object disagreeing. Avatar state arrives through a 100 ms jitter buffer.</p>
</section>
<section id="what-the-gyre-supplies" class="level2">
<h2 class="anchored" data-anchor-id="what-the-gyre-supplies">What the Gyre supplies</h2>
<p>The ward is a SQLite database over a VFS whose pages live in FoundationDB, and every Spark is a database of their own. Paying one is a transfer between two databases under the parallel commit protocol. Three invariants hold every cycle: scrip is conserved, salvage is unique, and one seed makes one ward.</p>
<p>The third is what constrains this design most. A cycle may not advance on wall-clock time, and no change may alter the RNG draw order, because <code>check</code> plays a seed twice and compares a fingerprint. A physics simulation that fed the ward would end that check, and PhysX is the reason Fiedler rejected lockstep in the first place.</p>
</section>
<section id="what-pragmata-supplies" class="level2">
<h2 class="anchored" data-anchor-id="what-pragmata-supplies">What PRAGMATA supplies</h2>
<p>Two characters played at once: Hugh moves, shoots and jumps while Diana hacks in real time. The interest is in the division of attention rather than in either half alone.</p>
<p>The Gyre has both halves already. Hands work at frame rate, and the Queen’s board of contracts, venues and cycles runs as a discrete layer that decides what the object in a hand is worth. The claim is the moment the two layers touch.</p>
</section>
<section id="the-memory-drive-as-the-worked-case" class="level2">
<h2 class="anchored" data-anchor-id="the-memory-drive-as-the-worked-case">The Memory Drive as the worked case</h2>
<p>RFD 0085 has Memory Drives housing a Spark’s consciousness, dropping on death, and available to be looted or ransomed. Every property that makes an object hard here is present at once. It is holdable, so it wants distributed authority. It is contested between players who benefit from taking it, so it cannot have distributed authority. It is unique in the fiction as strongly as in the ledger, because duplicating a drive duplicates a person.</p>
<p>The split resolves it. The shell is a physics object under the ordinary rules, and custody is a row that moves only by transaction. A player can carry, drop and hand over the shell at frame rate while the question of who owns the drive is answered somewhere that cannot be raced.</p>
</section>
<section id="failure-modes-to-design-against" class="level2">
<h2 class="anchored" data-anchor-id="failure-modes-to-design-against">Failure modes to design against</h2>
<p>A claim that inserts before it deletes duplicates the item for the width of the window. A claim that deletes before it inserts destroys it for the same width. The parallel commit exists to make both halves land together, and the claim is exactly the transaction it was built for.</p>
<p>An object committed by a cycle and still simulated afterwards exists twice, with the ledger aware of one copy. This shows up as a violated invariant on the next cycle rather than as a visual artefact, which is the reason to prefer this arrangement over one that reconciles later.</p>
<p>Distributed authority over anything priced in scrip lets a player take what they were not given. This is the whole content of Fiedler’s cooperative-only caveat applied to a setting that has an economy.</p>
<p>Physics feeding the seeded simulation ends the replay check silently. Nothing fails at the moment it happens, and <code>check</code> starts disagreeing with itself on runs that used to match.</p>
</section>
<section id="alternatives-considered" class="level2">
<h2 class="anchored" data-anchor-id="alternatives-considered">Alternatives considered</h2>
<p>Server-authoritative physics with client-side prediction. This removes the security caveat and the split, at the cost of rollback and resimulation for every interaction, and of a round trip in the hands of a player holding an object. Fiedler weighed the same trade and observed that for this class of interaction the security is effectively the same either way, since the client is trusted about its own hands regardless.</p>
<p>One consistency level for everything, chosen as the strong one. Every crate becomes a transaction, and throwing anything costs a commit. This holds every invariant and gives up the interaction the RFD exists to enable.</p>
<p>One consistency level for everything, chosen as the weak one. Scrip and drives converge like cubes do. This is fast, simple, and hands the treasury to whoever writes a client.</p>
<p>Deterministic lockstep across all clients. This gives exactness without a ledger, and requires a deterministic physics engine the stack does not have.</p>
</section>
<section id="tldr" class="level2">
<h2 class="anchored" data-anchor-id="tldr">TL;DR</h2>
<pre><code>gyre X networked physics in vr X pragmata</code></pre>
<p>The Gyre supplies an economy a database can prove. Networked physics in VR supplies hands that pass an object between two people across a network. PRAGMATA supplies the shape of playing both at once.</p>
</section>
<section id="consequences" class="level2">
<h2 class="anchored" data-anchor-id="consequences">Consequences</h2>
<p>The 100-byte entity packet and the interest filter in <code>transport-fanout</code> become the physics transport, which is what they were built for. <code>interactor-authority</code>’s single writer keeps the ledger and holds no opinion about where a crate is.</p>
<p>Two representations of one object exist while it is loose, and exactly one exists once it is claimed. A bug in the conversion surfaces as a violated invariant on the next cycle rather than as a visual glitch, which is the failure mode to want.</p>
<p>The second layer runs against the board the Queen already plays, so it costs content rather than architecture.</p>
</section>
<section id="open-questions" class="level2">
<h2 class="anchored" data-anchor-id="open-questions">Open questions</h2>
<p>Whether the host-arbiter topology survives a dedicated ward process. Fiedler’s host is a player; <code>queen serve</code> is not, and it already terminates WebTransport in its own process.</p>
<p>What a claim costs in latency, given it is a parallel commit across two databases while the player is holding the object.</p>
<p>Whether a drive’s physical shell can be thrown at all while its custody stays transactional, or whether the shell has to be inert to keep the two from disagreeing.</p>


</section>

 ]]></description>
  <guid>https://v-sekai-multiplayer-fabric.github.io/multiplayer-fabric-manuals/rfd/0119-salvage-you-can-hold/</guid>
  <pubDate>Tue, 18 Aug 2026 01:10:59 GMT</pubDate>
</item>
<item>
  <title>RFD 0118: A baker replaces the Godot editor</title>
  <link>https://v-sekai-multiplayer-fabric.github.io/multiplayer-fabric-manuals/rfd/0118-a-baker-replaces-the-godot-editor/</link>
  <description><![CDATA[ 





<p><em><a href="../../index.html">Manuals</a> › <a href="../../pages/rfd.html">RFDs</a> › RFD 0118: A baker replaces the Godot editor</em></p>
<p><strong>State:</strong> discussion · <strong>Scope:</strong> client build pipeline, asset delivery, engine modules</p>
<section id="problem" class="level2">
<h2 class="anchored" data-anchor-id="problem">Problem</h2>
<p>The Godot editor is a build-time dependency of every client artifact, and two steps are the reason. Resource import turns <code>.png</code> into <code>.ctex</code>, <code>.wav</code> into a sample, and mesh formats into meshes under <code>res://.godot/imported/</code>, and the importers live in <code>editor/</code>. Export builds the <code>.pck</code>, and <code>--export-release</code> lives there too. A <code>template_release</code> binary reads what both produce and can run neither.</p>
<p>That places a GUI application in the middle of an automated pipeline. <code>godot-images</code> builds and publishes editors for Windows and Linux so that CI can call <code>--export-release</code>, and <code>windows-editor.zip</code> alone is 482 MB. The assets already arrive at runtime from <code>transport-asset</code>, so the editor is doing work for content it never sees.</p>
</section>
<section id="decision" class="level2">
<h2 class="anchored" data-anchor-id="decision">Decision</h2>
<p><strong>Bake to <code>.tscn</code> in the backend.</strong> The text scene format is documented, and <code>ResourceFormatLoaderText</code> compiles into every target, so a baker that writes <code>.tscn</code> writes a file the release template already knows how to load. The editor is one program that happens to write the same format.</p>
<p><strong>Carry textures as Basis Universal inside glTF.</strong> Import exists to choose a GPU format ahead of time, and Basis transcodes on the device, so that step has nothing left to do. <code>KHR_texture_basisu</code> puts the texture inside the glTF and <code>GLTFDocument</code> loads it at runtime. <code>modules/basis_universal</code> and <code>modules/gltf</code> are already in <code>entities-godot</code>, and <code>godot-images</code> disables no modules on any platform.</p>
<p><strong>Run OpenUSD in the baker.</strong> openUSD is a C++ library. <code>idtx-flow</code> calls it from an editor import plugin; the baker calls the same library server-side and emits glTF and <code>.tscn</code>. This takes USD off artists’ machines and out of the engine.</p>
<p><strong>Mount the filesystem with a small C++ module in the release template.</strong> A <code>PackSource</code> resolves <code>res://</code> against a directory or against <code>transport-asset</code>’s content-addressed chunks, registered at <code>MODULE_INITIALIZATION_LEVEL_CORE</code> because <code>PackedData</code> is core and has to be serving before anything asks for a path. It has to be a module: GDExtension initializes after the filesystem is up, so it cannot serve boot.</p>
<p><strong>Keep gameplay out of the module.</strong> Logic stays RISC-V ELF in the sandbox, as <code>zone-guest-godot</code> and <code>zone-guest-middleham</code> already deliver it. The module is the one part of this stack no CDN can replace, so every line in it costs an engine release to change.</p>
<p><strong>Stop building the editor when the baker covers every asset class.</strong> Until then <code>godot-images</code> keeps building it, and <code>--export-release</code> stays available as a fallback.</p>
</section>
<section id="references" class="level2">
<h2 class="anchored" data-anchor-id="references">References</h2>
<ul>
<li><code>entities-godot</code>, the engine fork and where the module lands under <code>modules/</code>; <code>godot-images</code>, the engine builds and the release that now carries templates alone</li>
<li><code>zone-client-godot</code>, a client already shipping <code>template_release</code> and no editor; <code>zone-guest-godot</code> and <code>zone-guest-middleham</code>, the sandboxed guests the logic lives in</li>
<li><code>transport-asset</code>, the content-addressed store the pack source reads; <code>idtx-flow</code>, the USD import the baker takes over</li>
</ul>
</section>
<section id="detail" class="level2">
<h2 class="anchored" data-anchor-id="detail">Detail</h2>
</section>
<section id="what-requires-the-editor-today" class="level2">
<h2 class="anchored" data-anchor-id="what-requires-the-editor-today">What requires the editor today</h2>
<table class="caption-top table">
<colgroup>
<col style="width: 6%">
<col style="width: 69%">
<col style="width: 24%">
</colgroup>
<thead>
<tr class="header">
<th>Step</th>
<th>Mechanism</th>
<th>Available in a template</th>
</tr>
</thead>
<tbody>
<tr class="odd">
<td>Import</td>
<td><code>editor/import/*</code>, writing <code>res://.godot/imported/</code></td>
<td>No</td>
</tr>
<tr class="even">
<td>Export</td>
<td><code>--export-release</code>, <code>--export-debug</code>, implemented under <code>editor/</code></td>
<td>No</td>
</tr>
</tbody>
</table>
<p>Everything else a client does at runtime is already outside <code>editor/</code>. <code>ResourceFormatLoaderText</code> reads <code>.tscn</code>, <code>GLTFDocument</code> reads glTF, and the Basis transcoder reads supercompressed textures. All three compile into <code>target=template_release</code>.</p>
</section>
<section id="why-the-mount-is-a-module" class="level2">
<h2 class="anchored" data-anchor-id="why-the-mount-is-a-module">Why the mount is a module</h2>
<p><code>PackedData</code> accepts additional backends through <code>PackSource</code>, which is how the <code>.pck</code> reader and the ZIP reader are both registered. A third source resolving <code>res://</code> against a directory or a chunk store is the same shape of object.</p>
<p>The registration has to happen at core initialization. A GDExtension library loads after the filesystem is already serving, which is late enough that boot resources have been requested and failed. A module registered at <code>MODULE_INITIALIZATION_LEVEL_CORE</code> is in place before the first request.</p>
<p>The usual argument against a module is that it costs an engine rebuild. <code>godot-images</code> already builds the engine for every platform on every run, so that cost is already paid.</p>
</section>
<section id="three-ways-this-fails-quietly" class="level2">
<h2 class="anchored" data-anchor-id="three-ways-this-fails-quietly">Three ways this fails quietly</h2>
<p><code>#ifdef TOOLS_ENABLED</code> around the registration. <code>template_release</code> strips the define, so the block disappears: the module compiles, the template links, and <code>res://</code> resolves to nothing with no diagnostic naming the cause.</p>
<p>An unstable <code>uid://</code>. Godot 4 <code>.tscn</code> refers to external resources by UID. A baker that generates a fresh UID per run produces scenes whose references break on the second bake. Emitting plain <code>res://</code> paths avoids the class of bug entirely.</p>
<p>A reference to an importable file. <code>res://foo.png</code> inside a baked <code>.tscn</code> reintroduces the import step for one asset and takes the editor back with it. Every external reference has to name a runtime-loadable file or live inside the glTF.</p>
</section>
<section id="what-is-already-true" class="level2">
<h2 class="anchored" data-anchor-id="what-is-already-true">What is already true</h2>
<ul>
<li><code>entities-godot</code> carries <code>modules/basis_universal</code> and <code>modules/gltf</code>.</li>
<li><code>godot-images</code> has no <code>module_*_enabled=no</code> on any platform, so both are in every artifact it publishes.</li>
<li><code>zone-client-godot</code> builds <code>scons platform=web target=template_release</code> and ships <code>bin/godot.web.template_release.*</code> with no editor involved.</li>
<li><code>godot-images</code> now releases <code>windows-template-release.zip</code> and <code>linux-template-release.zip</code> alone. The editors still build, and <code>build-docker</code> still publishes <code>godot-editor-double</code> from that target.</li>
</ul>
</section>
<section id="alternatives-considered" class="level2">
<h2 class="anchored" data-anchor-id="alternatives-considered">Alternatives considered</h2>
<p>Keep the editor headless in CI. This works today and is what happens now. It keeps a 482 MB GUI binary in the pipeline to perform a step whose inputs the baker already holds, and it keeps import as a build stage that content delivered at runtime never passes through.</p>
<p>Commit <code>res://.godot/imported/</code>. This removes the import step from CI by moving its output into version control. The artifacts are engine-version-specific and binary, so an engine bump invalidates all of them at once, and review sees no meaningful diff.</p>
<p>Pre-pack with a third-party <code>.pck</code> writer. The format is documented and writers exist. This solves export without solving import, so the editor stays for textures and meshes. It remains the fallback if the pack source proves harder than expected.</p>
<p>Emit binary <code>.scn</code> instead of <code>.tscn</code>. Loading is faster. The binary format’s compatibility across engine versions is a weaker contract for a baker to hold than the text format’s, and the parse cost is paid once per scene load against a network fetch that costs more.</p>
</section>
<section id="consequences" class="level2">
<h2 class="anchored" data-anchor-id="consequences">Consequences</h2>
<p>The pipeline becomes scons for the template, the baker for the content, the module for the mount, and the sandbox for the logic. No step needs a GUI, and no step needs a machine with the editor installed.</p>
<p>The module is the new failure surface. Three ways it fails quietly are recorded in <code>DETAILS.md</code>, and the first one compiles, links, and resolves nothing.</p>
</section>
<section id="open-question" class="level2">
<h2 class="anchored" data-anchor-id="open-question">Open question</h2>
<p>Whether a release template boots an unpacked project directory or requires a <code>.pck</code>. The module answers this by construction once it exists. Until it does, the question is unsettled and a short experiment against <code>linux-template-release</code> would settle it.</p>


</section>

 ]]></description>
  <guid>https://v-sekai-multiplayer-fabric.github.io/multiplayer-fabric-manuals/rfd/0118-a-baker-replaces-the-godot-editor/</guid>
  <pubDate>Tue, 18 Aug 2026 01:10:59 GMT</pubDate>
</item>
<item>
  <title>RFD 0117: Retire the h2o tier record set under YAGNI</title>
  <link>https://v-sekai-multiplayer-fabric.github.io/multiplayer-fabric-manuals/rfd/0117-retire-the-h2o-tier-record-set/</link>
  <description><![CDATA[ 





<p><em><a href="../../index.html">Manuals</a> › <a href="../../pages/rfd.html">RFDs</a> › RFD 0117: Retire the h2o tier record set under YAGNI</em></p>
<p><strong>State:</strong> published · <strong>Scope:</strong> RFD lifecycle, records for the retired native tier</p>
<section id="problem" class="level2">
<h2 class="anchored" data-anchor-id="problem">Problem</h2>
<p><code>rfd/0116</code> replaces the native zone server with an Elixir epoll server, and returns the state to CockroachDB. That decision leaves 43 records that describe things the project does not build: a libh2o event loop, a FoundationDB C path, a libriscv guest runtime, and repositories that are already archived.</p>
<p>Every one of those records renders as live on the site. A reader cannot tell them from the records that still hold. Marking a record’s state does not help a reader who arrives through search.</p>
</section>
<section id="decision" class="level2">
<h2 class="anchored" data-anchor-id="decision">Decision</h2>
<p>Move all 43 records to <code>multiplayer-fabric-archive</code>, per <code>rfd/0106</code>. Keep no pointer stub, per <code>rfd/0000-conventions</code>.</p>
<p><code>rfd/0071</code> supplies the rule. Structure arrives when a real near-term need arrives. A record whose subject the project does not build is structure ahead of need. Retiring it costs nothing, because the archive keeps it and the number stays reserved.</p>
<p>The same rule settles the 18 records whose case cut both ways. Four have a live consumer today and stay. Fourteen do not, and go. A record comes back when a need arrives for it, rewritten against the tier that exists then, rather than restored on the guess that it still fits.</p>
<p>Citations change in one way. A citation that names a full path, as <code>rfd/NNNN-slug</code>, becomes a link into the archive, because it names a location. A citation that names a bare number stays as it is, because it names a record.</p>
</section>
<section id="references" class="level2">
<h2 class="anchored" data-anchor-id="references">References</h2>
<ul>
<li>The full list of 43, the four that stay, and the counts: <code>DETAILS.md</code></li>
<li><code>rfd/0116-elixir-epoll-zone-server-in-the-h2o-style</code>: the decision that retires them.</li>
</ul>
</section>
<section id="detail" class="level2">
<h2 class="anchored" data-anchor-id="detail">Detail</h2>
</section>
<section id="counts" class="level2">
<h2 class="anchored" data-anchor-id="counts">Counts</h2>
<table class="caption-top table">
<thead>
<tr class="header">
<th>Set</th>
<th>Count</th>
</tr>
</thead>
<tbody>
<tr class="odd">
<td>RFDs before this decision</td>
<td>105</td>
</tr>
<tr class="even">
<td>Retired to the archive</td>
<td>43</td>
</tr>
<tr class="odd">
<td>Live after this decision</td>
<td>62</td>
</tr>
<tr class="even">
<td>Live, plus <code>rfd/0116</code>, <code>rfd/0117</code></td>
<td>64</td>
</tr>
</tbody>
</table>
</section>
<section id="the-native-zone-tier-12-records" class="level2">
<h2 class="anchored" data-anchor-id="the-native-zone-tier-12-records">The native zone tier, 12 records</h2>
<table class="caption-top table">
<thead>
<tr class="header">
<th>RFD</th>
<th>Subject</th>
</tr>
</thead>
<tbody>
<tr class="odd">
<td><code>0001</code></td>
<td>Zonefabric roadmap and PERT order</td>
</tr>
<tr class="even">
<td><code>0003</code></td>
<td>CastSpell sandbox package format</td>
</tr>
<tr class="odd">
<td><code>0004</code></td>
<td>CastSpell embeds libgodot</td>
</tr>
<tr class="even">
<td><code>0010</code></td>
<td>Client transport handshake, gateway cycle 1</td>
</tr>
<tr class="odd">
<td><code>0072</code></td>
<td>Actor-lite worker pool</td>
</tr>
<tr class="even">
<td><code>0076</code></td>
<td>Macaroon with eBPF and XDP</td>
</tr>
<tr class="odd">
<td><code>0077</code></td>
<td>PERT critical path</td>
</tr>
<tr class="even">
<td><code>0078</code></td>
<td>plausible-witness-dag feature ablation</td>
</tr>
<tr class="odd">
<td><code>0080</code></td>
<td>Slotmap entity storage</td>
</tr>
<tr class="even">
<td><code>0082</code></td>
<td>Zonefabric scaling benchmark</td>
</tr>
<tr class="odd">
<td><code>0083</code></td>
<td>Replace FabricZone with zone-server-h2o</td>
</tr>
<tr class="even">
<td><code>0086</code></td>
<td>Defer NoGod.lean gossip authority</td>
</tr>
<tr class="odd">
<td><code>0088</code></td>
<td>picoquic and picotls over h2o’s own QUIC</td>
</tr>
</tbody>
</table>
</section>
<section id="foundationdb-7-records" class="level2">
<h2 class="anchored" data-anchor-id="foundationdb-7-records">FoundationDB, 7 records</h2>
<p>CockroachDB replaces it, so <code>rfd/0006</code> returns to force.</p>
<table class="caption-top table">
<thead>
<tr class="header">
<th>RFD</th>
<th>Subject</th>
</tr>
</thead>
<tbody>
<tr class="odd">
<td><code>0002</code></td>
<td>taskweft value narrowing in FDB encoding</td>
</tr>
<tr class="even">
<td><code>0073</code></td>
<td>Async FDB callback chain on the libh2o loop</td>
</tr>
<tr class="odd">
<td><code>0074</code></td>
<td>Binary value encoding for FDB values</td>
</tr>
<tr class="even">
<td><code>0075</code></td>
<td>FoundationDB over CockroachDB</td>
</tr>
<tr class="odd">
<td><code>0084</code></td>
<td>zstd compression for batched FDB values</td>
</tr>
<tr class="even">
<td><code>0097</code></td>
<td>Pubsub in userspace, FDB keeps the log</td>
</tr>
<tr class="odd">
<td><code>0103</code></td>
<td>Uro on ecto_foundationdb</td>
</tr>
</tbody>
</table>
</section>
<section id="the-libriscv-guest-runtime-5-records" class="level2">
<h2 class="anchored" data-anchor-id="the-libriscv-guest-runtime-5-records">The libriscv guest runtime, 5 records</h2>
<p>The Gyre survives. Its runtime does not.</p>
<table class="caption-top table">
<thead>
<tr class="header">
<th>RFD</th>
<th>Subject</th>
</tr>
</thead>
<tbody>
<tr class="odd">
<td><code>0037</code></td>
<td>Generated behavior as sandboxed RISC-V</td>
</tr>
<tr class="even">
<td><code>0079</code></td>
<td>Sandboxed Godot via raw libriscv</td>
</tr>
<tr class="odd">
<td><code>0092</code></td>
<td>ReBAC gates libriscv guest access</td>
</tr>
<tr class="even">
<td><code>0095</code></td>
<td>Two guest classes, a domain ABI, Bubblewrap</td>
</tr>
<tr class="odd">
<td><code>0096</code></td>
<td>Guest transport on Fly, measured</td>
</tr>
</tbody>
</table>
</section>
<section id="measurements-taken-on-the-retired-tier-5-records" class="level2">
<h2 class="anchored" data-anchor-id="measurements-taken-on-the-retired-tier-5-records">Measurements taken on the retired tier, 5 records</h2>
<p>Each number was measured against libh2o and FoundationDB. The measurement stands as history. It guides nothing on the new tier.</p>
<table class="caption-top table">
<thead>
<tr class="header">
<th>RFD</th>
<th>Subject</th>
</tr>
</thead>
<tbody>
<tr class="odd">
<td><code>0098</code></td>
<td>The rollback snapshot budget</td>
</tr>
<tr class="even">
<td><code>0100</code></td>
<td>256 kbps per client, 47 concurrent</td>
</tr>
<tr class="odd">
<td><code>0101</code></td>
<td>zstd delta on the wire</td>
</tr>
<tr class="even">
<td><code>0102</code></td>
<td>The whole deployment on 15 USD</td>
</tr>
<tr class="odd">
<td><code>0104</code></td>
<td>Hypothesis, 1000 concurrent users</td>
</tr>
</tbody>
</table>
</section>
<section id="records-whose-subject-is-an-archived-repository-8-records" class="level2">
<h2 class="anchored" data-anchor-id="records-whose-subject-is-an-archived-repository-8-records">Records whose subject is an archived repository, 8 records</h2>
<table class="caption-top table">
<colgroup>
<col style="width: 8%">
<col style="width: 56%">
<col style="width: 34%">
</colgroup>
<thead>
<tr class="header">
<th>RFD</th>
<th>Subject</th>
<th>Archived repository</th>
</tr>
</thead>
<tbody>
<tr class="odd">
<td><code>0014</code></td>
<td>Recursive art-game loop, prediscussion</td>
<td><code>friends-art-game-loop</code></td>
</tr>
<tr class="even">
<td><code>0027</code></td>
<td>Umbrella package installs every component</td>
<td><code>fabric-platform-central</code></td>
</tr>
<tr class="odd">
<td><code>0047</code></td>
<td>webtransportd out-of-process adapter</td>
<td>no such repository exists</td>
</tr>
<tr class="even">
<td><code>0056</code></td>
<td>Verification smokes as a quadlet queue</td>
<td><code>fabric-container-verify</code></td>
</tr>
<tr class="odd">
<td><code>0057</code></td>
<td>Repository map of the loot-action slice</td>
<td><code>godot-loop-slice</code></td>
</tr>
<tr class="even">
<td><code>0058</code></td>
<td>WebTransport on one persistent stream</td>
<td>follows <code>rfd/0047</code></td>
</tr>
<tr class="odd">
<td><code>0065</code></td>
<td>fabric-platform-central packaging</td>
<td><code>fabric-platform-central</code></td>
</tr>
<tr class="even">
<td><code>0069</code></td>
<td>Defer the loot-slice hardening scope</td>
<td><code>godot-loop-slice</code></td>
</tr>
<tr class="odd">
<td><code>0070</code></td>
<td>Loop-slice telemetry to the collector</td>
<td><code>godot-loop-slice</code></td>
</tr>
<tr class="even">
<td><code>0081</code></td>
<td>Three-layer verification with CBMC</td>
<td><code>zone-server-h2o</code></td>
</tr>
<tr class="odd">
<td><code>0087</code></td>
<td>Avatar IK with Align.lean over mink</td>
<td><code>zone-server-h2o</code></td>
</tr>
<tr class="even">
<td><code>0093</code></td>
<td>taskweft relations to linear automata</td>
<td><code>zone-server-h2o</code> ReBAC</td>
</tr>
</tbody>
</table>
</section>
<section id="the-four-that-stay-and-why" class="level2">
<h2 class="anchored" data-anchor-id="the-four-that-stay-and-why">The four that stay, and why</h2>
<p>YAGNI keeps a record when a live consumer needs it now.</p>
<ul>
<li><code>rfd/0005</code> glTF interactivity value types. <code>datasource-flow</code> and <code>datasource-flow-project</code> are live repositories, and this record describes their taxonomy.</li>
<li><code>rfd/0007</code> Godot double-precision <code>template_release</code>. The scope names an archived repository, and <code>zone-client-godot</code>, <code>zone-baker</code>, and <code>rfd/0031</code>’s merged authoring build all need double precision.</li>
<li><code>rfd/0045</code> Loot-action core-loop MVP. The record of what the game loop is. <code>rfd/0085</code>’s Gyre sits on this shell.</li>
<li><code>rfd/0067</code> Release tag progression. The scope names an archived repository, and <code>rfd/0090</code>’s live Burrito release of <code>uro</code> uses the convention.</li>
</ul>
</section>
<section id="inbound-citations" class="level2">
<h2 class="anchored" data-anchor-id="inbound-citations">Inbound citations</h2>
<p>53 citations in surviving files name a retired record. 16 name a full path and become links into the archive. The rest name a bare number and stay as citations, which follows <code>rfd/0106</code>.</p>
<p><code>data/measurements/README.md</code> opens with “Measurements: rfd/0096 through rfd/0103”. Both records are retired. The parquet files stay in the manuals repository, and whether the measurement data follows its records into the archive is open.</p>
</section>
<section id="related" class="level2">
<h2 class="anchored" data-anchor-id="related">Related</h2>
<ul>
<li><code>rfd/0071-yagni-times-structure-to-need</code>: the rule this applies.</li>
<li><code>rfd/0106-split-the-archive-into-its-own-repo</code>: the method and the destination.</li>
</ul>


</section>

 ]]></description>
  <guid>https://v-sekai-multiplayer-fabric.github.io/multiplayer-fabric-manuals/rfd/0117-retire-the-h2o-tier-record-set/</guid>
  <pubDate>Tue, 18 Aug 2026 01:10:59 GMT</pubDate>
</item>
<item>
  <title>RFD 0116: The zone server is Elixir with epoll, in the h2o style</title>
  <link>https://v-sekai-multiplayer-fabric.github.io/multiplayer-fabric-manuals/rfd/0116-elixir-epoll-zone-server-in-the-h2o-style/</link>
  <description><![CDATA[ 





<p><em><a href="../../index.html">Manuals</a> › <a href="../../pages/rfd.html">RFDs</a> › RFD 0116: The zone server is Elixir with epoll, in the h2o style</em></p>
<p><strong>State:</strong> published · <strong>Scope:</strong> zone server implementation, deployment tier</p>
<section id="problem" class="level2">
<h2 class="anchored" data-anchor-id="problem">Problem</h2>
<p><code>rfd/0083</code> replaced the Godot <code>FabricZone</code> server with <code>zone-server-h2o</code>, a native libh2o and FoundationDB server built with Fil-C. That tier works. <code>rfd/0095</code> records a Godot ELF that runs 57.9 million instructions and reads <code>project.godot</code> out of FoundationDB.</p>
<p>The tier is not affordable. It is a fourth language and a fourth runtime next to Elixir, CockroachDB, and Godot. Each one carries its own build, its own deployment, its own failure modes, and its own learning cost. <code>zone-server-h2o</code> is archived on GitHub for that reason.</p>
<p>An archive with no replacement record leaves the project with no answer to what serves a zone.</p>
</section>
<section id="decision" class="level2">
<h2 class="anchored" data-anchor-id="decision">Decision</h2>
<p>The zone server is Elixir, on an epoll loop, in the style h2o uses. It is slower than the native tier. The project accepts the slower server, because it costs no fifth thing to build and operate.</p>
<p>The affordable tier is three parts, and the zone server is inside the first:</p>
<ul>
<li>Elixir, which holds the zone server and <code>zone-backend</code>.</li>
<li>CockroachDB, which holds the state. <code>rfd/0006</code> returns to force.</li>
<li>Godot, which is the client and the content tool.</li>
</ul>
<p>“In the h2o style” names the shape, not the library. A small number of event loops own their own sockets, each loop reads without blocking, and no request holds a loop while it waits. The work that <code>rfd/0072</code>-<code>0084</code> did against libh2o does not port. Those records are retired by <code>rfd/0117</code>.</p>
<p>This supersedes <code>rfd/0083</code>.</p>
</section>
<section id="references" class="level2">
<h2 class="anchored" data-anchor-id="references">References</h2>
<ul>
<li>The cost argument, what carries over, and what does not: <code>DETAILS.md</code></li>
<li><a href="https://github.com/v-sekai-multiplayer-fabric/multiplayer-fabric-archive/tree/main/rfd/0083-zone-server-h2o-replaces-godot-fabriczone"><code>rfd/0083-zone-server-h2o-replaces-godot-fabriczone</code></a>: the decision this supersedes.</li>
</ul>
</section>
<section id="detail" class="level2">
<h2 class="anchored" data-anchor-id="detail">Detail</h2>
</section>
<section id="why-the-native-tier-is-not-affordable" class="level2">
<h2 class="anchored" data-anchor-id="why-the-native-tier-is-not-affordable">Why the native tier is not affordable</h2>
<p>The cost is not the machine. <code>rfd/0102</code> costed the deployment at 15 USD with the native server in it.</p>
<p>The cost is the tier. <code>zone-server-h2o</code> adds C to a project that already runs Elixir, CockroachDB, and Godot. C brings its own toolchain, its own memory-safety burden, its own vendored <code>thirdparty/</code> tree, and its own class of bug. <code>rfd/0095</code> shows where that leads. Seventeen syscall handlers, an invented POSIX layer over a key-value store, and a list of next failures with no end.</p>
<p>One person cannot carry four runtimes. The decision buys back a runtime and pays for it in throughput.</p>
</section>
<section id="what-carries-over" class="level2">
<h2 class="anchored" data-anchor-id="what-carries-over">What carries over</h2>
<p>The design records that describe the protocol, the state, and the game survive the host change. They are not about libh2o.</p>
<table class="caption-top table">
<colgroup>
<col style="width: 39%">
<col style="width: 60%">
</colgroup>
<thead>
<tr class="header">
<th>Record</th>
<th>Why it survives</th>
</tr>
</thead>
<tbody>
<tr class="odd">
<td><code>rfd/0046</code> server authority with deferred rollback</td>
<td>The authority model any zone server implements.</td>
</tr>
<tr class="even">
<td><code>rfd/0049</code> fabric channels as reliability classes</td>
<td>Wire protocol, independent of what serves it.</td>
</tr>
<tr class="odd">
<td><code>rfd/0050</code> five-second transaction limit</td>
<td>Bounded operations, proven in <code>lean-connection-fsm</code>.</td>
</tr>
<tr class="even">
<td><code>rfd/0053</code> integral entity-transform wire</td>
<td>Generated from <code>lean-entity-packet</code>.</td>
</tr>
<tr class="odd">
<td><code>rfd/0008</code>, <code>rfd/0023</code> WebTransport over HTTP/3</td>
<td>The transport choice. Elixir speaks it.</td>
</tr>
<tr class="even">
<td>The hexagon cores and their Lean workspaces</td>
<td><code>rfd/0044</code> puts Lean at build time behind flat C, which an Elixir NIF calls.</td>
</tr>
</tbody>
</table>
</section>
<section id="what-does-not-carry-over" class="level2">
<h2 class="anchored" data-anchor-id="what-does-not-carry-over">What does not carry over</h2>
<p>Every record scoped to the internals of the native host retires. The work was real and the measurements were real. Neither transfers to a BEAM process.</p>
<ul>
<li>The libh2o event loop and its callback discipline.</li>
<li>The FoundationDB C API path, its value encoding, and its compression.</li>
<li><code>libriscv</code> as the guest engine, and the guest ABI built on it.</li>
<li>eBPF and XDP in the request path.</li>
<li>CBMC as a verification layer, which needs C to check.</li>
</ul>
<p><code>rfd/0117</code> lists all 43 retired records.</p>
</section>
<section id="the-gyre-keeps-its-setting-not-its-host" class="level2">
<h2 class="anchored" data-anchor-id="the-gyre-keeps-its-setting-not-its-host">The Gyre keeps its setting, not its host</h2>
<p><code>rfd/0085</code>, <code>rfd/0091</code>, and <code>rfd/0094</code> stay live. Each one names <code>zone-server-h2o</code> as the host. Each carries an amendment naming this record instead.</p>
<p>The Gyre is a MUD setting and a web client. Neither needs a native host. <code>rfd/0094</code>’s UGC loop stays as an intent, and its libriscv mechanism does not.</p>
</section>
<section id="what-this-record-does-not-settle" class="level2">
<h2 class="anchored" data-anchor-id="what-this-record-does-not-settle">What this record does not settle</h2>
<p>The throughput of the Elixir server is not measured. Every number in <code>data/measurements/</code> came off the native tier and a FoundationDB probe. A record that states the new tier’s ceiling needs a new measurement, not an extrapolation from the old one.</p>
<p><code>ecto_foundationdb</code> and <code>ecto-bench-tpcc</code> are active repositories that exist to put Uro on FoundationDB. This decision removes their reason. Their repository state is open.</p>
</section>
<section id="related" class="level2">
<h2 class="anchored" data-anchor-id="related">Related</h2>
<ul>
<li><code>rfd/0006-cockroachdb-with-mtls-role-separation</code>: the database this tier returns to.</li>
<li><code>rfd/0046-server-authoritative-simulation-deferred-rollback</code>: the authority model the new server implements.</li>
<li><code>rfd/0117-retire-the-h2o-tier-record-set</code>: retires the records that described the native tier.</li>
</ul>


</section>

 ]]></description>
  <guid>https://v-sekai-multiplayer-fabric.github.io/multiplayer-fabric-manuals/rfd/0116-elixir-epoll-zone-server-in-the-h2o-style/</guid>
  <pubDate>Tue, 18 Aug 2026 01:10:59 GMT</pubDate>
</item>
<item>
  <title>RFD 0115: A planner domain document is a cheap-layer surface</title>
  <link>https://v-sekai-multiplayer-fabric.github.io/multiplayer-fabric-manuals/rfd/0115-a-planner-domain-document-is-a-cheap-layer-surface/</link>
  <description><![CDATA[ 





<p><em><a href="../../index.html">Manuals</a> › <a href="../../pages/rfd.html">RFDs</a> › RFD 0115: A planner domain document is a cheap-layer surface</em></p>
<p><strong>State:</strong> discussion · <strong>Scope:</strong> planner domain encoding, CBOR-LD, taskweft, QCBOR</p>
<section id="problem" class="level2">
<h2 class="anchored" data-anchor-id="problem">Problem</h2>
<p>RFD 0003 settled the cheap layer: CBOR-LD, <code>QCBOR</code> over <code>zcbor</code>, RFC 8949 section 4.2 determinism, and an offline <code>jsonld-cpp</code> authoring role against a pure-C decode role. It scoped that to one artifact, the manifest inside a sandboxed <code>libriscv</code> package.</p>
<p>A planner domain document is a different artifact. <code>taskweft/nif</code> reads one as its input, <code>fabric-store-domain</code> carries one for the ward, and <code>fabric-behaviour-domain</code> wants one when its planner decides what a body does. Three repos, one artifact, and no recorded encoding, so each answers alone. One already has: <code>DETAILS.md</code> records the encoder it reached for.</p>
</section>
<section id="decision" class="level2">
<h2 class="anchored" data-anchor-id="decision">Decision</h2>
<p><strong>A planner domain document is a cheap-layer surface.</strong> RFD 0003’s decisions apply to it unchanged. This RFD adds no terms of its own and exists to widen that scope.</p>
<p><strong>The store plane holds it, and an edge serves it.</strong> It is a row in SQLite over the FoundationDB VFS, in the same database as the world it describes, so it travels with that world at the speed of a page reference. <code>aria-storage</code> holds packages and assets.</p>
<p><strong>Determinism holds for a local reason.</strong> RFD 0003 wanted it for <code>casync</code> deduplication, which these documents never reach. A ward replays from a seed and compares fingerprints, so a document that encoded two ways for one logical input would break the check that catches every other drift.</p>
<p><strong>The authoring tool lives wherever it is first needed.</strong> No seam in it for a second consumer that has not asked yet.</p>
</section>
<section id="references" class="level2">
<h2 class="anchored" data-anchor-id="references">References</h2>
<ul>
<li>The encoder that prompted this, and what it got wrong: <code>DETAILS.md</code></li>
<li><code>rfd/0003-castspell-sandbox-package-and-manifest-encoding</code>: every term this RFD reuses, and the reasoning behind each.</li>
</ul>
</section>
<section id="related" class="level2">
<h2 class="anchored" data-anchor-id="related">Related</h2>
<ul>
<li><code>rfd/0093-compile-taskweft-to-linear-automata</code>: the planner this feeds.</li>
<li><code>rfd/0107-janet-scripting-over-a-c-taskweft-core</code>: the other consumer of a taskweft core in pure C.</li>
</ul>
</section>
<section id="detail" class="level2">
<h2 class="anchored" data-anchor-id="detail">Detail</h2>
</section>
<section id="the-encoder-that-prompted-this" class="level2">
<h2 class="anchored" data-anchor-id="the-encoder-that-prompted-this">The encoder that prompted this</h2>
<p><code>fabric-store-domain</code> reached for a hand-written CBOR codec before this RFD existed. The encoder walked a <code>tsl::ordered_map</code> and wrote pairs in insertion order, carrying a comment that called the order correct.</p>
<p>The comment was right about round-tripping. Encode a document, decode it, and the value that comes back matches the value that went in, every time. RFC 8949 section 4.2 wants map keys in lexicographic order, so one set of rules assembled by two paths becomes two byte strings, and nothing downstream can tell that apart from a genuine change.</p>
<p>A test that encodes and then decodes never sees this. It measures whether a writer agrees with its own reader, and the two agreed. It would surface later as a ward whose fingerprint moved with nothing in the game having moved, which is the one signal the replay check exists to make trustworthy.</p>
<p>That is the case for a library with production hours behind it. RFD 0003 picked <code>QCBOR</code> on exactly that basis, and the bug above is the kind other people already found in it.</p>
</section>
<section id="the-three-consumers" class="level2">
<h2 class="anchored" data-anchor-id="the-three-consumers">The three consumers</h2>
<p><code>taskweft/nif</code> reads a domain document through <code>TwLoader::load_domain</code>, which takes an already-decoded value rather than a string. That signature is what lets a decoder live outside the vendored headers, which matters because the vendored tree stays byte-identical to upstream and a fix in it belongs upstream.</p>
<p><code>fabric-store-domain</code> carries one for the ward, tracked in its issue #9.</p>
<p><code>fabric-behaviour-domain</code> pairs a planner with ARDY, and the thing that planner is told is a domain document too.</p>
</section>
<section id="why-the-store-plane-rather-than-aria-storage" class="level2">
<h2 class="anchored" data-anchor-id="why-the-store-plane-rather-than-aria-storage">Why the store plane rather than aria-storage</h2>
<p>A domain document describes a world. Keeping it in the same SQLite database as that world means a ward that migrates between machines does not leave its rules behind, since a page reference moves rather than a copy. It also makes the rules readable the way everything else there is readable, which is the premise <code>fabric-store-domain</code> runs on: what you can see of the game is what you can <code>SELECT</code>.</p>
<p><code>aria-storage</code> is content-addressed <code>casync</code> chunks for packages and assets. A domain document is neither, and RFD 0003’s determinism argument travels with <code>casync</code> rather than with the format.</p>
</section>
<section id="what-this-does-not-settle" class="level2">
<h2 class="anchored" data-anchor-id="what-this-does-not-settle">What this does not settle</h2>
<p>Whether the document is written into the ward at founding or read from the binary each run. Both end with the same bytes in the same place, and the choice is about who owns the migration when the schema changes.</p>


</section>

 ]]></description>
  <guid>https://v-sekai-multiplayer-fabric.github.io/multiplayer-fabric-manuals/rfd/0115-a-planner-domain-document-is-a-cheap-layer-surface/</guid>
  <pubDate>Tue, 18 Aug 2026 01:10:59 GMT</pubDate>
</item>
<item>
  <title>RFD 0114: Prove the store by breaking it</title>
  <link>https://v-sekai-multiplayer-fabric.github.io/multiplayer-fabric-manuals/rfd/0114-prove-the-store-by-breaking-it/</link>
  <description><![CDATA[ 





<p><em><a href="../../index.html">Manuals</a> › <a href="../../pages/rfd.html">RFDs</a> › RFD 0114: Prove the store by breaking it</em></p>
<p><strong>State:</strong> discussion · <strong>Scope:</strong> store verification, fault tolerance test, disaster recovery procedure</p>
<section id="problem" class="level2">
<h2 class="anchored" data-anchor-id="problem">Problem</h2>
<p>A FoundationDB cluster reports its own fault tolerance. A test that reads <code>Fault Tolerance - 1 zones</code> and stops there asserts nothing. A cluster that reports tolerance, and then loses data when a zone stops, passes that test.</p>
<p><code>ci.yml</code> in the store plane already names this failure: a green step that asserted nothing is the failure the suite exists to prevent.</p>
<p>A backup that nobody restores is a belief, not a backup.</p>
</section>
<section id="decision" class="level2">
<h2 class="anchored" data-anchor-id="decision">Decision</h2>
<p><strong>Stop a zone. Then read the data.</strong> The test writes 200 keys, stops one zone with <code>kill -9</code>, and then checks that the cluster still answers and that all 200 keys are still readable. It then restarts the zone and waits for tolerance to return.</p>
<p>Use many keys, not one. One key lives on one storage team. A stop can miss it, and placement luck then looks like redundancy. The first version of this test passed with one key under <code>single</code> redundancy, which holds no second copy at all.</p>
<p><strong>Stop two of three zones. Then confirm the cluster refuses to work.</strong> With no quorum, FoundationDB must stop answering. It must not take a write it cannot order. In this stage a working cluster is a failure, because a minority that answers can split-brain.</p>
<p><strong>Restore into a cluster that was wiped, and prove it was empty first.</strong> The disaster recovery test backs up 200 keys, deletes the data directories, confirms the new cluster holds 0 keys, and then restores. Without the empty check, the restore proves nothing.</p>
<p><strong>A backup is real only when <code>describe</code> says <code>Restorable: true</code>.</strong> Record the container, not the folder that the backup was sent to. <code>DETAILS.md</code> gives the trap.</p>
<p><strong>Run these as <code>systemd</code> oneshot units.</strong> The run survives the shell, the exit status stays readable, and the output goes to the journal with times.</p>
</section>
<section id="references" class="level2">
<h2 class="anchored" data-anchor-id="references">References</h2>
<ul>
<li>The two traps, and the negative control: <code>DETAILS.md</code></li>
<li><code>rfd/0113-foundationdb-on-windows-built-from-source</code>: the store under test.</li>
</ul>
</section>
<section id="related" class="level2">
<h2 class="anchored" data-anchor-id="related">Related</h2>
<ul>
<li><code>rfd/0109-two-tiers-with-foundationdb-as-the-store</code>: why the store is FoundationDB.</li>
</ul>
</section>
<section id="detail" class="level2">
<h2 class="anchored" data-anchor-id="detail">Detail</h2>
</section>
<section id="trap-one-a-backup-url-that-is-not-restorable-and-does-not-say-so" class="level2">
<h2 class="anchored" data-anchor-id="trap-one-a-backup-url-that-is-not-restorable-and-does-not-say-so">Trap one: a backup URL that is not restorable, and does not say so</h2>
<p><code>fdbbackup start -d file:///backup/</code> does not write the backup to that path. It makes a container under it, <code>backup-&lt;timestamp&gt;/</code>, and writes there.</p>
<p>Point <code>describe</code> at the path you gave, and it answers:</p>
<pre><code>URL: file:///backup/
Restorable: false
SnapshotBytes: 0</code></pre>
<p>It does not fail. It reports a backup that cannot be restored, with a zero exit status. A runbook that records the <code>-d</code> path looks like it holds backups. It finds out at recovery time that it does not.</p>
<p>A named subfolder does not correct this. It was measured. A backup sent to <code>named-by-procedure/</code> made <code>named-by-procedure/backup-&lt;timestamp&gt;/</code> inside it, and <code>describe</code> on the named folder still answered <code>Restorable: false</code> and <code>SnapshotBytes: 0</code>. The behaviour is unconditional, so no name is restorable by itself. A subfolder per run is still useful, because it lets one run expire without touching another. It is not a correction.</p>
<p>Two rules follow:</p>
<ol type="1">
<li>Find the container. Do not assume it. <code>fdbbackup list -b &lt;base&gt;</code> gives it, and it works for <code>blobstore://</code> as well, which <code>ls</code> does not.</li>
<li>Check <code>Restorable: true</code> before you trust a backup.</li>
</ol>
<p>The same procedure holds for S3. Only the URL changes, from <code>file://</code> to <code>blobstore://&lt;key&gt;:&lt;secret&gt;@&lt;host&gt;/&lt;name&gt;?bucket=&lt;bucket&gt;</code>.</p>
</section>
<section id="trap-two-a-key-count-that-reads-as-total-data-loss" class="level2">
<h2 class="anchored" data-anchor-id="trap-two-a-key-count-that-reads-as-total-data-loss">Trap two: a key count that reads as total data loss</h2>
<p><code>fdbcli</code> opens a quoted key with a backtick, not an apostrophe:</p>
<pre><code>`weft/qa/k0001' is `v1'</code></pre>
<p>A counter that matches a leading apostrophe counts zero keys. Zero keys after a restore reads exactly like total data loss. On a new platform this sends you to look for a durability defect in the storage layer that does not exist.</p>
</section>
<section id="the-negative-control" class="level2">
<h2 class="anchored" data-anchor-id="the-negative-control">The negative control</h2>
<p><code>single</code> redundancy holds one copy of every key. A zone that stops must lose data. The negative control runs the same test under <code>single</code> redundancy, and it must fail.</p>
<p>It found a real weakness. The first version wrote one key, and the test passed under <code>single</code> redundancy: the stopped zone did not hold that key. The test measured placement luck. With 200 keys, a zone that stops always holds some of them, and the control fails as it must.</p>
<p><code>SKIP_CLAIM=1</code> takes the control past the status line check, so that the stop, and not the reported number, is the thing under test.</p>
</section>
<section id="a-transaction-that-is-too-large-for-a-new-cluster" class="level2">
<h2 class="anchored" data-anchor-id="a-transaction-that-is-too-large-for-a-new-cluster">A transaction that is too large for a new cluster</h2>
<p>200 <code>set</code> commands in one <code>fdbcli</code> call return <code>commit_unknown_result (1021)</code> on a cluster that has just been configured. Data distribution is still settling and the whole batch is one commit.</p>
<p>Write in batches of 50, and retry. Error 1021 means what it says: the commit may have succeeded. A repeated <code>set</code> of the same value is safe.</p>
</section>
<section id="what-one-machine-cannot-test" class="level2">
<h2 class="anchored" data-anchor-id="what-one-machine-cannot-test">What one machine cannot test</h2>
<p>These tests stop a process. They do not stop a machine, and they do not cut a network. A process that stops exercises the consensus path and the recovery path. It does not make a network partition, and it does not make two halves that both believe they hold the data.</p>
<p>GitHub Actions runners cannot supply that. It needs two hosts.</p>


</section>

 ]]></description>
  <guid>https://v-sekai-multiplayer-fabric.github.io/multiplayer-fabric-manuals/rfd/0114-prove-the-store-by-breaking-it/</guid>
  <pubDate>Tue, 18 Aug 2026 01:10:59 GMT</pubDate>
</item>
<item>
  <title>RFD 0113: FoundationDB on Windows, built from source</title>
  <link>https://v-sekai-multiplayer-fabric.github.io/multiplayer-fabric-manuals/rfd/0113-foundationdb-on-windows-built-from-source/</link>
  <description><![CDATA[ 





<p><em><a href="../../index.html">Manuals</a> › <a href="../../pages/rfd.html">RFDs</a> › RFD 0113: FoundationDB on Windows, built from source</em></p>
<p><strong>State:</strong> discussion · <strong>Scope:</strong> store platform support, build toolchain, packaging</p>
<section id="problem" class="level2">
<h2 class="anchored" data-anchor-id="problem">Problem</h2>
<p><code>rfd/0109</code> selects FoundationDB as the store. The store must now also run on Windows. It must run the server, not only the client. A Linux virtual machine on the Windows host does not count.</p>
<p>FoundationDB supplies no Windows binaries. Release 7.3.76 has deb, rpm, <code>.so</code>, x86_64 and aarch64 assets. A scan of the last 60 releases finds no Windows, MSI, DLL or EXE asset.</p>
<p>Upstream looks like it tests this, and it does not. The <code>windows-boost-test.yml</code> workflow has a step with the name “Configure and Build”. That step runs only <code>cmake ..</code>. No step after configure runs on Windows.</p>
</section>
<section id="decision" class="level2">
<h2 class="anchored" data-anchor-id="decision">Decision</h2>
<p><strong>Build FoundationDB for Windows from source.</strong> Nine other stores were examined. Each one failed on the same two constraints: a native Windows server, or a store that a second machine can reach. <code>DETAILS.md</code> lists them.</p>
<p><strong>Use clang-cl. Do not use MSVC.</strong> Flow uses GNU <code>__attribute__</code> syntax in many files. It has never compiled with <code>cl.exe</code>. Use Ninja with clang-cl, and set the MSVC environment first. An attempt with the default MSVC toolset failed on <code>__PRETTY_FUNCTION__</code> and <code>swift_attr</code>. Those errors were the wrong compiler, not defects in FoundationDB.</p>
<p><strong>Build Linux in the official image</strong>, <code>foundationdb/build</code>. The image has all dependencies. Linux is the control. A red Windows job has no meaning until Linux is green.</p>
<p><strong>Package with fpm on Linux and macOS. Use FoundationDB’s own MSI on Windows.</strong> <code>packaging/msi/</code> already holds the WiX sources and <code>cmake/FindWIX.cmake</code>.</p>
<p><strong>If the port fails, use sharded PostgreSQL.</strong> It supplies a native Windows server today. The cost is per-shard replication and promotion, built by hand.</p>
</section>
<section id="references" class="level2">
<h2 class="anchored" data-anchor-id="references">References</h2>
<ul>
<li>The nine stores, the evidence, and the measurements: <code>DETAILS.md</code></li>
<li>Work in progress: <code>v-sekai-multiplayer-fabric/foundationdb</code>, branch <code>portability-consensus</code></li>
<li><code>rfd/0109-two-tiers-with-foundationdb-as-the-store</code>: why the store is FoundationDB.</li>
</ul>
</section>
<section id="related" class="level2">
<h2 class="anchored" data-anchor-id="related">Related</h2>
<ul>
<li><code>rfd/0114-prove-the-store-by-breaking-it</code>: how a built store is verified.</li>
</ul>
</section>
<section id="detail" class="level2">
<h2 class="anchored" data-anchor-id="detail">Detail</h2>
</section>
<section id="the-nine-stores-and-why-each-one-failed" class="level2">
<h2 class="anchored" data-anchor-id="the-nine-stores-and-why-each-one-failed">The nine stores, and why each one failed</h2>
<p>The store must satisfy four constraints at the same time: free and open source, reachable from a second machine, a native Windows server, and linear scaling per machine. It must also supply an ordered byte keyspace, atomic multi-key commits, and read-write conflict detection for the fence.</p>
<table class="caption-top table">
<colgroup>
<col style="width: 32%">
<col style="width: 67%">
</colgroup>
<thead>
<tr class="header">
<th>Store</th>
<th>Why it failed</th>
</tr>
</thead>
<tbody>
<tr class="odd">
<td>sqlite-in-sqlite, RocksDB, LeanStore, FASTER</td>
<td>Embedded. No second machine can reach the data.</td>
</tr>
<tr class="even">
<td>TiKV, Cassandra, ScyllaDB, LeanStore</td>
<td>No native Windows server.</td>
</tr>
<tr class="odd">
<td>TiKV, LeanStore</td>
<td>Snapshot isolation. <code>check_fence</code> has no mechanism.</td>
</tr>
<tr class="even">
<td>FASTER</td>
<td>A hash index. No ordered range reads.</td>
</tr>
<tr class="odd">
<td>Cassandra</td>
<td>Partition size limits against 10 GiB actors. No isolation across partitions.</td>
</tr>
<tr class="even">
<td>Zenoh, iceoryx2 tunnels and gateways</td>
<td>Message transport, not a store. Asynchronous replication gives split-brain and silent loss.</td>
</tr>
<tr class="odd">
<td>Ra</td>
<td>A Raft library. You write the store yourself, on the BEAM this plane exists to leave.</td>
</tr>
<tr class="even">
<td>FerretDB and MongoDB forks</td>
<td>The wrong data model. Transactions have time and size limits.</td>
</tr>
</tbody>
</table>
<p>Two stores satisfy all four constraints: FoundationDB, if it can be built for Windows, and sharded PostgreSQL.</p>
<p>A message transport cannot replace the store. If a commit is acknowledged locally and sent later, a machine that stops loses an unknown number of commits, and it does so silently. If the commit waits for a remote acknowledgement, it pays a network round trip. The local speed was the missing guarantee.</p>
</section>
<section id="what-the-build-already-shows" class="level2">
<h2 class="anchored" data-anchor-id="what-the-build-already-shows">What the build already shows</h2>
<ul>
<li><code>release-7.3</code> and <code>main</code> both complete <code>cmake</code> configure on Windows.</li>
<li>The Windows disk path is complete in <code>release-7.3</code>. <code>fdbrpc/include/fdbrpc/AsyncFileWinASIO.actor.h</code> exists under <code>#ifdef WIN32</code>. It implements <code>IAsyncFile</code> over <code>boost::asio</code>, which uses IOCP on Windows. <code>AsyncFileKAIO</code> is <code>#ifdef __linux__</code>. <code>Net2FileSystem.cpp</code> selects KAIO only on Linux and falls through to <code>Net2AsyncFile</code> elsewhere. <strong>Nothing needs a rewrite, and io_uring is not a blocker in 7.3.</strong></li>
<li>The Flow actor compiler is Python on <code>main</code>, and the C# implementation is a fallback behind <code>FDB_USE_CSHARP_TOOLS</code>. An earlier estimate called the C# compiler a blocker. It is not one.</li>
<li><code>lattice-world-weft/weft-warp-loop</code> builds Flow, including the asio layer, green on <code>windows-latest</code>, with no Windows patch to the source.</li>
</ul>
<p>Windows IORing is not used. Its operation set started read-only, it needs Windows 11 or Server 2022, and it makes a storage engine faster that must first compile. <code>AsyncFileWinASIO</code> already supplies IOCP through <code>boost::asio</code>.</p>
</section>
<section id="dependencies-that-stop-the-build" class="level2">
<h2 class="anchored" data-anchor-id="dependencies-that-stop-the-build">Dependencies that stop the build</h2>
<p>Each one was found by a failure, and each one cost a full CI round:</p>
<ul>
<li><code>jemalloc</code>. Configure stops without it.</li>
<li><code>fmt</code>. Configure stops without it.</li>
<li>Jinja2. <code>flow/protocolversion/protocol_version.py</code> generates <code>ProtocolVersion.h</code>.</li>
<li>Boost needs <code>format</code>, <code>asio</code> and <code>lockfree</code>. Upstream’s Windows workflow installs a component list that is enough to configure and not enough to compile.</li>
</ul>
<p>The official <code>foundationdb/build</code> image removes all of these on Linux. Use it instead of a dependency list.</p>
</section>
<section id="cost-measured" class="level2">
<h2 class="anchored" data-anchor-id="cost-measured">Cost, measured</h2>
<p><code>bench_vfs 2000</code> on one machine, in operations per second per core. The unit is the one <code>weft/limits.hpp</code> uses.</p>
<table class="caption-top table">
<colgroup>
<col style="width: 32%">
<col style="width: 12%">
<col style="width: 27%">
<col style="width: 28%">
</colgroup>
<thead>
<tr class="header">
<th>Operation</th>
<th>Local file</th>
<th>FoundationDB, one node</th>
<th>FoundationDB, consensus</th>
</tr>
</thead>
<tbody>
<tr class="odd">
<td>Insert, one commit each</td>
<td>262,438</td>
<td>941</td>
<td>433</td>
</tr>
<tr class="even">
<td>Insert, one commit for all</td>
<td>618,078</td>
<td>399,456</td>
<td>315,869</td>
</tr>
<tr class="odd">
<td>Point read</td>
<td>2,056,234</td>
<td>1,892,751</td>
<td>2,027,633</td>
</tr>
<tr class="even">
<td>Scan</td>
<td>12,149,044</td>
<td>14,370,707</td>
<td>15,144,402</td>
</tr>
</tbody>
</table>
<p>Read the first row with care. The local file column uses <code>journal_mode=MEMORY</code> and sets no <code>synchronous</code> value, so it is not durable. A durable local SQLite gives 2,008 commits per second per core, not 262,438. FoundationDB passes it at about four concurrent writers, because FoundationDB groups commits and a file per writer cannot.</p>
<p><strong>Reads are already at parity.</strong> A point read costs 2,027,633 against a local file’s 2,056,234. No store choice can improve the read rows. The choice is decided on the commit row alone.</p>


</section>

 ]]></description>
  <guid>https://v-sekai-multiplayer-fabric.github.io/multiplayer-fabric-manuals/rfd/0113-foundationdb-on-windows-built-from-source/</guid>
  <pubDate>Tue, 18 Aug 2026 01:10:59 GMT</pubDate>
</item>
<item>
  <title>RFD 0112: Lexical for the slash command interface</title>
  <link>https://v-sekai-multiplayer-fabric.github.io/multiplayer-fabric-manuals/rfd/0112-lexical-for-the-slash-command-interface/</link>
  <description><![CDATA[ 





<p><em><a href="../../index.html">Manuals</a> › <a href="../../pages/rfd.html">RFDs</a> › RFD 0112: Lexical for the slash command interface</em></p>
<p><strong>State:</strong> abandoned · <strong>Scope:</strong> the Queen of the Gyre, and what serves her</p>
<section id="status" class="level2">
<h2 class="anchored" data-anchor-id="status">Status</h2>
<p>The slash command field is not being built. The client the Gyre is getting has hands in it rather than a caret: RFD 0119 puts salvage in a headset under distributed physics authority, and RFD 0118 takes the pipeline to a Godot release template driven by sandboxed guests. A Lexical editor serves neither.</p>
<p>The argument that chose Lexical over ProseMirror was about the caret – that a parameter must be one thing to it, and that a decorator node crosses in one key press where an atom node can trap. That argument was correct and it no longer has a subject.</p>
<p>What survives is the boundary the RFD drew rather than the widget it chose. Commands still arrive as a command in and reply bytes out, <code>thirdparty/interactor</code> still carries that contract, and the server still checks on receipt whatever a client chose to show, because a filtered menu is a convenience and never an authorization. The browser client in <code>fabric-store-domain/client/</code> reached a working editor and a passing caret suite, and it never opened a live session outside its own test.</p>
</section>
<section id="problem" class="level2">
<h2 class="anchored" data-anchor-id="problem">Problem</h2>
<p><code>fabric-store-domain/src/queen.c</code> has no client. Its <code>main()</code> founds a ward, runs the cycles, prints, and exits, with no socket and no instance that outlives the run. A slash command interface needs a live game and a field to type into, and neither exists.</p>
<p>The field is the harder half. When a player types <code>/commission</code>, it must show the parameter as an inline block that the player cannot edit, with editable space around it. An HTML form control holds plain text only. A <code>contenteditable</code> element can show a block, and written by hand it fails on the caret, on mobile autocorrect, on IME composition, and on paste.</p>
</section>
<section id="decision" class="level2">
<h2 class="anchored" data-anchor-id="decision">Decision</h2>
<p>Use Lexical, from Meta. Its decorator node is one block to the caret, which therefore crosses a parameter in one key press, and Meta tests the mobile and IME paths at Facebook scale. ProseMirror is rejected, because its atom node can hold the caret and needs a plugin to correct it. The cost is developer speed, so the version is pinned.</p>
<p>The Queen’s rows are entities and they take coordinates. A Spark, a venue, and a contract each become an entity in the zone model, so the interest filter in <code>fabric-fanout-edge</code> decides who sees a change. A Spark moving to a contract is local by intent and reaches every box it enters. <code>/restart</code> reaches everyone, because one ward serves all players.</p>
<p>The ward is served the way a stream is served. One primary holds the fence and writes. Secondaries read the same pages out of FoundationDB, and one is hosted when the primary goes, which <code>check_fence</code> makes safe by refusing the old writer. A player may hold a secondary as a fallback, and a background tab holds its seat with a keepalive.</p>
<p>The menu shows a command only when the caller’s rebac relations permit it. The server checks again on receipt. <code>fabric-asset-edge</code> serves the built client on Fly as a transport layer, and <code>queen</code> gains a transport layer of its own.</p>
</section>
<section id="references" class="level2">
<h2 class="anchored" data-anchor-id="references">References</h2>
<ul>
<li>Lexical: <a href="https://lexical.dev" class="uri">https://lexical.dev</a>. ProseMirror: <a href="https://prosemirror.net" class="uri">https://prosemirror.net</a></li>
<li>RFD 0111 sets the words transport layer and service. RFD 0085 holds the setting.</li>
<li>RFD 0049 holds the channel classes the ward scalars need. RFD 0050 sets the keepalive.</li>
<li>The comparison, the entity mapping, and three hazards: <code>DETAILS.md</code></li>
</ul>
</section>
<section id="detail" class="level2">
<h2 class="anchored" data-anchor-id="detail">Detail</h2>
</section>
<section id="context-and-problem-statement" class="level2">
<h2 class="anchored" data-anchor-id="context-and-problem-statement">Context and problem statement</h2>
<p><code>fabric-store-domain/src/queen.c</code> is the Queen of the Gyre. Its <code>main()</code> takes <code>play|check &lt;cycles&gt; [seed] [sparks]</code>, founds a ward, runs the cycles, prints a chronicle, and exits. There is no socket, no server, and no instance that outlives the run. The README says the same thing from the other side: the game has no renderer, no client, and no engine, and what you can see of it is what you can <code>SELECT</code>.</p>
<p>A slash command interface needs a live game and a place to type into. Neither exists yet.</p>
<p>The typing part is the harder half. When a player types <code>/commission</code>, the field must show the parameter as an inline block that the player cannot edit, with editable space around it. An <code>&lt;input&gt;</code> and a <code>&lt;textarea&gt;</code> hold plain text, so neither can show such a block. A <code>contenteditable</code> element can. Written by hand it fails on the caret, on mobile autocorrect, on IME composition, and on paste, and it fails in the input path of every command.</p>
</section>
<section id="decision-drivers" class="level2">
<h2 class="anchored" data-anchor-id="decision-drivers">Decision drivers</h2>
<ul>
<li>The interface is the input path of every command. A caret fault there stops play.</li>
<li>The framework must have production use at scale for more than one year.</li>
<li>Mobile keyboards and IME software are the hardest part of a <code>contenteditable</code> element, and they are the part a small project cannot test alone.</li>
</ul>
</section>
<section id="the-two-candidates" class="level2">
<h2 class="anchored" data-anchor-id="the-two-candidates">The two candidates</h2>
<table class="caption-top table">
<colgroup>
<col style="width: 16%">
<col style="width: 43%">
<col style="width: 39%">
</colgroup>
<thead>
<tr class="header">
<th></th>
<th>Lexical</th>
<th>ProseMirror</th>
</tr>
</thead>
<tbody>
<tr class="odd">
<td>Origin</td>
<td>Meta</td>
<td>Marijn Haverbeke</td>
</tr>
<tr class="even">
<td>Runs in</td>
<td>Facebook, WhatsApp Web, Workplace</td>
<td>Atlassian, The New York Times</td>
</tr>
<tr class="odd">
<td>Model</td>
<td>a state model away from the DOM, with decorator nodes</td>
<td>a schema and a transaction model, with atom nodes</td>
</tr>
<tr class="even">
<td>Shape it targets</td>
<td>short chat input, with mentions and emoji</td>
<td>long documents</td>
</tr>
<tr class="odd">
<td>Caret across a block</td>
<td>the decorator node is one block, and the caret crosses it</td>
<td>the atom node can hold the caret, and a plugin must correct it</td>
</tr>
<tr class="even">
<td>Mobile and IME</td>
<td>built for aggressive autocorrect and composition in chat</td>
<td>reliable for text, weaker around complex inline nodes</td>
</tr>
<tr class="odd">
<td>Production time</td>
<td>passes</td>
<td>passes</td>
</tr>
<tr class="even">
<td>Work with a language model</td>
<td>poor, because the v0.x API moves and models confuse it with Draft.js</td>
<td>good, because the API is stable and well represented</td>
</tr>
</tbody>
</table>
</section>
<section id="decision-outcome" class="level2">
<h2 class="anchored" data-anchor-id="decision-outcome">Decision outcome</h2>
<p>Lexical wins on the interface, and loses on developer speed. The interface decides, because this field is the input path of the game and the developer cost is paid once.</p>
<p>ProseMirror is the stronger choice for a team writing boilerplate with a language model. That advantage does not reach the player. A caret that sticks on a parameter block does reach the player, on every command.</p>
<p>The mitigation is written into the work. The <code>lexical</code> and <code>@lexical/*</code> versions are pinned exactly, the official documents are the source, and any prompt to a language model carries the current API in the prompt itself, because a model answers from an older version otherwise.</p>
</section>
<section id="one-instance-of-the-ward" class="level2">
<h2 class="anchored" data-anchor-id="one-instance-of-the-ward">One instance of the ward</h2>
<p>The game runs as one instance. Every player sees the same ward, and a command from one player changes what all of them see. <code>found_ward</code> drops and recreates every table, so <code>/restart</code> re-founds the ward for everyone at once.</p>
<p>One instance follows from the code rather than from a preference. <code>open_db</code> sets <code>PRAGMA locking_mode=EXCLUSIVE</code>, and the Queen is the single writer of the ward. The Fly app therefore stays one machine, which is the shape <code>min_machines_running = 1</code> and <code>auto_stop_machines = false</code> already describe elsewhere in the org. The client bundle is immutable, so whatever serves it scales without limit. Separating the two is what lets each follow its own rule.</p>
</section>
<section id="the-ward-as-a-zone-and-its-rows-as-entities" class="level2">
<h2 class="anchored" data-anchor-id="the-ward-as-a-zone-and-its-rows-as-entities">The ward as a zone, and its rows as entities</h2>
<p>The Queen’s rows are entities, and they take spatial coordinates. That puts the ward inside the zone and entity model the rest of the stack already uses, and it makes the interest filter apply to this game without inventing anything.</p>
<table class="caption-top table">
<colgroup>
<col style="width: 28%">
<col style="width: 37%">
<col style="width: 34%">
</colgroup>
<thead>
<tr class="header">
<th>the row in <code>queen.c</code></th>
<th>count</th>
<th>what it becomes</th>
</tr>
</thead>
<tbody>
<tr class="odd">
<td>a Spark, one SQLite database each</td>
<td>up to <code>MAX_SPARKS</code>, which is 64</td>
<td>a moving entity, with a purse and wear</td>
</tr>
<tr class="even">
<td>a venue from <code>VENUES</code></td>
<td><code>NVENUES</code>, which is 6</td>
<td>a fixed entity at the place it was built</td>
</tr>
<tr class="odd">
<td>the Queen</td>
<td>1</td>
<td>the authority, and the single writer</td>
</tr>
<tr class="even">
<td>a contract on the board</td>
<td><code>BOARD_SIZE</code> 6, or 9 with the Transit Rails</td>
<td>a marker at the place the work is</td>
</tr>
</tbody>
</table>
<p><code>spark_t</code> holds <code>id</code>, <code>purse</code>, and <code>wear</code> today, and no position. Adding <code>pos_um_x</code>, <code>pos_um_y</code>, and <code>pos_um_z</code> is the change that connects the two models, because those are the fields <code>XRGridEntityPacket</code> already carries in int64 micrometres.</p>
</section>
<section id="how-a-local-command-becomes-a-global-one" class="level2">
<h2 class="anchored" data-anchor-id="how-a-local-command-becomes-a-global-one">How a local command becomes a global one</h2>
<p>The interest filter decides how many players see a change. RFD 0111 retires the word plane, and the filter lives in <code>fabric-fanout-edge</code>, which is a transport layer.</p>
<p><code>lean-interest-mgmt/core/AuthorityInterest.lean</code> separates authority from interest. Exactly one zone advances an entity each tick, and a neighbour holds a read-only ghost. An entity enters that neighbour’s interest when its k-tick kinematic expansion overlaps the neighbour’s volume, with <code>interestLookahead</code> of 6 ticks. Then <code>fabric-fanout-edge/src/fanout.cpp</code> filters per subscriber: <code>ghost_aabb_of</code> widens each entity by <code>ENTITY_EXT_UM</code> of 500000 and by <code>GHOST_TICKS</code> of 2 along its velocity, and <code>aabb_overlaps</code> tests that against <code>subscriber_t.interest</code>.</p>
<p>So the reach of a command is the overlap between the entities it touches and the boxes the players hold. The Queen’s own verbs cover the whole range.</p>
<table class="caption-top table">
<colgroup>
<col style="width: 15%">
<col style="width: 28%">
<col style="width: 56%">
</colgroup>
<thead>
<tr class="header">
<th>command</th>
<th>what it changes in <code>queen.c</code></th>
<th>how far it reaches</th>
</tr>
</thead>
<tbody>
<tr class="odd">
<td><code>look</code></td>
<td>nothing</td>
<td>The caller only. A read changes no entity, so the filter never runs.</td>
</tr>
<tr class="even">
<td>a Spark takes a contract</td>
<td>that Spark’s position, as it goes to the work</td>
<td>Every subscriber whose box the Spark enters or leaves. This is the common case.</td>
</tr>
<tr class="odd">
<td><code>/pay &lt;spark&gt;</code></td>
<td>one purse, in one Spark database</td>
<td>Every subscriber whose box overlaps that Spark.</td>
</tr>
<tr class="even">
<td><code>/commission &lt;venue&gt;</code></td>
<td><code>venue.built</code>, and the ward treasury</td>
<td>The venue appears to every box that overlaps its place. The treasury reaches no box at all.</td>
</tr>
<tr class="odd">
<td><code>/restart</code></td>
<td>every table, through <code>found_ward</code></td>
<td>Every subscriber. No box excludes it.</td>
</tr>
</tbody>
</table>
<p>The Spark taking a contract is the one to hold on to. It is local by intent, and the filter turns it into a message to an arbitrary number of players, because the Spark leaves one set of boxes and enters another. Neither the player nor the command knows how many that is.</p>
</section>
<section id="primary-and-secondary-streams" class="level2">
<h2 class="anchored" data-anchor-id="primary-and-secondary-streams">Primary and secondary streams</h2>
<p>The ward is served the way a stream is served. One primary carries the live game. Secondaries carry the same ward to their own subscribers. When the primary goes, a secondary is hosted in its place, and a player may pick a secondary as a fallback while playing, so a promotion costs no reconnect.</p>
<p>The relay tree in <code>fanout.cpp</code> is already this shape. Its header says each relay process runs the leaf for its own subscriber set on its own NIC, so aggregate egress scales with the relay count. A secondary is one of those leaves.</p>
<p>The store plane supplies the promotion, and it needs no new mechanism. <code>thirdparty/store-plane/prove_handoff.c</code> proves the property the whole thing rests on: a database has no local file, so a different process reads it with no copy and no restore. A secondary therefore opens the same ward out of FoundationDB and is already current.</p>
<table class="caption-top table">
<colgroup>
<col style="width: 35%">
<col style="width: 64%">
</colgroup>
<thead>
<tr class="header">
<th>the stream idea</th>
<th>what it is in the ward</th>
</tr>
</thead>
<tbody>
<tr class="odd">
<td>the primary stream</td>
<td>the process holding the current fence, which is the only one that may write</td>
</tr>
<tr class="even">
<td>a secondary stream</td>
<td>a follower reading the same ward pages, holding no fence, writing nothing</td>
</tr>
<tr class="odd">
<td>hosting a secondary when the primary goes</td>
<td>the FENCE key moves, and the promoted process becomes the writer</td>
</tr>
<tr class="even">
<td>a fallback chosen while playing</td>
<td>a subscription to a second leaf, held open so promotion costs no reconnect</td>
</tr>
<tr class="odd">
<td>a viewer who switches away</td>
<td>a subscriber held by a keepalive, with the slices stopped</td>
</tr>
</tbody>
</table>
<p>The fence is what makes promotion safe. <code>check_fence</code> reads the FENCE key on every write transaction and refuses the write with <code>SQLITE_READONLY</code> when the value has moved. Its comment records why it exists: the VFS locks are no-ops, so two writers both believed they held the write lock, both reported success, <code>PRAGMA integrity_check</code> passed, and one writer’s 300 rows were gone. The fence turns that into a refusal the caller can see. So an old primary that comes back after a promotion is refused rather than silently losing the ward.</p>
<p><code>PRAGMA locking_mode=EXCLUSIVE</code> in <code>open_db</code> does not block a promotion. <code>fdb_lock</code>, <code>fdb_unlock</code>, and <code>fdb_check_lock</code> all return success without doing anything, and the pragma is there to stop SQLite re-reading page one, which over a network database is a round trip per query. A dead primary holds nothing that a new one must break.</p>
<p>A tab that goes to the background stops being worth sending to. The browser throttles a hidden tab and reports the change through <code>visibilitychange</code>, so the client stops asking for slices and holds the session with a keepalive. The leaf keeps the <code>subscriber_t</code> and sends nothing. When the tab comes back the slices resume, with no reconnect and no second authorization. RFD 0050 already sets a five-second transaction limit, which is the natural place to start for the keepalive interval.</p>
<p>The keepalive is also what makes a fallback cheap. A player picks a second leaf while playing and holds it by keepalive alone, so it costs a seat and no egress until the primary goes and the fallback starts sending. A fallback that streamed continuously would double that player’s share of the cap in the next section.</p>
<p>One thing is worth stating plainly. <code>queen</code> implements none of this today, and the fence lives in the VFS rather than in the game, so promotion is supported and unwritten.</p>
</section>
<section id="three-hazards-this-exposes" class="level2">
<h2 class="anchored" data-anchor-id="three-hazards-this-exposes">Three hazards this exposes</h2>
<p><code>MAX_SPARKS</code> is 64 and <code>MAX_SLICE_ENTITIES</code> is 64. Those two constants were written in different repositories for different reasons, and they are equal. A ward at full size therefore fills one subscriber’s slice with Sparks alone, and <code>fanout_one</code> stops at the cap, so the six venues, the Queen, and every contract marker fall off the end. A full ward is 64 Sparks, 6 venues, the Queen, and up to 9 contracts, which is 80 entities against a cap of 64.</p>
<p>The client cannot detect the loss. The receiver recovers the count as <code>len / 100</code>, so a truncated batch looks exactly like a small one.</p>
<p><code>/commission</code> shows the second hazard. It changes two things at once: a venue, which has a place, and the treasury, which does not. An interest box can carry the first and can never carry the second. The ward scalars <code>treasury</code>, <code>debt</code>, <code>issued</code>, <code>retired</code>, and <code>spent</code> are not spatial, and no <code>Aabb</code> describes them. They need the reliable control stream, and RFD 0049 holds the channel classes for it.</p>
<p>The third hazard follows from the second. <code>honest()</code> checks that <code>treasury + purses + retired + spent == issued</code> across the ward and every Spark database. That sum is global. A player holding one interest slice sees a few Sparks and cannot compute it, so the invariant stays a property the authority checks and the client takes on trust.</p>
</section>
<section id="what-serves-it-and-where" class="level2">
<h2 class="anchored" data-anchor-id="what-serves-it-and-where">What serves it, and where</h2>
<p><code>fabric-asset-edge</code> serves the built client on Fly. It is a transport layer, so it holds the listening socket. It shares nothing per tick, so it needs no ring, and it belongs to no service. That is the same reason its README already gives for standing alone.</p>
<p><code>queen</code> gains a transport layer of its own for the live game, because it has none today. RFD 0111 renames these git repositories, and the names here are the names on disk.</p>
</section>
<section id="findings-that-change-the-work" class="level2">
<h2 class="anchored" data-anchor-id="findings-that-change-the-work">Findings that change the work</h2>
<table class="caption-top table">
<colgroup>
<col style="width: 44%">
<col style="width: 55%">
</colgroup>
<thead>
<tr class="header">
<th>what was found</th>
<th>consequence</th>
</tr>
</thead>
<tbody>
<tr class="odd">
<td><code>queen</code> is a batch program. <code>main()</code> plays cycles and exits, with no socket and no loop.</td>
<td>The live game has to be built before any client can attach.</td>
</tr>
<tr class="even">
<td><code>spark_t</code> carries no position.</td>
<td>Coordinates are new fields, and the ward needs a map.</td>
</tr>
<tr class="odd">
<td><code>fabric-asset-edge</code> holds one h2o handler and no <code>main()</code>. There is no server loop, no Containerfile, and no fly.toml.</td>
<td>Serving anything from it means writing the server first.</td>
</tr>
<tr class="even">
<td><code>GET /chunk/{hash}</code> answers <code>application/octet-stream</code> and 403s unless <code>rebac_check</code> passes.</td>
<td>The client needs its own route with real content types, and the public read has to be decided rather than a weakening of <code>resolve_caller_relations</code>.</td>
</tr>
</tbody>
</table>
</section>
<section id="implementation-steps" class="level2">
<h2 class="anchored" data-anchor-id="implementation-steps">Implementation steps</h2>
<ol type="1">
<li>Give the Queen’s entities coordinates. Add the position fields to <code>spark_t</code>, place the six venues, and place a contract at the work it names.</li>
<li>Make <code>queen</code> a live instance. Keep <code>play</code> and <code>check</code> for CI, and add a served mode that holds the ward open and accepts commands.</li>
<li>Add a bundler and the client to <code>fabric-store-domain</code>. Pin <code>lexical</code> and each <code>@lexical/*</code> package to an exact version.</li>
<li>Add <code>CommandPillNode</code>, which extends <code>DecoratorNode</code> and draws the parameter block. Use the plain DOM decorator, because there is no React here.</li>
<li>Add a plugin that opens the menu on <code>/</code> and inserts the blocks for the command the player picks. Filter the menu by the caller’s rebac relations, so <code>/restart</code> stays hidden from a player who may not run it. The server checks again on receipt, because a filtered menu is a convenience and never an authorization.</li>
<li>Write the server, the routes, and the Fly deploy in <code>fabric-asset-edge</code>.</li>
<li>Raise <code>MAX_SLICE_ENTITIES</code>, or send venues on their own channel, before a ward runs at 64 Sparks.</li>
</ol>
</section>
<section id="verification" class="level2">
<h2 class="anchored" data-anchor-id="verification">Verification</h2>
<p>The existing <code>check</code> mode already holds the game to its arithmetic, and it stays the gate: one seed makes one ward, and <code>honest()</code> runs inside every cycle. <code>docker compose run --rm ci</code> runs that against a real FoundationDB.</p>
<p>For the interface, add Playwright tests for the caret crossing a block in one key press, for a block that cannot be edited in place, for backspace removing a block whole, and for the serialized command matching what the server accepts.</p>
<p>Two constraints carry into any Fly app here. Every <code>*.fly.dev</code> host is on the HSTS preload list, so a browser upgrades the request to HTTPS and a 443 listener is required. An IPv6-only app can defeat Chromium’s resolver where curl succeeds, so a test navigates to the bracketed IPv6 literal.</p>
</section>
<section id="consequences" class="level2">
<h2 class="anchored" data-anchor-id="consequences">Consequences</h2>
<ul>
<li>The Queen gets a client, which the README currently records as absent.</li>
<li><code>fabric-asset-edge</code> becomes a deployed transport layer, which it has never been.</li>
<li>The ward joins the zone and entity model, so the interest filter and the entity packet apply to it with no new mechanism.</li>
<li>Two constants that were independent are now coupled. <code>MAX_SPARKS</code> and <code>MAX_SLICE_ENTITIES</code> have to be read together from here on.</li>
<li>Lexical work costs more developer time per change than ProseMirror work costs.</li>
</ul>


</section>

 ]]></description>
  <guid>https://v-sekai-multiplayer-fabric.github.io/multiplayer-fabric-manuals/rfd/0112-lexical-for-the-slash-command-interface/</guid>
  <pubDate>Tue, 18 Aug 2026 01:10:59 GMT</pubDate>
</item>
<item>
  <title>RFD 0111: Convert the deployment words to the hexagon vocabulary</title>
  <link>https://v-sekai-multiplayer-fabric.github.io/multiplayer-fabric-manuals/rfd/0111-convert-the-deployment-words/</link>
  <description><![CDATA[ 





<p><em><a href="../../index.html">Manuals</a> › <a href="../../pages/rfd.html">RFDs</a> › RFD 0111: Convert the deployment words to the hexagon vocabulary</em></p>
<p><strong>State:</strong> published · <strong>Scope:</strong> architecture vocabulary across all repositories</p>
<section id="problem" class="level2">
<h2 class="anchored" data-anchor-id="problem">Problem</h2>
<p>This stack names one component twice. RFD 0028 names its structure; a second set names its deployment — plane, edge plane, domain — with no RFD, defined in <code>lib/weft.ex</code>, copied into ten or more READMEs, and giving domain, edge, and port two meanings each where ASD-STE100 permits one.</p>
</section>
<section id="decision" class="level2">
<h2 class="anchored" data-anchor-id="decision">Decision</h2>
<p>Retire plane, edge plane, and domain; the Netflix formulation supplies each replacement.</p>
<table class="caption-top table">
<colgroup>
<col style="width: 16%">
<col style="width: 22%">
<col style="width: 61%">
</colgroup>
<thead>
<tr class="header">
<th>retired</th>
<th>replacement</th>
<th>the meaning that carries over</th>
</tr>
</thead>
<tbody>
<tr class="odd">
<td>plane</td>
<td>interactor</td>
<td>a process that holds entities and actions</td>
</tr>
<tr class="even">
<td>edge plane</td>
<td>transport layer</td>
<td>the input that triggers an interactor</td>
</tr>
<tr class="odd">
<td>domain</td>
<td>service</td>
<td>the set that shares a ring, and a machine</td>
</tr>
<tr class="even">
<td>store plane</td>
<td>data source</td>
<td>the implementation behind a repository</td>
</tr>
</tbody>
</table>
<p>The rules do not change with the words. An interactor opens no listening socket. A transport layer holds no authority, runs no simulation, and keeps no durable state. A service is the set of interactors that share a ring, because a ring is shared memory and forces co-location. “Control plane” and “data plane” stay: they name a class of traffic rather than a process. A git repository name states its type first and drops the <code>fabric-</code> prefix the organisation already carries, so <code>fabric-authority-plane</code> becomes <code>interactor-authority</code>. Thirty are renamed, five carrying no retired word.</p>
</section>
<section id="references" class="level2">
<h2 class="anchored" data-anchor-id="references">References</h2>
<ul>
<li><a href="https://netflixtechblog.com/ready-for-changes-with-hexagonal-architecture-b315ec967749">Netflix on hexagonal architecture</a>, and <a href="https://alistair.cockburn.us/hexagonal-architecture">Cockburn</a></li>
<li>RFD 0028, amended by this RFD; RFD 0121 amends this one against the tree. The term table, the collisions, and the rename list are in <code>DETAILS.md</code></li>
</ul>
</section>
<section id="detail" class="level2">
<h2 class="anchored" data-anchor-id="detail">Detail</h2>
</section>
<section id="context-and-problem-statement" class="level2">
<h2 class="anchored" data-anchor-id="context-and-problem-statement">Context and problem statement</h2>
<p>RFD 0028 made every component a hexagon with a <code>core/</code> + <code>ports/</code> + <code>adapters/</code> layout. It took the pattern from Cockburn and added four words that Cockburn does not use: core, domain logic, edges, and the <code>*_source</code> / <code>*_sink</code> pair.</p>
<p>A second vocabulary grew later, in code rather than in an RFD. The <code>Weft</code> moduledoc in <code>fabric-weft-plane</code> defines plane, edge plane, and domain. Ten or more READMEs repeat those definitions. <code>fabric-zone-domain</code> and <code>fabric-behaviour-domain</code> carry the same sentence word for word.</p>
<p><code>fabric-harness</code> states the risk in its own README: a decision written twice drifts, and the stale copy still reads as authoritative. <code>Weft.VocabularyTest</code> exists to catch that. It reads the files of one git repository only, and it finds only retired words, so a live word with two meanings is invisible to it.</p>
</section>
<section id="decision-drivers" class="level2">
<h2 class="anchored" data-anchor-id="decision-drivers">Decision drivers</h2>
<ul>
<li>ASD-STE100 permits one meaning for one word. RFD 0000 applies STE to all prose here.</li>
<li><code>Weft</code> states the same rule, and applied it once already. Khronos CATSG says “entity” for the controlling human or AI. weft says “controller”, because entity already names the simulated unit.</li>
<li>Cockburn writes about the inside of one component. He gives no word for a process or a machine. Netflix writes about the same inside, in the microservice framing, and gives nouns this stack can use.</li>
<li>Two names for one component cost more than the deployment words earn. A reader of <code>fabric-authority-plane</code> has to learn that a plane is a process, that the process holds a tick, and that the tick is what RFD 0028 calls the inside. One name removes two of those steps.</li>
</ul>
</section>
<section id="the-netflix-terms" class="level2">
<h2 class="anchored" data-anchor-id="the-netflix-terms">The Netflix terms</h2>
<p>The article defines five terms. Each quotation below is from it.</p>
<table class="caption-top table">
<colgroup>
<col style="width: 12%">
<col style="width: 87%">
</colgroup>
<thead>
<tr class="header">
<th>term</th>
<th>definition</th>
</tr>
</thead>
<tbody>
<tr class="odd">
<td>Entity</td>
<td>“domain objects (e.g., a Movie or a Shooting Location) — they have no knowledge of where they’re stored”</td>
</tr>
<tr class="even">
<td>Repository</td>
<td>“interfaces to getting entities as well as creating and changing them”</td>
</tr>
<tr class="odd">
<td>Interactor</td>
<td>“classes that orchestrate and perform domain actions”</td>
</tr>
<tr class="even">
<td>Data source</td>
<td>“adapters to different storage implementations”</td>
</tr>
<tr class="odd">
<td>Transport layer</td>
<td>“can trigger an interactor to perform business logic. We treat it as an input for our system”</td>
</tr>
</tbody>
</table>
<p>The rule that holds them together is that all dependencies point inward.</p>
</section>
<section id="the-conversion" class="level2">
<h2 class="anchored" data-anchor-id="the-conversion">The conversion</h2>
<table class="caption-top table">
<colgroup>
<col style="width: 11%">
<col style="width: 14%">
<col style="width: 73%">
</colgroup>
<thead>
<tr class="header">
<th>retired word</th>
<th>replacement</th>
<th>why the replacement fits</th>
</tr>
</thead>
<tbody>
<tr class="odd">
<td>plane</td>
<td>interactor</td>
<td>a plane holds entities and the actions on them, and it holds no transport</td>
</tr>
<tr class="even">
<td>edge plane</td>
<td>transport layer</td>
<td>an edge plane terminates a transport and triggers an interactor</td>
</tr>
<tr class="odd">
<td>domain</td>
<td>service</td>
<td>the microservice is the unit that deploys, and the ring decides its members</td>
</tr>
<tr class="even">
<td>store plane</td>
<td>data source</td>
<td>it implements repositories over FoundationDB and SQLite</td>
</tr>
</tbody>
</table>
<p>“Service” is the weakest of the four, because the Netflix article names microservices without defining one. It is the only deployment word the formulation has, and the ecosystem uses it the same way. This RFD takes it and states the local meaning: a service is the set of interactors that share a ring.</p>
</section>
<section id="one-name-for-one-concept" class="level2">
<h2 class="anchored" data-anchor-id="one-name-for-one-concept">One name for one concept</h2>
<table class="caption-top table">
<colgroup>
<col style="width: 18%">
<col style="width: 71%">
<col style="width: 10%">
</colgroup>
<thead>
<tr class="header">
<th>word</th>
<th>the one meaning</th>
<th>source</th>
</tr>
</thead>
<tbody>
<tr class="odd">
<td>entity</td>
<td>one simulated thing in a zone, with position and velocity</td>
<td>weft</td>
</tr>
<tr class="even">
<td>repository</td>
<td>an interface that gets, creates, and changes entities</td>
<td>Netflix</td>
</tr>
<tr class="odd">
<td>interactor</td>
<td>a process that performs actions on entities</td>
<td>Netflix</td>
</tr>
<tr class="even">
<td>data source</td>
<td>an implementation of a repository</td>
<td>Netflix</td>
</tr>
<tr class="odd">
<td>transport layer</td>
<td>the input that triggers an interactor</td>
<td>Netflix</td>
</tr>
<tr class="even">
<td>service</td>
<td>the set of interactors that share a ring</td>
<td>Netflix</td>
</tr>
<tr class="odd">
<td>contract</td>
<td>what two sides compose against, checked at the seam</td>
<td>weft</td>
</tr>
<tr class="even">
<td>ring</td>
<td>the iceoryx2 shared memory bus</td>
<td>weft</td>
</tr>
<tr class="odd">
<td>port</td>
<td>a TCP or UDP listening socket</td>
<td>ordinary</td>
</tr>
<tr class="even">
<td>actor</td>
<td>a runtime process with a single writer</td>
<td>weft</td>
</tr>
<tr class="odd">
<td>controller</td>
<td>the human or the AI that controls an avatar</td>
<td>weft</td>
</tr>
</tbody>
</table>
<p>“Contract” is not one of the five Netflix terms, and this RFD adds it. The article has no word for the interface an interactor and a transport both compose against, because it writes about classes inside one component rather than about git repositories that must build without each other. The word is weft’s and it is already load-bearing here: <code>PredictiveBvh.core.CurveContract</code> asserts a property against whatever the manifest pins, and <code>fabric-interactor</code>’s README says the boundary is held by the linker. Both are the agreement at a seam, checked rather than assumed, so one meaning covers them.</p>
<p>weft and Netflix agree on “entity”. Netflix says an entity does not know where it is stored. weft says an entity is the unit the data plane moves. Both keep the entity away from its storage, so one meaning covers both.</p>
</section>
<section id="where-the-terms-land-in-this-stack" class="level2">
<h2 class="anchored" data-anchor-id="where-the-terms-land-in-this-stack">Where the terms land in this stack</h2>
<table class="caption-top table">
<thead>
<tr class="header">
<th>the thing that exists today</th>
<th>the word for it</th>
</tr>
</thead>
<tbody>
<tr class="odd">
<td><code>XRGridEntityPacket</code> from <code>lean-entity-packet</code></td>
<td>entity</td>
</tr>
<tr class="even">
<td><code>src/authority_tick.c</code> in <code>fabric-authority-plane</code></td>
<td>interactor</td>
</tr>
<tr class="odd">
<td>the reducer in <code>combat</code>, the roll in <code>loot</code></td>
<td>interactor</td>
</tr>
<tr class="even">
<td><code>fabric-gateway-edge</code>, <code>fabric-ingest-edge</code></td>
<td>transport layer</td>
</tr>
<tr class="odd">
<td><code>Weft.Actor.Store</code>, the store API</td>
<td>repository</td>
</tr>
<tr class="even">
<td>FoundationDB behind the SQLite VFS</td>
<td>data source</td>
</tr>
<tr class="odd">
<td><code>fanout_sink_t</code> in <code>fabric-fanout-edge</code></td>
<td>repository</td>
</tr>
<tr class="even">
<td>the WebTransport sink behind it</td>
<td>data source</td>
</tr>
<tr class="odd">
<td>a recorded fixture under CI</td>
<td>data source</td>
</tr>
<tr class="even">
<td>the members of <code>fabric-zone-domain</code></td>
<td>one service</td>
</tr>
</tbody>
</table>
<p><code>fabric-fanout-edge</code> shows that the pattern is in the code already. Its README says that a WebTransport sink can replace the default sink, and that nothing above it changes. That is one repository with two data sources.</p>
</section>
<section id="the-collisions-this-resolves" class="level2">
<h2 class="anchored" data-anchor-id="the-collisions-this-resolves">The collisions this resolves</h2>
<p>The word “domain” had three meanings: the deployment packing, the “domain logic” of RFD 0028, and a field on the wire in RFD 0091. The first becomes “service”. The second becomes “entity” and “interactor”. The third needs its own decision, because a change on the wire is a protocol change.</p>
<p>The word “edge” had two meanings. RFD 0028 said “concrete I/O at the edges”, and an edge plane was a process. RFD 0028 now says “outside the interactor”, and the process is a transport layer. No meaning is left to collide.</p>
<p>The word “port” had two meanings. Netflix uses no word “port”, so the listening socket keeps it, and <code>fabric-zone-domain</code> needs no edit.</p>
</section>
<section id="the-collision-this-creates" class="level2">
<h2 class="anchored" data-anchor-id="the-collision-this-creates">The collision this creates</h2>
<p>Netflix says “repository” for an interface to entities. This project says “repository” for a git repository, in RFD 0000, RFD 0062, RFD 0063, and RFD 0064. That is a new word with two meanings, and this RFD makes it.</p>
<p>The resolution is to write “git repository” in full every time, and to keep “repository” alone for the interface. Keeping Cockburn’s word “port” for the interface would avoid this, and it would revive the collision with the listening socket, which is in the code of every transport layer. A collision in prose costs less than a collision in code.</p>
</section>
<section id="what-plane-keeps" class="level2">
<h2 class="anchored" data-anchor-id="what-plane-keeps">What “plane” keeps</h2>
<p>“Control plane” and “data plane” stay whole. They come from networking, they name a class of traffic, and neither one names a process. <code>Weft.DataPlane</code> keeps its name. Bare “plane”, as a noun for a process, is retired.</p>
</section>
<section id="the-name-shape" class="level2">
<h2 class="anchored" data-anchor-id="the-name-shape">The name shape</h2>
<p>A converted name puts the type first and drops the <code>fabric-</code> prefix: <code>&lt;type&gt;-&lt;name&gt;</code>. <code>fabric-authority-plane</code> becomes <code>interactor-authority</code>.</p>
<p>Type first sorts the git repositories by role. Every interactor is then adjacent, and the number of each type is the length of a run, so the list above can be read off the organisation page. Type last sorted them by subsystem, which kept <code>fabric-store-domain</code> beside <code>fabric-store-plane</code> and put the seven planes in seven places. One order is available or the other, not both, and role is what a reader of this RFD is looking for.</p>
<p>The <code>fabric-</code> prefix goes because the organisation name already carries it. That also frees the bare word, which one git repository takes below.</p>
</section>
<section id="the-rename-list" class="level2">
<h2 class="anchored" data-anchor-id="the-rename-list">The rename list</h2>
<p>Thirty git repositories are renamed: twenty-five carry a retired word, three carry the right word in the wrong order, and two state a type that is not theirs.</p>
<table class="caption-top table">
<thead>
<tr class="header">
<th>now</th>
<th>after</th>
</tr>
</thead>
<tbody>
<tr class="odd">
<td><code>fabric-authority-plane</code></td>
<td><code>interactor-authority</code></td>
</tr>
<tr class="even">
<td><code>fabric-crowd-plane</code></td>
<td><code>interactor-crowd</code></td>
</tr>
<tr class="odd">
<td><code>fabric-janet-plane</code></td>
<td><code>interactor-janet</code></td>
</tr>
<tr class="even">
<td><code>fabric-motion-plane</code></td>
<td><code>interactor-motion</code></td>
</tr>
<tr class="odd">
<td><code>fabric-taskweft-plane</code></td>
<td><code>interactor-taskweft</code></td>
</tr>
<tr class="even">
<td><code>fabric-tool-plane</code></td>
<td><code>interactor-tool</code></td>
</tr>
<tr class="odd">
<td><code>fabric-weft-plane</code></td>
<td><code>interactor-weft</code></td>
</tr>
<tr class="even">
<td><code>gyreplane</code></td>
<td><code>interactor-gyre</code></td>
</tr>
<tr class="odd">
<td><code>fabric-physics-interactor</code></td>
<td><code>interactor-physics</code></td>
</tr>
<tr class="even">
<td><code>fabric-asset-edge</code></td>
<td><code>transport-asset</code></td>
</tr>
<tr class="odd">
<td><code>fabric-fanout-edge</code></td>
<td><code>transport-fanout</code></td>
</tr>
<tr class="even">
<td><code>fabric-gateway-edge</code></td>
<td><code>transport-gateway</code></td>
</tr>
<tr class="odd">
<td><code>fabric-ingest-edge</code></td>
<td><code>transport-ingest</code></td>
</tr>
<tr class="even">
<td><code>fabric-edge</code></td>
<td><code>transport-picoquic</code></td>
</tr>
<tr class="odd">
<td><code>fabric-behaviour-domain</code></td>
<td><code>service-behaviour</code></td>
</tr>
<tr class="even">
<td><code>fabric-store-domain</code></td>
<td><code>service-store</code></td>
</tr>
<tr class="odd">
<td><code>fabric-zone-domain</code></td>
<td><code>service-zone</code></td>
</tr>
<tr class="even">
<td><code>fabric-godot-service</code></td>
<td><code>service-godot</code></td>
</tr>
<tr class="odd">
<td><code>fabric-physics-service</code></td>
<td><code>service-physics</code></td>
</tr>
<tr class="even">
<td><code>fabric-store-plane</code></td>
<td><code>datasource-store</code></td>
</tr>
<tr class="odd">
<td><code>fabric-flow-adapters</code></td>
<td><code>datasource-flow</code></td>
</tr>
<tr class="even">
<td><code>fabric-flow-adapters-project</code></td>
<td><code>datasource-flow-project</code></td>
</tr>
<tr class="odd">
<td><code>lean-combat-core</code></td>
<td><code>entities-lean-combat</code></td>
</tr>
<tr class="even">
<td><code>lean-loot-core</code></td>
<td><code>entities-lean-loot</code></td>
</tr>
<tr class="odd">
<td><code>lean-progression-core</code></td>
<td><code>entities-lean-progression</code></td>
</tr>
<tr class="even">
<td><code>lean-rebac-core</code></td>
<td><code>entities-lean-rebac</code></td>
</tr>
<tr class="odd">
<td><code>lean-shared-core</code></td>
<td><code>entities-lean-shared</code></td>
</tr>
<tr class="even">
<td><code>fabric-godot-core</code></td>
<td><code>entities-godot</code></td>
</tr>
<tr class="odd">
<td><code>fabric-interactor</code></td>
<td><code>contract-command</code></td>
</tr>
<tr class="even">
<td><code>fabric-service-meta</code></td>
<td><code>fabric</code></td>
</tr>
</tbody>
</table>
<p>GitHub redirects a renamed git repository, so a <code>git subtree</code> remote and a Lake <code>require ... from git</code> continue to resolve. Each pin gets updated in the same pass.</p>
<p>Three git repositories hold the RFD 0028 triad on disk: <code>loot</code>, <code>combat</code>, and <code>progression</code>. Each <code>core/</code> becomes <code>entities/</code>, each <code>ports/</code> becomes <code>repositories/</code>, and each <code>adapters/</code> becomes <code>datasources/</code>.</p>
</section>
<section id="the-names-the-shape-does-not-decide" class="level2">
<h2 class="anchored" data-anchor-id="the-names-the-shape-does-not-decide">The names the shape does not decide</h2>
<p>Six entries above do not follow from the conversion table, and each one is a decision this RFD makes rather than a rule it applies.</p>
<p><code>fabric-edge</code> holds both an ingest edge and a gateway edge over picoquic, and the two also exist as git repositories of their own. It is one transport layer that the other two are built from, not an umbrella over them, so it takes the name of the transport it terminates: <code>transport-picoquic</code>. Bare <code>transport</code> would read as a name that went missing.</p>
<p><code>gyreplane</code> carries the retired word inside a compound. It is a process that holds entities, so the word is retired there as well and the name is regularised to <code>interactor-gyre</code>. A compound is not an exemption.</p>
<p><code>fabric-flow-adapters</code> and its <code>-project</code> companion carry RFD 0028’s “adapters”, which this RFD retires for directories. The retirement extends to git repository names. A word retired in the tree and live in the name is the same drift this RFD exists to stop, and the name is the copy a reader sees first.</p>
<p><code>fabric-godot-core</code> becomes <code>entities-godot</code>, and the rename is of the git repository name only. The <code>core/</code> directory inside it is the Godot engine’s, it comes from upstream, and it is not the “core” of RFD 0028. Renaming it would fork the vocabulary of an engine this project tracks rather than owns.</p>
<p><code>fabric-interactor</code> is not an interactor. It holds the contract that interactors and transports compose against — <code>include/weft/interactor.h</code> declares both — and its README states the reason it is a git repository of its own: “a contract that lives in either side makes the other its dependent”. A name of <code>interactor-anything</code> picks the side the git repository exists to avoid picking. The type is contract and the subject is the command, so it is <code>contract-command</code>. “Contract” is the one type word here that Netflix does not supply, and the entry above says where it comes from. <code>fabric-harness</code> is the same argument about the bus and the limits, and becomes <code>contract-bus</code> when that pass is taken.</p>
<p><code>fabric-service-meta</code> is not a service. It holds the <code>.meta</code> manifest that names every git repository here, and the conventions over them. <code>service-meta</code> would assert the thing that is false, and <code>meta</code> alone collides with a company that owns the word in every search. It takes the bare <code>fabric</code>, which the dropped prefix frees: after this pass the word is in one name, and that name is the git repository the other names are listed in. <code>openusd-fabric</code> keeps the OpenUSD scene-data meaning and keeps its qualifier, the same resolution this RFD uses for “git repository”.</p>
<p>Fourteen <code>lean-*</code> git repositories are not hexagon cores, and they keep the language-first name while five siblings move. <code>lean-</code> marks a Lean workspace that this RFD does not classify. The convention applies where there is a word for the thing, and nowhere else.</p>
</section>
<section id="considered-and-rejected-the-agent-noun" class="level2">
<h2 class="anchored" data-anchor-id="considered-and-rejected-the-agent-noun">Considered and rejected: the agent noun</h2>
<p>“Interactor” was challenged on the ground that RFD 0000 applies ASD-STE100, that STE forbids a nominalized verb, and that “interact” fails to discriminate, because a transport layer and a data source interact as well. Both objections hold. The word is kept anyway.</p>
<p>The alternatives were checked against the words this workspace already uses, across every checkout. <code>tick</code> appears 1091 times and this RFD itself says the process holds a tick. <code>warp</code> collides with the GPU warp in <code>SIMT.lean</code> and with the MJX-Warp tree vendored inside the physics git repository. <code>cell</code> appears 2651 times, <code>kernel</code> 8615, and <code>shard</code> 663. <code>hexagon</code> is already the house word for the code shape, and a transport layer has that shape too, so it recreates the collision this RFD closes. <code>loom</code> was the only clean candidate at zero uses, and it is a metaphor that would have to be taught.</p>
<p>Netflix supplies “interactor”, two git repositories already carry it, and a word with a stated weakness costs less than a word this project invents. The objection is recorded here so that the next reader of eight new names finds it answered rather than open.</p>
</section>
<section id="a-gap-this-pass-found" class="level2">
<h2 class="anchored" data-anchor-id="a-gap-this-pass-found">A gap this pass found</h2>
<p>The <code>lean-*-core</code> READMEs state a “Hexagon layout” of <code>core/</code>, <code>ports/</code>, and <code>adapters/</code>. Those directories do not exist. Each of those git repositories holds one Lean namespace directory, such as <code>Shared</code> or <code>Rebac</code>. The documents describe a layout that no git repository has. The rename pass corrects the READMEs to the directories that are there.</p>
</section>
<section id="consequences" class="level2">
<h2 class="anchored" data-anchor-id="consequences">Consequences</h2>
<ul>
<li>One component has one name. A reader learns one vocabulary.</li>
<li>Each word has one meaning, and STE holds across the git repositories.</li>
<li>The definitions leave the READMEs and live here, so the copies stop drifting.</li>
<li><code>Weft.VocabularyTest</code> gains the retired words: plane, edge plane, domain, core, domain logic, and adapters. The test then holds this decision. The entries for “control plane” and “data plane” must not match, so the pattern needs a word boundary and a check for the preceding word. “Core” needs the same treatment, because <code>fabric-godot-core</code>’s <code>core/</code> directory is upstream’s and must not match either.</li>
<li>The test reads one git repository, so it holds the decision only where it runs. Thirty git repositories are renamed here and one of them can hold a copy of the definitions, which is the arrangement the test cannot check. That gap is not closed by this RFD.</li>
<li>The rename touches thirty git repository names, three directory triads, and every README that copies a definition. Every one is mechanical, and none changes a build.</li>
<li>Twenty-five of the thirty carry a retired word. The other five do not, and a reader will not predict them from the conversion table. <code>fabric-physics-interactor</code>, <code>fabric-godot-service</code>, and <code>fabric-physics-service</code> are already converted and are renamed for the order alone. <code>fabric-interactor</code> and <code>fabric-service-meta</code> state a type that is not theirs, which the conversion table cannot catch, because it looks for the words that are wrong rather than for the words that are misapplied.</li>
<li>This RFD does not rename the <code>domain</code> field on the wire in RFD 0091. That is a protocol change and needs its own RFD.</li>
</ul>


</section>

 ]]></description>
  <guid>https://v-sekai-multiplayer-fabric.github.io/multiplayer-fabric-manuals/rfd/0111-convert-the-deployment-words/</guid>
  <pubDate>Tue, 18 Aug 2026 01:10:59 GMT</pubDate>
</item>
<item>
  <title>RFD 0110: One pinned Godot build, hosted as a Rivet actor</title>
  <link>https://v-sekai-multiplayer-fabric.github.io/multiplayer-fabric-manuals/rfd/0110-h2o-libriscv-as-a-rivet-actor/</link>
  <description><![CDATA[ 





<p><em><a href="../../index.html">Manuals</a> › <a href="../../pages/rfd.html">RFDs</a> › RFD 0110: One pinned Godot build, hosted as a Rivet actor</em></p>
<p><strong>State:</strong> discussion · <strong>Scope:</strong> zone server implementation, orchestration, guest delivery</p>
<section id="problem" class="level2">
<h2 class="anchored" data-anchor-id="problem">Problem</h2>
<p><code>rfd/0109</code> makes the server tier an Elixir presence relay. A relay is not server-authoritative, and it runs every room in one BEAM node, so one fault reaches every room.</p>
<p>The h2o and libriscv work in <code>zone-server-h2o</code> already exists. <code>rfd/0083</code> built the host, and <code>rfd/0094</code> keeps <code>thirdparty/libriscv</code> and <code>thirdparty/QCBOR</code> vendored so they do not rot. What that host lacked was orchestration. <code>rfd/0072</code>, <code>rfd/0082</code>, and <code>rfd/0086</code> exist only because one process had to hold many zones.</p>
<p>No record asks whether something else can supply the orchestration.</p>
</section>
<section id="decision" class="level2">
<h2 class="anchored" data-anchor-id="decision">Decision</h2>
<p>A zone is a child process under Rivet’s <code>container-runner</code>, one process per room. The child is headless Godot, which the runner documentation names directly.</p>
<p>The engine is pinned to one tag, <code>v2026.06.27.1907-multiplayer-fabric</code>, which resolves to commit <code>2cecde75</code> and reports Godot 4.7.0 beta. That tag’s <code>modules/</code> already carries <code>sandbox</code> for libriscv guests, <code>http3</code> for WebTransport, <code>multiplayer_fabric</code> and <code>multiplayer_fabric_asset</code> for the zone, <code>xr_grid</code> for presence, and <code>openxr</code> with <code>webxr</code> for XR.</p>
<p>So one engine build is the client, the zone server, and the guest host. No C host is needed to load an <code>.elf</code>, because <code>sandbox</code> does it, and <code>rfd/0037</code> already makes generated behavior sandboxed RISC-V.</p>
<p>The tag carries no release artifacts, because the repository publishes no GitHub releases. <code>godot-images</code> builds it and publishes the container image, which is what <code>container-runner</code> needs. <code>rfd/0009</code> puts that build in the consuming repository.</p>
<p>Rivet supplies what the old host hand-rolled. It cold-starts the container, injects <code>PORT</code>, spawns the binary as a child, and proxies gateway traffic to <code>127.0.0.1:&lt;child port&gt;</code>. The engine owns placement, routing, and lifecycle. Rivet is Apache 2.0 and self-hosted.</p>
<p>The child contract is small. Listen on <code>PORT</code>. Open it within the readiness timeout, 30 seconds by default. Handle <code>SIGTERM</code> and exit within the grace period, 25 seconds by default. Raw HTTP arrives under a <code>/request/*</code> prefix that the runner strips. WebSocket clients use the <code>rivet</code> subprotocol.</p>
<p>One process per room removes the multi-zone problem. The binary hosts one room, so it needs no zone routing and no cross-zone authority. <code>rfd/0072</code>, <code>rfd/0082</code>, and <code>rfd/0086</code> become unnecessary, and <code>rfd/0080</code> narrows to one room’s entities.</p>
<p>The CBOR actor input names the child command, its args, and its env, and it names the scene or guest the room loads. That is <code>rfd/0094</code>’s guest delivery, moved to the actor boundary.</p>
<p>A C child, h2o with libriscv, stays possible and is not built. Its only advantage is memory per process, and that advantage is unmeasured. Build it when a measurement forces it.</p>
<p>FoundationDB backs both the Rivet engine and the zone state, so <code>rfd/0109</code>’s store selection stands. This record amends only <code>rfd/0109</code>’s server tier.</p>
</section>
<section id="references" class="level2">
<h2 class="anchored" data-anchor-id="references">References</h2>
<ul>
<li>The contract, what it deletes, and the transport question: <code>DETAILS.md</code></li>
<li><code>https://rivet.dev/docs/deploy/container-runner/</code></li>
</ul>
</section>
<section id="related" class="level2">
<h2 class="anchored" data-anchor-id="related">Related</h2>
<ul>
<li><code>rfd/0083-zone-server-h2o-replaces-godot-fabriczone</code>: the decision this reverses, because the zone returns to Godot.</li>
<li><code>rfd/0037-generated-behavior-sandboxed-riscv</code>: the libriscv path inside the engine.</li>
<li><code>rfd/0020-pin-engine-to-frozen-godot-4-7</code>: the pinning practice this applies to one named tag.</li>
<li><code>rfd/0109-two-tiers-with-foundationdb-as-the-store</code>: the store and the relay this replaces.</li>
</ul>
</section>
<section id="detail" class="level2">
<h2 class="anchored" data-anchor-id="detail">Detail</h2>
</section>
<section id="the-child-contract" class="level2">
<h2 class="anchored" data-anchor-id="the-child-contract">The child contract</h2>
<table class="caption-top table">
<colgroup>
<col style="width: 21%">
<col style="width: 78%">
</colgroup>
<thead>
<tr class="header">
<th>Item</th>
<th>Value</th>
</tr>
</thead>
<tbody>
<tr class="odd">
<td>Port</td>
<td>The <code>PORT</code> environment variable</td>
</tr>
<tr class="even">
<td>Readiness</td>
<td>The port must open within 30 seconds, by default</td>
</tr>
<tr class="odd">
<td>Shutdown</td>
<td><code>SIGTERM</code>, then 25 seconds grace, then <code>SIGKILL</code>, by default</td>
</tr>
<tr class="even">
<td>Raw HTTP</td>
<td>Arrives under <code>/request/*</code>, which the runner strips</td>
</tr>
<tr class="odd">
<td>WebSocket</td>
<td>Clients use the <code>rivet</code> subprotocol</td>
</tr>
<tr class="even">
<td>Per-actor config</td>
<td>CBOR <code>input</code>, with <code>command</code>, <code>args</code>, and <code>env</code></td>
</tr>
</tbody>
</table>
<p>The engine calls <code>POST /api/rivet/start</code> on <code>RIVET_PORT</code>. That is the runner’s interface, not the child’s. The child sees a plain port.</p>
<p>The <code>rivet</code> subprotocol is the one non-obvious requirement. h2o’s WebSocket upgrade path must accept it.</p>
</section>
<section id="what-one-process-per-room-deletes" class="level2">
<h2 class="anchored" data-anchor-id="what-one-process-per-room-deletes">What one process per room deletes</h2>
<p>The old host held many zones in one process. Rivet holds one room per process, so the machinery for the first case has no subject.</p>
<table class="caption-top table">
<colgroup>
<col style="width: 14%">
<col style="width: 85%">
</colgroup>
<thead>
<tr class="header">
<th>Record</th>
<th>Status under this decision</th>
</tr>
</thead>
<tbody>
<tr class="odd">
<td><code>rfd/0072</code></td>
<td>Unnecessary. Worker dispatch across zones in one process</td>
</tr>
<tr class="even">
<td><code>rfd/0082</code></td>
<td>Unnecessary. Keyspace design for many zones in one server</td>
</tr>
<tr class="odd">
<td><code>rfd/0086</code></td>
<td>Unnecessary. The engine owns placement, so gossip has no job</td>
</tr>
<tr class="even">
<td><code>rfd/0080</code></td>
<td>Narrows. Slotmap storage for one room’s entities</td>
</tr>
<tr class="odd">
<td><code>rfd/0094</code></td>
<td>Applies. The CBOR input names the guest, as this record uses</td>
</tr>
<tr class="even">
<td><code>rfd/0092</code></td>
<td>Applies. ReBAC still gates which principal loads which guest</td>
</tr>
</tbody>
</table>
<p>Fault isolation improves as a side effect. A guest fault kills one room’s process, not every room. <code>rfd/0095</code> wanted that from Bubblewrap, and process-per-actor supplies it.</p>
</section>
<section id="the-actor-input-from-container-runnersrcinput.rs" class="level2">
<h2 class="anchored" data-anchor-id="the-actor-input-from-container-runnersrcinput.rs">The actor input, from <code>container-runner/src/input.rs</code></h2>
<p>Four optional fields, CBOR-encoded per the RivetKit convention:</p>
<table class="caption-top table">
<colgroup>
<col style="width: 11%">
<col style="width: 30%">
<col style="width: 58%">
</colgroup>
<thead>
<tr class="header">
<th>Field</th>
<th>Type</th>
<th>Meaning</th>
</tr>
</thead>
<tbody>
<tr class="odd">
<td><code>command</code></td>
<td><code>Option&lt;Vec&lt;String&gt;&gt;</code></td>
<td>Replaces the CLI command template entirely</td>
</tr>
<tr class="even">
<td><code>args</code></td>
<td><code>Vec&lt;String&gt;</code></td>
<td>Appended after the command template</td>
</tr>
<tr class="odd">
<td><code>env</code></td>
<td><code>HashMap&lt;String,String&gt;</code></td>
<td>Extra environment variables for the child</td>
</tr>
<tr class="even">
<td><code>port</code></td>
<td><code>Option&lt;u16&gt;</code></td>
<td>The child’s local port, also exported as <code>PORT</code></td>
</tr>
</tbody>
</table>
<p>Anything omitted falls back to the CLI template, which is <code>rivet-container-runner -- &lt;command...&gt;</code>. An omitted <code>port</code> falls back to the runner’s <code>--child-port</code>.</p>
<p>Two properties matter for a zone. The decoded input is also the actor’s persisted state, so a woken actor restores the same launch spec. Unknown fields are ignored rather than rejected, so adding a field does not break actors that wake on an older binary.</p>
<p>So a room’s scene, its guest, and its tuning all travel in the actor input. No zone-side registry is needed.</p>
</section>
<section id="packaging" class="level2">
<h2 class="anchored" data-anchor-id="packaging">Packaging</h2>
<p><code>container-runner/Dockerfile.release</code> builds a standalone linux/amd64 <code>rivet-container-runner</code> binary, described in its own comments as the artifact users curl into their Dockerfile. Extract it with a build targeting <code>artifact</code>, and the output is one x86_64 ELF.</p>
<p>The zone image is therefore the engine build plus that binary as the entrypoint, with the engine command after <code>--</code>. <code>godot-images</code> already publishes engine images to GHCR, so this adds an entrypoint rather than a pipeline.</p>
</section>
<section id="the-pinned-engine-verified" class="level2">
<h2 class="anchored" data-anchor-id="the-pinned-engine-verified">The pinned engine, verified</h2>
<p>The tag <code>v2026.06.27.1907-multiplayer-fabric</code> is annotated, and it resolves to commit <code>2cecde75</code>. Its <code>version.py</code> reports major 4, minor 7, patch 0, status beta.</p>
<p><code>fabric-godot-core</code> publishes no GitHub releases, so the tag page offers source archives only. A build step is required, and <code>godot-images</code> already publishes engine images to GHCR. <code>rfd/0009</code> puts that build in the consuming repository.</p>
<p>Modules present at that tag, checked against the tree:</p>
<table class="caption-top table">
<colgroup>
<col style="width: 61%">
<col style="width: 38%">
</colgroup>
<thead>
<tr class="header">
<th>Module</th>
<th>Role</th>
</tr>
</thead>
<tbody>
<tr class="odd">
<td><code>sandbox</code></td>
<td>libriscv guest execution</td>
</tr>
<tr class="even">
<td><code>http3</code></td>
<td>WebTransport over QUIC</td>
</tr>
<tr class="odd">
<td><code>multiplayer_fabric</code>, <code>multiplayer_fabric_asset</code></td>
<td>The zone and its assets</td>
</tr>
<tr class="even">
<td><code>xr_grid</code></td>
<td>Presence, head and hand orbs</td>
</tr>
<tr class="odd">
<td><code>openxr</code>, <code>webxr</code></td>
<td>XR on desktop and on web</td>
</tr>
<tr class="even">
<td><code>cassie</code>, <code>speech</code>, <code>tinyexr</code></td>
<td>Authoring, voice, HDR textures</td>
</tr>
</tbody>
</table>
<p>One build therefore serves three roles: the client, the zone server, and the guest host. No h2o, no Janet, and no separate guest runtime.</p>
<p>No <code>resonance</code> module appears at this tag. <code>rfd/0022</code> describes spatial audio through a patched Resonance Audio, and whether that arrives as core changes rather than a module is unverified. Check it before you rely on spatial audio.</p>
</section>
<section id="a-second-child-kind-kept-in-reserve" class="level2">
<h2 class="anchored" data-anchor-id="a-second-child-kind-kept-in-reserve">A second child kind, kept in reserve</h2>
<p><code>container-runner</code> hosts any process that listens on <code>PORT</code>, and the CBOR <code>command</code> field selects the binary. So a second child kind costs no architecture, only a build.</p>
<p>The candidate is h2o with libriscv in C. Its one advantage is memory per process, because a headless Godot process is larger than a small C binary. With one process per room, that difference multiplies by the room count.</p>
<p>Neither figure is measured here. Measure the resident size of the headless build, divide the machine’s memory by it, and compare the result to the expected room count. Build the C child only if that comparison fails.</p>
<p><code>rfd/0095</code> found one runtime serving two unlike guest classes, and it split them with a domain ABI and Bubblewrap. Process-per-actor splits them without either mechanism.</p>
</section>
<section id="the-transport-settled-by-the-runners-source" class="level2">
<h2 class="anchored" data-anchor-id="the-transport-settled-by-the-runners-source">The transport, settled by the runner’s source</h2>
<p><code>container-runner</code> is TCP-only. Forcing HTTP/3 through it is not possible, and four facts in its source say so.</p>
<table class="caption-top table">
<colgroup>
<col style="width: 83%">
<col style="width: 16%">
</colgroup>
<thead>
<tr class="header">
<th>Fact</th>
<th>File</th>
</tr>
</thead>
<tbody>
<tr class="odd">
<td>Forwards to <code>http://127.0.0.1:{child_port}</code> and the <code>ws://</code> form</td>
<td><code>src/proxy.rs</code></td>
</tr>
<tr class="even">
<td>Readiness is <code>TcpStream::connect((Ipv4Addr::LOCALHOST, child_port))</code></td>
<td><code>src/child.rs</code></td>
</tr>
<tr class="odd">
<td>“HTTP/WebSocket (arriving over Rivet’s tunnel) to the child’s local port”</td>
<td><code>src/main.rs</code></td>
</tr>
<tr class="even">
<td>No <code>udp</code>, <code>quic</code>, <code>http3</code>, or <code>h2</code> appears in the runner source</td>
<td>all four files</td>
</tr>
</tbody>
</table>
<p>A UDP-only child never reports ready, because readiness is a TCP connect. <code>TCP_NODELAY</code> also does not appear in the runner source, so Nagle is unmitigated there.</p>
<p>Rivet’s own gateway has no QUIC yet, and WebTransport is planned rather than shipped.</p>
<section id="two-sockets-in-one-child" class="level3">
<h3 class="anchored" data-anchor-id="two-sockets-in-one-child">Two sockets in one child</h3>
<p>The zone process listens twice.</p>
<ul>
<li>A TCP port satisfies Rivet’s contract and carries the control plane. Rivet proxies it, readiness passes, and the actor lifecycle works.</li>
<li>A UDP port carries game traffic outside Rivet’s tunnel, with the engine’s <code>http3</code> picoquic module on it. The host maps that port.</li>
</ul>
<p>One process, one actor, two sockets. Rivet never touches the pose stream, so <code>rfd/0008</code>, <code>rfd/0023</code>, <code>rfd/0049</code>, <code>rfd/0058</code>, and <code>rfd/0088</code> all stand.</p>
</section>
<section id="what-that-shape-costs" class="level3">
<h3 class="anchored" data-anchor-id="what-that-shape-costs">What that shape costs</h3>
<p>Rivet’s gateway does not route the UDP port, so a client needs the host address and port. Something must publish it, and that is a zone directory. <code>rfd/0109</code> left the Uro role optional, and this design requires it.</p>
</section>
<section id="if-the-pose-stream-runs-on-websocket-instead" class="level3">
<h3 class="anchored" data-anchor-id="if-the-pose-stream-runs-on-websocket-instead">If the pose stream runs on WebSocket instead</h3>
<p>That is workable and worse. Three mitigations are mandatory: set <code>TCP_NODELAY</code> on the child socket, send one latest-pose frame per tick, and keep the frame at the 100 bytes of <code>rfd/0053</code>.</p>
<p>It stays wrong under loss. Head-of-line blocking costs about one round trip, which at a 50 ms RTT delays roughly three ticks and then delivers stale poses. WebSocket also gives no way to discard queued frames, so <code>rfd/0049</code>’s unreliable state channel has no transport.</p>
</section>
<section id="measure-before-building" class="level3">
<h3 class="anchored" data-anchor-id="measure-before-building">Measure before building</h3>
<p><code>container-runner/examples/e2e-test/load-test.mjs</code> opens WebSockets through the gateway, pings, and reports <code>p50</code>, <code>p95</code>, <code>p99</code>, and <code>max</code> in milliseconds. Run it against a Godot child and compare <code>p95</code> to the 15.6 ms tick.</p>
<p><code>rfd/0096</code> states the rule this follows. Ranking transports from documentation produces wrong answers.</p>
</section>
</section>
<section id="cost-and-risk" class="level2">
<h2 class="anchored" data-anchor-id="cost-and-risk">Cost and risk</h2>
<p>One OS process per room is heavier than one BEAM process per room. The room count decides whether that matters, and this record does not know it.</p>
<p>Rivet adds a Rust engine to operate. That is a dependency to run, not a language to write in, because the zone is C.</p>
<p><code>pegboard-gateway</code> and <code>pegboard-gateway2</code> both exist in the repository, so that layer changes actively. Pin a version.</p>
</section>
<section id="what-stays-open-from-rfd-0109" class="level2">
<h2 class="anchored" data-anchor-id="what-stays-open-from-rfd-0109">What stays open from RFD 0109</h2>
<p><strong>Where Elixir and Uro sit.</strong> Identity and the room directory still need a home. Rivet has namespaces and its own API, and neither replaces an identity service. <code>rfd/0090</code> is Uro’s live release shape.</p>
<p><strong>Interest management.</strong> <code>lean-interest-mgmt</code> is still the only lever on the 256 kbps figure of <code>rfd/0100</code>.</p>
<p><strong>Server authority.</strong> A zone process that runs the simulation can be authoritative, unlike the relay of <code>rfd/0109</code>. So <code>rfd/0046</code> returns as reachable rather than ruled out.</p>


</section>

 ]]></description>
  <guid>https://v-sekai-multiplayer-fabric.github.io/multiplayer-fabric-manuals/rfd/0110-h2o-libriscv-as-a-rivet-actor/</guid>
  <pubDate>Tue, 18 Aug 2026 01:10:59 GMT</pubDate>
</item>
<item>
  <title>RFD 0109: Two tiers, with FoundationDB as the store</title>
  <link>https://v-sekai-multiplayer-fabric.github.io/multiplayer-fabric-manuals/rfd/0109-two-tiers-with-foundationdb-as-the-store/</link>
  <description><![CDATA[ 





<p><em><a href="../../index.html">Manuals</a> › <a href="../../pages/rfd.html">RFDs</a> › RFD 0109: Two tiers, with FoundationDB as the store</em></p>
<p><strong>State:</strong> published · <strong>Scope:</strong> tier count, store selection</p>
<section id="problem" class="level2">
<h2 class="anchored" data-anchor-id="problem">Problem</h2>
<p><code>rfd/0107</code> describes four runtimes: h2o, Janet, a C core, and Godot. <code>rfd/0108</code> adds local C ABI guests to that host. One person cannot carry four runtimes, and neither record is built.</p>
<p>The tier count moved three times in one day, and the store moved five times, with no record of either. The database is not the constraint. Bandwidth is.</p>
<p>No record names the tier count or the store. Without one, each session re-derives a different answer.</p>
</section>
<section id="decision" class="level2">
<h2 class="anchored" data-anchor-id="decision">Decision</h2>
<p><strong>Egress dominates the bill, and the store does not.</strong> Bandwidth per client sets the cost, and one node serves the database load. No probe measures a running deployment, so this record carries no cost figure.</p>
<p><strong>Two tiers.</strong></p>
<ul>
<li>Zone client: Godot. Rendering, XR, IK, and local physics.</li>
<li>Server: Elixir and Phoenix. Identity, room directory, and the presence relay.</li>
</ul>
<p>The server relays presence rather than simulating it. Phoenix supplies identity, sessions, and migrations, which a Godot server would need written in GDScript.</p>
<p><strong>The store is FoundationDB</strong>, through <code>ecto_foundationdb</code>. <code>rfd/0075</code> stands, and it is Apache 2.0, C++, write-optimized, and linearly scalable by process. <code>rfd/0103</code>’s limits are accepted, so no joins, no <code>or</code>, and no aggregates in the database.</p>
<p>That trade holds because the relational need is analytical, and the analytical half does not use this store. Event logs and measurements stay zstd Parquet in essential tuple normal form, read by DuckDB. OLTP access is point lookups by player and room identity.</p>
<p><strong>taskweft stays an Elixir NIF.</strong> <code>V-Sekai-fire/multiplayer-fabric-taskweft</code> already has that shape. Janet and the C <code>libtaskweft</code> are dropped.</p>
<p>This supersedes <code>rfd/0107</code> and <code>rfd/0108</code>.</p>
</section>
<section id="references" class="level2">
<h2 class="anchored" data-anchor-id="references">References</h2>
<ul>
<li>The stores considered, Rivet examined, and the open questions: <code>DETAILS.md</code></li>
<li><code>rfd/0075-fdb-over-cockroachdb-for-zone-state</code></li>
</ul>
</section>
<section id="related" class="level2">
<h2 class="anchored" data-anchor-id="related">Related</h2>
<ul>
<li><code>rfd/0103-uro-on-ecto-foundationdb</code>: the adapter and its limits.</li>
<li><code>rfd/0046-server-authoritative-simulation-deferred-rollback</code>: the authority model a relay does not provide.</li>
</ul>
</section>
<section id="detail" class="level2">
<h2 class="anchored" data-anchor-id="detail">Detail</h2>
</section>
<section id="egress-is-the-cost-and-the-store-is-not" class="level2">
<h2 class="anchored" data-anchor-id="egress-is-the-cost-and-the-store-is-not">Egress is the cost, and the store is not</h2>
<p>Bandwidth per client sets the bill. <code>rfd/0100</code> caps a client at 256 kbps, and egress scales with concurrent clients and hours played. No deployment runs, so no monthly figure exists.</p>
</section>
<section id="the-database-is-not-what-the-money-buys" class="level2">
<h2 class="anchored" data-anchor-id="the-database-is-not-what-the-money-buys">The database is not what the money buys</h2>
<p>Measured ceilings, <code>run_id = fly-shared-1x-1g</code> and <code>local-16core</code> in <code>data/measurements/</code>: 2159.1 ops/s on one Fly <code>shared-cpu-1x</code>, and 6086.5 ops/s on a 16 vcpu workstation. Both are the concurrency 32 row, which is the highest the probe ran.</p>
<div class="code-copy-outer-scaffold"><div class="sourceCode" id="cb1" style="background: #f1f3f5;"><pre class="sourceCode sql code-with-copy"><code class="sourceCode sql"><span id="cb1-1"><span class="kw" style="color: #003B4F;
background-color: null;
font-weight: bold;
font-style: inherit;">SELECT</span> run_id, ops <span class="op" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">/</span> seconds <span class="kw" style="color: #003B4F;
background-color: null;
font-weight: bold;
font-style: inherit;">AS</span> ops_per_sec</span>
<span id="cb1-2"><span class="kw" style="color: #003B4F;
background-color: null;
font-weight: bold;
font-style: inherit;">FROM</span> read_parquet(<span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">'throughput.parquet'</span>) <span class="kw" style="color: #003B4F;
background-color: null;
font-weight: bold;
font-style: inherit;">WHERE</span> concurrency <span class="op" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">=</span> <span class="dv" style="color: #AD0000;
background-color: null;
font-style: inherit;">32</span>;</span></code></pre></div></div>
<p>Presence never reaches the store. A pose is transient, and the next tick supersedes it. Persistent writes are login, avatar change, and room join.</p>
</section>
<section id="stores-considered" class="level2">
<h2 class="anchored" data-anchor-id="stores-considered">Stores considered</h2>
<p>The search ran under four rules: FOSS, non-viral license, relational form, and linear scaling. Rust was banned and then unbanned during the search, which returned TiDB to the list.</p>
<table class="caption-top table">
<colgroup>
<col style="width: 20%">
<col style="width: 80%">
</colgroup>
<thead>
<tr class="header">
<th>Store</th>
<th>Outcome</th>
</tr>
</thead>
<tbody>
<tr class="odd">
<td>FoundationDB</td>
<td>Chosen. Apache 2.0, C++, linear, write-optimized</td>
</tr>
<tr class="even">
<td>TiDB</td>
<td>Viable, not chosen. Apache 2.0, MySQL wire, so <code>myxql</code> connects</td>
</tr>
<tr class="odd">
<td>YDB</td>
<td>Viable, not chosen. Apache 2.0, C++, and no Ecto adapter exists</td>
</tr>
<tr class="even">
<td>Apache Ignite</td>
<td>Viable, not chosen. Apache 2.0, Java, and no Ecto adapter exists</td>
</tr>
<tr class="odd">
<td>YugabyteDB</td>
<td>Vetoed</td>
</tr>
<tr class="even">
<td>Stock PostgreSQL</td>
<td>Blocklisted for operational reasons, which are vacuum and heap bloat</td>
</tr>
<tr class="odd">
<td>SQLite</td>
<td>Rejected. Weaker constraint enforcement than the requirement</td>
</tr>
<tr class="even">
<td>CockroachDB</td>
<td>Source-available since 2019, so it fails the FOSS rule</td>
</tr>
<tr class="odd">
<td>Citus</td>
<td>AGPL, so it fails the non-viral rule</td>
</tr>
<tr class="even">
<td>MariaDB, MySQL</td>
<td>GPL, so they fail the non-viral rule</td>
</tr>
<tr class="odd">
<td>ScyllaDB, MongoDB</td>
<td>AGPL or SSPL, so they fail the non-viral rule</td>
</tr>
<tr class="even">
<td>DuckDB, for OLTP</td>
<td>0.919 ms point read, the slowest of four measured in <code>rfd/0103</code></td>
</tr>
<tr class="odd">
<td>Rivet 2.0</td>
<td>Apache 2.0. Actor logic is TypeScript. See the section below</td>
</tr>
</tbody>
</table>
<p>TiDB is the option that keeps relational form with no adapter work. It costs more processes, because a cluster wants PD, TiDB servers, and three TiKV nodes. It also carries MySQL’s weaker constraint set, with no EXCLUDE constraints and no DOMAIN types.</p>
</section>
<section id="rivet-2.0-examined" class="level2">
<h2 class="anchored" data-anchor-id="rivet-2.0-examined">Rivet 2.0, examined</h2>
<p>An earlier draft of this record excluded Rivet because its SDK is TypeScript and it has no Elixir or Godot client. That reason is not accurate, and this section replaces it.</p>
<p>Rivet reaches an actor without an SDK. Raw HTTP goes to <code>https://api.rivet.dev/gateway/{actorId}/request/{...path}</code>, and a raw WebSocket goes to <code>wss://api.rivet.dev/gateway/{actorId}@{token}/websocket/{...path}</code>. So a Godot client can connect. The repository’s own <code>Vanilla HTTP API</code> documentation page is a <code>TODO</code> stub, so that path is real and undocumented.</p>
<p>What Rivet is, verified from the repository: Apache 2.0, 60.7 percent Rust, and pushed on 2026-08-07. The engine has four parts. Pegboard orchestrates actors, Gasoline runs durable execution, Guard routes traffic, and Epoxy is a multi-region key-value store over EPaxos. Self hosting is one binary or <code>docker run -p 6420:6420 rivetdev/engine</code>. Storage is SQLite for local development, FoundationDB for self-hosted deployments, or Postgres.</p>
<p>The fit is genuine on one point. An actor per room with the <code>onWebSocket</code> handler is a presence relay, and <code>options: { canHibernateWebSocket: true }</code> makes an empty room cost nothing.</p>
<p>The reason to decline is the comparison Rivet draws. Its published figures measure against Kubernetes: about 20 ms cold start against 6 s for a pod, and 0.6 KB per actor against 50 MB. This project’s alternative is a BEAM process, not a pod. Phoenix already supplies a process per connection, a process per topic, PubSub fan-out, and supervision. A BEAM process spawns in microseconds and sits in the same size order as 0.6 KB.</p>
<p>Adopting Rivet moves room logic into TypeScript and adds a Rust engine. That is two runtimes replacing one, against this record’s two-tier decision.</p>
<p>Three Rivet features have no Elixir equivalent to hand: Guard’s multi-region routing, Epoxy’s multi-region consensus, and Gasoline’s durable execution. A global player base is the case that would justify revisiting this.</p>
</section>
<section id="the-trade-this-record-accepts" class="level2">
<h2 class="anchored" data-anchor-id="the-trade-this-record-accepts">The trade this record accepts</h2>
<p><code>rfd/0103</code> states the FoundationDB limits directly. A query is valid only when one Get or one GetRange satisfies it. No joins. No <code>or</code>. No aggregates in the database, so filtering and grouping run in Elixir. One Between clause per query, on an indexed field.</p>
<p>Those limits bite reporting, and reporting does not use this store. <code>data/measurements/</code> already holds the analytical record as zstd Parquet, and <code>lean-duckdb</code> already reads it. DuckDB supplies the joins and the aggregates.</p>
<p>Point-read latency from <code>rfd/0103</code>: 0.405 ms for a new transaction per read, and 0.157 ms inside one transaction. PostgreSQL 16 measured 0.084 ms, so FoundationDB is 4.8 and 1.9 times that, in the same order rather than a different one.</p>
</section>
<section id="open-questions" class="level2">
<h2 class="anchored" data-anchor-id="open-questions">Open questions</h2>
<p><strong>Player-hosted zones, or server-hosted.</strong> A client that hosts its zone brings its own upstream, so capacity grows with players rather than with the bill. This record picks server-hosted and does not price the alternative.</p>
<p><strong>Interest management.</strong> <code>lean-interest-mgmt</code> exists, and it is the lever that moves the 256 kbps figure. Nothing in this record changes that number.</p>
<p><strong>Server authority.</strong> A relay is not server-authoritative, so a client can assert a false pose. <code>rfd/0046</code> does not hold for presence. That is tolerable for social VR and wrong for the loot-action combat loop.</p>
<p><strong>WebTransport interoperation.</strong> Whether the Godot client of <code>rfd/0023</code> speaks to a Phoenix endpoint is untested here. WebSocket is the fallback, at a cost in latency.</p>
<p><strong>How many FoundationDB processes, and where.</strong> One machine hosting every process is one failure domain. Real availability needs separate machines, and this record does not fund them.</p>


</section>

 ]]></description>
  <guid>https://v-sekai-multiplayer-fabric.github.io/multiplayer-fabric-manuals/rfd/0109-two-tiers-with-foundationdb-as-the-store/</guid>
  <pubDate>Tue, 18 Aug 2026 01:10:59 GMT</pubDate>
</item>
<item>
  <title>RFD 0108: Local C ABI zone guests, with fan-out inside the tick</title>
  <link>https://v-sekai-multiplayer-fabric.github.io/multiplayer-fabric-manuals/rfd/0108-local-cabi-guests-and-in-tick-fanout/</link>
  <description><![CDATA[ 





<p><em><a href="../../index.html">Manuals</a> › <a href="../../pages/rfd.html">RFDs</a> › RFD 0108: Local C ABI zone guests, with fan-out inside the tick</em></p>
<p><strong>State:</strong> abandoned · <strong>Scope:</strong> zone guest boundary, pubsub fan-out, h2o routing</p>
<section id="problem" class="level2">
<h2 class="anchored" data-anchor-id="problem">Problem</h2>
<p><code>rfd/0107</code> makes h2o the ingress and the reverse proxy, and it leaves the guest boundary open. <code>rfd/0094</code> has guests arrive as CDN-delivered riscv64 ELFs under libriscv. That model carries a runtime, a sandbox, and a delivery path, and simulation code inside the zone needs none of the three.</p>
<p><code>rfd/0097</code> decided that real-time pubsub runs in userspace, and it measured why. It did not say how a guest subscribes, or which event loop owns a subscriber. A fan-out that copies once per subscriber, or that wakes a thread once per message, does not hold at h2o’s rate.</p>
</section>
<section id="decision" class="level2">
<h2 class="anchored" data-anchor-id="decision">Decision</h2>
<p>A simulation guest is a local shared object, <code>.so</code> or <code>.dylib</code>, loaded over the plain C ABI. This is local mode. h2o routes to it, and the call carries no network hop, because the guest shares the zone process.</p>
<p>The CDN keeps one job, which is bulk asset delivery. It no longer delivers guest programs for simulation.</p>
<p>Fan-out runs inside the tick:</p>
<ul>
<li>One publish ring per zone. One writer, which is the tick. Many readers, each with its own cursor and sequence number.</li>
<li>Serialize once into the 100-byte packet of <code>rfd/0053</code>, then fan out a pointer and a refcount. Never serialize per subscriber.</li>
<li>Bind a subscriber to the event loop that owns its connection. Deliver across loops with one batched wakeup per tick, not one per message.</li>
<li>A local guest registers a callback and receives the fan-out as a call, with no copy and no syscall.</li>
<li>Gate a guest once at subscribe time, with the ReBAC actions of <code>rfd/0092</code>. Do not gate per message.</li>
</ul>
<p>FoundationDB stays off the fan-out path. The tick owns the <code>libfdb_c</code> callback chain of <code>rfd/0073</code>, and it appends the durable log off-tick. Watches are not used.</p>
</section>
<section id="references" class="level2">
<h2 class="anchored" data-anchor-id="references">References</h2>
<ul>
<li>The measurements, the back-pressure rule, and the sandbox trade: <code>DETAILS.md</code></li>
<li><code>rfd/0097-pubsub-belongs-in-userspace</code>, <code>fdbpub.c</code></li>
</ul>
</section>
<section id="related" class="level2">
<h2 class="anchored" data-anchor-id="related">Related</h2>
<ul>
<li><code>rfd/0107-janet-scripting-over-a-c-taskweft-core</code>: the tier this fills in.</li>
<li><code>rfd/0049-fabric-channels-as-reliability-classes</code>: the vocabulary the back-pressure rule uses.</li>
<li><code>rfd/0094-minimum-ugc-game-loop-guest-composition</code>: the UGC loop this record does not serve.</li>
</ul>
</section>
<section id="detail" class="level2">
<h2 class="anchored" data-anchor-id="detail">Detail</h2>
</section>
<section id="the-measurements-this-rests-on" class="level2">
<h2 class="anchored" data-anchor-id="the-measurements-this-rests-on">The measurements this rests on</h2>
<p>Every number comes from <code>rfd/0097</code> and <code>rfd/0096</code>. One ZoneTick at 64 Hz is 15.6 ms.</p>
<table class="caption-top table">
<colgroup>
<col style="width: 55%">
<col style="width: 14%">
<col style="width: 29%">
</colgroup>
<thead>
<tr class="header">
<th>Path</th>
<th>Median</th>
<th>Fraction of one tick</th>
</tr>
</thead>
<tbody>
<tr class="odd">
<td>FDB watch fire, publish to subscriber</td>
<td>12214.3 us</td>
<td>78 percent</td>
</tr>
<tr class="even">
<td>FDB commit, one publish</td>
<td>3552.9 us</td>
<td>23 percent</td>
</tr>
<tr class="odd">
<td>FDB versionstamp log append</td>
<td>2509.5 us</td>
<td>16 percent</td>
</tr>
<tr class="even">
<td><code>AF_UNIX</code> round trip, from <code>rfd/0096</code></td>
<td>8.9 us</td>
<td>0.06 percent</td>
</tr>
</tbody>
</table>
<p>A local C ABI call removes the 8.9 us hop as well, because the guest shares the address space.</p>
</section>
<section id="why-watches-stay-unused" class="level2">
<h2 class="anchored" data-anchor-id="why-watches-stay-unused">Why watches stay unused</h2>
<p>Three facts from <code>rfd/0097</code> rule them out, and each one is enough:</p>
<ul>
<li>The watch path is about 1371 times slower than an <code>AF_UNIX</code> round trip.</li>
<li>The probe held exactly 10000 concurrent watches, then failed with error 1032, <code>Too many watches currently set</code>. That cap belongs to the database, and a fabric of many zones shares it.</li>
<li>A watch is edge-triggered. It reports that a key changed, not what the value became. A subscriber re-reads the key, and values that change between the fire and the re-read are lost.</li>
</ul>
<p>The third fact is the one that disqualifies a watch as an event stream, independent of speed.</p>
</section>
<section id="the-ring-and-why-one-writer" class="level2">
<h2 class="anchored" data-anchor-id="the-ring-and-why-one-writer">The ring, and why one writer</h2>
<p>The tick is the only writer, so the write side needs no lock. A reader holds a cursor, and it advances at its own rate.</p>
<p>Serializing once is the point. Fan-out to N subscribers costs one serialization and N pointer writes, rather than N serializations. The packet is 100 bytes and integral, per <code>rfd/0053</code>, so a subscriber reads the same bytes the wire carries.</p>
</section>
<section id="wakeup-batching" class="level2">
<h2 class="anchored" data-anchor-id="wakeup-batching">Wakeup batching</h2>
<p>A cross-loop delivery needs a wakeup, and a wakeup is a syscall. One wakeup per message does not hold at this rate.</p>
<p>Signal a loop once per tick instead, after the ring advances. At 64 Hz that caps a loop near 64 wakeups per second, whatever the message count.</p>
</section>
<section id="back-pressure-which-needs-a-rule-per-channel" class="level2">
<h2 class="anchored" data-anchor-id="back-pressure-which-needs-a-rule-per-channel">Back-pressure, which needs a rule per channel</h2>
<p>A lagging reader must never stall the writer. A cursor that falls out of the ring gets dropped, and the subscriber receives a resync signal rather than a gap.</p>
<p><code>rfd/0049</code> makes fabric channels reliability classes, and that gives the rule its vocabulary. A state channel may drop, because the next tick supersedes the last one. A control channel may not drop, so it needs its own path rather than the ring.</p>
</section>
<section id="the-sandbox-this-removes" class="level2">
<h2 class="anchored" data-anchor-id="the-sandbox-this-removes">The sandbox this removes</h2>
<p>A local <code>.so</code> shares the zone’s address space. A guest fault takes the zone down, and a guest reads any memory the zone holds.</p>
<p><code>rfd/0083</code> chose Fil-C for memory safety against untrusted client input. <code>rfd/0095</code> built two guest classes for the same reason. Local mode has neither property.</p>
<p>That is correct for first-party simulation code, which the project writes and builds. It is wrong for user-generated code. <code>rfd/0094</code>’s UGC loop therefore needs a mechanism this record does not supply.</p>
</section>
<section id="what-this-resolves-in-rfd-0107" class="level2">
<h2 class="anchored" data-anchor-id="what-this-resolves-in-rfd-0107">What this resolves in RFD 0107</h2>
<p>Two of the four open questions close.</p>
<p>The tick drives the <code>libfdb_c</code> callbacks, and h2o’s ingress loop does not. The callback chain of <code>rfd/0073</code> moves with the tick, and the durable append runs off-tick.</p>
<p>The guest boundary sits in-process, over the C ABI, for simulation guests.</p>
<p>Two questions stay open, and one arrives.</p>
<p>Where Elixir and <code>zone-backend</code> sit is unchanged by this record.</p>
<p>Which part terminates a WebTransport session stays open, and it now matters more. This record binds a subscriber to the loop that owns its connection. <code>rfd/0097</code> states that the zone server holds subscriber connections through picoquic and WebTransport, and <code>rfd/0088</code> chose picoquic over h2o’s own QUIC. So the owning loop is h2o’s thread for HTTP and picoquic’s loop for WebTransport. Whether those unify decides how many cross-loop wakeups a tick needs.</p>
<p>How a UGC guest runs is the new question. Local mode does not sandbox, so the UGC loop needs its own record.</p>


</section>

 ]]></description>
  <guid>https://v-sekai-multiplayer-fabric.github.io/multiplayer-fabric-manuals/rfd/0108-local-cabi-guests-and-in-tick-fanout/</guid>
  <pubDate>Tue, 18 Aug 2026 01:10:59 GMT</pubDate>
</item>
<item>
  <title>RFD 0107: Janet scripting over a C taskweft core, behind h2o</title>
  <link>https://v-sekai-multiplayer-fabric.github.io/multiplayer-fabric-manuals/rfd/0107-janet-scripting-over-a-c-taskweft-core/</link>
  <description><![CDATA[ 





<p><em><a href="../../index.html">Manuals</a> › <a href="../../pages/rfd.html">RFDs</a> › RFD 0107: Janet scripting over a C taskweft core, behind h2o</em></p>
<p><strong>State:</strong> abandoned · <strong>Scope:</strong> zone server tier, taskweft implementation language</p>
<section id="problem" class="level2">
<h2 class="anchored" data-anchor-id="problem">Problem</h2>
<p><code>rfd/0083</code> built the zone server as native libh2o with FoundationDB. The tier held. Its cost was questioned, and <code>zone-server-h2o</code> was archived on 2026-08-08 on the argument that a fourth runtime was more than the project could carry.</p>
<p>The archive removed the host and left no answer to what serves a zone. Candidate answers came and went inside one session: an Elixir epoll server, then h2o with a taskweft NIF, then a CockroachDB variant of this record. None reached the site.</p>
<p>A tier needs one record. Without it, each session re-derives the stack from memory, and the inventory in <code>rfd/0062</code> describes a host that is gone.</p>
</section>
<section id="decision" class="level2">
<h2 class="anchored" data-anchor-id="decision">Decision</h2>
<p>Four parts, each with one job:</p>
<ul>
<li>h2o is the frontend HTTP ingress and the reverse proxy. It terminates the connection. It does not hold game logic.</li>
<li>Janet is the dynamic layer: routing glue, configuration parsing, and scripting.</li>
<li><code>libtaskweft</code> is a C library carrying the heavy work: HTN state-space search, graph traversal, and game state. It exposes C bindings, and Janet loads it as a native module, <code>.so</code> or <code>.dylib</code>.</li>
<li>Godot is the client and the visualization frontend, over GDExtension and the C++ API.</li>
</ul>
<p>FoundationDB stays the database, reached through the <code>libfdb_c</code> C API. <code>rfd/0075</code> stands, and CockroachDB is not the store. <code>rfd/0073</code>’s async callback chain is the integration pattern, because <code>libfdb_c</code> is callback-based and needs an event loop to drive it.</p>
<p>Rust is out. The core is C. <code>zone-server-h2o</code> is unarchived and is the host repository again.</p>
</section>
<section id="references" class="level2">
<h2 class="anchored" data-anchor-id="references">References</h2>
<ul>
<li>The claims this rests on, what is not measured, and the open questions: <code>DETAILS.md</code></li>
<li><code>v-sekai-multiplayer-fabric/zone-server-h2o</code>, <a href="https://github.com/v-sekai-multiplayer-fabric/fabric-flow-adapters">fabric-flow-adapters</a></li>
</ul>
</section>
<section id="related" class="level2">
<h2 class="anchored" data-anchor-id="related">Related</h2>
<ul>
<li><code>rfd/0083-zone-server-h2o-replaces-godot-fabriczone</code>: the host this amends rather than replaces.</li>
<li><code>rfd/0075-fdb-over-cockroachdb-for-zone-state</code>: the database choice this keeps.</li>
<li><code>rfd/0093-compile-taskweft-to-linear-automata</code>: the taskweft evaluation model the C core implements.</li>
</ul>
</section>
<section id="detail" class="level2">
<h2 class="anchored" data-anchor-id="detail">Detail</h2>
</section>
<section id="role-split" class="level2">
<h2 class="anchored" data-anchor-id="role-split">Role split</h2>
<table class="caption-top table">
<colgroup>
<col style="width: 17%">
<col style="width: 72%">
<col style="width: 10%">
</colgroup>
<thead>
<tr class="header">
<th>Part</th>
<th>Job</th>
<th>Language</th>
</tr>
</thead>
<tbody>
<tr class="odd">
<td>h2o</td>
<td>HTTP ingress and reverse proxy. Connection termination.</td>
<td>C</td>
</tr>
<tr class="even">
<td>Janet</td>
<td>Routing glue, configuration parsing, dynamic scripting.</td>
<td>Janet</td>
</tr>
<tr class="odd">
<td><code>libtaskweft</code></td>
<td>HTN state-space search, graph traversal, game state.</td>
<td>C</td>
</tr>
<tr class="even">
<td>Godot</td>
<td>Client and visualization frontend, GDExtension and C++.</td>
<td>C++</td>
</tr>
<tr class="odd">
<td>FoundationDB</td>
<td>State, reached through <code>libfdb_c</code>.</td>
<td>—</td>
</tr>
</tbody>
</table>
<p>The boundary that matters is between Janet and <code>libtaskweft</code>. Janet holds what changes often and runs rarely. The C core holds what runs on every tick.</p>
</section>
<section id="the-database-keeps-its-record-set" class="level2">
<h2 class="anchored" data-anchor-id="the-database-keeps-its-record-set">The database keeps its record set</h2>
<p>FoundationDB stays, so the records that describe it stay in force:</p>
<table class="caption-top table">
<thead>
<tr class="header">
<th>Record</th>
<th>What it holds</th>
</tr>
</thead>
<tbody>
<tr class="odd">
<td><code>rfd/0075</code></td>
<td>FoundationDB over CockroachDB, for a write-heavy load</td>
</tr>
<tr class="even">
<td><code>rfd/0073</code></td>
<td>The async callback chain <code>libfdb_c</code> requires</td>
</tr>
<tr class="odd">
<td><code>rfd/0074</code></td>
<td>Binary value encoding for FDB values</td>
</tr>
<tr class="even">
<td><code>rfd/0084</code></td>
<td>zstd compression for batched zone-state values</td>
</tr>
<tr class="odd">
<td><code>rfd/0097</code></td>
<td>Pubsub in userspace, with FDB as the durable log</td>
</tr>
<tr class="even">
<td><code>rfd/0103</code></td>
<td>Uro on <code>ecto_foundationdb</code></td>
</tr>
<tr class="odd">
<td><code>rfd/0002</code></td>
<td>taskweft value narrowing in the FDB value encoding</td>
</tr>
</tbody>
</table>
<p><code>ecto_foundationdb</code> and <code>ecto-bench-tpcc</code> therefore keep a live consumer, and <code>rfd/0006</code>’s CockroachDB with mTLS does not describe this tier’s store.</p>
</section>
<section id="claims-and-what-is-not-measured" class="level2">
<h2 class="anchored" data-anchor-id="claims-and-what-is-not-measured">Claims, and what is not measured</h2>
<p>Three claims come with this tier:</p>
<ul>
<li>Startup is fast.</li>
<li>Memory stays under 10 MB.</li>
<li>Execution speed is near native.</li>
</ul>
<p>None of the three is measured in this project. <code>data/measurements/</code> holds throughput and latency parquet for a FoundationDB probe and the libh2o host, and nothing for Janet or for a C taskweft core.</p>
<p>A record that states this tier’s ceiling needs a measurement of this tier. A number extrapolated from a 32-worker probe is not a measurement of 1000 concurrent users, and this project keeps no such extrapolation.</p>
</section>
<section id="why-rust-is-out" class="level2">
<h2 class="anchored" data-anchor-id="why-rust-is-out">Why Rust is out</h2>
<p>Rust is blocklisted for this project. The core is C, and the C bindings Janet loads are the native-module boundary. This removes the option of a Rust core with C bindings, which is otherwise the common shape for this pattern.</p>
</section>
<section id="what-this-replaces" class="level2">
<h2 class="anchored" data-anchor-id="what-this-replaces">What this replaces</h2>
<p><code>libtaskweft</code> in C replaces the Elixir NIF form of taskweft. <code>V-Sekai-fire/multiplayer-fabric-taskweft</code> is the re-entrant temporal HTN planner and ReBAC engine today, and it is an Elixir NIF. A C library with Janet bindings is a different artifact, and whether it is a rewrite in that repository or a new repository is open.</p>
<p><code>rfd/0093</code> compiles taskweft relation expressions to linear automata. That evaluation model is what the C core implements, and the record survives the language change.</p>
</section>
<section id="open-questions-this-record-does-not-settle" class="level2">
<h2 class="anchored" data-anchor-id="open-questions-this-record-does-not-settle">Open questions this record does not settle</h2>
<p>This record is abandoned, so none of the four questions below stay open in the form they take here. Each one asks where a part goes in a four-part stack that the project does not build. The records that carry the same subject forward are these.</p>
<p><code>rfd/0109</code> places Elixir and the store. The tier is Elixir plus FoundationDB, and <code>rfd/0103</code> puts Uro on <code>ecto_foundationdb</code>, so the Elixir tier drives the database path. <code>rfd/0110</code> places the guest runtime and the transport. One pinned Godot build hosts libriscv guests through its <code>sandbox</code> module and terminates WebTransport through its <code>http3</code> module, and no Janet layer exists to compete with either. <code>rfd/0108</code> closed the first and third questions before it was abandoned in turn.</p>
<p>The four questions stay written below as the record left them.</p>
<p><strong>Which part talks to FoundationDB.</strong> <code>rfd/0073</code> drove <code>libfdb_c</code> callbacks from the libh2o event loop, when h2o was the whole server. If h2o is ingress and reverse proxy only, the loop that drives the callbacks may belong to <code>libtaskweft</code> instead. Janet has no <code>libfdb_c</code> binding in this project today.</p>
<p><strong>Where Elixir goes.</strong> The four parts name no Elixir. <code>zone-backend</code> is Uro, a Phoenix and Elixir application, and it is the live identity, zone-directory, and planner service with a push on 2026-08-08. <code>rfd/0090</code> is its production release shape, and <code>rfd/0103</code> puts it on <code>ecto_foundationdb</code>. Nothing here retires it, and nothing here places it.</p>
<p><strong>Whether Janet replaces the libriscv guests.</strong> <code>rfd/0094</code> keeps a UGC game loop where guests arrive as CDN-delivered riscv64 ELFs, run under libriscv, and are gated by ReBAC per <code>rfd/0092</code>. Janet is also a dynamic layer loaded at runtime. Two runtimes for user code is one more than the tier argument allows.</p>
<p><strong>Where the game transport lives.</strong> <code>rfd/0008</code> and <code>rfd/0023</code> put game traffic on WebTransport over HTTP/3, and <code>rfd/0088</code> chose picoquic and picotls over h2o’s own QUIC. If h2o terminates HTTP as ingress, the record that says which part terminates a WebTransport session does not exist.</p>


</section>

 ]]></description>
  <guid>https://v-sekai-multiplayer-fabric.github.io/multiplayer-fabric-manuals/rfd/0107-janet-scripting-over-a-c-taskweft-core/</guid>
  <pubDate>Tue, 18 Aug 2026 01:10:59 GMT</pubDate>
</item>
<item>
  <title>RFD 0106: Split the archive into its own repository</title>
  <link>https://v-sekai-multiplayer-fabric.github.io/multiplayer-fabric-manuals/rfd/0106-split-the-archive-into-its-own-repo/</link>
  <description><![CDATA[ 





<p><em><a href="../../index.html">Manuals</a> › <a href="../../pages/rfd.html">RFDs</a> › RFD 0106: Split the archive into its own repository</em></p>
<p><strong>State:</strong> published · <strong>Scope:</strong> manuals repository layout, archived records</p>
<section id="problem" class="level2">
<h2 class="anchored" data-anchor-id="problem">Problem</h2>
<p>The manuals repository holds live design records and dead ones in the same tree. <code>_archive/decisions/</code> holds 26 MADR records that are rejected or superseded. A leading underscore keeps Quarto from rendering them, so the site hides them, and the repository still carries them.</p>
<p>Two RFDs are superseded and still render as current. <code>rfd/0089</code> supersedes <code>rfd/0061</code> on the deployment target. <code>rfd/0011</code> is superseded on the observability stack. A reader who opens either one reads a dead design with no marking on the page.</p>
<p>Hiding a record from the site is not the same as retiring it. The repository needs one place for retired records.</p>
</section>
<section id="decision" class="level2">
<h2 class="anchored" data-anchor-id="decision">Decision</h2>
<p>Create <code>multiplayer-fabric-archive</code>, a public repository in this organization. It is the standing home for retired records. It holds plain Markdown, with no Quarto site, no build, and no checks, because archived content does not change.</p>
<p>Move <code>_archive/</code>, its attachments, <code>rfd/0011</code>, and <code>rfd/0061</code> there. Delete them here. Keep no pointer stub, per <code>rfd/0000-conventions</code>.</p>
<p>The archive repository carries the manuals history up to the split, so each moved file keeps every commit that touched it at its original path.</p>
<p><code>rfd/0011</code> and <code>rfd/0061</code> keep their numbers. This repository does not reuse a number after a move.</p>
</section>
<section id="references" class="level2">
<h2 class="anchored" data-anchor-id="references">References</h2>
<ul>
<li>The link repairs a move forces, and the record of what moved: <code>DETAILS.md</code></li>
<li><code>v-sekai-multiplayer-fabric/multiplayer-fabric-archive</code></li>
</ul>
</section>
<section id="related" class="level2">
<h2 class="anchored" data-anchor-id="related">Related</h2>
<ul>
<li><code>rfd/0000-conventions</code>: the no-pointer-stub rule this follows.</li>
<li><code>rfd/0071-yagni-times-structure-to-need</code>: the rule that sends a superseded record to the archive.</li>
<li><code>rfd/0062-repository-and-capability-inventory</code>: lists the new repository.</li>
</ul>
</section>
<section id="detail" class="level2">
<h2 class="anchored" data-anchor-id="detail">Detail</h2>
</section>
<section id="what-moved" class="level2">
<h2 class="anchored" data-anchor-id="what-moved">What moved</h2>
<table class="caption-top table">
<thead>
<tr class="header">
<th>Path</th>
<th>Records</th>
</tr>
</thead>
<tbody>
<tr class="odd">
<td><code>_archive/decisions/</code></td>
<td>26</td>
</tr>
<tr class="even">
<td><code>_archive/decisions/attachments/</code></td>
<td>1 image</td>
</tr>
<tr class="odd">
<td><code>rfd/0011-observability-stack-victoriatraces/</code></td>
<td>1</td>
</tr>
<tr class="even">
<td><code>rfd/0061-quadlets-on-fedora-44-instead-of-harvester/</code></td>
<td>1</td>
</tr>
</tbody>
</table>
<p><code>_archive/README.md</code> moves with the directory. Its text now states that the leading underscore does no work in a repository that builds no site, and that the path stays as it was so older links keep their shape.</p>
</section>
<section id="link-repair-which-a-move-forces" class="level2">
<h2 class="anchored" data-anchor-id="link-repair-which-a-move-forces">Link repair, which a move forces</h2>
<p>A deleted file leaves no stub. A live Markdown link to that file leaves a 404 on the published site. So a move repairs every inbound link, and that is not a stub.</p>
<table class="caption-top table">
<colgroup>
<col style="width: 46%">
<col style="width: 53%">
</colgroup>
<thead>
<tr class="header">
<th>File</th>
<th>Repair</th>
</tr>
</thead>
<tbody>
<tr class="odd">
<td><code>changelog/2026/20260606-deck-log.md</code></td>
<td>Four relative links into <code>_archive/decisions/</code> now point at the archive repository.</td>
</tr>
<tr class="even">
<td><code>rfd/0071-yagni-times-structure-to-need/index.md</code></td>
<td>Two pointers to <code>_archive</code> now name the archive repository.</td>
</tr>
<tr class="odd">
<td><code>rfd/0089-flyio-over-podman-quadlets/index.md</code></td>
<td>The <code>rfd/0061</code> reference now links to the archive.</td>
</tr>
<tr class="even">
<td><code>rfd/0060-org-scoped-github-app-token-for-gh-access/index.md</code></td>
<td>The <code>rfd/0061</code> reference now links to the archive.</td>
</tr>
<tr class="odd">
<td><code>rfd/0070-loop-slice-telemetry-to-the-observability-collector/index.md</code></td>
<td>The <code>rfd/0011</code> reference now links to the archive.</td>
</tr>
<tr class="even">
<td><code>rfd/0062-repository-and-capability-inventory/DETAILS.md</code></td>
<td>The two-org split decision path now links to the archive.</td>
</tr>
<tr class="odd">
<td><code>_quarto.yml</code></td>
<td>Drops the <code>decisions/attachments</code> resource, which no longer exists.</td>
</tr>
</tbody>
</table>
<p>Inline prose in <code>rfd/0089</code>’s <code>DETAILS.md</code> still names <code>rfd/0061</code> in code spans, without links. Those read as citations of a numbered record, so they stay.</p>
</section>
<section id="a-broken-link-the-move-exposed" class="level2">
<h2 class="anchored" data-anchor-id="a-broken-link-the-move-exposed">A broken link the move exposed</h2>
<p>Two archived records cite the debug-orb image as <code>attachments/20260606_vsekai-mpf_xr-grid-debug-orbs_0001.png</code>, relative to <code>_archive/decisions/</code>. The image sat in <code>decisions/attachments/</code>, two directories away, so the link never resolved. No rendered page exercised it, because Quarto skips <code>_archive</code>.</p>
<p>The archive repository puts the image at <code>_archive/decisions/attachments/</code>, where the existing links resolve.</p>
</section>
<section id="two-archive-destinations-now-exist" class="level2">
<h2 class="anchored" data-anchor-id="two-archive-destinations-now-exist">Two archive destinations now exist</h2>
<p><code>V-Sekai-archive</code> is a separate organization that already holds three archived repositories: <code>lean-predictive-bvh</code>, <code>viser</code>, and <code>usd-converter-for-vrchat</code>. <code>multiplayer-fabric-archive</code> is a repository for archived records inside this organization.</p>
<p>A whole repository goes to the <code>V-Sekai-archive</code> organization. A record inside the manuals goes to <code>multiplayer-fabric-archive</code>. A future record that consolidates the two supersedes this one.</p>
</section>
<section id="counts-after-the-split" class="level2">
<h2 class="anchored" data-anchor-id="counts-after-the-split">Counts after the split</h2>
<table class="caption-top table">
<thead>
<tr class="header">
<th>Set</th>
<th>Count</th>
</tr>
</thead>
<tbody>
<tr class="odd">
<td>Repos in <code>v-sekai-multiplayer-fabric</code></td>
<td>73</td>
</tr>
<tr class="even">
<td>Active</td>
<td>49</td>
</tr>
<tr class="odd">
<td>Archived</td>
<td>24</td>
</tr>
</tbody>
</table>
</section>
<section id="what-stays" class="level2">
<h2 class="anchored" data-anchor-id="what-stays">What stays</h2>
<p><code>decisions/</code> keeps <code>madr-proposal-template.md</code>, the live template. The directory holds no records, because <code>rfd/0000-conventions</code> migrates each MADR into an RFD and deletes the MADR file.</p>
<p><code>AGENTS.md</code> still directs a new image to <code>decisions/attachments/</code>, and that directory returns when the next asset arrives.</p>


</section>

 ]]></description>
  <guid>https://v-sekai-multiplayer-fabric.github.io/multiplayer-fabric-manuals/rfd/0106-split-the-archive-into-its-own-repo/</guid>
  <pubDate>Tue, 18 Aug 2026 01:10:59 GMT</pubDate>
</item>
<item>
  <title>RFD 0105: Archive the self-host-era support repos and the empty stubs</title>
  <link>https://v-sekai-multiplayer-fabric.github.io/multiplayer-fabric-manuals/rfd/0105-archive-self-host-era-support-repos/</link>
  <description><![CDATA[ 





<p><em><a href="../../index.html">Manuals</a> › <a href="../../pages/rfd.html">RFDs</a> › RFD 0105: Archive the self-host-era support repos and the empty stubs</em></p>
<p><strong>State:</strong> published · <strong>Scope:</strong> org-wide repository lifecycle</p>
<section id="problem" class="level2">
<h2 class="anchored" data-anchor-id="problem">Problem</h2>
<p><code>rfd/0089</code> moved the deployment target to Fly.io and archived the seven quadlet repos and <code>infra</code>. It did not reach the repos that supported those quadlets from one step away. A verification queue that targets quadlets, a casync CDN, a scoop bucket, and a Burrito packaging repo all stayed active with no live consumer.</p>
<p>Two repos also carry no content at all. <code>vulkan-video-godot</code> holds 0 KB. <code>steamdeck</code> holds 1 KB. Both appear in the inventory as if they hold work.</p>
<p>An active repo with no consumer costs a reader time. The reader cannot tell a parked repo from a live one.</p>
</section>
<section id="decision" class="level2">
<h2 class="anchored" data-anchor-id="decision">Decision</h2>
<p>Archive six repos. Each one is tombstoned, not deleted, so the history stays reachable.</p>
<ul>
<li><code>fabric-container-verify</code>: the <code>rfd/0056</code> quadlet verification queue. Every quadlet it targets is archived by <code>rfd/0089</code>.</li>
<li><code>fabric-casync-central</code>: 0 KB. No content was ever pushed.</li>
<li><code>fabric-scoop-central</code>: the scoop bucket for the Windows demo of <code>godot-loop-slice</code>, which is archived.</li>
<li><code>fabric-platform-central</code>: the <code>rfd/0065</code> Burrito and casync packaging path.</li>
<li><code>vulkan-video-godot</code>: 0 KB.</li>
<li><code>steamdeck</code>: 1 KB.</li>
</ul>
<p><code>fabric-godot-assembly</code> is archived and then restored on the same day. It stays active. The <code>gitassembly</code> recipe of <code>rfd/0019</code> still composes the engine.</p>
<p>The dormant Lean cores stay active. <code>rfd/0057</code> makes them the canonical proven reference, and a reference repo needs no recent push.</p>
</section>
<section id="references" class="level2">
<h2 class="anchored" data-anchor-id="references">References</h2>
<ul>
<li>Per-repo record, the open inconsistency, and the counts: <code>DETAILS.md</code></li>
<li><code>rfd/0062-repository-and-capability-inventory</code>: the inventory this decision updates.</li>
</ul>
</section>
<section id="related" class="level2">
<h2 class="anchored" data-anchor-id="related">Related</h2>
<ul>
<li><code>rfd/0089-flyio-over-podman-quadlets</code>: archives the quadlets that these repos support.</li>
<li><code>rfd/0065-fabric-platform-central-elixir-burrito-casync</code>: the decision that <code>fabric-platform-central</code> carries.</li>
</ul>
</section>
<section id="detail" class="level2">
<h2 class="anchored" data-anchor-id="detail">Detail</h2>
</section>
<section id="what-each-repo-carried" class="level2">
<h2 class="anchored" data-anchor-id="what-each-repo-carried">What each repo carried</h2>
<table class="caption-top table">
<colgroup>
<col style="width: 22%">
<col style="width: 3%">
<col style="width: 9%">
<col style="width: 64%">
</colgroup>
<thead>
<tr class="header">
<th>Repo</th>
<th>Size</th>
<th>Last push</th>
<th>Basis for the archive</th>
</tr>
</thead>
<tbody>
<tr class="odd">
<td><code>fabric-container-verify</code></td>
<td>0 KB</td>
<td>2026-06-20</td>
<td><code>rfd/0056</code> quadlet queue. <code>rfd/0089</code> archives every quadlet it targets.</td>
</tr>
<tr class="even">
<td><code>fabric-casync-central</code></td>
<td>0 KB</td>
<td>2026-06-24</td>
<td>Empty. No content was ever pushed.</td>
</tr>
<tr class="odd">
<td><code>fabric-scoop-central</code></td>
<td>8 KB</td>
<td>2026-06-30</td>
<td>Scoop bucket for the Windows demo of the archived <code>godot-loop-slice</code>.</td>
</tr>
<tr class="even">
<td><code>fabric-platform-central</code></td>
<td>—</td>
<td>2026-06-25</td>
<td><code>rfd/0065</code> Burrito and casync packaging.</td>
</tr>
<tr class="odd">
<td><code>vulkan-video-godot</code></td>
<td>0 KB</td>
<td>2026-05-28</td>
<td>Empty placeholder.</td>
</tr>
<tr class="even">
<td><code>steamdeck</code></td>
<td>1 KB</td>
<td>2026-06-14</td>
<td>Empty placeholder.</td>
</tr>
</tbody>
</table>
<p><code>fabric-container-verify</code> reports 0 KB for its packed size. Its GDScript content is present in history.</p>
</section>
<section id="the-fabric-platform-central-risk" class="level2">
<h2 class="anchored" data-anchor-id="the-fabric-platform-central-risk">The <code>fabric-platform-central</code> risk</h2>
<p><code>rfd/0090</code> describes a live Burrito release of <code>uro</code> on Fly.io. <code>rfd/0065</code> puts the Burrito and casync packaging in <code>fabric-platform-central</code>. An archive makes the repo read-only. If the current <code>zone-backend</code> deploy still reads from this repo, its pushes and its Actions stop.</p>
<p>The archive is reversible with one API call. This record states the risk so a later failure has a written cause.</p>
</section>
<section id="counts-after-the-sweep" class="level2">
<h2 class="anchored" data-anchor-id="counts-after-the-sweep">Counts after the sweep</h2>
<p>These counts predate the archive split of <code>rfd/0106</code>, which adds one repository.</p>
<table class="caption-top table">
<thead>
<tr class="header">
<th>Set</th>
<th>Count</th>
</tr>
</thead>
<tbody>
<tr class="odd">
<td>Total repos</td>
<td>72</td>
</tr>
<tr class="even">
<td>Archived</td>
<td>24</td>
</tr>
<tr class="odd">
<td>Active</td>
<td>48</td>
</tr>
</tbody>
</table>
</section>
<section id="open-inconsistency-not-resolved-here" class="level2">
<h2 class="anchored" data-anchor-id="open-inconsistency-not-resolved-here">Open inconsistency, not resolved here</h2>
<p><code>zone-server-h2o</code> is archived. <code>rfd/0094</code> names it the host of the minimum UGC game loop, and <code>rfd/0095</code> carries it in scope. Its last push is 2026-08-07. An archived repo accepts no push.</p>
<p>This record does not resolve the state of <code>zone-server-h2o</code>. Either the archive is an error, or a decision that supersedes <code>rfd/0094</code> is missing from <code>rfd/</code>. One of the two needs a record.</p>
</section>
<section id="stale-names-found-in-the-inventory" class="level2">
<h2 class="anchored" data-anchor-id="stale-names-found-in-the-inventory">Stale names found in the inventory</h2>
<p>Every repository name in <code>rfd/0062</code>’s inventory still resolves, because GitHub keeps a redirect after a rename. A redirect hides the rename from a reader. <code>rfd/0064</code> sets kebab-case repository names, and these renames follow it.</p>
<table class="caption-top table">
<thead>
<tr class="header">
<th>Name in the old inventory</th>
<th>Current name</th>
</tr>
</thead>
<tbody>
<tr class="odd">
<td><code>godot</code></td>
<td><code>fabric-godot-core</code></td>
</tr>
<tr class="even">
<td><code>merge</code></td>
<td><code>fabric-godot-assembly</code></td>
</tr>
<tr class="odd">
<td><code>observability</code></td>
<td><code>fabric-game-observability</code></td>
</tr>
<tr class="even">
<td><code>zone-server-image</code></td>
<td><code>zone-server-quadlet</code></td>
</tr>
<tr class="odd">
<td><code>zone-baker-image</code></td>
<td><code>zone-baker-quadlet</code></td>
</tr>
<tr class="even">
<td><code>zone-backend-image</code></td>
<td><code>zone-backend-quadlet</code></td>
</tr>
<tr class="odd">
<td><code>gha-runner-image</code></td>
<td><code>gha-runner-quadlet</code></td>
</tr>
<tr class="even">
<td><code>sccache-cache-image</code></td>
<td><code>sccache-cache-quadlet</code></td>
</tr>
<tr class="odd">
<td><code>manuals</code></td>
<td><code>multiplayer-fabric-manuals</code></td>
</tr>
</tbody>
</table>
<p>Three repos moved to a third organization, <code>V-Sekai-archive</code>, and are archived there: <code>lean-predictive-bvh</code>, <code>viser</code>, and <code>usd-converter-for-vrchat</code>.</p>


</section>

 ]]></description>
  <guid>https://v-sekai-multiplayer-fabric.github.io/multiplayer-fabric-manuals/rfd/0105-archive-self-host-era-support-repos/</guid>
  <pubDate>Tue, 18 Aug 2026 01:10:59 GMT</pubDate>
</item>
<item>
  <title>RFD 0103: Uro on ecto_foundationdb</title>
  <link>https://v-sekai-multiplayer-fabric.github.io/multiplayer-fabric-manuals/rfd/0103-uro-on-ecto-foundationdb/</link>
  <description><![CDATA[ 





<p><em><a href="../../index.html">Manuals</a> › <a href="../../pages/rfd.html">RFDs</a> › RFD 0103: Uro on ecto_foundationdb</em></p>
<p><strong>State:</strong> discussion · <strong>Scope:</strong> zone-backend, aria-storage</p>
<section id="decision" class="level2">
<h2 class="anchored" data-anchor-id="decision">Decision</h2>
<p>Uro stores its data through <strong><code>ecto_foundationdb</code></strong>, in the FoundationDB cluster <code>rfd/0102</code> already deploys. No second datastore, and no machine for a database.</p>
</section>
<section id="cost" class="level2">
<h2 class="anchored" data-anchor-id="cost">Cost</h2>
<table class="caption-top table">
<colgroup>
<col style="width: 59%">
<col style="width: 12%">
<col style="width: 28%">
</colgroup>
<thead>
<tr class="header">
<th>Option</th>
<th>Fixed USD</th>
<th>Concurrent at 4 h/day</th>
</tr>
</thead>
<tbody>
<tr class="odd">
<td><strong><code>ecto_foundationdb</code>, no database machine</strong></td>
<td><strong>4.19</strong></td>
<td><strong>39.1</strong></td>
</tr>
<tr class="even">
<td>PostgreSQL colocated at 512 MB</td>
<td>5.64</td>
<td>33.9</td>
</tr>
<tr class="odd">
<td>PostgreSQL on its own machine</td>
<td>6.36</td>
<td>31.2</td>
</tr>
<tr class="even">
<td>Self-hosted CockroachDB, 1 GB</td>
<td>10.26</td>
<td>17.1</td>
</tr>
<tr class="odd">
<td>Neon, paid, always on at 1 CU</td>
<td>81.59</td>
<td>over budget</td>
</tr>
</tbody>
</table>
<p>The database is free here, because <code>rfd/0102</code> already runs <code>fdbserver</code> for zone state.</p>
</section>
<section id="latency-measured" class="level2">
<h2 class="anchored" data-anchor-id="latency-measured">Latency, measured</h2>
<table class="caption-top table">
<thead>
<tr class="header">
<th>Engine</th>
<th>Point read</th>
</tr>
</thead>
<tbody>
<tr class="odd">
<td>PostgreSQL 16</td>
<td>0.084 ms</td>
</tr>
<tr class="even">
<td><strong>FoundationDB, new transaction per read</strong></td>
<td><strong>0.405 ms</strong></td>
</tr>
<tr class="odd">
<td><strong>FoundationDB, inside one transaction</strong></td>
<td><strong>0.157 ms</strong></td>
</tr>
<tr class="even">
<td>DuckDB</td>
<td>0.919 ms</td>
</tr>
</tbody>
</table>
<p>0.405 ms is the floor for a <code>Repo.get</code>, and 0.157 ms is the floor inside <code>Repo.transactional/2</code>. That is 4.8 and 1.9 times PostgreSQL, in the same order rather than a different one.</p>
<p>Provenance: <code>run_id = ci-container</code> in <code>data/measurements/</code>. All four rows are stored rows, and the two ratios are arithmetic over them. The PostgreSQL row has <code>samples = 1</code>, so it carries no distribution.</p>
<div class="code-copy-outer-scaffold"><div class="sourceCode" id="cb1" style="background: #f1f3f5;"><pre class="sourceCode sql code-with-copy"><code class="sourceCode sql"><span id="cb1-1"><span class="kw" style="color: #003B4F;
background-color: null;
font-weight: bold;
font-style: inherit;">SELECT</span> subject, median_ns <span class="op" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">/</span> <span class="fl" style="color: #AD0000;
background-color: null;
font-style: inherit;">1e6</span> <span class="kw" style="color: #003B4F;
background-color: null;
font-weight: bold;
font-style: inherit;">AS</span> median_ms, samples</span>
<span id="cb1-2"><span class="kw" style="color: #003B4F;
background-color: null;
font-weight: bold;
font-style: inherit;">FROM</span> read_parquet(<span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">'latency.parquet'</span>)</span>
<span id="cb1-3"><span class="kw" style="color: #003B4F;
background-color: null;
font-weight: bold;
font-style: inherit;">WHERE</span> operation <span class="kw" style="color: #003B4F;
background-color: null;
font-weight: bold;
font-style: inherit;">LIKE</span> <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">'point_%'</span> <span class="kw" style="color: #003B4F;
background-color: null;
font-weight: bold;
font-style: inherit;">ORDER</span> <span class="kw" style="color: #003B4F;
background-color: null;
font-weight: bold;
font-style: inherit;">BY</span> median_ns;</span></code></pre></div></div>
</section>
<section id="what-this-gives-up" class="level2">
<h2 class="anchored" data-anchor-id="what-this-gives-up">What this gives up</h2>
<p><code>ecto_foundationdb</code> is a layer over a key-value store. Its own documentation states the limit directly. A query is valid only when one Get or one GetRange satisfies it.</p>
<ul>
<li>No joins.</li>
<li>No <code>or</code>. “A query with an <code>or</code> condition is not possible.”</li>
<li>No aggregates in the database. Filtering, aggregation and grouping “must be done by your Elixir code”.</li>
<li>One Between clause per query, on an indexed field, and index field order decides which ranges work.</li>
<li>Migrations cannot rename tables or fields, delete fields, drop indexes, or roll back. The standard <code>mix ecto.migrate</code> tasks do not apply.</li>
<li>Tenants are mandatory. Omitting one raises at run time.</li>
<li>Transactions run under FoundationDB’s 5 second limit, may re-execute on conflict, and must have no side effects. So no PubSub publish inside one.</li>
</ul>
</section>
<section id="the-gate" class="level2">
<h2 class="anchored" data-anchor-id="the-gate">The gate</h2>
<p>This decision depends on Uro’s queries fitting one Get or one GetRange. That is not audited, and it is the one thing that can overturn this RFD.</p>
<p>Audit <code>zone-backend</code>’s queries first. A join or an <code>or</code> in a hot path means restructuring the schema, not configuring an adapter.</p>
<p>The migration constraints matter as much. Uro has existing Ecto migrations, and this adapter cannot roll back or drop an index.</p>
</section>
<section id="why-not-the-others" class="level2">
<h2 class="anchored" data-anchor-id="why-not-the-others">Why not the others</h2>
<p><strong>FRL.</strong> Its own ADR 0003 accepts costs this avoids entirely:</p>
<ul>
<li>No crash isolation, permanently</li>
<li>A JDK and a Rust toolchain required to <code>mix compile</code></li>
<li>Every call on Rustler’s <code>DirtyIo</code></li>
<li>No per-call timeout</li>
</ul>
<p><code>ecto_foundationdb</code> uses <code>erlfdb</code>, so there is no JVM.</p>
<p>A separate measurement of FRL at 45.259 ms per point read is withdrawn. It went through JDBC, and ADR 0003 removed that transport in v0.2. FRL is not ruled out on speed here.</p>
<p><strong>PostgreSQL.</strong> Faster per point read at 0.084 ms, and it costs a machine and a second datastore. It stays the fallback if the query audit fails.</p>
<p><strong>DuckDB.</strong> 0.919 ms, one writing process, and a rewrite off Ecto. It stays for analytics and telemetry writing ETNF Parquet.</p>
<p><strong>Neon.</strong> Its free plan forces scale-to-zero, suspending after 5 minutes idle. A login cannot cold start. Paid compute at 0.106 USD per CU-hour is about 77 USD for one CU held for a month.</p>
<p><strong>CockroachDB.</strong> <code>rfd/0006</code> chose it, Cloud Basic is on the blocklist, and self-hosting at 2 GB is 15.45 USD before any bandwidth.</p>
</section>
<section id="recovery" class="level2">
<h2 class="anchored" data-anchor-id="recovery">Recovery</h2>
<p><code>rfd/0100</code> already sends <code>fdbbackup</code> to Tigris for disaster recovery, and the volume carries live data. Uro’s data joins that path rather than adding one.</p>
</section>
<section id="open" class="level2">
<h2 class="anchored" data-anchor-id="open">Open</h2>
<p>Uro’s queries are not audited against the one-Get-or-GetRange rule.</p>
<p><code>ecto_foundationdb</code>’s tenant model is not mapped onto Uro’s schema.</p>
<p>Latency above is raw FoundationDB through the C client. The adapter adds Elixir and <code>erlfdb</code> on top, and that overhead is unmeasured.</p>
</section>
<section id="sources" class="level2">
<h2 class="anchored" data-anchor-id="sources">Sources</h2>
<ul>
<li><a href="https://github.com/foundationdb-beam/ecto_foundationdb">ecto_foundationdb</a>, v0.7.6, Apache-2.0</li>
<li><a href="https://ecto-foundationdb.hexdocs.pm/">Ecto.Adapters.FoundationDB docs</a>, for the query and migration limits</li>
<li>Measurements above, <code>pointget.c</code> and the PostgreSQL and DuckDB probes</li>
<li><code>rfd/0102</code> for the deployment budget, <code>rfd/0006</code> for CockroachDB</li>
</ul>


</section>

 ]]></description>
  <guid>https://v-sekai-multiplayer-fabric.github.io/multiplayer-fabric-manuals/rfd/0103-uro-on-ecto-foundationdb/</guid>
  <pubDate>Tue, 18 Aug 2026 01:10:59 GMT</pubDate>
</item>
</channel>
</rss>
