<?xml version="1.0" encoding="UTF-8"?>
<rss  xmlns:atom="http://www.w3.org/2005/Atom" 
      xmlns:media="http://search.yahoo.com/mrss/" 
      xmlns:content="http://purl.org/rss/1.0/modules/content/" 
      xmlns:dc="http://purl.org/dc/elements/1.1/" 
      version="2.0">
<channel>
<title>V-Sekai Fire — Manuals</title>
<link>https://v-sekai-multiplayer-fabric.github.io/manuals/pages/rfd.html</link>
<atom:link href="https://v-sekai-multiplayer-fabric.github.io/manuals/pages/rfd.xml" rel="self" type="application/rss+xml"/>
<description></description>
<generator>quarto-1.10.18</generator>
<lastBuildDate>Fri, 07 Aug 2026 19:00:59 GMT</lastBuildDate>
<item>
  <title>RFD 0099: Server authority on Fly, and what the client is allowed to do</title>
  <link>https://v-sekai-multiplayer-fabric.github.io/manuals/rfd/0099-server-authority-on-fly/</link>
  <description><![CDATA[ 





<p><em><a href="../../index.html">Manuals</a> › RFD 0099: Server authority on Fly, and what the client is allowed to do</em></p>
<p><strong>State:</strong> discussion · <strong>Scope:</strong> zone-server-h2o, zone-guest-godot</p>
<section id="problem" class="level2">
<h2 class="anchored" data-anchor-id="problem">Problem</h2>
<p><code>rfd/0095</code> through <code>rfd/0098</code> decided how guests run, how they store data, how they talk, and what rollback costs. None of them says who decides what is true.</p>
<p>The server must be authoritative. A client that reports its own position can report any position. <code>rfd/0092</code>’s ReBAC planes do not help here, because the client holds a legitimate relation to its own avatar.</p>
<p>This RFD states the authority boundary. It also states the budget the boundary must fit, because a 256 MB machine with one shared vCPU cannot hold an unbounded design.</p>
</section>
<section id="what-the-code-already-provides" class="level2">
<h2 class="anchored" data-anchor-id="what-the-code-already-provides">What the code already provides</h2>
<p>Three facts came out of reading the tree, and each one changes the design.</p>
<section id="the-physics-is-already-integer-so-determinism-is-already-solved" class="level3">
<h3 class="anchored" data-anchor-id="the-physics-is-already-integer-so-determinism-is-already-solved">The physics is already integer, so determinism is already solved</h3>
<p><code>src/gen/xr_grid_entity_packet.h</code> is explicit: “no floats at all – position is int64 absolute micrometers, velocity is int16”.</p>
<p><code>src/zf_zonetick.h</code> gives the update rule:</p>
<pre><code>pos_um += (int64_t)vel_i16 * V_MAX_PHYSICAL_DEFAULT_UM_PER_TICK / INT16_MAX</code></pre>
<p>That is integer arithmetic, applied per tick. It produces identical results on every machine, with no dependence on compiler, operating system, or instruction set.</p>
<p>Glenn Fiedler rejects deterministic lockstep for a shooter because float determinism across platforms is nearly impossible. This codebase does not have that problem, and it does not need libriscv to avoid it. Fixed-point integers already avoid it.</p>
<p>libriscv earns its place for UNTRUSTED guest code, per <code>rfd/0095</code>. It is not what makes the core entity update deterministic.</p>
</section>
<section id="the-packet-already-carries-an-owner-and-a-clock" class="level3">
<h3 class="anchored" data-anchor-id="the-packet-already-carries-an-owner-and-a-clock">The packet already carries an owner and a clock</h3>
<p><code>xr_grid_entity_packet_t</code> holds <code>class_owner</code>, which is <code>(class &lt;&lt; 24) | owner</code>, and <code>hlc</code>, which is <code>(frame &lt;&lt; 8) | counter</code>.</p>
<p>So ownership and a hybrid logical clock exist on the wire already. The authority model does not need a new field. It needs a rule about what <code>owner</code> means.</p>
</section>
<section id="the-entity-record-is-100-bytes" class="level3">
<h3 class="anchored" data-anchor-id="the-entity-record-is-100-bytes">The entity record is 100 bytes</h3>
<p><code>XR_PACKET_SIZE</code> is 100. <code>rfd/0002</code> sizes a zone at 200 entities per tick.</p>
</section>
</section>
<section id="decisions" class="level2">
<h2 class="anchored" data-anchor-id="decisions">Decisions</h2>
<section id="ownership-is-not-authority" class="level3">
<h3 class="anchored" data-anchor-id="ownership-is-not-authority">1. Ownership is not authority</h3>
<p><code>class_owner</code> names who a client speaks for. It does not make the client correct.</p>
<p>A client sends INPUT. The server computes STATE. The server writes <code>zf/entity/{z_id}/{e_id}</code>, and a client never writes it.</p>
<p>This is the whole boundary, and every decision below follows from it.</p>
</section>
<section id="clients-predict-with-the-same-integer-rule" class="level3">
<h3 class="anchored" data-anchor-id="clients-predict-with-the-same-integer-rule">2. Clients predict with the same integer rule</h3>
<p>A client runs the update rule from <code>zf_zonetick.h</code> locally, on its own inputs, and draws the result immediately.</p>
<p>Because the rule is integer, the client and the server compute identical values from identical inputs. Divergence therefore means a missing input or a rejected input. It never means float drift.</p>
<p>That is a stronger property than a float engine can offer, and it comes free with the existing packet format.</p>
</section>
<section id="server-side-lag-compensation-is-a-ring-of-packets-not-a-machine-snapshot" class="level3">
<h3 class="anchored" data-anchor-id="server-side-lag-compensation-is-a-ring-of-packets-not-a-machine-snapshot">3. Server-side lag compensation is a ring of packets, not a machine snapshot</h3>
<p>To validate a hit, the server rewinds entity positions to what the shooter saw, then tests the shot.</p>
<p>That needs entity history, not a virtual machine snapshot. The size is small:</p>
<pre><code>200 entities x 100 bytes x 7 ticks = 140000 bytes</code></pre>
<p>140 KB covers about 100 ms of compensation at 64 Hz. A full second of history is 1.28 MB.</p>
<p><code>rfd/0098</code> measured a libriscv snapshot of an engine guest at 64.04 MB, and 7 of them at 448.26 MB, which does not fit the machine. Lag compensation does not pay that cost, because it does not snapshot a machine. It copies 100-byte records.</p>
<p>Keep these two mechanisms separate. Lag compensation uses the ring. Guest rollback uses forking, under <code>rfd/0098</code>’s 8 MB budget.</p>
</section>
<section id="interest-management-is-required-not-an-optimization" class="level3">
<h3 class="anchored" data-anchor-id="interest-management-is-required-not-an-optimization">4. Interest management is required, not an optimization</h3>
<p>The bandwidth arithmetic settles this.</p>
<pre><code>200 entities x 100 bytes            = 20000 bytes per tick
20000 bytes x 64 Hz                 = 1280000 bytes per second
                                    = 10.2 Mbit per second, per client</code></pre>
<p>Ten clients on one zone is 102 Mbit per second of egress. That is implausible for a client connection and expensive on Fly.</p>
<p>So a zone must not send every entity to every client. Interest management and delta compression are load-bearing requirements of the design. <code>rfd/0002</code> and the interest-management literature already cover the algorithms.</p>
<p>State this in the fan-out work (<code>zone-server-h2o</code> issue #41) rather than discovering it after the loop exists.</p>
</section>
<section id="the-per-tick-budget-on-fly" class="level3">
<h3 class="anchored" data-anchor-id="the-per-tick-budget-on-fly">5. The per-tick budget on Fly</h3>
<p>One tick is 15625 us. Measured and known costs:</p>
<table class="caption-top table">
<colgroup>
<col style="width: 36%">
<col style="width: 49%">
<col style="width: 14%">
</colgroup>
<thead>
<tr class="header">
<th>Item</th>
<th>Cost</th>
<th>Source</th>
</tr>
</thead>
<tbody>
<tr class="odd">
<td>FDB commit for the tick</td>
<td>3552.9 us</td>
<td><code>rfd/0097</code></td>
</tr>
<tr class="even">
<td>Entity history ring write</td>
<td>20000 byte copy</td>
<td>this RFD</td>
</tr>
<tr class="odd">
<td>Fan-out serialize</td>
<td>one encode per entity</td>
<td>issue #41</td>
</tr>
<tr class="even">
<td>Fan-out send</td>
<td>one queued datagram per subscriber</td>
<td>issue #41</td>
</tr>
</tbody>
</table>
<p>The database commit is 23 percent of the tick before any game logic runs. That is the dominant fixed cost, and it grows on a real cluster, because <code>rfd/0097</code> measured a single node with memory storage.</p>
</section>
<section id="authority-must-not-stall-behind-the-database" class="level3">
<h3 class="anchored" data-anchor-id="authority-must-not-stall-behind-the-database">6. Authority must not stall behind the database</h3>
<p><code>zonetick_fdb_this_zone()</code> skips a tick while the previous FDB commit stays open. Fan-out must not sit inside that guard.</p>
<p>If it does, a slow database freezes every client’s view of the world. Measured p99 commit is 5112.5 us against a 15625 us tick, so there is headroom today, and a real cluster removes some of it.</p>
<p>Send authoritative state every tick. Let the database write fall behind when it must. The server stays authoritative either way, because authority lives in the server’s own state, and the database is where that state is made durable.</p>
</section>
<section id="rebac-gates-what-a-client-may-propose" class="level3">
<h3 class="anchored" data-anchor-id="rebac-gates-what-a-client-may-propose">7. ReBAC gates what a client may propose</h3>
<p><code>rfd/0092</code>’s use plane decides whether a client may act on an entity at all. <code>class_owner</code> decides which entity a client speaks for.</p>
<p>Neither decides whether the result is correct. Decision 1 does.</p>
</section>
</section>
<section id="stale-numbers-found-while-writing-this" class="level2">
<h2 class="anchored" data-anchor-id="stale-numbers-found-while-writing-this">Stale numbers found while writing this</h2>
<p>The tree names three tick rates. <code>webtransport_server.h:20</code> defines <code>ZONE_TICK_HZ</code> as 64, which is what runs. <code>zf_zonetick.h:19</code> refers to a “fixed 30Hz assumption”, and <code>webtransport_server.h:12</code> mentions <code>PBVH_SIM_TICK_HZ = 20</code> from the original Godot implementation.</p>
<p>64 is correct. The other two are stale comments. Correct them, because every budget in <code>rfd/0096</code> through <code>rfd/0099</code> divides by 15625 us.</p>
</section>
<section id="consequences" class="level2">
<h2 class="anchored" data-anchor-id="consequences">Consequences</h2>
<p>The client is a predictor and an input source. It is never a source of truth.</p>
<p>Two rewind mechanisms exist, and they must not be confused. Lag compensation rewinds 100-byte entity records in a 140 KB ring. Guest rollback forks a libriscv machine under an 8 MB working-set budget.</p>
<p>Interest management moves from future work to a requirement, because 10.2 Mbit per second per client is not shippable.</p>
<p>Determinism of the core simulation does not depend on libriscv. It depends on the integer packet format, which already exists and already has golden vectors.</p>
</section>
<section id="sources" class="level2">
<h2 class="anchored" data-anchor-id="sources">Sources</h2>
<ul>
<li><code>src/gen/xr_grid_entity_packet.h</code>, the integer packet format</li>
<li><code>src/zf_zonetick.h</code>, the integer per-tick update rule</li>
<li><a href="https://gafferongames.com/post/deterministic_lockstep/">Deterministic Lockstep, Gaffer On Games</a></li>
<li><a href="https://gafferongames.com/post/floating_point_determinism/">Floating Point Determinism, Gaffer On Games</a></li>
<li><a href="https://doi.org/10.1145/3519023">A Survey and Taxonomy of Latency Compensation Techniques for Network Computer Games</a></li>
<li><a href="https://doi.org/10.1145/2535417">Interest Management for Distributed Virtual Environments: A Survey</a></li>
</ul>


</section>

 ]]></description>
  <guid>https://v-sekai-multiplayer-fabric.github.io/manuals/rfd/0099-server-authority-on-fly/</guid>
  <pubDate>Fri, 07 Aug 2026 19:00:59 GMT</pubDate>
</item>
<item>
  <title>RFD 0098: The rollback snapshot budget, measured</title>
  <link>https://v-sekai-multiplayer-fabric.github.io/manuals/rfd/0098-rollback-snapshot-budget/</link>
  <description><![CDATA[ 





<p><em><a href="../../index.html">Manuals</a> › RFD 0098: The rollback snapshot budget, measured</em></p>
<p><strong>State:</strong> discussion · <strong>Scope:</strong> zone-server-h2o, zone-guest-godot</p>
<section id="problem" class="level2">
<h2 class="anchored" data-anchor-id="problem">Problem</h2>
<p><code>rfd/0095</code> puts hybrid rollback-kinematic networking on libriscv. The stated reason is that <code>serialize_to</code> (<code>machine.hpp:431</code>) and forking (<code>machine.hpp:61</code>) give bit-exact snapshot and replay, and that a Linux process cannot.</p>
<p>That reason is correct and it was never measured. This RFD measures it.</p>
<p>The measurement matters because rollback has a hard budget. One tick at 64 Hz is 15625 us. Lag compensation of 100 ms rewinds about 7 ticks. So the host takes one snapshot every tick and holds about 7 of them.</p>
<p>If a snapshot costs more than a tick, rollback is impossible. If 7 snapshots do not fit in memory, rollback is impossible. Neither number was known.</p>
</section>
<section id="prior-art" class="level2">
<h2 class="anchored" data-anchor-id="prior-art">Prior art</h2>
<p>This approach is not new, and the reason to use a virtual machine is well understood.</p>
<p>Glenn Fiedler rejects deterministic lockstep for a first-person shooter on two grounds. Float determinism across compilers, operating systems and instruction sets is nearly impossible. Also, every player waits for the most lagged player.</p>
<p>A virtual machine answers the first objection, because the guest executes the same instructions everywhere. Rollback answers the second, because rollback predicts and re-simulates instead of waiting.</p>
<p>WebAssembly is the common choice for this. <code>bevy_ggrs</code> runs rollback under WASM, and Easel builds a language that guarantees determinism. The requirement is the same in every case: snapshot the state, restore it, and re-simulate.</p>
<p>No prior art was found for RISC-V as the determinism boundary. The technique is established. The choice of libriscv rather than WASM is what <code>rfd/0095</code> decides, and what this RFD tests.</p>
</section>
<section id="method" class="level2">
<h2 class="anchored" data-anchor-id="method">Method</h2>
<p>The probe loads a guest ELF, runs 2 million instructions to warm it, then measures <code>serialize_to</code>, <code>deserialize_from</code>, and fork construction 200 times each. It reports the median and the 99th percentile.</p>
<p>It also replays the same snapshot twice and hashes all 32 integer registers plus the program counter. Rollback is worthless if two replays differ.</p>
<p>Two guests, chosen as the two ends of the range:</p>
<ul>
<li><code>kv_smoke.elf</code>, a 4016 byte script guest.</li>
<li><code>godot_instance_harness</code>, an 84982776 byte engine guest.</li>
</ul>
<p>Both ran with a 64 MB arena.</p>
</section>
<section id="finding-0-serialize_to-needs-the-fast-path-turned-off" class="level2">
<h2 class="anchored" data-anchor-id="finding-0-serialize_to-needs-the-fast-path-turned-off">Finding 0: serialize_to needs the fast path turned off</h2>
<p><code>serialize_to</code> refuses to run against libriscv’s default memory configuration. <code>serialize.cpp:99</code> throws <code>FEATURE_DISABLED</code>, with the message <code>Serialize is incompatible with flat read-write arena</code>.</p>
<p>So <code>MachineOptions::use_memory_arena</code> must be false to snapshot at all. The flat arena is the fast memory path. Snapshotting costs it.</p>
<p>Every number below therefore describes a machine that already gave up the arena.</p>
</section>
<section id="data" class="level2">
<h2 class="anchored" data-anchor-id="data">Data</h2>
<table class="caption-top table">
<colgroup>
<col style="width: 35%">
<col style="width: 28%">
<col style="width: 35%">
</colgroup>
<thead>
<tr class="header">
<th>Measure</th>
<th>Script guest, 4016 B</th>
<th>Engine guest, 84982776 B</th>
</tr>
</thead>
<tbody>
<tr class="odd">
<td>Snapshot size</td>
<td>41864 B (0.04 MB)</td>
<td>67148424 B (<strong>64.04 MB</strong>)</td>
</tr>
<tr class="even">
<td>7 live snapshots</td>
<td>0.28 MB</td>
<td><strong>448.26 MB</strong></td>
</tr>
<tr class="odd">
<td><code>serialize_to</code> median</td>
<td>0.8 us</td>
<td><strong>12370.2 us</strong></td>
</tr>
<tr class="even">
<td><code>serialize_to</code> p99</td>
<td>1.2 us</td>
<td><strong>17063.0 us</strong></td>
</tr>
<tr class="odd">
<td><code>deserialize_from</code> median</td>
<td>1.9 us</td>
<td>15227.0 us</td>
</tr>
<tr class="even">
<td>Fork construct median</td>
<td>0.3 us</td>
<td><strong>384.2 us</strong></td>
</tr>
<tr class="odd">
<td>Determinism, 2 replays</td>
<td>IDENTICAL</td>
<td>IDENTICAL</td>
</tr>
</tbody>
</table>
</section>
<section id="decisions" class="level2">
<h2 class="anchored" data-anchor-id="decisions">Decisions</h2>
<section id="determinism-holds-and-that-was-the-core-claim" class="level3">
<h3 class="anchored" data-anchor-id="determinism-holds-and-that-was-the-core-claim">1. Determinism holds, and that was the core claim</h3>
<p>Two replays of the same snapshot produced identical register state and program counter on both guests. <code>rfd/0095</code>’s central argument survives measurement.</p>
</section>
<section id="serialize_to-cannot-snapshot-an-engine-guest" class="level3">
<h3 class="anchored" data-anchor-id="serialize_to-cannot-snapshot-an-engine-guest">2. <code>serialize_to</code> cannot snapshot an engine guest</h3>
<p>The engine guest takes 12370 us to serialize. One tick is 15625 us. So a single snapshot consumes 79 percent of a tick at the median.</p>
<p>At the 99th percentile it takes 17063 us, which is longer than the whole tick. The snapshot alone misses the deadline.</p>
<p>Memory disqualifies it a second time. Seven live snapshots need 448.26 MB. A Fly machine in <code>fly/fly.toml</code> has 256 MB, and <code>rfd/0096</code> measured 212188 kB of that as usable. The rewind window does not fit.</p>
</section>
<section id="snapshot-cost-tracks-the-working-set-so-cap-the-working-set" class="level3">
<h3 class="anchored" data-anchor-id="snapshot-cost-tracks-the-working-set-so-cap-the-working-set">3. Snapshot cost tracks the working set, so cap the working set</h3>
<p>The script guest snapshots to 41864 bytes from the same 64 MB arena that produced 64.04 MB for the engine guest. Size follows what the guest touches, not what it may address.</p>
<p>Both figures divide out near memory bandwidth, at roughly 5 to 50 GB per second. Snapshot cost is a memory copy, so it is predictable:</p>
<pre><code>snapshot_us ~= working_set_bytes / 5 GB per second</code></pre>
<p>Turned into a budget: keep snapshot cost under 10 percent of a tick, which is 1562 us. That allows a working set of approximately 8 MB.</p>
<p><strong>8 MB is the rollback guest budget.</strong> A program on the rollback path must fit it. That is a design constraint on the guest, and it is not a tuning parameter of the host.</p>
</section>
<section id="prefer-forking-and-treat-serialize_to-as-the-fallback" class="level3">
<h3 class="anchored" data-anchor-id="prefer-forking-and-treat-serialize_to-as-the-fallback">4. Prefer forking, and treat <code>serialize_to</code> as the fallback</h3>
<p>Fork construction costs 384.2 us for the engine guest, against 12370.2 us to serialize the same machine. That is roughly 32 times faster, because copy-on-write does not copy untouched pages.</p>
<p>For the script guest it is 0.3 us against 0.8 us.</p>
<p>Fork is therefore the mechanism for the rollback window. <code>serialize_to</code> stays useful for a durable snapshot that must outlive the process, and for moving a machine between hosts.</p>
</section>
<section id="engine-guests-do-not-roll-back-and-that-is-already-the-design" class="level3">
<h3 class="anchored" data-anchor-id="engine-guests-do-not-roll-back-and-that-is-already-the-design">5. Engine guests do not roll back, and that is already the design</h3>
<p><code>rfd/0095</code> puts engine guests under Bubblewrap as separate processes. They were never on the rollback path.</p>
<p>This measurement confirms that boundary rather than moving it. An engine-sized guest cannot meet the rollback budget under libriscv either. The split was right, and the reason is now numeric.</p>
</section>
</section>
<section id="open-question-a-rolling-window-of-forks" class="level2">
<h2 class="anchored" data-anchor-id="open-question-a-rolling-window-of-forks">Open question: a rolling window of forks</h2>
<p><code>machine.hpp:61</code> states that the main machine must outlive its forks, and that it must not be modified while a fork runs.</p>
<p>A rollback window needs about 7 snapshots at different ticks. Whether that is expressible as a chain of forks, or needs one frozen parent per tick, is not established here. It was not measured.</p>
<p>Resolve this before building the rollback window, because decision 4 depends on it.</p>
</section>
<section id="consequences" class="level2">
<h2 class="anchored" data-anchor-id="consequences">Consequences</h2>
<p>The rollback path carries an explicit size limit of approximately 8 MB of working set. State that limit in the guest ABI documentation, so a guest author learns it before writing a program that cannot roll back.</p>
<p><code>use_memory_arena</code> must be false on any machine that will snapshot. Measure the cost of losing the arena before enabling snapshots on a hot path.</p>
<p><code>rfd/0095</code> claimed <code>serialize_to</code> and forking as equivalent evidence for the same argument. They are not equivalent. Forking is roughly 32 times faster on an engine-sized machine, and <code>serialize_to</code> cannot meet the tick deadline there at all.</p>
</section>
<section id="sources" class="level2">
<h2 class="anchored" data-anchor-id="sources">Sources</h2>
<p>Probe source is <code>rollback.cpp</code> beside this RFD.</p>
<ul>
<li><a href="https://gafferongames.com/post/floating_point_determinism/">Floating Point Determinism, Gaffer On Games</a></li>
<li><a href="https://gafferongames.com/post/deterministic_lockstep/">Deterministic Lockstep, Gaffer On Games</a></li>
<li><a href="https://github.com/mas-bandwidth/fps">mas-bandwidth/fps</a></li>
<li><a href="https://johanhelsing.studio/posts/extreme-bevy">Extreme Bevy, rollback in Rust and WASM</a></li>
<li><a href="https://easel.games/docs/learn/multiplayer/rollback-netcode">Easel, deterministic rollback netcode</a></li>
</ul>


</section>

 ]]></description>
  <guid>https://v-sekai-multiplayer-fabric.github.io/manuals/rfd/0098-rollback-snapshot-budget/</guid>
  <pubDate>Fri, 07 Aug 2026 19:00:59 GMT</pubDate>
</item>
<item>
  <title>RFD 0097: Pubsub belongs in userspace, and FoundationDB keeps the durable log</title>
  <link>https://v-sekai-multiplayer-fabric.github.io/manuals/rfd/0097-pubsub-belongs-in-userspace/</link>
  <description><![CDATA[ 





<p><em><a href="../../index.html">Manuals</a> › RFD 0097: Pubsub belongs in userspace, and FoundationDB keeps the durable log</em></p>
<p><strong>State:</strong> discussion · <strong>Scope:</strong> zone-server-h2o, zone-guest-godot</p>
<section id="problem" class="level2">
<h2 class="anchored" data-anchor-id="problem">Problem</h2>
<p>A zone fabric needs to publish. A zone server produces state at 64 Hz. Clients subscribe. Zones tell other zones about events.</p>
<p>Three mechanisms were candidates. FoundationDB is already a dependency, it is linearizable, and it has a watch API that looks like exactly this. Userspace fan-out is already possible, because the zone server already holds every subscriber connection. eBPF was raised as a third option.</p>
<p><code>rfd/0096</code> established that ranking these from documentation produces wrong answers. So this RFD measures first.</p>
</section>
<section id="method" class="level2">
<h2 class="anchored" data-anchor-id="method">Method</h2>
<p>A probe ran against FoundationDB in the CI image, single node, memory storage engine.</p>
<p>That configuration is the FASTEST case. A real cluster replicates, crosses machines, and adds latency. Every FoundationDB number below is therefore a lower bound for production.</p>
<p>The comparison figure is <code>rfd/0096</code>’s measured <code>AF_UNIX</code> <code>SOCK_SEQPACKET</code> round trip on Fly: 8910 ns. One ZoneTick at 64 Hz is 15.6 ms.</p>
</section>
<section id="data" class="level2">
<h2 class="anchored" data-anchor-id="data">Data</h2>
<table class="caption-top table">
<colgroup>
<col style="width: 48%">
<col style="width: 12%">
<col style="width: 12%">
<col style="width: 25%">
</colgroup>
<thead>
<tr class="header">
<th>Operation</th>
<th>Median</th>
<th>p99</th>
<th>Fraction of one tick</th>
</tr>
</thead>
<tbody>
<tr class="odd">
<td>FDB commit, one publish</td>
<td>3552.9 us</td>
<td>5112.5 us</td>
<td>23 percent</td>
</tr>
<tr class="even">
<td>FDB watch fire, publish to subscriber</td>
<td>12214.3 us</td>
<td>12579.5 us</td>
<td><strong>78 percent</strong></td>
</tr>
<tr class="odd">
<td>FDB versionstamp log append</td>
<td>2509.5 us</td>
<td>3691.2 us</td>
<td>16 percent</td>
</tr>
<tr class="even">
<td><code>AF_UNIX</code> round trip, from <code>rfd/0096</code></td>
<td>8.9 us</td>
<td>–</td>
<td>0.06 percent</td>
</tr>
</tbody>
</table>
<p>Concurrent watches, measured to the failure point:</p>
<pre><code>FDBPUB concurrent watches held = 10000,
       stopped at error 1032 (Too many watches currently set)</code></pre>
</section>
<section id="decisions" class="level2">
<h2 class="anchored" data-anchor-id="decisions">Decisions</h2>
<section id="real-time-pubsub-runs-in-userspace" class="level3">
<h3 class="anchored" data-anchor-id="real-time-pubsub-runs-in-userspace">1. Real-time pubsub runs in userspace</h3>
<p>A watch fires 12214 us after the publish. That is 78 percent of a 15.6 ms tick, on the fastest possible FoundationDB configuration.</p>
<p>An <code>AF_UNIX</code> round trip is 8.9 us. The watch path is approximately 1371 times slower.</p>
<p>A tick cannot spend 78 percent of itself on notification. The zone server already holds every subscriber connection through picoquic and WebTransport. Fan-out belongs there.</p>
</section>
<section id="foundationdb-is-not-a-message-bus-and-the-watch-cap-proves-it" class="level3">
<h3 class="anchored" data-anchor-id="foundationdb-is-not-a-message-bus-and-the-watch-cap-proves-it">2. FoundationDB is not a message bus, and the watch cap proves it</h3>
<p>The probe held exactly 10000 concurrent watches and then failed with error 1032, <code>Too many watches currently set</code>.</p>
<p>That cap belongs to the database, not to a zone. A fabric of many zones shares it. Ten thousand subscribers is a plausible target for a multiplayer fabric, so this is a real ceiling and not a theoretical one.</p>
<p>Watches also carry semantics that do not match a message bus. A watch is edge-triggered and reports only that a key changed. It does not report what the value became. A subscriber must read the key again, and values that changed between the fire and the re-read are lost.</p>
<p>That behavior is correct for “configuration changed, load it again”. It is wrong for an event stream.</p>
</section>
<section id="foundationdb-keeps-the-durable-event-log" class="level3">
<h3 class="anchored" data-anchor-id="foundationdb-keeps-the-durable-event-log">3. FoundationDB keeps the durable event log</h3>
<p>A versionstamp append commits in 2509 us. Versionstamps give a total order, assigned by the cluster, without coordination between writers.</p>
<p>This is the right tool for events that must survive a process, need an order across zones, and arrive at a low rate. Zone lifecycle, guest load and unload, administrative actions, and ReBAC changes all fit.</p>
<p>The rate limit is explicit. At 2509 us per commit, a publisher gets approximately 400 appends per second in the best case. Do not put a 64 Hz state stream here.</p>
</section>
<section id="watches-are-for-rare-signals-only" class="level3">
<h3 class="anchored" data-anchor-id="watches-are-for-rare-signals-only">4. Watches are for rare signals only</h3>
<p>A watch is acceptable when the event is rare and the subscriber count is small. Configuration reload is the example.</p>
<p>Never place a watch on a per-tick key. Never allocate a watch per subscriber, because decision 2 gives the ceiling.</p>
</section>
<section id="ebpf-does-not-fan-out" class="level3">
<h3 class="anchored" data-anchor-id="ebpf-does-not-fan-out">5. eBPF does not fan out</h3>
<p><code>SOCKMAP</code> and <code>sk_msg</code> redirect a message from one socket to another. Redirection moves a message. It does not copy one message to many.</p>
<p>Pubsub fan-out is one message to N subscribers. That is not the operation these programs perform, so no custom eBPF program replaces the userspace loop.</p>
<p>This is the same category error that <code>rfd/0096</code> corrected for uBPF. eBPF is a filter and redirect facility. It is not a transport, and it is not a message bus.</p>
<p>One eBPF case stays open and unmeasured. <code>SOCKMAP</code> splicing suits a RELAY, which is what the zone server becomes between a Godot guest and a WebTransport guest. That is one to one, so decision 5 does not exclude it. It needs its own measurement.</p>
</section>
</section>
<section id="the-pattern" class="level2">
<h2 class="anchored" data-anchor-id="the-pattern">The pattern</h2>
<p>This is the same split that <code>rfd/0095</code> made for storage, applied to messaging.</p>
<p><code>rfd/0095</code> put small hot state in FoundationDB and content in an object store, because each store does one job well. The same reasoning applies here. FoundationDB gives durability, ordering, and linearizability. It does not give low latency or fan-out. Userspace gives low latency and fan-out. It does not give durability.</p>
<p>Route by what the message needs:</p>
<table class="caption-top table">
<colgroup>
<col style="width: 27%">
<col style="width: 28%">
<col style="width: 44%">
</colgroup>
<thead>
<tr class="header">
<th>Message</th>
<th>Path</th>
<th>Why</th>
</tr>
</thead>
<tbody>
<tr class="odd">
<td>Per-tick state to clients</td>
<td>Userspace, WebTransport</td>
<td>64 Hz, needs 8.9 us and not 12214 us</td>
</tr>
<tr class="even">
<td>Guest to zone</td>
<td><code>AF_UNIX</code> <code>SOCK_SEQPACKET</code></td>
<td><code>rfd/0096</code></td>
</tr>
<tr class="odd">
<td>Durable cross-zone event</td>
<td>FDB versionstamp log</td>
<td>Ordering and survival matter, rate is low</td>
</tr>
<tr class="even">
<td>Configuration changed</td>
<td>FDB watch</td>
<td>Rare, few subscribers</td>
</tr>
</tbody>
</table>
</section>
<section id="consequences" class="level2">
<h2 class="anchored" data-anchor-id="consequences">Consequences</h2>
<p>The zone server owns fan-out. That work is a loop over subscriber connections, and it needs no new dependency.</p>
<p>The 10000 watch ceiling is a fabric-wide budget. Any design that allocates a watch per subscriber, or per zone-and-subscriber pair, must state how it stays under that number.</p>
<p>Every FoundationDB figure here is a lower bound, because the probe ran single node with memory storage. Re-measure against the deployed cluster before treating any of them as a headroom calculation.</p>
<p><code>rfd/0096</code>’s lesson repeats. FoundationDB looked like a message bus because it has a watch API. Measurement showed the watch path costs 78 percent of a tick and stops at 10000 subscribers.</p>
</section>
<section id="sources" class="level2">
<h2 class="anchored" data-anchor-id="sources">Sources</h2>
<p>Probe source and raw output live beside this RFD in <code>fdbpub.c</code>.</p>
<ul>
<li><a href="https://apple.github.io/foundationdb/developer-guide.html#watches">FoundationDB developer guide, watches</a></li>
<li><a href="https://apple.github.io/foundationdb/known-limitations.html">FoundationDB known limitations</a></li>
<li><code>rfd/0096</code> for the 8910 ns <code>AF_UNIX</code> measurement on Fly</li>
</ul>


</section>

 ]]></description>
  <guid>https://v-sekai-multiplayer-fabric.github.io/manuals/rfd/0097-pubsub-belongs-in-userspace/</guid>
  <pubDate>Fri, 07 Aug 2026 19:00:59 GMT</pubDate>
</item>
<item>
  <title>RFD 0096: Guest transport on Fly, measured</title>
  <link>https://v-sekai-multiplayer-fabric.github.io/manuals/rfd/0096-guest-transport-measured-on-fly/</link>
  <description><![CDATA[ 





<p><em><a href="../../index.html">Manuals</a> › RFD 0096: Guest transport on Fly, measured</em></p>
<p><strong>State:</strong> discussion · <strong>Scope:</strong> zone-server-h2o, zone-guest-godot</p>
<section id="problem" class="level2">
<h2 class="anchored" data-anchor-id="problem">Problem</h2>
<p><code>rfd/0095</code> says an engine guest reaches its zone over one <code>AF_UNIX</code> socket. It does not say why not something faster.</p>
<p>That gap invited a reasonable question. Shared-memory rings are much faster than sockets in published benchmarks. io_uring can remove syscalls from an event loop. uBPF exists. Each looked like a candidate.</p>
<p>The candidates were ranked twice from documentation. Both rankings were wrong in places. Documentation describes Linux. This system runs on one specific machine shape, and that shape decides the answer.</p>
</section>
<section id="method" class="level2">
<h2 class="anchored" data-anchor-id="method">Method</h2>
<p>A probe went to Fly.io and measured the machine that runs production.</p>
<ul>
<li>App: a throwaway app, destroyed after the run.</li>
<li>Machine: <code>shared-cpu-1x</code>, 256 MB, region <code>iad</code>.</li>
<li>Image: <code>ubuntu:24.04</code>, which is also the CI image.</li>
<li>Kernel: <code>6.12.91-fly</code>.</li>
<li>Reported memory: 212188 kB. Reported <code>nproc</code>: 1.</li>
</ul>
<p>Each latency figure is a round trip between two processes. Each test does 20000 iterations. The table reports the median.</p>
<p>A second run on a 16-core development machine gives contrast.</p>
</section>
<section id="data" class="level2">
<h2 class="anchored" data-anchor-id="data">Data</h2>
<table class="caption-top table">
<colgroup>
<col style="width: 46%">
<col style="width: 29%">
<col style="width: 23%">
</colgroup>
<thead>
<tr class="header">
<th>Probe</th>
<th>Fly <code>shared-cpu-1x</code></th>
<th>Local, 16 cores</th>
</tr>
</thead>
<tbody>
<tr class="odd">
<td><code>AF_UNIX</code> <code>SOCK_SEQPACKET</code></td>
<td>8910 ns</td>
<td>59821 ns</td>
</tr>
<tr class="even">
<td><code>AF_UNIX</code> <code>SOCK_STREAM</code></td>
<td>9520 ns</td>
<td>50181 ns</td>
</tr>
<tr class="odd">
<td>Busy-polled shared-memory ring</td>
<td>did not finish</td>
<td>110 ns</td>
</tr>
</tbody>
</table>
<p>Capability results on Fly:</p>
<table class="caption-top table">
<thead>
<tr class="header">
<th>Probe</th>
<th>Result</th>
</tr>
</thead>
<tbody>
<tr class="odd">
<td><code>bwrap --unshare-all</code> as root</td>
<td>OK</td>
</tr>
<tr class="even">
<td><code>bwrap --unshare-net</code></td>
<td>OK</td>
</tr>
<tr class="odd">
<td><code>bwrap</code> unprivileged, uid 1000</td>
<td>OK</td>
</tr>
<tr class="even">
<td>Interfaces inside <code>--unshare-net</code></td>
<td>1</td>
</tr>
<tr class="odd">
<td>Interfaces outside</td>
<td>4</td>
</tr>
<tr class="even">
<td><code>kernel.io_uring_disabled</code></td>
<td>0</td>
</tr>
<tr class="odd">
<td><code>io_uring_setup</code></td>
<td>OK, returned fd 3</td>
</tr>
<tr class="even">
<td><code>memfd_create</code></td>
<td>OK</td>
</tr>
<tr class="odd">
<td><code>fly-global-services</code></td>
<td>172.19.15.243, <code>AF_INET</code></td>
</tr>
</tbody>
</table>
</section>
<section id="decisions" class="level2">
<h2 class="anchored" data-anchor-id="decisions">Decisions</h2>
<section id="af_unix-sock_seqpacket-is-the-guest-transport" class="level3">
<h3 class="anchored" data-anchor-id="af_unix-sock_seqpacket-is-the-guest-transport">1. <code>AF_UNIX</code> <code>SOCK_SEQPACKET</code> is the guest transport</h3>
<p>The measured round trip is 8910 ns. One ZoneTick at 64 Hz is 15.6 ms. Transport cost is therefore 0.06 percent of a tick.</p>
<p>There is no performance problem to solve at this tick rate.</p>
</section>
<section id="no-busy-polled-shared-memory-ring" class="level3">
<h3 class="anchored" data-anchor-id="no-busy-polled-shared-memory-ring">2. No busy-polled shared-memory ring</h3>
<p>The ring is the fastest option on hardware with spare cores. It measured 110 ns on 16 cores, which is 81 times faster than the socket on the same machine.</p>
<p>On Fly it did not complete 20000 round trips in more than 8 minutes. The socket completed the same 20000 in less than one second.</p>
<p><code>ps</code> shows the cause. Both processes stayed in <code>R</code> state at 24 percent CPU each, on a machine where <code>nproc</code> is 1. They consumed the shared-cpu burst balance and then spun against the throttle floor.</p>
<p>The published ring benchmarks assume a spare core to spin on. <code>shared-cpu-1x</code> does not give one. A number measured on a workstation does not transfer to this deployment.</p>
<p>This decision is conditional on the machine shape. A dedicated-CPU machine would change it, and this RFD does not forbid revisiting the choice there.</p>
</section>
<section id="keep-epoll.-do-not-adopt-io_uring-for-the-event-loop" class="level3">
<h3 class="anchored" data-anchor-id="keep-epoll.-do-not-adopt-io_uring-for-the-event-loop">3. Keep epoll. Do not adopt io_uring for the event loop</h3>
<p>io_uring is reachable on Fly. <code>io_uring_setup</code> returned a descriptor, and <code>kernel.io_uring_disabled</code> is 0. Availability is not the obstacle.</p>
<p>Value is the obstacle. io_uring saves syscalls, and it pays back at high event rates. This system ticks at 64 Hz, and its transport already costs 0.06 percent of a tick.</p>
<p><code>IORING_SETUP_SQPOLL</code> is the mode that removes syscalls completely. It spawns a kernel poller thread. That is the same shape as the busy-poll ring, so decision 2 applies to it unchanged.</p>
</section>
<section id="guests-must-not-reach-io_uring" class="level3">
<h3 class="anchored" data-anchor-id="guests-must-not-reach-io_uring">4. Guests must not reach io_uring</h3>
<p>A seccomp filter examines the syscalls a task makes. io_uring operations run on <code>io-wq</code> kernel worker threads, outside the filter of the task that submitted them. A filter cannot screen a call the task never makes.</p>
<p>A guest that holds an io_uring descriptor can therefore perform work that the rest of the filter denies.</p>
<p>This is settled practice. Android 12 and later block <code>io_uring_setup</code> in the default application policy. Chrome OS does the same. The Chrome renderer and GPU sandboxes reject it. gVisor rejects it.</p>
<p><code>rfd/0095</code>’s seccomp filter must deny <code>io_uring_setup</code>, <code>io_uring_enter</code>, and <code>io_uring_register</code> by name.</p>
</section>
<section id="ubpf-is-not-a-transport" class="level3">
<h3 class="anchored" data-anchor-id="ubpf-is-not-a-transport">5. uBPF is not a transport</h3>
<p>uBPF is a userspace virtual machine that executes eBPF bytecode. It has no sockets, no I/O, and no packet path. It exists because the kernel implementation is GPL, and uBPF carries an Apache license.</p>
<p>uBPF is therefore an alternative to libriscv, not an alternative to an event loop. <code>rfd/0095</code> already selects libriscv, and it gives a reason that uBPF does not answer: <code>serialize_to</code> and forking give bit-exact snapshot and replay.</p>
<p>Kernel eBPF does accelerate networking, through <code>SOCKMAP</code> and AF_XDP. Neither applies here. A guest under <code>--unshare-net</code> has no sockets to redirect, and decision 4 and <code>rfd/0095</code> both deny the bpf family to guests.</p>
</section>
<section id="udp-must-bind-fly-global-services-and-it-is-ipv4" class="level3">
<h3 class="anchored" data-anchor-id="udp-must-bind-fly-global-services-and-it-is-ipv4">6. UDP must bind <code>fly-global-services</code>, and it is IPv4</h3>
<p>Fly requires a UDP service to bind <code>fly-global-services</code>. <code>0.0.0.0</code>, <code>*</code>, and <code>INADDR_ANY</code> do not work, because Linux then selects the wrong source address for replies.</p>
<p><code>src/transport/webtransport_server.c:233</code> binds <code>in6addr_any</code>. The socket binds, packets arrive, and replies leave with the wrong source address. <code>fly/fly.toml</code> already declares <code>protocol = "udp"</code> on <code>internal_port = 7443</code>, so this is live configuration.</p>
<p>The probe found a second fact that a documentation-only fix would miss. <code>fly-global-services</code> resolves to <code>172.19.15.243</code>, which is <code>AF_INET</code>. The current socket is <code>AF_INET6</code>. The fix needs an <code>AF_INET</code> socket or a v4-mapped address. It is not an address substitution.</p>
<p>TCP cannot use <code>fly-global-services</code> and must bind the wildcard. The application therefore needs asymmetric binds.</p>
</section>
<section id="bubblewrap-works-on-fly-including-unprivileged" class="level3">
<h3 class="anchored" data-anchor-id="bubblewrap-works-on-fly-including-unprivileged">7. Bubblewrap works on Fly, including unprivileged</h3>
<p>All three <code>bwrap</code> probes passed, including the unprivileged case at uid 1000. <code>--unshare-net</code> left 1 interface inside the sandbox against 4 outside.</p>
<p><code>rfd/0095</code> predicted this, because a Fly machine is a Firecracker VM and the process is root in its own kernel. The prediction is now a measurement.</p>
<p>Note that Docker is the harder case, not Fly. The same probe failed every <code>bwrap</code> test under Docker, where user namespaces are blocked by default. CI must therefore grant <code>--cap-add SYS_ADMIN</code> and <code>--security-opt seccomp=unconfined</code>, or use podman.</p>
</section>
</section>
<section id="consequences" class="level2">
<h2 class="anchored" data-anchor-id="consequences">Consequences</h2>
<p>The engine guest transport is a plain <code>AF_UNIX</code> <code>SOCK_SEQPACKET</code> socket. It needs no new dependency and no tuning.</p>
<p><code>memfd_create</code> works, so a bulk path over <code>SCM_RIGHTS</code> stays available if a future measurement justifies it. Size any such buffer against 212 MB of usable memory, not against 256 MB.</p>
<p>Two guest-facing denials are now mandatory rather than advisory: io_uring and bpf.</p>
<p>The UDP bind defect is real and unfixed. Nothing that this RFD decides matters while replies leave with the wrong source address.</p>
</section>
<section id="what-this-rfd-corrects" class="level2">
<h2 class="anchored" data-anchor-id="what-this-rfd-corrects">What this RFD corrects</h2>
<p>Three claims in earlier rankings were wrong, and the measurement corrected each one.</p>
<p>Shared-memory rings were ranked as the fastest option. They are the fastest option on a machine with spare cores, and they are unusable on this one.</p>
<p>io_uring was ranked as unverified. It is available, and it is undesirable for separate reasons.</p>
<p>Dropping h2o for <code>picoquic_packet_loop()</code> was ranked first at one point. The asymmetric bind requirement needs a TCP listener as well, and <code>picoquic_packet_loop()</code> serves UDP only.</p>
</section>
<section id="sources" class="level2">
<h2 class="anchored" data-anchor-id="sources">Sources</h2>
<ul>
<li><a href="https://fly.io/docs/networking/udp-and-tcp/">Running apps on UDP and TCP, Fly Docs</a></li>
<li><a href="https://community.fly.io/t/fly-machines-security-update-dirtyfrag-fragnesia-and-copyfail/28018">Fly Machines Security Update, kernel v6.12.91</a></li>
<li><a href="https://kernel-internals.org/io-uring/security/">io_uring security model</a></li>
<li><a href="https://blog.0x74696d.com/posts/iouring-and-seccomp/">io_uring and seccomp</a></li>
<li><a href="https://docs.kernel.org/admin-guide/sysctl/kernel.html">Documentation for /proc/sys/kernel</a></li>
<li><a href="https://github.com/iovisor/ubpf">uBPF</a></li>
<li><a href="https://victoranderssen.com/blog/linux-ipc-benchmark/">Linux IPC Shootout, shared memory against Unix domain sockets</a></li>
</ul>


</section>

 ]]></description>
  <guid>https://v-sekai-multiplayer-fabric.github.io/manuals/rfd/0096-guest-transport-measured-on-fly/</guid>
  <pubDate>Fri, 07 Aug 2026 19:00:59 GMT</pubDate>
</item>
<item>
  <title>RFD 0095: Two guest classes, a domain ABI, and Bubblewrap</title>
  <link>https://v-sekai-multiplayer-fabric.github.io/manuals/rfd/0095-two-guest-classes-domain-abi-and-bubblewrap/</link>
  <description><![CDATA[ 





<p><em><a href="../../index.html">Manuals</a> › RFD 0095: Two guest classes, a domain ABI, and Bubblewrap</em></p>
<p><strong>State:</strong> discussion · <strong>Scope:</strong> zone-server-h2o, zone-guest-godot, fabric-godot-core</p>
<section id="problem" class="level2">
<h2 class="anchored" data-anchor-id="problem">Problem</h2>
<p><code>rfd/0094</code> makes <code>zone-server-h2o</code> the host of the minimum UGC game loop. It says guests are CDN-delivered riscv64 ELFs. It does not say how a guest reaches the host, and it does not say that all guests are alike.</p>
<p>The implementation found that both gaps are real.</p>
<p><code>zone-server-h2o</code> PR #23 gave the host a POSIX filesystem on top of FoundationDB. The filesystem worked. A Godot engine ELF started under it, ran 57.9 million instructions, and read <code>project.godot</code> out of FoundationDB.</p>
<p>The cost was 17 syscall handlers. The host supplied <code>/dev/urandom</code>, <code>/dev/null</code>, <code>/dev/zero</code>, <code>getrandom</code>, <code>chdir</code>, <code>getcwd</code>, <code>mkdirat</code>, <code>unlinkat</code>, and process-spawn stubs. The next two failures in the log were the mbedTLS entropy pool and the Godot scene loader. The list after those has no end. It ends when <code>zone-server-h2o</code> contains a Linux kernel.</p>
<p>There are two causes. Neither one is libriscv.</p>
<p><em>The file abstraction forces the emulation.</em> FoundationDB is a key-value store. The guest layer made it look like POSIX. It added handles, offsets, <code>lseek</code>, a working directory, and device nodes.</p>
<p>This codebase invented every one of those concepts. This codebase then defends every one of them. The guests only ever wanted get and put.</p>
<p><em>One runtime served two unlike guest classes.</em> A UGC script needs approximately ten host operations. It runs on every tick. A complete game engine needs a userland.</p>
<p>libriscv calls a guest function 5.25 to 7.25 times in the time Wasmtime calls one. It is the best tool available for the first class. It is the wrong tool for the second.</p>
</section>
<section id="decision" class="level2">
<h2 class="anchored" data-anchor-id="decision">Decision</h2>
<p>There are two guest classes. Each class gets the runtime that suits it. The host gives guests a domain ABI. The host does not emulate Linux.</p>
<p>Stated as deployment, which is how it is built and paid for:</p>
<ul>
<li>Zone guests run under Bubblewrap on Fly.io.</li>
<li>libriscv <code>.elf</code> programs carry hybrid rollback-kinematic networking between Godot zone servers and Godot zone clients.</li>
</ul>
<section id="guest-classes" class="level3">
<h3 class="anchored" data-anchor-id="guest-classes">Guest classes</h3>
<p>A <em>script guest</em> is user-generated content. It is small. It runs on every tick. It runs in-process under libriscv. This is the class that carries rollback-kinematic prediction and reconciliation.</p>
<p>An <em>engine guest</em> is a complete application, such as a Godot server build. It is large. It runs for the life of a zone. It runs as a separate process under Bubblewrap.</p>
</section>
<section id="the-dividing-line-is-rollback-determinism" class="level3">
<h3 class="anchored" data-anchor-id="the-dividing-line-is-rollback-determinism">The dividing line is rollback determinism</h3>
<p>The two runtimes are not a compromise. Each one answers a different requirement.</p>
<p>libriscv re-executes bit-exactly. <code>machine.serialize_to</code> (<code>machine.hpp:431</code>) writes complete machine state. Machine forking (<code>machine.hpp:61</code>) copies it. Together they give true snapshot and replay of a running guest.</p>
<p>Hybrid rollback-kinematic networking needs exactly this. It runs between a Godot zone server and a Godot zone client. Both sides predict state from a kinematic model. Both sides then reconcile.</p>
<p>Reconciliation replays inputs from a snapshot. If the replay diverges from the recorded run, the reconciliation is wrong. It is wrong silently, and it is wrong on the client that did nothing unusual.</p>
<p>A Bubblewrap process cannot do this. It is a Linux process. It holds kernel state, timers, file descriptors, and scheduler position. Nothing snapshots it faithfully.</p>
<p>Therefore, hybrid rollback-kinematic networking between Godot zone servers and Godot zone clients runs as libriscv <code>.elf</code> programs, in the style of godot-sandbox. Bubblewrap covers everything off that path, where isolation is the requirement and replay is not.</p>
</section>
<section id="the-domain-abi" class="level3">
<h3 class="anchored" data-anchor-id="the-domain-abi">The domain ABI</h3>
<p><code>src/sandbox/zone_abi.h</code> is the complete set of host operations a script guest can reach. The host and every guest include the same header, so the two cannot disagree about a number.</p>
<p>The precedent is godot-sandbox. It is libriscv’s own production Godot integration. It numbers its ABI from 500. It emulates no Linux. Its guests call <code>ECALL_VCALL</code> and <code>ECALL_GET_NODE</code>, which are domain operations. This ABI numbers from 600, which leaves the 500 block free so one guest can speak both.</p>
<table class="caption-top table">
<thead>
<tr class="header">
<th>Call</th>
<th>Purpose</th>
</tr>
</thead>
<tbody>
<tr class="odd">
<td><code>ZONE_KV_GET</code></td>
<td>Read one key</td>
</tr>
<tr class="even">
<td><code>ZONE_KV_SET</code></td>
<td>Write one key</td>
</tr>
<tr class="odd">
<td><code>ZONE_KV_DEL</code></td>
<td>Delete one key</td>
</tr>
<tr class="even">
<td><code>ZONE_KV_LIST</code></td>
<td>List keys under a prefix</td>
</tr>
<tr class="odd">
<td><code>ZONE_PRINT</code></td>
<td>One zone-tagged log line</td>
</tr>
<tr class="even">
<td><code>ZONE_ENTROPY</code></td>
<td>Random bytes from the host</td>
</tr>
<tr class="odd">
<td><code>ZONE_OBJ_GET</code></td>
<td>Read object bytes</td>
</tr>
<tr class="even">
<td><code>ZONE_OBJ_PUT</code></td>
<td>Publish an object</td>
</tr>
</tbody>
</table>
<p>Below the ABI, the host installs 7 Linux syscalls. These are libriscv’s <code>setup_minimal_syscalls()</code>: <code>close</code>, <code>lseek</code>, <code>write</code>, <code>fstat</code>, <code>exit</code>, <code>brk</code>, and <code>ebreak</code>. They are a guest’s <code>printf</code> and heap. They are not a userland.</p>
</section>
<section id="two-stores-and-the-split-is-deliberate" class="level3">
<h3 class="anchored" data-anchor-id="two-stores-and-the-split-is-deliberate">Two stores, and the split is deliberate</h3>
<p><code>ZONE_KV_*</code> reaches FoundationDB. FoundationDB is used as what it is, which is a linearizable transactional datastore. It holds small, mutable, hot state. Values cap at 32768 bytes, which is well below FoundationDB’s 100000-byte limit. No chunking layer is needed. A value over the cap returns <code>-E2BIG</code>, because at that size the data is content and not state.</p>
<p><code>ZONE_OBJ_*</code> reaches the object store. The format is casync content-defined chunking, with <code>.caibx</code> indexes over a CDN and an S3-shaped backend. Objects are immutable, deduplicated, and cached.</p>
<p>Do not virtualize a filesystem over either store. The filesystem is what forced the 17 handlers.</p>
<p>Content in FoundationDB would pay transaction cost, replication, and quota for bytes that never change and are identical in every zone. Hot state in an object store would give up linearizability. Each store does the one job it is good at.</p>
<p>Two implementations of the casync format already exist. <code>aria-storage</code> is Elixir, and it is the publish side. <code>fabric-godot-core</code>’s <code>modules/multiplayer_fabric_asset</code> is C++, and it is the fetch and verify side. Its header states that its constants are canonical.</p>
<p>Those constants are:</p>
<ul>
<li>SHA-512/256 chunk identifiers</li>
<li>Chunks of 16 KB to 256 KB</li>
<li>AES-128-GCM with a 24-hour key lifetime</li>
<li>Uro for the access check</li>
</ul>
<p>The C++ module is the reference for this host. Do not write a third implementation.</p>
</section>
<section id="quota-pressure-is-latency-not-an-error" class="level3">
<h3 class="anchored" data-anchor-id="quota-pressure-is-latency-not-an-error">Quota pressure is latency, not an error</h3>
<p>A write past a zone’s storage quota blocks. It does not fail. Storage pressure is a slow disk. It is not a condition a guest must code around.</p>
<p>The consequence is stated plainly. A single guest with nothing else freeing space stays blocked until an administrative actor deletes keys or raises the quota. That is <code>rfd/0092</code>’s budget extension as a ReBAC relation. It is not an error path.</p>
</section>
<section id="host-supplied-entropy-is-a-replay-requirement" class="level3">
<h3 class="anchored" data-anchor-id="host-supplied-entropy-is-a-replay-requirement">Host-supplied entropy is a replay requirement</h3>
<p><code>ZONE_ENTROPY</code> is not only a way to avoid emulating <code>/dev/urandom</code>.</p>
<p>A zone server that replays a journal, in the shape <code>rfd/0083</code> gives, cannot let guests draw entropy from the host random number generator behind its back. The replay would diverge from the recorded run. Routing every guest random byte through one host call makes guest randomness reproducible, because the host owns the seed.</p>
</section>
<section id="a-guest-cannot-write-an-object-directly" class="level3">
<h3 class="anchored" data-anchor-id="a-guest-cannot-write-an-object-directly">A guest cannot write an object directly</h3>
<p><code>ZONE_OBJ_PUT</code> is an administrative-plane action. It is <code>rfd/0092</code>’s <code>CAN_GRANT</code> plane. A guest never holds that plane.</p>
<p>A guest can hold a <em>delegation</em>. A delegation is a ReBAC edge from the guest subject to a principal that does hold administrative capability. The edge permits the guest to publish on that principal’s authority.</p>
<p>The host answers <code>ZONE_OBJ_PUT</code> by checking for that edge. It does not check the guest. Without an edge the answer is <code>-EPERM</code>. With an edge the publish happens, attributed to the delegating principal, because authority stays with whoever holds it.</p>
<p>This is the mechanism that lets UGC scripting produce content. The alternative is a host that refuses every guest write, or a host that gives guests administrative capability. Both are wrong.</p>
</section>
<section id="zone-guests-under-bubblewrap-on-fly.io" class="level3">
<h3 class="anchored" data-anchor-id="zone-guests-under-bubblewrap-on-fly.io">Zone guests under Bubblewrap on Fly.io</h3>
<p>Zone guests run under Bubblewrap, and they run on Fly.io. Name the deployment, because the deployment is what makes the choice work.</p>
<p>A Fly machine is a Firecracker VM. The process is root in its own kernel. Unprivileged user namespaces are therefore available, and <code>bwrap</code> needs them. <code>rfd/0089</code> already selects Fly.</p>
<p>Bubblewrap is one unprivileged binary. It is built on Linux namespaces. Flatpak uses it. There is no daemon and no userspace kernel.</p>
<p><code>--unshare-net</code> gives the guest no network namespace. <code>rfd/0094</code>’s rule that guests never reach the networking loop stops being a property this codebase maintains by omission. It becomes a property the kernel enforces. The engine guest cannot reach the h2o loop or the FoundationDB port, because it has no interfaces.</p>
<table class="caption-top table">
<thead>
<tr class="header">
<th>Requirement</th>
<th>Flag</th>
</tr>
</thead>
<tbody>
<tr class="odd">
<td>No network</td>
<td><code>--unshare-net</code></td>
</tr>
<tr class="even">
<td>Read-only content pack</td>
<td><code>--ro-bind &lt;pack&gt; /app</code></td>
</tr>
<tr class="odd">
<td>Writable scratch only</td>
<td><code>--bind &lt;dir&gt; /tmp</code></td>
</tr>
<tr class="even">
<td>No host filesystem</td>
<td><code>--unshare-all</code></td>
</tr>
<tr class="odd">
<td>Dies with the zone</td>
<td><code>--die-with-parent</code></td>
</tr>
<tr class="even">
<td>Syscall limits</td>
<td><code>--seccomp &lt;fd&gt;</code></td>
</tr>
</tbody>
</table>
<p>An engine guest is a separate operating-system process. It cannot share a <code>libriscv::Machine</code> address space, so the in-process ABI does not apply to it. It reaches the zone over one <code>AF_UNIX</code> socket that is bind-mounted into the sandbox.</p>
<p>Stated plainly: an engine guest is a subprocess with one pipe to the zone. This is a simpler system than an in-process engine guest. It is the same shape <code>rfd/0083</code> chose when it moved the server off Godot.</p>
<p>Bubblewrap ships no seccomp filter by default. Namespaces alone do not restrict system calls. The filter is this project’s to write, and it is the piece that makes this a sandbox and not a chroot. Deny at minimum: <code>socket</code>, <code>execve</code> beyond the entry point, <code>ptrace</code>, <code>mount</code>, and the keyring and bpf families.</p>
</section>
</section>
<section id="evidence" class="level2">
<h2 class="anchored" data-anchor-id="evidence">Evidence</h2>
<p><code>zone-server-h2o</code> PR #24 implements this decision. The test guest <code>test/guest/kv_smoke.c</code> ran against a real FoundationDB. All 8 assertions passed.</p>
<pre><code>zone 0: guest booting (4016 byte ELF, 512 MB mem, 16000 Minstr budget)
zone 0 guest: ok: set/get round-trip
zone 0 guest: ok: missing key reports an error
zone 0 guest: ok: multi-chunk value round-trip
zone 0 guest: ok: short read reports the full length
zone 0 guest: ok: list found the written keys
zone 0 guest: ok: delete removes the key
zone 0 guest: ok: entropy
zone 0 guest: ok: openat/socket/execve denied, guest still running
zone 0 guest: kv_smoke: PASS
zone 0: guest exited, status 0, 221987 instructions</code></pre>
<p>Storage is real and not an in-process buffer that behaves like one. <code>fdbcli getrange</code> shows the 20000-byte value. It shows a usage counter of <code>0x4e20</code>, which is 20000, and which is correct after the delete. The counter and the data agree because one transaction writes both.</p>
<p>Isolation holds by omission. <code>openat</code> (56), <code>socket</code> (198), and <code>execve</code> (221) reach the <code>-ENOSYS</code> catch-all, and the guest survives. An unimplemented call is not a crash.</p>
<p>The test guest is 4016 bytes and is built with <code>-nostdlib</code>. An earlier build linked static glibc. <code>__libc_start_main</code> then reached <code>writev</code>, <code>exit_group</code>, and <code>set_tid_address</code> before <code>main</code>. Those three looked like guest requirements. They were startup code for a userland the test does not use. An implementation of those three repeats this RFD’s own mistake, one layer lower.</p>
</section>
<section id="what-rfd0094-keeps" class="level2">
<h2 class="anchored" data-anchor-id="what-rfd0094-keeps">What rfd/0094 keeps</h2>
<p>This RFD amends <code>rfd/0094</code>. It does not replace it.</p>
<p><code>rfd/0094</code> is correct that <code>zone-server-h2o</code> hosts the loop. It is correct that guests arrive over a CDN as riscv64 ELFs and are not in-tree code. It is correct that administrative capability loads guests and normal capability interacts with them. It is correct that guests never reach the networking loop.</p>
<p><code>rfd/0092</code>’s two planes are unchanged.</p>
</section>
<section id="consequences" class="level2">
<h2 class="anchored" data-anchor-id="consequences">Consequences</h2>
<p>The host implements 8 domain calls and installs 7 minimal Linux syscalls. It does not grow toward a kernel, because there is no next syscall to add.</p>
<p>This project operates two runtimes instead of one. That is the cost of the rollback-determinism requirement, and this RFD accepts it.</p>
<p><code>ZONE_OBJ_GET</code> and <code>ZONE_OBJ_PUT</code> return <code>-ENOSYS</code> until <code>modules/multiplayer_fabric_asset</code> is extracted from the Godot build. The alternative is a third casync implementation, which is worse.</p>
<p>Bubblewrap needs unprivileged user namespaces. On Fly this is available, because a Fly machine is a Firecracker VM and the process is root in its own kernel. Inside Docker, <code>clone(CLONE_NEWUSER)</code> is blocked by default, so local runs need <code>--cap-add SYS_ADMIN</code> and <code>--security-opt seccomp=unconfined</code>, or podman with a user namespace. Ubuntu 24.04 and later restrict namespace creation through AppArmor, and the CI image is <code>ubuntu:24.04</code>. Verify this early.</p>
</section>
<section id="rebac-is-the-one-authorization-model" class="level2">
<h2 class="anchored" data-anchor-id="rebac-is-the-one-authorization-model">ReBAC is the one authorization model</h2>
<p><code>modules/multiplayer_fabric_asset</code> performs an <code>acl_check</code> against Uro. That looked like a second authorization system next to <code>rfd/0092</code>’s ReBAC planes. It is not one.</p>
<p><code>acl_check</code> POSTs a tuple to Uro’s <code>/acl/check</code> endpoint. The tuple is <code>(object, relation, subject)</code>, and Uro resolves it against the relation graph. Its components are strings such as <code>"asset:123"</code>, <code>"viewer"</code>, and <code>"user:456"</code>. That is ReBAC.</p>
<p>There is one model and two evaluators. Each evaluator answers a different question. The split is this:</p>
<ul>
<li>Uro resolves the graph. It answers which relations a subject holds on an object. It is the source of truth for the relation graph.</li>
<li><code>zone-server-h2o</code> decides the action. <code>rebac_check</code> (<code>src/gen/rebac.h:62</code>) takes a resolved relation set and one of <code>REBAC_ACTION_OBSERVE</code>, <code>REBAC_ACTION_INTERACT</code>, or <code>REBAC_ACTION_MODIFY</code>. It does not walk a graph.</li>
</ul>
<p>ReBAC is the model everywhere. Where the two evaluators overlap, the host is authoritative for host actions. <code>rebac_check</code> decides whether the host performs an action. An <code>acl_check</code> boolean alone does not gate a host-side action.</p>
<p><code>lean-rebac-core</code> generates <code>src/gen/rebac.c</code>. Keep the host decision there, and keep it generated. Do not add a second decision procedure that Uro and the host must then agree about.</p>
<p>There is one consequence for the guest path. Guest calls must not block on an HTTP round trip to Uro. Relations resolve when the host loads a guest, and the host holds the resolved set for the life of that guest.</p>


</section>

 ]]></description>
  <guid>https://v-sekai-multiplayer-fabric.github.io/manuals/rfd/0095-two-guest-classes-domain-abi-and-bubblewrap/</guid>
  <pubDate>Fri, 07 Aug 2026 19:00:59 GMT</pubDate>
</item>
<item>
  <title>RFD 0094: zone-server-h2o hosts the minimum UGC game loop</title>
  <link>https://v-sekai-multiplayer-fabric.github.io/manuals/rfd/0094-minimum-ugc-game-loop-guest-composition/</link>
  <description><![CDATA[ 





<p><em><a href="../../index.html">Manuals</a> › RFD 0094: zone-server-h2o hosts the minimum UGC game loop</em></p>
<p><strong>State:</strong> discussion · <strong>Scope:</strong> zone-server-h2o, zone-guest-middleham, zone-guest-gyre, zone-guest-godot</p>
<section id="problem" class="level2">
<h2 class="anchored" data-anchor-id="problem">Problem</h2>
<p><code>zone-server-h2o</code>’s PR #22 removed the MUD subsystem (the phase-2 CDN-guest move). The first cut of that PR also deleted <code>thirdparty/libriscv</code> and <code>thirdparty/QCBOR</code>, because their only in-tree consumer was the MUD orchestrator. That deletion was wrong, and the review that caught it exposed an undocumented decision.</p>
<p><code>rfd/0092</code> defines <em>how</em> the host gates guest access (ReBAC, a use plane and an admin plane). No RFD defines <em>what</em> the host repo must implement to close a user-generated-content game loop. No RFD says which repo is that host, or how many guests compose in one zone.</p>
<p>Without that decision on record, the same mistake repeats. A cleanup of guest-adjacent code can delete host-side machinery that looks orphaned but is load-bearing for the loop.</p>
</section>
<section id="decision" class="level2">
<h2 class="anchored" data-anchor-id="decision">Decision</h2>
<p><code>zone-server-h2o</code> is the host of the minimum UGC game loop. The loop is: a principal with administrative capability loads guest programs, and principals with normal capability interact with what those guests run. Guests are CDN-delivered riscv64 ELFs, not in-tree code.</p>
<section id="what-the-host-keeps-vendored" class="level3">
<h3 class="anchored" data-anchor-id="what-the-host-keeps-vendored">What the host keeps vendored</h3>
<ul>
<li><code>thirdparty/libriscv</code>: the guest execution engine. It is not MUD-specific.</li>
<li><code>thirdparty/QCBOR</code>: the serialization layer for the host-to-guest ABI. The host must speak the same CBOR encoding the guests do.</li>
</ul>
<p>Both stay in <code>zone-server-h2o</code> even while no in-tree consumer exists. The QCBOR build wiring stays live in <code>CMakeLists.txt</code>, so the vendored copy compiles in CI and does not rot.</p>
</section>
<section id="two-capability-tiers" class="level3">
<h3 class="anchored" data-anchor-id="two-capability-tiers">Two capability tiers</h3>
<p>The tiers are <code>rfd/0092</code>’s two planes, bound to the ReBAC actions that <code>src/gen/rebac.{c,h}</code> already generates from <code>lean-rebac-core</code>:</p>
<ul>
<li><strong>Administrative capabilities</strong> (admin plane): fetch a guest ELF, load it, unload it, and grant it capabilities. These map to <code>REBAC_ACTION_MODIFY</code>, which <code>Action.minRelation</code> gates at <code>REBAC_RELATION_OWNER</code>. The orchestrator runs every admin check. A guest never reaches this plane.</li>
<li><strong>Normal capabilities</strong> (use plane): the verbs a running guest exposes to players. These map to <code>REBAC_ACTION_INTERACT</code> and <code>REBAC_ACTION_OBSERVE</code>. Observe is public. Interact needs a real relation.</li>
</ul>
</section>
<section id="guest-composition" class="level3">
<h3 class="anchored" data-anchor-id="guest-composition">Guest composition</h3>
<p>One zone process loads more than one guest, each as its own <code>libriscv::Machine</code> and its own ReBAC subject (per <code>rfd/0092</code>). The initial composition is:</p>
<ul>
<li><code>zone-guest-middleham</code>: the game-logic guest, and the first to load. Its ELF recipe already worked in-tree, and it is the smallest.</li>
<li><code>zone-guest-gyre</code>: the presentation guest (the web client and its deploy), CDN-delivered on the same pipeline.</li>
<li><code>zone-guest-godot</code>: Godot rv64 under <code>rvlinux</code>. This is the stress test of the same capability table, not a separate design.</li>
</ul>
</section>
<section id="what-closes-the-loop" class="level3">
<h3 class="anchored" data-anchor-id="what-closes-the-loop">What closes the loop</h3>
<p>The minimum loop closes when three items land in <code>zone-server-h2o</code>:</p>
<ol type="1">
<li>A guest loader that links <code>thirdparty/libriscv</code> and boots one ELF. The last loader lived in <code>mud/orchestrator/main.cpp</code> and moved to <code>zone-guest-middleham</code>.</li>
<li>A capability table: which host functions a loaded guest may call, keyed by the guest subject’s ReBAC relations, with QCBOR as the wire format.</li>
<li><code>rebac_check</code> call sites at both tiers: at load time (modify, owner only) and per guest syscall (interact or observe).</li>
</ol>
<p>Two prerequisites sit outside those three items. First, identity: the server’s TLS cert and key are still <code>NULL</code>/<code>NULL</code>, and the admin plane cannot authorize a subject that does not exist. Second, an artifact to fetch: <code>zone-guest-middleham</code>’s release workflow fails on a <code>fabric-godot-core@feat/sandbox</code> checkout for libriscv, so no guest ELF exists on a CDN yet.</p>
</section>
</section>
<section id="consequences" class="level2">
<h2 class="anchored" data-anchor-id="consequences">Consequences</h2>
<ul>
<li>The <code>fly/Containerfile</code> libriscv build and the riscv64-musl cross toolchain stay removed until item 1 lands. Building a static lib that nothing links is dead image weight.</li>
<li>Repo cleanups must treat <code>thirdparty/libriscv</code> and <code>thirdparty/QCBOR</code> as host infrastructure, not guest leftovers.</li>
<li>The client-command path (parse a datagram payload, write an entity, gate it with <code>rebac_check</code>) can close a create-modify-observe loop before any sandbox work. That path is compatible with, and smaller than, items 1 through 3.</li>
</ul>
</section>
<section id="references" class="level2">
<h2 class="anchored" data-anchor-id="references">References</h2>
<ul>
<li><code>v-sekai-multiplayer-fabric/zone-server-h2o</code> PR #22: the removal that exposed this decision.</li>
<li><code>v-sekai-multiplayer-fabric/zone-guest-middleham</code>, <code>zone-guest-gyre</code>, <code>zone-guest-godot</code>: the guests.</li>
<li><code>sinew-mocap/solve</code> org’s <code>lean-rebac-core</code>, <code>Rebac/core/ReBAC.lean</code>: the source the C types generate from.</li>
</ul>
</section>
<section id="related" class="level2">
<h2 class="anchored" data-anchor-id="related">Related</h2>
<ul>
<li><code>rfd/0092-rebac-gates-libriscv-guest-access</code>: the gating model this RFD binds to a host repo and a loop definition.</li>
<li><code>rfd/0079-sandboxed-godot-in-zone-server-h2o-via-raw-libriscv</code>: the sandboxing decision behind <code>zone-guest-godot</code>.</li>
<li><code>rfd/0083-zone-server-h2o-replaces-godot-fabriczone</code>: the rule that ReBAC types generate from <code>lean-rebac-core</code>.</li>
</ul>


</section>

 ]]></description>
  <guid>https://v-sekai-multiplayer-fabric.github.io/manuals/rfd/0094-minimum-ugc-game-loop-guest-composition/</guid>
  <pubDate>Fri, 07 Aug 2026 19:00:59 GMT</pubDate>
</item>
<item>
  <title>RFD 0093: Compile taskweft relation expressions to linear automata</title>
  <link>https://v-sekai-multiplayer-fabric.github.io/manuals/rfd/0093-compile-taskweft-to-linear-automata/</link>
  <description><![CDATA[ 





<p><em><a href="../../index.html">Manuals</a> › RFD 0093: Compile taskweft relation expressions to linear automata</em></p>
<p><strong>State:</strong> discussion · <strong>Scope:</strong> taskweft-nif evaluation, zone-server-h2o ReBAC</p>
<section id="problem" class="level2">
<h2 class="anchored" data-anchor-id="problem">Problem</h2>
<p><code>taskweft</code> evaluates a relation expression by recursive descent with a fuel counter (<code>check_expr</code>). It searches the graph with a depth-first walk that copies its visited set at every branch (<code>_rebac_dfs</code>). Both backtrack. Neither has a termination bound except fuel, and a fuel-zero result denies silently.</p>
<p>This runs on two hot paths, not one. <code>tw_domain.hpp</code> routes every planner goal binding through <code>check_expr</code>, so a backtracking planner calls a backtracking evaluator. <code>rfd/0092</code> then puts the same evaluator in front of a <code>libriscv</code> guest host call, which costs 3ns.</p>
</section>
<section id="decision" class="level2">
<h2 class="anchored" data-anchor-id="decision">Decision</h2>
<p>Compile, the way <code>google/re2</code> compiles. Treat a relation expression as a regular path query over edge labels. Build the automaton once, per named definition, with a Thompson construction. Then evaluate by simulating that automaton against the graph, as a product of automaton state and graph node. No backtracking runs, and no visited set gets copied.</p>
<p>Take the algorithm, not the library. <code>re2</code> matches bytes. This alphabet is relation labels.</p>
<p>Build the deterministic automaton lazily, and hold it in a bounded cache. Flush that cache when it fills, exactly as <code>re2</code> does. This is the mechanism behind <code>rfd/0092</code>’s resolution-equivalence theorem, and it retires the <code>p_fuel</code> parameter, because termination becomes structural.</p>
<p>Two constructs resist this. Zanzibar already answers the first one. <code>difference</code> is non-monotonic, so follow Leopard’s rule: denormalize the monotone fragment only, and evaluate exclusion at query time over the compiled sets.</p>
<p>The second stays open, and it decides feasibility. <code>tuple_to_userset</code> moves the object as well as the subject, and nobody checked whether it stays regular.</p>
</section>
<section id="references" class="level2">
<h2 class="anchored" data-anchor-id="references">References</h2>
<ul>
<li>Construction, the two hard constructs, and Zanzibar’s own answer to the negation problem: <code>DETAILS.md</code></li>
<li><code>v-sekai-multiplayer-fabric/zone-server-h2o</code>, <code>thirdparty/taskweft-nif/standalone/tw_rebac.hpp</code> and <code>tw_domain.hpp</code></li>
<li><code>google/re2</code>, <code>doc/syntax.html</code> and its linear-time guarantee</li>
</ul>
</section>
<section id="related" class="level2">
<h2 class="anchored" data-anchor-id="related">Related</h2>
<ul>
<li><code>rfd/0092-rebac-gates-libriscv-guest-access</code>: supplies the hot path this compilation serves, and open questions 4 and 7 that it closes.</li>
<li><code>rfd/0002-taskweft-value-narrowing-primitives-and-refs</code>: the value model these expressions carry.</li>
</ul>
</section>
<section id="detail" class="level2">
<h2 class="anchored" data-anchor-id="detail">Detail</h2>
</section>
<section id="status" class="level2">
<h2 class="anchored" data-anchor-id="status">Status</h2>
<p>Discussion only. Nobody wrote a compiler, and nobody proved the two hard constructs below stay regular. That proof decides whether this RFD is buildable at all.</p>
</section>
<section id="what-backtracks-today" class="level2">
<h2 class="anchored" data-anchor-id="what-backtracks-today">What backtracks today</h2>
<p><code>tw_rebac.hpp</code> carries two evaluators, and both backtrack.</p>
<p><code>check_expr</code> walks the expression tree by recursive descent. It decrements <code>p_fuel</code> at each <code>union</code>, <code>intersection</code>, <code>difference</code>, and <code>tuple_to_userset</code> node, and it returns <code>false</code> when fuel reaches zero. <code>check_base</code> recurses the same way through an <code>IS_MEMBER_OF</code> chain.</p>
<p><code>_rebac_dfs</code> searches depth-first for a capability path. It copies its visited set at every branch:</p>
<pre><code>std::vector&lt;std::string&gt; sub_path;
std::unordered_set&lt;std::string&gt; sub_visited = p_visited;
if (_rebac_dfs(p_g, e.object, p_target, sub_visited, sub_path, p_depth - 1)) {</code></pre>
<p>Each element of that set is a heap-allocated string, so each branch allocates.</p>
<p>Two hot paths call this work. <code>tw_domain.hpp</code>’s own goal-satisfaction comment records the first. A goal binding whose <code>var</code> is a JSON object gets:</p>
<pre><code>parsed as a full RelationExpr (union, intersection, difference,
tuple_to_userset, …) and evaluated via check_expr.</code></pre>
<p>The planner therefore calls <code>check_expr</code> per binding, per candidate state, while it backtracks. <code>rfd/0092</code> adds the second: the same evaluator in front of a <code>libriscv</code> guest host call, whose own cost is 3ns.</p>
</section>
<section id="what-re2-does-instead" class="level2">
<h2 class="anchored" data-anchor-id="what-re2-does-instead">What <code>re2</code> does instead</h2>
<p><code>re2</code> refuses to backtrack. It compiles a pattern to a program, then simulates that program so every input position advances exactly once. Its guarantee is linear time in input length, with bounded memory, and no catastrophic blowup on an adversarial pattern. It builds the deterministic automaton lazily, and when the state cache fills, it flushes the cache rather than growing without limit.</p>
<p>The same three moves apply here:</p>
<ol type="1">
<li>Compile once. A named definition in <code>TwReBACGraph::definitions</code> compiles to an automaton over relation labels, by a Thompson construction. <code>union</code> becomes an alternation. Concatenation comes from <code>tuple_to_userset</code>’s pivot. <code>IS_MEMBER_OF</code> inheritance becomes a Kleene star on that one label.</li>
<li>Simulate, do not search. Evaluation runs the product of automaton state and graph node. States times edges bounds the work. Nothing copies a visited set, because the product state <em>is</em> the visited marker.</li>
<li>Bound the cache. Hold the lazy deterministic states in a fixed budget, and flush when full.</li>
</ol>
<p><code>p_fuel</code> then disappears. It exists only because a backtracking search has no natural bound. A product simulation terminates because the product state space is finite.</p>
</section>
<section id="this-is-not-a-stretch-relation-expressions-are-path-queries" class="level2">
<h2 class="anchored" data-anchor-id="this-is-not-a-stretch-relation-expressions-are-path-queries">This is not a stretch: relation expressions are path queries</h2>
<p>A ReBAC relation expression describes a set of label paths from a subject to an object. That is a regular path query, the same object graph databases already evaluate. Zanzibar’s userset rewrite rules describe a restricted regular path language for the same reason. The <code>base</code> case is a single label. <code>IS_MEMBER_OF</code> inheritance is that label starred. So most of <code>check_expr</code> already denotes a regular language, and a compiler for it is a translation, not an invention.</p>
</section>
<section id="the-two-constructs-that-resist" class="level2">
<h2 class="anchored" data-anchor-id="the-two-constructs-that-resist">The two constructs that resist</h2>
<p><strong><code>difference</code> needs complement.</strong> Complement needs a deterministic automaton, and determinization is worst-case exponential in the number of states. <code>re2</code> lives with the same fact and answers it with a lazy deterministic automaton plus a flushable cache. The same answer applies here.</p>
<p>The sharper problem is not cost. It is monotonicity. An expression of the form “A and not B” turns a grant into a denial as soon as an edge satisfies B. An earlier <code>rfd/0092</code> draft claimed the opposite, as an append-only monotonicity theorem, and used it to justify caching a resolved capability table. <strong><code>difference</code> makes that theorem false.</strong></p>
<p>Zanzibar already answers this, because Zanzibar ships exclusion. A userset expression there combines sub-expressions:</p>
<pre><code>by operations such as union, intersection, and exclusion</code></pre>
<p>Google never claims monotonicity. Their cache correctness rests on a version instead:</p>
<pre><code>We avoid reusing results evaluated from a different snapshot by
encoding snapshot timestamps in cache keys.</code></pre>
<p><code>rfd/0092</code> now carries that correction. Its table keys on the FoundationDB read version that <code>zf_zonetick.c</code> already opens per tick. That correction closes the conflict, and negation stays available.</p>
<section id="leopards-rule-do-not-denormalize-through-exclusion" class="level3">
<h3 class="anchored" data-anchor-id="leopards-rule-do-not-denormalize-through-exclusion">Leopard’s rule: do not denormalize through exclusion</h3>
<p>Zanzibar’s index draws a sharper line, and this RFD adopts it. Leopard denormalizes only the monotone fragment. Its two set types are <code>GROUP2GROUP</code> and <code>MEMBER2GROUP</code>, and both express pure reachability:</p>
<pre><code>Group membership can be considered as a reachability problem in a
graph, where nodes represent groups and users and edges represent
direct membership.</code></pre>
<p>Exclusion never enters the index. It runs at query time over the indexed sets:</p>
<pre><code>A query evaluates an expression of union, intersection, or exclusion
of named sets</code></pre>
<p>So the rule is not “ban <code>difference</code>”. The rule is: compile and denormalize the monotone fragment, then apply <code>union</code>, <code>intersection</code>, and <code>difference</code> at query time over those compiled sets. A negated sub-expression stays a live query, and it never becomes a cached edge set.</p>
<p>Google also treats this indexing as opt-in, “For selected namespaces that exhibit such structure”, not as a universal transform. This RFD takes the same position for taskweft’s own definitions.</p>
<p><strong><code>tuple_to_userset</code> moves the object.</strong> A plain regular path query fixes its target and varies the path. <code>tuple_to_userset</code> pivots through one relation and then evaluates an inner expression against a <em>different</em> subject, as <code>check_expr</code> shows:</p>
<pre><code>for (size_t idx : sit-&gt;second) {
    const TwEdge &amp;e = p_g.edges[idx];
    if (e.rel == pivot) {
        if (check_expr(p_g, e.object, iit-&gt;second, p_obj, p_fuel - 1)) {</code></pre>
<p>Whether that stays regular is the open question that decides this whole design. Nobody checked it. If it does stay regular, the product construction above holds as written. If it does not, this RFD needs a pushdown model or a restriction on nesting depth, and the linear-time claim weakens accordingly.</p>
</section>
</section>
<section id="verification-this-needs" class="level2">
<h2 class="anchored" data-anchor-id="verification-this-needs">Verification this needs</h2>
<p><code>taskweft</code> already sets the precedent for proving a fast path equal to a slow path. <code>tw_rebac.hpp</code>’s own comment on the <code>member_edges</code> index reads:</p>
<pre><code>Formally justified by Planner.ExpandIndex: expand_index_equiv proves
that iterating member_edges gives the same result as scanning all
edges.</code></pre>
<p>The same obligation applies, at a larger scale:</p>
<ul>
<li>Compilation equivalence. The compiled automaton accepts exactly the subject and object pairs <code>check_expr</code> grants. This is <code>rfd/0092</code>’s resolution-equivalence theorem, and this RFD supplies its mechanism.</li>
<li>Termination without fuel. The product simulation halts on every input, with no fuel parameter.</li>
<li>A cache flush changes no answer. Flushing lazy deterministic states affects speed only, never a grant or a denial.</li>
<li>Differential test. Run the compiled evaluator and the existing <code>check_expr</code> against the same graphs, and compare every answer, in the same shape <code>test/unit/test_xr_grid_entity_packet.c</code> compares a generated codec against golden vectors.</li>
</ul>
</section>
<section id="consequences" class="level2">
<h2 class="anchored" data-anchor-id="consequences">Consequences</h2>
<p>Good: one mechanism closes <code>rfd/0092</code>’s open question 4 (fuel has no owner) and open question 7 (cost on the 64 Hz path). The fuel parameter stops being a silent denial source. Planning and access control share one evaluator, because both already call <code>check_expr</code>.</p>
<p>Bad: this is a compiler, and nobody wrote it. Nobody answered the <code>tuple_to_userset</code> regularity question, and a negative answer weakens the central claim. None of this work has a caller yet, because <code>thirdparty/taskweft-nif</code> is not wired into <code>zone-server-h2o</code>, and <code>rfd/0092</code>’s guest surface does not exist either. Adopting Leopard’s split also costs something real: a negated sub-expression stays a live query forever, so it never gains the compiled path’s speed.</p>
</section>
<section id="revisit-when" class="level2">
<h2 class="anchored" data-anchor-id="revisit-when">Revisit when</h2>
<p>Somebody answers the <code>tuple_to_userset</code> regularity question. That answer decides whether to build the product construction as written, or to fall back to a pushdown model with a bounded nesting depth. It is now the one open question that decides this design, because Zanzibar already settled the <code>difference</code> question above.</p>


</section>

 ]]></description>
  <guid>https://v-sekai-multiplayer-fabric.github.io/manuals/rfd/0093-compile-taskweft-to-linear-automata/</guid>
  <pubDate>Fri, 07 Aug 2026 19:00:59 GMT</pubDate>
</item>
<item>
  <title>RFD 0092: ReBAC gates libriscv guest access, not five callbacks</title>
  <link>https://v-sekai-multiplayer-fabric.github.io/manuals/rfd/0092-rebac-gates-libriscv-guest-access/</link>
  <description><![CDATA[ 





<p><em><a href="../../index.html">Manuals</a> › RFD 0092: ReBAC gates libriscv guest access, not five callbacks</em></p>
<p><strong>State:</strong> discussion · <strong>Scope:</strong> zone-guest-middleham, godot-sandbox-style libriscv hosts</p>
<section id="problem" class="level2">
<h2 class="anchored" data-anchor-id="problem">Problem</h2>
<p><code>rfd/0079</code> picked raw <code>libriscv</code> for sandboxed guest code (now living in <code>zone-guest-middleham</code>), but never defined what host capabilities a guest may reach. <code>rfd/0079</code>’s own “Consequences” section says so directly: “The exact <code>GodotInstance</code>/scene API <code>godot_tick</code> uses to apply entity input and read state back out is not yet nailed down.”</p>
<p><code>godot-sandbox</code>, the reference <code>libriscv</code>-Godot integration, already solved this once, with five independent allow-callbacks: class, object, method, property, and resource. Each callback returns a plain boolean. None of the five shares state with the others, and none returns a reason or a path a reviewer could audit later.</p>
</section>
<section id="decision" class="level2">
<h2 class="anchored" data-anchor-id="decision">Decision</h2>
<p>Gate guest access through ReBAC, not five separate callbacks, and not a two-tier core/user split like <code>godot-luau-script</code>’s.</p>
<p>Neither existing engine fits as it stands. <code>lean-rebac-core</code>’s <code>rebacCheck</code> carries real Lean proofs, but it is a five-rank total order, which is a tier model. <code>tw_rebac.hpp</code> has the right relationship-graph shape, but it carries no proofs.</p>
<p>So extend <code>lean-rebac-core</code> in Lean to the relationship-graph shape, and generate the C from it. This keeps <code>rfd/0083</code>’s existing rule: ReBAC types generate from <code>lean-rebac-core</code>, not hand-duplicated per language. <code>tw_rebac.hpp</code> becomes the reference shape and the differential-test oracle, not the shipped engine. <code>DETAILS.md</code> lists the theorems this migration must carry.</p>
<p>Model each guest <code>libriscv::Machine</code> instance as a ReBAC subject. Model each of <code>godot-sandbox</code>’s five gated things (a class, an object, a method, a property, a resource path) as a ReBAC object. Replace each boolean callback with one <code>check_expr</code>/<code>can()</code> call: does this guest subject have <code>HAS_CAPABILITY</code> (or a defined computed relation) to this object.</p>
<p>Split the graph into a use plane and an admin plane. On the use plane a guest asks whether it may reach a capability. On the admin plane a principal asks whether it may add an edge. A <code>CAN_GRANT</code> relation governs the admin plane. The orchestrator runs every admin check, and a guest never reaches that plane.</p>
<p>Also gate <code>libriscv</code>’s own execution-budget primitives (instruction- counted timeout, pause/resume) as ReBAC actions, not hardcoded constants. Budget extension becomes an authorized relationship.</p>
</section>
<section id="references" class="level2">
<h2 class="anchored" data-anchor-id="references">References</h2>
<ul>
<li>Engine comparison, mapping table, the <code>godot-sandbox</code> API this replaces, and ten open questions: <code>DETAILS.md</code></li>
<li><code>v-sekai-multiplayer-fabric/zone-server-h2o</code>, <code>thirdparty/taskweft-nif/standalone/tw_rebac.hpp</code></li>
<li><code>sinew-mocap/solve</code> org’s <code>lean-rebac-core</code>, <code>Rebac/core/ReBAC.lean</code></li>
<li><code>libriscv/godot-sandbox</code>, <code>docs/host_langs/godot_integration/godot_docs/restrictions</code></li>
</ul>
</section>
<section id="related" class="level2">
<h2 class="anchored" data-anchor-id="related">Related</h2>
<ul>
<li><code>rfd/0079-sandboxed-godot-in-zone-server-h2o-via-raw-libriscv</code>: the sandboxing decision this gates.</li>
<li><code>rfd/0086-defer-nogod-gossip-zone-authority</code>: the other place <code>lean-rebac-core</code> is already in scope.</li>
</ul>
</section>
<section id="detail" class="level2">
<h2 class="anchored" data-anchor-id="detail">Detail</h2>
</section>
<section id="status" class="level2">
<h2 class="anchored" data-anchor-id="status">Status</h2>
<p>Discussion only. No implementation exists yet. Nothing today calls <code>check_expr</code>, <code>rebac_can_json</code>, or any <code>lean-rebac-core</code> predicate from a <code>libriscv</code> host callback.</p>
</section>
<section id="what-godot-sandbox-documents-today" class="level2">
<h2 class="anchored" data-anchor-id="what-godot-sandbox-documents-today">What <code>godot-sandbox</code> documents today</h2>
<p><code>godot-sandbox</code> is the reference <code>libriscv</code>-Godot integration. <code>libriscv</code> itself calls “pause and resume … a first-class citizen” in its own README, and documents an “optional execution timeout using instruction counting”. <code>godot-sandbox</code> exposes a <code>restrictions</code> property. Setting it <code>true</code> blocks all external access by default, then five independent callbacks each grant a narrow exception:</p>
<ul>
<li><code>set_class_allowed_callback(func(sandbox, name): ...)</code> — gates class instantiation. Checked with <code>is_allowed_class(name)</code>.</li>
<li><code>add_allowed_object(obj)</code> / <code>remove_allowed_object(obj)</code> / <code>clear_allowed_objects()</code>, plus <code>set_object_allowed_callback(func(sandbox, obj): ...)</code> for anything not in the explicit list.</li>
<li><code>set_method_allowed_callback(func(sandbox, obj, method): ...)</code> — gates method calls.</li>
<li><code>set_property_allowed_callback(func(sandbox, obj, prop): ...)</code> — gates property reads/writes.</li>
<li><code>set_resource_allowed_callback(func(sandbox, path): ...)</code>, checked with <code>is_allowed_resource(path)</code> — gates resource loads.</li>
</ul>
<p>Each callback returns a plain boolean, independently of the other four. None of them shares a subject/object model, a delegation concept, or an audit trail. One documented workaround for building a real whitelist is to run once in trace mode. This logs every access request a callback sees, then a person hand-promotes the log into a static allow-list. This RFD’s ReBAC model closes that gap structurally, not by manual log review.</p>
</section>
<section id="the-existing-rebac-engine-this-reuses" class="level2">
<h2 class="anchored" data-anchor-id="the-existing-rebac-engine-this-reuses">The existing ReBAC engine this reuses</h2>
<p><code>thirdparty/taskweft-nif/standalone/tw_rebac.hpp</code> (in <code>zone-server-h2o</code>, ported from <code>plan_memory/rebac.py</code>, no Godot dependency) already implements:</p>
<ul>
<li>A relation vocabulary: <code>HAS_CAPABILITY</code>, <code>CONTROLS</code>, <code>OWNS</code>, <code>IS_MEMBER_OF</code>, <code>DELEGATED_TO</code>, <code>SUPERVISOR_OF</code>, <code>PARTNER_OF</code>, plus an <code>UNKNOWN</code>/string-named escape hatch for domain-specific relations.</li>
<li>A boolean expression algebra over relations: <code>base</code>, <code>union</code>, <code>intersection</code>, <code>difference</code>, <code>tuple_to_userset</code> (Zanzibar’s own “pivot through another relation” pattern) — <code>check_expr()</code>.</li>
<li><code>IS_MEMBER_OF</code> transitive inheritance and <code>CONTROLS</code>/<code>DELEGATED_TO</code> inversion, both handled inside <code>check_base()</code>, not bolted on per caller.</li>
<li><code>can()</code>/<code>rebac_can_json()</code> — a DFS capability search that returns the actual path, not just a boolean, format: <code>{"authorized":bool,"path":[...]}</code>. This is the audit trail <code>godot-sandbox</code>’s five callbacks do not have today.</li>
<li>JSON (de)serialization (<code>graph_from_json</code>/<code>graph_to_json</code>), so a guest’s capability graph is itself data, not code.</li>
</ul>
</section>
<section id="the-two-engines-differ-and-neither-one-fits-as-it-stands" class="level2">
<h2 class="anchored" data-anchor-id="the-two-engines-differ-and-neither-one-fits-as-it-stands">The two engines differ, and neither one fits as it stands</h2>
<p>An earlier draft of this RFD called <code>lean-rebac-core</code>’s <code>rebacCheck</code> an interchangeable alternative to <code>tw_rebac.hpp</code>. That was wrong. The two carry different authorization semantics, and the difference decides this RFD’s whole premise.</p>
<p>Read <code>zone-server-h2o</code>’s own port, <code>src/gen/rebac.h</code>. <code>rebac_check</code> grants access if and only if the claim’s highest relation rank is greater than or equal to the action’s minimum rank. The ranks form a fixed total order: <code>REBAC_RELATION_PUBLIC = 0</code>, <code>INSTANCE_MEMBER = 1</code>, <code>FRIEND = 2</code>, <code>GUILD_MEMBER = 3</code>, <code>OWNER = 4</code>. The actions are <code>REBAC_ACTION_OBSERVE</code>, <code>INTERACT</code>, and <code>MODIFY</code>.</p>
<p>That is a five-tier lattice, not a relationship graph. It has no subjects, no objects, and no edges. This RFD exists to replace a two-tier model. A five-tier model is the same shape with three more tiers, so it does not answer the problem statement.</p>
<p><code>lean-rebac-core</code> keeps its own value, and <code>rfd/0083</code> and <code>rfd/0086</code> both still depend on it. Its theorems are real (<code>rebac_empty_denied</code>, <code>rebac_public_observe</code>, the owner-only <code>.modify</code> boundary). It answers a different question: what rank a player claim needs for a world action. It does not answer what a sandboxed guest may reach on the host.</p>
<p>The other candidate has the opposite problem. <code>tw_rebac.hpp</code> carries the right shape and no proofs.</p>
</section>
<section id="migrate-lean-rebac-core-to-the-graph-shape-do-not-replace-it" class="level2">
<h2 class="anchored" data-anchor-id="migrate-lean-rebac-core-to-the-graph-shape-do-not-replace-it">Migrate <code>lean-rebac-core</code> to the graph shape, do not replace it</h2>
<p>Do not ship the unproved engine. Extend <code>lean-rebac-core</code> in Lean from a rank lattice to a relationship graph, then generate the C from it. <code>rfd/0083</code> already sets this rule for this exact module: ReBAC types generate from <code>lean-rebac-core</code>, and they never get hand-duplicated per language. <code>zone-server-h2o</code>’s <code>src/gen/rebac.c</code> is already that generated artifact. This work extends the Lean source behind it, instead of adding a second, unproved authorization engine beside it.</p>
<p><code>tw_rebac.hpp</code> keeps two roles in this plan. It is the reference shape, because its relation vocabulary and expression algebra already describe the target. It is also the differential-test oracle, in the same way <code>test/unit/test_xr_grid_entity_packet.c</code> checks a generated codec against golden vectors.</p>
<p>Precedent exists inside <code>tw_rebac.hpp</code> itself. Its own comment on the <code>member_edges</code> index reads:</p>
<pre><code>Formally justified by Planner.ExpandIndex: expand_index_equiv proves
that iterating member_edges gives the same result as scanning all
edges.</code></pre>
<p>So <code>taskweft</code> already proves one fast path equal to its slow path, in Lean, for this same header. This migration generalizes that habit rather than inventing it.</p>
<section id="theorems-this-migration-must-carry" class="level3">
<h3 class="anchored" data-anchor-id="theorems-this-migration-must-carry">Theorems this migration must carry</h3>
<ul>
<li><code>rebac_empty_denied</code>, generalized: an empty graph denies every request. The rank version already proves this.</li>
<li>Fuel soundness. If <code>check_expr</code> grants at fuel <code>n</code>, it grants at fuel <code>n + 1</code>. More search never reverses a grant, and a fuel-zero denial is therefore always conservative. This closes open question 4.</li>
<li>Snapshot soundness. A resolved capability table, built at read version <code>V</code>, answers exactly as a graph walk at read version <code>V</code> answers. This replaces an earlier append-only monotonicity theorem, which <code>difference</code> makes false. See <code>rfd/0093</code>.</li>
<li>Resolution equivalence. The flat capability table, built at bind time, answers exactly as a full graph walk answers. This is the same theorem shape as <code>expand_index_equiv</code>, and it is what makes the enforcement path trustworthy.</li>
<li>Plane separation. No use-plane derivation grants <code>CAN_GRANT</code>. A guest therefore cannot reach the admin plane. This closes open question 2.</li>
<li>Non-delegability, if the team takes that fork. No derivation grants <code>CAN_GRANT</code> to a subject that does not already hold a host-seeded <code>CAN_GRANT</code>.</li>
</ul>
</section>
</section>
<section id="administrative-relations-who-may-write-edges" class="level2">
<h2 class="anchored" data-anchor-id="administrative-relations-who-may-write-edges">Administrative relations: who may write edges</h2>
<p>A capability graph that its own subjects may edit grants nothing. <code>godot-luau-script</code> answers this with a coarse split, where core scripts may set their own permissions and user scripts may not. This RFD answers it inside the graph instead, with administrative relations.</p>
<p>Every check belongs to one of two planes:</p>
<table class="caption-top table">
<colgroup>
<col style="width: 6%">
<col style="width: 49%">
<col style="width: 44%">
</colgroup>
<thead>
<tr class="header">
<th>Plane</th>
<th>Question</th>
<th>Relations</th>
</tr>
</thead>
<tbody>
<tr class="odd">
<td>Use</td>
<td>May this guest call <code>X</code>?</td>
<td><code>HAS_CAPABILITY</code>, <code>CONTROLS</code>, <code>OWNS</code></td>
</tr>
<tr class="even">
<td>Admin</td>
<td>May this principal add edge <code>(S, O, R)</code>?</td>
<td><code>CAN_GRANT</code></td>
</tr>
</tbody>
</table>
<p>A guest lives only on the use plane. Each grant request runs a <code>check_expr</code> on the admin plane first, and the orchestrator runs that check. <code>tw_rebac.hpp</code> supports <code>CAN_GRANT</code> today with no enum change, because <code>parse_rel</code> returns <code>UNKNOWN</code> for an unrecognized string and <code>check_base</code> then matches on <code>rel_name</code> directly. Reuse of the existing <code>SUPERVISOR_OF</code>, <code>OWNS</code>, or <code>DELEGATED_TO</code> relations needs a deliberate decision, because <code>check_base</code> already gives <code>DELEGATED_TO</code> a delegation meaning, not an administration meaning.</p>
<p>Three sub-decisions stay open:</p>
<ul>
<li>Bootstrap. If edge-writing authority is itself an edge, some agent must write the first edge. That seed grant belongs outside the graph, host-side, before any guest runs. <code>godot-luau-script</code> seeds at <code>SandboxService</code> in <code>init.lua</code>. The equivalent here is the orchestrator at <code>mud_boot</code> time.</li>
<li>Delegability. May a <code>CAN_GRANT</code> holder grant <code>CAN_GRANT</code> itself? A yes makes the relation viral, and one compromised administrator then owns the graph. Open question 3 below records that no revocation exists, so that capture stays permanent. Recommendation: make <code>CAN_GRANT</code> non-delegable in the first version.</li>
<li>Scope. A global admin right and a scoped one differ completely. A scoped admin edge must name what it governs, such as one relation type or an object prefix. <code>tuple_to_userset</code> already expresses scoped administration, because it pivots through one relation to evaluate another.</li>
</ul>
</section>
<section id="proposed-mapping" class="level2">
<h2 class="anchored" data-anchor-id="proposed-mapping">Proposed mapping</h2>
<table class="caption-top table">
<colgroup>
<col style="width: 26%">
<col style="width: 17%">
<col style="width: 19%">
<col style="width: 35%">
</colgroup>
<thead>
<tr class="header">
<th><code>godot-sandbox</code> axis</th>
<th>ReBAC subject</th>
<th>ReBAC object</th>
<th>Relation</th>
</tr>
</thead>
<tbody>
<tr class="odd">
<td>Class instantiation</td>
<td>the guest <code>Machine</code></td>
<td>the class name</td>
<td><code>HAS_CAPABILITY</code></td>
</tr>
<tr class="even">
<td>Object access</td>
<td>the guest <code>Machine</code></td>
<td>the object instance</td>
<td><code>HAS_CAPABILITY</code> or <code>CONTROLS</code></td>
</tr>
<tr class="odd">
<td>Method call</td>
<td>the guest <code>Machine</code></td>
<td><code>&lt;object&gt;.&lt;method&gt;</code></td>
<td><code>HAS_CAPABILITY</code></td>
</tr>
<tr class="even">
<td>Property access</td>
<td>the guest <code>Machine</code></td>
<td><code>&lt;object&gt;.&lt;property&gt;</code></td>
<td><code>HAS_CAPABILITY</code></td>
</tr>
<tr class="odd">
<td>Resource load</td>
<td>the guest <code>Machine</code></td>
<td>the resource path</td>
<td><code>HAS_CAPABILITY</code></td>
</tr>
<tr class="even">
<td>Instruction-budget extension</td>
<td>the guest <code>Machine</code></td>
<td>the “more gas” action</td>
<td><code>DELEGATED_TO</code> from a supervisor/owner</td>
</tr>
</tbody>
</table>
<p>Delegation, membership, and capability inheritance fall out of the expression algebra already in <code>tw_rebac.hpp</code>. A guest that is <code>IS_MEMBER_OF</code> a trusted group inherits that group’s <code>HAS_CAPABILITY</code> edges automatically, with no new code path per axis. <code>godot-sandbox</code> needs a new hand-written callback for each such case today.</p>
</section>
<section id="consequences" class="level2">
<h2 class="anchored" data-anchor-id="consequences">Consequences</h2>
<p>Good: one authorization model instead of five independent callbacks plus a two-tier VM split. A denied check returns a real reason (no path found), not just <code>false</code>. The engine is not new, untested code. <code>tw_rebac.hpp</code> already has real callers in <code>taskweft-nif</code>.</p>
<p>Bad: the team has not implemented this yet. <code>zone-guest-middleham</code>’s <code>mud-sandbox-orchestrator</code> calls exactly two <code>vmcall</code>-reachable guest functions today (<code>mud_boot</code>, <code>mud_step</code>). It has no host-capability surface yet for this model to gate. The design has nothing to attach to until that surface grows. No one wired <code>thirdparty/taskweft-nif</code> into <code>zone-server-h2o</code> yet — <code>mud/domains/README.md</code> already says the MUD guest does not use any of its three domains yet. So reusing <code>tw_rebac.hpp</code> here means depending on code that is itself not yet load-bearing anywhere.</p>
<p>Bad, and stated plainly: the Lean migration above is real work, and nobody started it. Six theorems need proving, and two of them (resolution equivalence, plane separation) carry the security argument. Until they exist, this design has a proved rank model and an unproved graph model, and neither one gates a guest today.</p>
</section>
<section id="the-graph-authors-permissions.-it-does-not-enforce-them." class="level2">
<h2 class="anchored" data-anchor-id="the-graph-authors-permissions.-it-does-not-enforce-them.">The graph authors permissions. It does not enforce them.</h2>
<p>A guest runs at near-native speed. <code>libriscv</code>’s own README records a <code>vmcall</code> cost of 3ns, against 50 to 150ns for other sandboxes. The same README records a CoreMark score of 38223 against 41382 native, about 92%. Guest code therefore reaches the host boundary at native-code frequency.</p>
<p><code>_rebac_dfs</code> allocates. It copies a <code>std::unordered_set&lt;std::string&gt;</code> at every branch (<code>sub_visited = p_visited</code>), and each element is a heap-allocated string. Nobody measured that cost yet, so this RFD states no figure. But an allocating graph search in front of a 3ns call inverts the cost model. The check would dominate the call it protects, and it would discard the one property <code>rfd/0079</code> chose <code>libriscv</code> for.</p>
<p>So the ReBAC graph is the authoring model, not the enforcement model. Resolve the graph once, at guest boot time and again at each grant, into a flat capability table. The hot path then reads that table by index. It never walks the graph.</p>
<p>Key that table by snapshot version, the way Zanzibar keys its cache. An earlier draft of this RFD justified the table with an append-only monotonicity argument, where a resolved grant stays valid forever. Google’s own system rejects that argument, and <code>rfd/0093</code> records why: Zanzibar supports exclusion, so a later edge can revoke a grant.</p>
<p>Zanzibar’s answer is a timestamp, not a monotonicity claim:</p>
<pre><code>We avoid reusing results evaluated from a different snapshot by
encoding snapshot timestamps in cache keys.</code></pre>
<p><code>zone-server-h2o</code> already has the equivalent. FoundationDB is multi-version, and <code>zf_zonetick.c</code> already opens one transaction per tick, so every tick carries a read version. Key the resolved table by that read version. Correctness then rests on multi-version reads, not on a monotonicity property that <code>difference</code> breaks.</p>
<p>Zanzibar also quantizes its timestamps to “a coarse granularity, such as one or ten seconds” so that recent checks share cache entries. A per-tick read version already gives this design the same effect, with no extra rounding.</p>
<p><code>godot-sandbox</code> documents the same artifact from the other direction. Its trace-mode workaround produces a static allow-list by hand. This design produces that artifact from the graph instead, which removes the manual review step.</p>
<p>This also reframes open question 6. <code>godot-sandbox</code> documents that “Objects passed as function arguments remain accessible regardless of restrictions.” That reads as a bypass. It is instead an object-capability design: the handle itself is the authorization, checked once when the guest receives it. That choice is deliberate and fast. It needs a decision here, not an accident.</p>
</section>
<section id="open-questions" class="level2">
<h2 class="anchored" data-anchor-id="open-questions">Open questions</h2>
<p>Ten questions stay open. Items 1 to 3 block implementation. The rest are design work.</p>
<ol type="1">
<li><p>Engine choice. Resolved above: extend <code>lean-rebac-core</code> to the graph shape and generate the C, with <code>tw_rebac.hpp</code> as the reference shape and test oracle. Recorded here because an earlier draft called the two engines interchangeable, which is wrong.</p></li>
<li><p>Who may write edges. Answered above by the use/admin plane split, but its three sub-decisions (bootstrap, delegability, scope) stay open.</p></li>
<li><p>Revocation does not exist. <code>tw_rebac.hpp</code> defines <code>add_edge</code> and <code>define</code>, and no counterpart that removes either. A search for <code>remove</code>, <code>revoke</code>, <code>erase</code>, and <code>delete</code> in that header returns nothing. The graph is append-only. Nobody can revoke a capability mid-session, and no rule covers a guest paused inside a <code>vmcall</code> when its access disappears.</p>
<p>Zanzibar shows the shape of the fix. Leopard’s incremental layer stores each update as a <code>(T, s, e, t, d)</code> tuple. The paper defines the last two fields:</p>
<pre><code>t is the timestamp of the update and d is a deletion marker</code></pre>
<p>A query merges every update at or below its own timestamp on top of the offline index. So revocation is a tombstone plus a version merge, and the Lean migration above must carry both. An append-only graph is not the design. It is the current gap.</p></li>
<li><p>Fuel has no owner. <code>check_expr</code> and <code>check_base</code> return <code>false</code> at zero fuel. That fails closed, which is correct. But a legitimate deep delegation chain then denies silently, and authorization depends on graph depth. Only <code>tw_expand</code> carries a default (<code>p_fuel = 3</code>). <code>check_expr</code>, <code>check_base</code>, and <code>rebac_can_json</code> each require the caller to supply one. No rule says who picks it.</p></li>
<li><p>Identity and ID reuse. Subjects and objects are <code>std::string</code>. Godot recycles object instance IDs after a free. A stale <code>HAS_CAPABILITY</code> edge plus a recycled ID grants access to a different object than the authorized one. This RFD names no stable identity scheme.</p></li>
<li><p>An inherited bypass. <code>godot-sandbox</code>’s own restrictions page states this: “Objects passed as function arguments remain accessible regardless of restrictions.” A check at the call boundary misses handles the guest already holds. No rule covers re-checking a held handle on a later tick.</p></li>
<li><p>Cost on the 64 Hz path. <code>_rebac_dfs</code> copies the visited set at every branch (<code>sub_visited = p_visited</code>). A graph search per host call, on the zone tick, has no budget in this RFD.</p></li>
<li><p>Gas checking is circular. If budget extension is itself a ReBAC check, and that check costs time, no rule says whose budget pays.</p></li>
<li><p>Denial leaks the graph. <code>rebac_can_json</code> returns <code>{"authorized":bool,"path":[...]}</code>. That path is the audit-trail advantage over <code>godot-sandbox</code>. Returning it to untrusted guest code leaks capability-graph structure.</p></li>
<li><p>Which process may answer. <code>src/gen/rebac.h</code>’s own comment quotes the Lean source: only the authority zone evaluates <code>.interact</code> and <code>.modify</code>, and interest zones may evaluate <code>.observe</code> locally. <code>rfd/0086</code> defers that authority mechanism.</p></li>
</ol>
</section>
<section id="revisit-when" class="level2">
<h2 class="anchored" data-anchor-id="revisit-when">Revisit when</h2>
<p><code>zone-guest-middleham</code>’s guest surface grows past <code>mud_boot</code>/<code>mud_step</code> into calling named host functions/classes/objects (the shape <code>rfd/0079</code> left open). At that point, build the five-axis mapping table above as real code, instead of ad hoc per-axis callbacks. Do the Lean migration first, and close open questions 2 and 3 with it. Prove resolution equivalence before anything trusts the flat capability table on the hot path.</p>


</section>

 ]]></description>
  <guid>https://v-sekai-multiplayer-fabric.github.io/manuals/rfd/0092-rebac-gates-libriscv-guest-access/</guid>
  <pubDate>Fri, 07 Aug 2026 19:00:59 GMT</pubDate>
</item>
<item>
  <title>RFD 0091: The Gyre as a second MUD domain</title>
  <link>https://v-sekai-multiplayer-fabric.github.io/manuals/rfd/0091-the-gyre-mud-domain-and-mode-selector/</link>
  <description><![CDATA[ 





<p><em><a href="../../index.html">Manuals</a> › RFD 0091: The Gyre as a second MUD domain</em></p>
<p><strong>State:</strong> published · <strong>Scope:</strong> zone-server-h2o mud/guest, mud/web</p>
<section id="problem" class="level2">
<h2 class="anchored" data-anchor-id="problem">Problem</h2>
<p><code>mud/guest/mud_guest.cpp</code> served one setting, Middleham. Middleham is a spy-thriller MUD, with gate, market, and temple rooms. It has trust and suspicion objectives. The starting room, the item list, and the objective logic were all fixed to that one world. <code>rfd/0085</code> proposes a second setting, The Gyre. <code>zone-server-h2o</code> needed a way to add it without a second server architecture.</p>
</section>
<section id="decision" class="level2">
<h2 class="anchored" data-anchor-id="decision">Decision</h2>
<p><code>mud_boot()</code> reads a new <code>domain</code> CBOR field (<code>"middleham"</code> default, <code>"the_gyre"</code> new). <code>MiddlehamStateMachine</code> keeps its name and its existing behavior unchanged for the default domain. A <code>domain_</code> member and three guarded branches cover the new domain: the start-room pick, <code>clone_rooms()</code>, and <code>objective_complete()</code>. A new <code>gyre_room_templates()</code> backs them, with two rooms, <code>decanting_floor</code> and <code>splicers_den</code>, one exit each way, no items, and no non-player characters (NPCs). This is the smallest possible loop (look, go, look), not <code>rfd/0085</code>’s full room graph.</p>
<p>The <code>domain</code> field threads through the same chain the <code>objective</code> field already used, ending at <code>POST /api/mud/command</code>. <code>mud/web/index.html</code> and <code>mud.js</code> get a mode selector (Middleham or The Gyre). Each mode keeps its own <code>localStorage</code> session ID, because the server reads a session’s domain only once, on that session’s first request.</p>
</section>
<section id="references" class="level2">
<h2 class="anchored" data-anchor-id="references">References</h2>
<ul>
<li>Full verification log, wire-protocol detail, and open follow-up items: <code>DETAILS.md</code></li>
<li>Source: <code>v-sekai-multiplayer-fabric/zone-server-h2o</code>, <code>docs/0003-the-gyre-mud-domain-and-mode-selector.md</code>. This RFD ports that file’s content here. Git history in that repo still keeps the original file.</li>
</ul>
</section>
<section id="related" class="level2">
<h2 class="anchored" data-anchor-id="related">Related</h2>
<ul>
<li><code>rfd/0085-the-gyre-mud-setting-on-the-loot-action-shell</code>: the design RFD this implements the smallest code-side loop for.</li>
</ul>
</section>
<section id="detail" class="level2">
<h2 class="anchored" data-anchor-id="detail">Detail</h2>
</section>
<section id="status" class="level2">
<h2 class="anchored" data-anchor-id="status">Status</h2>
<p>Done for the smallest loop (<code>zone-server-h2o</code> PR #5). Not verified end to end.</p>
</section>
<section id="wire-protocol" class="level2">
<h2 class="anchored" data-anchor-id="wire-protocol">Wire protocol</h2>
<p>The <code>domain</code> field flows through the same chain the <code>objective</code> field already used: <code>mud_cbor_encode_boot_config()</code>, then <code>mud_session_get_or_create()</code>, then <code>on_mud_command()</code>’s <code>POST /api/mud/command</code> handler. That handler now reads <code>domain</code> from the request body and picks a matching default objective (<code>explore_gyre</code> for The Gyre, <code>gain_watch_trust</code> for Middleham). The handler’s own prior comment already flagged an objective/domain picker as website-UI scope, not its own. This work fills that gap rather than inventing a new extension point.</p>
</section>
<section id="verification" class="level2">
<h2 class="anchored" data-anchor-id="verification">Verification</h2>
<p>Real, not assumed:</p>
<ul>
<li><code>mud/guest/test/gyre_smoke_test.cpp</code>: a native (non-riscv64) link of <code>mud_guest.cpp</code> that drives <code>mud_boot()</code> and <code>mud_step()</code> through the whole Gyre loop (look, go east, look). Built and run locally against QCBOR (<code>laurencelundblade/QCBOR</code>). It produces real narration text and real <code>post_room</code> transitions, and <code>objective_complete()</code> returns true after the player visits both rooms. A native Middleham boot, run the same way with the same seed <code>DIFFERENTIAL_TEST.md</code> uses, produced byte-identical “Middleham City Gate” narration to before this change. The default path did not regress.</li>
<li>The client change (mode selector, per-domain session ID, <code>domain</code> field on the wire) went through real end-to-end tests. The team used Playwright, in both Chromium and Camoufox (a Firefox-family browser), against a throwaway local Node stub that matched the real API shape. The team did not commit that stub. The team threw it away once it confirmed the client logic was correct.</li>
<li><code>mud/web/test/gyre.spec.ts</code> is the real, committed spec. It is Camoufox-driven, and it follows <code>mud.spec.ts</code>’s own house rule: <code>MUD_BASE_URL</code> must point at a real reachable instance, with no mock. It has not run against one yet. It stays red until a real deployment with the Gyre domain exists — the same state <code>mud.spec.ts</code> itself documented before its own first real deploy.</li>
</ul>
<p>The team did not verify a <code>riscv64-musl</code> and <code>libriscv</code> build and run of the changed guest code. The team’s own environment had no cross toolchain for that. The team did not attempt an FDB or H2O real build or deploy either.</p>
</section>
<section id="revisit-when" class="level2">
<h2 class="anchored" data-anchor-id="revisit-when">Revisit when</h2>
<p>A real <code>-z</code> deploy exists with the Gyre domain reachable. At that point, run <code>gyre.spec.ts</code> for real. Also run a real riscv64 differential test, matching <code>DIFFERENTIAL_TEST.md</code>’s own Middleham precedent. Only then treat this as more than a reviewed, natively tested diff.</p>
<p><code>rfd/0085</code>’s fuller room graph, contract catalog, and item set stay design only. Porting them past the two-room smallest loop is a separate, larger task. This RFD does not claim the team finished that larger task.</p>


</section>

 ]]></description>
  <guid>https://v-sekai-multiplayer-fabric.github.io/manuals/rfd/0091-the-gyre-mud-domain-and-mode-selector/</guid>
  <pubDate>Fri, 07 Aug 2026 19:00:59 GMT</pubDate>
</item>
<item>
  <title>RFD 0090: uro deploys as a Burrito-wrapped mix release, not iex -S mix</title>
  <link>https://v-sekai-multiplayer-fabric.github.io/manuals/rfd/0090-uro-burrito-release/</link>
  <description><![CDATA[ 





<p><em><a href="../../index.html">Manuals</a> › RFD 0090: uro deploys as a Burrito-wrapped mix release, not iex -S mix</em></p>
<p><strong>State:</strong> published · <strong>Scope:</strong> zone-backend (uro) production boot shape</p>
<section id="problem" class="level2">
<h2 class="anchored" data-anchor-id="problem">Problem</h2>
<p><code>zone-backend</code>’s root <code>Dockerfile</code> boots <code>uro</code> for local <code>docker-compose</code> use via <code>ENTRYPOINT iex -S mix do ecto.create, ecto.migrate, run priv/repo/test_seeds.exs, phx.server</code> — a dev-shaped boot that seeds dummy test data on every start. <code>build-image.yml</code>’s own header said its built image is “consumed by <code>v-sekai-multiplayer-fabric/infra</code> via the <code>gateway_image</code>/<code>uro_image</code> tofu variable”; <code>rfd/0089</code> archives <code>infra</code>, so that path no longer exists either. <code>zone-backend</code> needs its own real production build, not a dev image and not a dependency on an archived repo.</p>
<p><code>config/config.exs</code> used a <code>COMPILE_PHASE</code> environment flag (<code>Uro.Config.Helpers.get_env/2</code>) so the Docker <em>build</em> step could run <code>mix compile</code> without real secrets present: <code>iex -S mix</code> re-evaluates <code>config.exs</code> on every invocation, so the container’s actual boot command re-ran <code>mix</code> a second time with <code>COMPILE_PHASE=false</code> and real secrets in the environment. A real OTP release (<code>mix release</code>) evaluates <code>config.exs</code>/<code>prod.exs</code> once, permanently, at build time — there is no second pass at boot — so this trick does not carry over to a real release at all.</p>
</section>
<section id="decision" class="level2">
<h2 class="anchored" data-anchor-id="decision">Decision</h2>
<p>Build <code>uro</code> as a real OTP release, wrapped by <a href="https://github.com/burrito-elixir/burrito">Burrito</a> into one self-contained, statically-linked executable (musl libc, bundled ERTS), cross-compiled via Zig. <code>mix.exs</code> gains a <code>releases:</code> block targeting <code>linux_musl</code> (matching Fly’s runner and this repo’s existing Alpine-based images).</p>
<p>Every deploy-time secret or URL moves to <code>config/runtime.exs</code> (<code>if config_env() == :prod do ... end</code>), which a real release genuinely re-evaluates every time it starts. <code>config.exs</code>/<code>prod.exs</code> keep only what is safe to bake in at compile time. The <code>COMPILE_PHASE</code> flag and <code>Uro.Config.Helpers</code> are deleted — confirmed unused anywhere outside <code>config/*.exs</code> before removal.</p>
<p><code>lib/uro/release.ex</code> adds <code>Uro.Release.migrate/0</code>: a compiled release has no <code>mix</code> tasks at runtime, so this runs <code>Ecto.Migrator</code> directly against <code>Uro.Repo.Migration</code> (<code>gateway_admin</code>, DDL-capable) — never <code>Uro.Repo</code> (<code>gateway_writer</code>, DML only), matching <code>AGENTS.md</code>’s documented role separation. <code>docker/uro/fly-start.sh</code> is the script <code>AGENTS.md</code> has described for a while (migrate as <code>gateway_admin</code>, then serve as <code>gateway_writer</code>) but which never actually existed in this repo until now.</p>
</section>
<section id="references" class="level2">
<h2 class="anchored" data-anchor-id="references">References</h2>
<ul>
<li><code>v-sekai-multiplayer-fabric/zone-backend</code> PR #52.</li>
<li><code>config/runtime.exs</code>, <code>lib/uro/release.ex</code>, <code>docker/uro/Dockerfile</code>, <code>docker/uro/fly-start.sh</code>.</li>
</ul>
</section>
<section id="related" class="level2">
<h2 class="anchored" data-anchor-id="related">Related</h2>
<ul>
<li><code>rfd/0089-flyio-over-podman-quadlets</code>: the deployment target this release ships to.</li>
<li><code>rfd/0065-fabric-platform-central-elixir-burrito-casync</code>: Burrito’s prior use elsewhere in this org.</li>
</ul>
</section>
<section id="detail" class="level2">
<h2 class="anchored" data-anchor-id="detail">Detail</h2>
</section>
<section id="context-and-problem-statement" class="level2">
<h2 class="anchored" data-anchor-id="context-and-problem-statement">Context and problem statement</h2>
<p><code>zone-backend</code>’s root <code>Dockerfile</code> (used for local <code>docker-compose</code>) boots <code>uro</code> via <code>iex -S mix do ecto.create, ecto.migrate, run priv/repo/test_seeds.exs, phx.server</code>. This is dev-shaped on purpose — seeding <code>test_seeds.exs</code> on every boot is fine for local development — but <code>build-image.yml</code> reused the same root <code>Dockerfile</code> for its <code>ghcr.io</code> image, described as “consumed by <code>v-sekai-multiplayer-fabric/infra</code> via the <code>gateway_image</code>/<code>uro_image</code> tofu variable.” <code>rfd/0089</code> archives <code>infra</code>. Even without that, using a test-seeding dev image for a real production deploy is a real correctness problem, not a stopgap worth keeping.</p>
<p><code>config.exs</code>’s <code>COMPILE_PHASE</code> flag existed to let <code>mix compile</code> (the Docker build step) succeed with no real secrets present, by returning a placeholder value instead of raising. This only works because <code>iex -S mix ...</code> (not a compiled release) re-evaluates <code>config.exs</code> fresh at container start, this time with <code>COMPILE_PHASE=false</code> and real Fly secrets in the environment. A real <code>mix release</code> does not have this second evaluation: <code>config.exs</code> and <code>prod.exs</code> are baked in once, at build time, forever. Deploying an actual OTP release needed a different mechanism for secrets entirely.</p>
</section>
<section id="considered-options" class="level2">
<h2 class="anchored" data-anchor-id="considered-options">Considered options</h2>
<ol type="1">
<li><strong>Keep the <code>iex -S mix</code> boot shape for production</strong>, and rely on a different image build for the archived <code>infra</code> consumer. Rejected with <code>infra</code>’s archival — nothing consumes that image path anymore, and the seeded test data on every boot was never appropriate for a real deploy regardless.</li>
<li><strong>A real <code>mix release</code>, config moved to <code>config/runtime.exs</code>, Burrito wrapping it into a single static executable.</strong> Standard Phoenix production pattern (<code>runtime.exs</code>) plus this org’s own established Burrito precedent (<code>rfd/0065</code>).</li>
<li><strong>A real <code>mix release</code> without Burrito</strong>, shipped as a normal <code>_build/prod/rel/uro</code> directory inside a slim Erlang-installed runtime image. Avoids the Zig/Burrito build dependency, but the runtime image then needs a matching Erlang/OTP install instead of just CA certs, and this org already has a working Burrito toolchain and precedent from <code>fabric-platform-central</code>.</li>
</ol>
</section>
<section id="decision-outcome" class="level2">
<h2 class="anchored" data-anchor-id="decision-outcome">Decision outcome</h2>
<p>Chosen: <strong>option 2.</strong> <code>mix.exs</code> gains a <code>releases:</code> block:</p>
<div class="code-copy-outer-scaffold"><div class="sourceCode" id="cb1" style="background: #f1f3f5;"><pre class="sourceCode elixir code-with-copy"><code class="sourceCode elixir"><span id="cb1-1"><span class="kw" style="color: #003B4F;
background-color: null;
font-weight: bold;
font-style: inherit;">defp</span> releases <span class="kw" style="color: #003B4F;
background-color: null;
font-weight: bold;
font-style: inherit;">do</span></span>
<span id="cb1-2">  <span class="ot" style="color: #003B4F;
background-color: null;
font-style: inherit;">[</span></span>
<span id="cb1-3">    <span class="va" style="color: #111111;
background-color: null;
font-style: inherit;">uro:</span> <span class="ot" style="color: #003B4F;
background-color: null;
font-style: inherit;">[</span></span>
<span id="cb1-4">      <span class="va" style="color: #111111;
background-color: null;
font-style: inherit;">steps:</span> <span class="ot" style="color: #003B4F;
background-color: null;
font-style: inherit;">[</span><span class="va" style="color: #111111;
background-color: null;
font-style: inherit;">:assemble</span>, <span class="op" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">&amp;</span><span class="cn" style="color: #8f5902;
background-color: null;
font-style: inherit;">Burrito</span><span class="op" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">.</span>wrap<span class="op" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">/</span><span class="dv" style="color: #AD0000;
background-color: null;
font-style: inherit;">1</span><span class="ot" style="color: #003B4F;
background-color: null;
font-style: inherit;">]</span>,</span>
<span id="cb1-5">      <span class="va" style="color: #111111;
background-color: null;
font-style: inherit;">burrito:</span> <span class="ot" style="color: #003B4F;
background-color: null;
font-style: inherit;">[</span></span>
<span id="cb1-6">        <span class="va" style="color: #111111;
background-color: null;
font-style: inherit;">targets:</span> <span class="ot" style="color: #003B4F;
background-color: null;
font-style: inherit;">[</span></span>
<span id="cb1-7">          <span class="va" style="color: #111111;
background-color: null;
font-style: inherit;">linux_musl:</span> <span class="ot" style="color: #003B4F;
background-color: null;
font-style: inherit;">[</span><span class="va" style="color: #111111;
background-color: null;
font-style: inherit;">os:</span> <span class="va" style="color: #111111;
background-color: null;
font-style: inherit;">:linux</span>, <span class="va" style="color: #111111;
background-color: null;
font-style: inherit;">cpu:</span> <span class="va" style="color: #111111;
background-color: null;
font-style: inherit;">:x86_64</span>, <span class="va" style="color: #111111;
background-color: null;
font-style: inherit;">libc:</span> <span class="va" style="color: #111111;
background-color: null;
font-style: inherit;">:musl</span><span class="ot" style="color: #003B4F;
background-color: null;
font-style: inherit;">]</span></span>
<span id="cb1-8">        <span class="ot" style="color: #003B4F;
background-color: null;
font-style: inherit;">]</span></span>
<span id="cb1-9">      <span class="ot" style="color: #003B4F;
background-color: null;
font-style: inherit;">]</span></span>
<span id="cb1-10">    <span class="ot" style="color: #003B4F;
background-color: null;
font-style: inherit;">]</span></span>
<span id="cb1-11">  <span class="ot" style="color: #003B4F;
background-color: null;
font-style: inherit;">]</span></span>
<span id="cb1-12"><span class="kw" style="color: #003B4F;
background-color: null;
font-weight: bold;
font-style: inherit;">end</span></span></code></pre></div></div>
<p><code>linux_musl</code> matches Fly’s own runner and this repo’s existing Alpine-based images, avoiding a glibc/musl mismatch between the build and run stages.</p>
<p><code>config/runtime.exs</code> now holds everything that previously depended on <code>Helpers.get_env</code>/<code>COMPILE_PHASE</code>: <code>URL</code>, <code>ROOT_ORIGIN</code>, <code>FRONTEND_URL</code>, both <code>Repo</code>s’ connection config and CockroachDB mTLS SSL options, <code>Uro.Endpoint</code> (port, secret key base, HTTPS options), <code>cors_plug</code>’s origin, <code>joken</code>’s signer, <code>Turnstile</code>’s secret, <code>pow_assent</code>’s OAuth2 provider list, the mailer, <code>ex_aws</code>/S3 config, OpenTelemetry’s endpoint, and <code>aria_storage</code>’s bucket/DB path — all gated under <code>if config_env() == :prod do</code>, so <code>dev.exs</code>/<code>test.exs</code> (which already define their own complete, static <code>Uro.Repo</code> config) are unaffected. <code>config.exs</code>/<code>prod.exs</code> keep only what is genuinely safe to bake in at compile time: <code>ecto_repos</code>, <code>hammer</code>, logger format, <code>pow</code>’s static config, and similar.</p>
<p><code>Uro.Config.Helpers</code> (the <code>COMPILE_PHASE</code> module) is deleted — confirmed via <code>grep -rln "Uro\.Config\.Helpers\|compile_phase" lib/</code> that nothing outside <code>config/*.exs</code> referenced it.</p>
<p><code>lib/uro/release.ex</code> adds <code>Uro.Release.migrate/0</code>, run via the release’s own <code>eval</code> command (<code>bin/uro eval "Uro.Release.migrate()"</code>), since a compiled release has no <code>mix</code> tasks at runtime — <code>mix ecto.migrate</code> does not exist inside it. This runs <code>Ecto.Migrator.with_repo/2</code> against <code>Uro.Repo.Migration</code> only, matching <code>AGENTS.md</code>’s documented DDL/DML role split.</p>
<p><code>docker/uro/Dockerfile</code> is a real two-stage build: a build stage (Elixir/Alpine, plus <code>zig</code>, <code>cmake</code>, <code>ninja-build</code>, and the <code>riscv-none-elf-gcc</code> toolchain <code>.github/workflows/casync-interop.yml</code> already installs, for <code>WeftWarpBurrito.SandboxNif</code>’s <code>c_src</code> build) running <code>mix release uro</code>, and a runtime stage that is just <code>alpine:3.20</code> plus <code>ca-certificates</code> — no Elixir/Erlang install at all, since Burrito’s output is fully static. <code>docker/uro/fly-start.sh</code> runs <code>Uro.Release.migrate/0</code>, then <code>exec</code>s <code>start</code> — the script <code>AGENTS.md</code> has described for a while but which never actually existed in this repo.</p>
</section>
<section id="consequences" class="level2">
<h2 class="anchored" data-anchor-id="consequences">Consequences</h2>
<p>Good: a real production boot path exists that does not depend on the now-archived <code>infra</code> repo, does not seed test data on every start, and matches this org’s own established Burrito precedent (<code>rfd/0065-fabric-platform-central-elixir-burrito-casync</code>). The runtime image needs nothing but the static binary and CA certs (outbound TLS to CockroachDB, S3-compatible storage, and OTLP all need real CA verification), which is a smaller, simpler runtime surface than an Erlang-installed image.</p>
<p>Bad: the build stage is heavier than before — Zig, CMake, Ninja, and a RISC-V cross-compiler, on top of what <code>mix compile</code> already needed per <code>zone-backend</code>’s own <code>docs/decisions/0017</code>. <code>config/runtime.exs</code> is a real, sizeable file (over 150 lines) that concentrates every deploy-time secret in one place; a mistake there is a real production config bug, not a compile error.</p>
</section>
<section id="confirmation" class="level2">
<h2 class="anchored" data-anchor-id="confirmation">Confirmation</h2>
<p>Verified locally before this record, not assumed: <code>MIX_ENV=prod mix release uro</code> (with the <code>riscv-none-elf-gcc</code> toolchain on <code>PATH</code>) produces <code>burrito_out/uro_linux_musl</code>, confirmed via <code>file</code> to be a real <code>ELF 64-bit LSB executable, x86-64 ... statically linked, stripped</code>, about 20MB. Running it with placeholder prod secrets (<code>URL</code>, <code>ROOT_ORIGIN</code>, <code>DATABASE_URL</code>, <code>PHOENIX_KEY_BASE</code>, etc. set) via both <code>start</code> and <code>eval</code> genuinely boots <code>Uro.Application</code>: real <code>Postgrex</code> connection attempts to the given (unreachable, in this local test) <code>DATABASE_URL</code>, and a real <code>[info] Running Uro.Endpoint with Bandit 1.6.11 at 0.0.0.0:&lt;port&gt; (http)</code> log line confirming the HTTP listener actually started. Not yet done: an actual Fly deploy of this image (<code>zone-backend</code> PR #52 is open, not yet merged as of this record).</p>


</section>

 ]]></description>
  <guid>https://v-sekai-multiplayer-fabric.github.io/manuals/rfd/0090-uro-burrito-release/</guid>
  <pubDate>Fri, 07 Aug 2026 19:00:59 GMT</pubDate>
</item>
<item>
  <title>RFD 0089: Fly.io, not self-hosted podman quadlets, is the deployment target</title>
  <link>https://v-sekai-multiplayer-fabric.github.io/manuals/rfd/0089-flyio-over-podman-quadlets/</link>
  <description><![CDATA[ 





<p><em><a href="../../index.html">Manuals</a> › RFD 0089: Fly.io, not self-hosted podman quadlets, is the deployment target</em></p>
<p><strong>State:</strong> published · <strong>Scope:</strong> deployment target for fabric runtime services</p>
<section id="problem" class="level2">
<h2 class="anchored" data-anchor-id="problem">Problem</h2>
<p><code>rfd/0061-quadlets-on-fedora-44-instead-of-harvester</code> chose self-hosted podman quadlets on Fedora 44, provisioned by the <code>infra</code> repo’s OpenTofu, and put Fly.io “off the table.” Since then, real production work on <code>zone-backend</code> (<code>multiplayer-fabric-uro</code>, <code>multiplayer-fabric-crdb</code>) has run entirely against Fly.io: a genuine crash-loop bug in <code>multiplayer-fabric-crdb</code> was found and fixed against a real Fly deploy, and a real Burrito-wrapped <code>mix release</code> for <code>uro</code> was built and verified against Fly’s secrets model (<code>config/runtime.exs</code>). The quadlet repos (<code>zone-backend-quadlet</code>, <code>cockroach-crdb-quadlet</code>, <code>zone-server-quadlet</code>, <code>zone-baker-quadlet</code>, <code>restic-backup-quadlet</code>, <code>gha-runner-quadlet</code>, <code>sccache-cache-quadlet</code>) exist but were last pushed 2026-06-13, before any of this real Fly.io deployment work happened.</p>
</section>
<section id="decision" class="level2">
<h2 class="anchored" data-anchor-id="decision">Decision</h2>
<p>Use Fly.io as the real deployment target. This supersedes <code>rfd/0061</code>’s choice of self-hosted podman quadlets on Fedora 44.</p>
<p>The <code>infra</code> repo (OpenTofu provisioning for the Fedora 44 quadlet hosts) and all seven quadlet repos above are archived — tombstoned, not deleted, so their history stays reachable, but they are no longer the deployment path for anything. <code>multiplayer-fabric-crdb</code> and <code>multiplayer-fabric-uro</code> on Fly.io are the real, currently-running services this applies to first.</p>
</section>
<section id="references" class="level2">
<h2 class="anchored" data-anchor-id="references">References</h2>
<ul>
<li><code>rfd/0061-quadlets-on-fedora-44-instead-of-harvester</code>: the decision this supersedes.</li>
<li><code>v-sekai-multiplayer-fabric/zone-backend</code>, <code>docs/decisions/0011-fly-redeploy-scope-uro-and-crdb-only.md</code>: the prior Fly-app-loss record this RFD’s Fly.io work continues from.</li>
<li><code>v-sekai-multiplayer-fabric/infra</code>, <code>v-sekai-multiplayer-fabric/zone-backend-quadlet</code>, <code>cockroach-crdb-quadlet</code>, <code>zone-server-quadlet</code>, <code>zone-baker-quadlet</code>, <code>restic-backup-quadlet</code>, <code>gha-runner-quadlet</code>, <code>sccache-cache-quadlet</code>: archived by this decision.</li>
</ul>
</section>
<section id="related" class="level2">
<h2 class="anchored" data-anchor-id="related">Related</h2>
<ul>
<li><code>rfd/0090-uro-burrito-release</code>: the real production release shape this decision’s Fly.io deploy of <code>uro</code> uses.</li>
</ul>
</section>
<section id="detail" class="level2">
<h2 class="anchored" data-anchor-id="detail">Detail</h2>
</section>
<section id="context-and-problem-statement" class="level2">
<h2 class="anchored" data-anchor-id="context-and-problem-statement">Context and problem statement</h2>
<p><code>rfd/0061</code> chose podman quadlets under systemd on self-hosted Fedora 44 hosts over both Fly.io and the earlier Harvester HCI plan, provisioned by the <code>infra</code> repo’s OpenTofu. Seven quadlet repos were created for this: <code>zone-backend-quadlet</code>, <code>cockroach-crdb-quadlet</code>, <code>zone-server-quadlet</code>, <code>zone-baker-quadlet</code>, <code>restic-backup-quadlet</code>, <code>gha-runner-quadlet</code>, and <code>sccache-cache-quadlet</code>, each with an <code>install.sh</code> and a real <code>.container</code> unit. All seven last received a push on 2026-06-13.</p>
<p>Independently of that decision, real production work on <code>zone-backend</code> continued against Fly.io. <code>docs/decisions/0011-fly-redeploy-scope-uro-and-crdb-only.md</code> records a Fly app loss and a scoped redeploy of <code>multiplayer-fabric-uro</code> and <code>multiplayer-fabric-crdb</code> only. That redeploy sat broken for weeks: <code>multiplayer-fabric-crdb</code> crash-looped on every boot (<code>/docker-entrypoint.sh: No such file or directory</code> — the image’s real entrypoint is <code>/cockroach/cockroach.sh</code>, confirmed via <code>skopeo inspect --config docker://ghcr.io/v-sekai/cockroach:latest</code>; whatever Dockerfile previously set that override was never committed anywhere), and <code>multiplayer-fabric-uro</code> had 14 secrets staged but never deployed.</p>
<p>This mismatch — a published RFD choosing quadlets, while the actual service that exists and needs fixing runs on Fly — needed a real decision, not a silent continuation of whichever path happened to have momentum.</p>
</section>
<section id="considered-options" class="level2">
<h2 class="anchored" data-anchor-id="considered-options">Considered options</h2>
<ol type="1">
<li><strong>Fix <code>zone-backend</code>’s Fly deploy, treat quadlets as the long-term target.</strong> Keeps <code>rfd/0061</code> in force; the Fly.io fixes become a stopgap, and someone later ports <code>uro</code>/<code>crdb</code> to <code>zone-backend-quadlet</code>/<code>cockroach-crdb-quadlet</code>.</li>
<li><strong>Adopt Fly.io as the real target, tombstone the quadlet repos and <code>infra</code>.</strong> Matches what has actually been built, verified, and deployed: a real <code>multiplayer-fabric-crdb</code> fix, confirmed live via Fly’s own health checks and boot logs, and a real Burrito-wrapped release for <code>uro</code>, verified locally (a genuine static ELF binary that boots <code>Uro.Endpoint</code>, <code>Uro.Repo</code>, and <code>Uro.Repo.Migration</code> correctly).</li>
<li>Running both, service by service, avoids picking a single target but doubles the real ops surface <code>rfd/0061</code>’s own problem statement (Harvester’s “full virtualization tier” wraps the same workloads twice) already argued against, for a different reason.</li>
</ol>
</section>
<section id="decision-outcome" class="level2">
<h2 class="anchored" data-anchor-id="decision-outcome">Decision outcome</h2>
<p>Chosen: <strong>option 2.</strong> Fly.io is the real deployment target. <code>rfd/0061</code> is superseded. <code>infra</code> and all seven quadlet repos are archived (GitHub’s archive, not deletion — history stays reachable, nothing currently depends on them being writable).</p>
<p>The concrete evidence this decision is grounded in, not asserted:</p>
<ul>
<li><code>multiplayer-fabric-crdb</code>: three real, distinct bugs found and fixed by iterating against actual Fly boot logs, not guessed — the lost entrypoint, a missing <code>chmod +x</code> on its replacement, and a CockroachDB cert-lifetime rounding collision (the node cert requested the exact same lifetime as its CA; CockroachDB 22.1 rejects a cert lifetime <code>&gt;=</code> its CA’s). Health check passing, <code>uro</code> database and <code>gateway_writer</code>/<code>gateway_admin</code> roles bootstrapped, confirmed live.</li>
<li><code>multiplayer-fabric-uro</code>: a real Burrito-wrapped <code>mix release</code> replaces the dev-shaped root <code>Dockerfile</code> (<code>iex -S mix do ecto.create, ecto.migrate, run priv/repo/test_seeds.exs, phx.server</code>, which seeded dummy test data on every boot). Verified locally: the release build genuinely produces a static, ~20MB ELF binary, and both its <code>start</code> and <code>eval</code> commands boot <code>Uro.Application</code> for real (<code>Uro.Repo</code>, <code>Uro.Repo.Migration</code>, and <code>Uro.Endpoint</code> with Bandit all start, confirmed via real Postgrex connection attempts and a real HTTP listener log line).</li>
<li><code>zone-backend-quadlet</code>’s <code>README.md</code> documents the same <code>DATABASE_URL</code>/<code>MIGRATION_URL</code>/mTLS-cert shape as <code>zone-backend</code>’s <code>AGENTS.md</code> already does for Fly — the two paths were never going to diverge in what they configure, only in where. Nothing about the quadlet path had a working, deployed instance to point to; the Fly path now does.</li>
</ul>
</section>
<section id="consequences" class="level2">
<h2 class="anchored" data-anchor-id="consequences">Consequences</h2>
<p>Good: one real, currently-working deployment target for <code>zone-backend</code>’s two services, matching engineering effort already spent and verified rather than requiring a second port to a path with no live deployment. <code>rfd/0089</code>’s tombstoning also removes a real source of drift: two competing, both-real deploy configs for the same service (<code>AGENTS.md</code>’s Fly secrets vs.&nbsp;<code>zone-backend-quadlet</code>’s <code>/etc/zone-backend/env</code>) is worse than committing to one.</p>
<p>Bad: this reverses a published, deliberate decision (<code>rfd/0061</code>) made after weighing Harvester HCI, Fly.io, and quadlets directly against each other. That record’s own stated reasons for excluding Fly.io are not re-litigated here in detail; this RFD documents that the org’s real, current choice is Fly.io, not a re-derivation of why quadlets were rejected the first time. A future RFD revisiting deployment target again should read both <code>rfd/0061</code> and this record.</p>
<p><code>zone-server-h2o</code> and <code>zone-client-godot</code> are not yet covered by a real Fly.io production deployment; see <code>rfd/0091</code> and <code>rfd/0092</code>.</p>
</section>
<section id="confirmation" class="level2">
<h2 class="anchored" data-anchor-id="confirmation">Confirmation</h2>
<p><code>infra</code>, <code>zone-backend-quadlet</code>, <code>cockroach-crdb-quadlet</code>, <code>zone-server-quadlet</code>, <code>zone-baker-quadlet</code>, <code>restic-backup-quadlet</code>, <code>gha-runner-quadlet</code>, and <code>sccache-cache-quadlet</code> are archived, confirmed via <code>gh repo view --json isArchived</code> on each. <code>multiplayer-fabric-crdb</code>’s health check is passing on Fly, confirmed via <code>fly machine status</code>. <code>multiplayer-fabric-uro</code>’s real release build is verified locally; its actual Fly deploy is tracked as a follow-up (<code>zone-backend</code> PRs #51, #52), not yet merged and deployed as of this record.</p>


</section>

 ]]></description>
  <guid>https://v-sekai-multiplayer-fabric.github.io/manuals/rfd/0089-flyio-over-podman-quadlets/</guid>
  <pubDate>Fri, 07 Aug 2026 19:00:59 GMT</pubDate>
</item>
<item>
  <title>RFD 0088: zone-server-h2o’s transport is picoquic + picotls, not h2o’s own QUIC</title>
  <link>https://v-sekai-multiplayer-fabric.github.io/manuals/rfd/0088-picoquic-picotls-transport-for-zone-server-h2o/</link>
  <description><![CDATA[ 





<p><strong>State:</strong> published · <strong>Scope:</strong> zone-server-h2o transport</p>
<section id="problem" class="level2">
<h2 class="anchored" data-anchor-id="problem">Problem</h2>
<p><code>zone-server-h2o</code> needs WebTransport and datagrams to talk to the Godot client. <code>h2o</code> (the base this repo ports from) has no WebTransport or datagram support at all — a search of its source tree confirms this. What QUIC stack should carry that traffic?</p>
</section>
<section id="decision" class="level2">
<h2 class="anchored" data-anchor-id="decision">Decision</h2>
<p>Use <code>picoquic</code> + <code>picotls</code>, the same QUIC stack the Godot client’s <code>WebTransportPeer</code> (<code>godot/modules/http3/</code>) already builds on, not <code>h2o</code>’s own stack. <code>picoquic</code> has working WebTransport, datagrams, and MASQUE support that <code>h2o</code> lacks entirely. Client and server share one proven QUIC stack instead of two different, independently verified ones.</p>
<p><code>thirdparty/picoquic</code> and <code>thirdparty/picotls</code> are git subtrees, checked in at the exact commits that fork vendors. <code>src/transport/webtransport_server.c</code> bridges this QUIC transport into <code>h2o</code>’s own event loop. <code>src/transport/wt_session.c</code> handles H3/WebTransport session negotiation on top of it.</p>
</section>
<section id="references" class="level2">
<h2 class="anchored" data-anchor-id="references">References</h2>
<ul>
<li><code>cmake/picoquic.cmake</code> (mirrors the Godot module’s own <code>SCsub</code>)</li>
<li><code>v-sekai-multiplayer-fabric/zone-server-h2o</code></li>
</ul>
</section>
<section id="related" class="level2">
<h2 class="anchored" data-anchor-id="related">Related</h2>
<ul>
<li><code>rfd/0023-webtransport-http3-transport</code>: the client-side <code>modules/http3</code> decision this shares a QUIC stack with.</li>
<li><code>rfd/0083-zone-server-h2o-replaces-godot-fabriczone</code></li>
</ul>


</section>

 ]]></description>
  <guid>https://v-sekai-multiplayer-fabric.github.io/manuals/rfd/0088-picoquic-picotls-transport-for-zone-server-h2o/</guid>
  <pubDate>Fri, 07 Aug 2026 19:00:59 GMT</pubDate>
</item>
<item>
  <title>RFD 0087: Avatar IK uses sinew-mocap/solve’s Align.lean, not mink</title>
  <link>https://v-sekai-multiplayer-fabric.github.io/manuals/rfd/0087-avatar-ik-sinew-mocap-align-over-mink/</link>
  <description><![CDATA[ 





<p><strong>State:</strong> published · <strong>Scope:</strong> zone-server-h2o, avatar posing and entity physics</p>
<section id="problem" class="level2">
<h2 class="anchored" data-anchor-id="problem">Problem</h2>
<p><code>sinew-mocap/solve</code> was flagged as less trusted than <code>kevinzakka/ mink</code>, a widely-used differential IK library. Should <code>zone-server-h2o</code> port <code>mink</code> for avatar IK? Separately, entity/prop contact physics used vendored MuJoCo — does that stay?</p>
</section>
<section id="decision" class="level2">
<h2 class="anchored" data-anchor-id="decision">Decision</h2>
<p>No port of <code>mink</code>. Reading <code>mink</code>’s own core shows it assembles a full quadratic program each solve, dispatched through a separate QP-solver library plus its own Lie group math — a large, multi-session port, not a small one.</p>
<p><code>sinew-mocap/solve</code>’s own Lean source (<code>core/spec/Sinew/Align.lean</code>) is not a QP solver at all. It is Kabsch-style rotation fitting. This ports directly (<code>src/gen/sinew_align.{c,h}</code>), verified against <code>Align.lean</code>’s own known-rotation-recovery test, to floating-point precision. <code>mink</code>’s remaining features stay deferred.</p>
<p>Vendored MuJoCo (entity/prop contact physics) is dropped. Godot’s own Jolt physics already covers that role.</p>
</section>
<section id="references" class="level2">
<h2 class="anchored" data-anchor-id="references">References</h2>
<ul>
<li>Full context, the mink QP breakdown, and revision history: <code>DETAILS.md</code></li>
<li><code>v-sekai-multiplayer-fabric/sinew-mocap/solve</code></li>
<li><code>v-sekai-multiplayer-fabric/mujoco-riscv64</code>: dropped MuJoCo’s home</li>
</ul>
</section>
<section id="related" class="level2">
<h2 class="anchored" data-anchor-id="related">Related</h2>
<ul>
<li><code>rfd/0083-zone-server-h2o-replaces-godot-fabriczone</code></li>
</ul>


</section>

 ]]></description>
  <guid>https://v-sekai-multiplayer-fabric.github.io/manuals/rfd/0087-avatar-ik-sinew-mocap-align-over-mink/</guid>
  <pubDate>Fri, 07 Aug 2026 19:00:59 GMT</pubDate>
</item>
<item>
  <title>RFD 0086: Defer NoGod.lean’s gossip zone authority</title>
  <link>https://v-sekai-multiplayer-fabric.github.io/manuals/rfd/0086-defer-nogod-gossip-zone-authority/</link>
  <description><![CDATA[ 





<p><strong>State:</strong> published · <strong>Scope:</strong> zone-server-h2o, multi-process zone authority</p>
<section id="problem" class="level2">
<h2 class="anchored" data-anchor-id="problem">Problem</h2>
<p><code>lean-rebac-core</code>’s <code>NoGod.lean</code> is a proven, coordinator-free gossip protocol for zone authority: vector clocks, Hilbert-range ownership, and theorems that no two zones ever claim the same range. Should <code>zone-server-h2o</code> port it now, alongside the rest of <code>ReBAC.lean</code>?</p>
</section>
<section id="decision" class="level2">
<h2 class="anchored" data-anchor-id="decision">Decision</h2>
<p>Not yet. <code>zone-server-h2o</code> today hardcodes one zone ID per process (<code>z_id = 0</code> in <code>src/transport/webtransport_server.c</code>). One process has no peer to gossip with and no second zone’s clock to order events against. Porting the full gossip/vector-clock system now would build structure for a need that does not exist yet.</p>
<p>The narrow, reusable part — <code>ZoneRange</code> and <code>geometricAuthority</code>, about ten lines of pure C — ports cleanly whenever a second concurrent zone is real work. The gossip layer (<code>VClock</code>, <code>HLC</code>, <code>GossipMsg</code>) only earns its cost once there is a second process to coordinate with.</p>
</section>
<section id="references" class="level2">
<h2 class="anchored" data-anchor-id="references">References</h2>
<ul>
<li>Full context and the exact revisit trigger: <code>DETAILS.md</code></li>
<li><code>v-sekai-multiplayer-fabric/zone-server-h2o</code></li>
<li><code>sinew-mocap/solve</code> org’s <code>lean-rebac-core</code>, <code>Rebac/core/NoGod.lean</code></li>
</ul>
</section>
<section id="related" class="level2">
<h2 class="anchored" data-anchor-id="related">Related</h2>
<ul>
<li><code>rfd/0083-zone-server-h2o-replaces-godot-fabriczone</code>: the consolidation decision this defers within.</li>
</ul>


</section>

 ]]></description>
  <guid>https://v-sekai-multiplayer-fabric.github.io/manuals/rfd/0086-defer-nogod-gossip-zone-authority/</guid>
  <pubDate>Fri, 07 Aug 2026 19:00:59 GMT</pubDate>
</item>
<item>
  <title>RFD 0085: The Gyre — a MUD setting on the loot-action core-loop shell</title>
  <link>https://v-sekai-multiplayer-fabric.github.io/manuals/rfd/0085-the-gyre-mud-setting-on-the-loot-action-shell/</link>
  <description><![CDATA[ 





<p><em><a href="../../index.html">Manuals</a> › RFD 0085: The Gyre — a MUD setting on the loot-action core-loop shell</em></p>
<p><strong>State:</strong> discussion · <strong>Scope:</strong> zone-server-h2o game content, web client</p>
<p>Players awaken as “Sparks” — digitized human consciousnesses legally owned by a defunct hyper-corporation. They are currently downloaded into “Frames,” which are cheap, mass-produced synthetic worker chassis. Abandoned on The Gyre, a massive, slowly failing ring-station orbiting a toxic gas giant, players must survive cycle by cycle. They take on grueling localized contracts, scavenge for parts to prevent their Frames from seizing up, and try to buy their digital freedom from the station’s automated debt-collection algorithms.</p>
<section id="decision" class="level2">
<h2 class="anchored" data-anchor-id="decision">Decision</h2>
<p>The Gyre is content for a game and is not a new game architecture.</p>
<p>We defined a loop with two areas: a Hub area and a Field area. Players start in the Hub area. They travel to a Field area to do one task. Then they return to the Hub area.</p>
<p>In The Gyre, two Hub locations stand in for the Hub area: the Under-Market and the Commons. Each contract stands in for a Field task. A contract is one of these: a scavenge run, a hack run, an exploration run, or (sometimes) a short combat run.</p>
<p>We also defined five core systems: Budgeter, Combat, Loot, Presence, and Progression. These five core systems stay as they are. The Gyre adds new content on top of them: rooms, non-player characters (NPCs), items, and a story frame called the Debt Clock.</p>
<p>Players explore more than they fight. Combat happens less often than in a typical Dungeons &amp; Dragons session. A party has 2 to 4 players. If a party has fewer than 4 players, NPC companions can fill the empty spots.</p>
</section>
<section id="references" class="level2">
<h2 class="anchored" data-anchor-id="references">References</h2>
<ul>
<li>Room graph, NPCs, contract catalog, item table, session pacing, party/tone detail, implementation status, and open questions: <code>DETAILS.md</code></li>
<li>Implementation: <code>zone-server-h2o</code></li>
</ul>
</section>
<section id="related" class="level2">
<h2 class="anchored" data-anchor-id="related">Related</h2>
<ul>
<li><code>rfd/0045-loot-action-core-loop-mvp-vertical-slice</code>: defines the Hub/Field loop and the five core systems that The Gyre reuses.</li>
<li><code>rfd/0028-hexagonal-core-ports-adapters</code>, <code>rfd/0043-hexagon-progression-core</code>: describe the shape a future web/OAuth save adapter would follow.</li>
</ul>
</section>
<section id="detail" class="level2">
<h2 class="anchored" data-anchor-id="detail">Detail</h2>
</section>
<section id="context" class="level2">
<h2 class="anchored" data-anchor-id="context">Context</h2>
<p><code>rfd/0045-loot-action-core-loop-mvp-vertical-slice</code> shipped a playable shell: a Hub where players gather, an instanced Field room for one loot-action loop, and five hexagonal cores (Combat, Loot, Presence, Progression, Budgeter) behind ports. That slice targets SteamVR, one melee combo, one loot drop, four players.</p>
<p>The Gyre is a proposed setting: a sci-fi, gig-economy-survival MUD where players are “Sparks” occupying disposable “Frames” on a decaying station, working contracts to pay down a Debt Clock. The design lives in <code>zone-server-h2o</code> issue #4 and is reproduced here, remapped onto the existing shell’s terms.</p>
</section>
<section id="party-composition-and-tone" class="level2">
<h2 class="anchored" data-anchor-id="party-composition-and-tone">Party composition and tone</h2>
<p>Playtester feedback (screen name “cthulhoo,” mostly GURPS in the last 15 years, D&amp;D from 3e onward) shapes this revision. Their favorite campaigns “feel like anime”: exploration-led, with far fewer combat encounters per session than D&amp;D’s recommended pacing, run for 2-4 players plus NPC party members.</p>
<p>Apply that directly:</p>
<ul>
<li>Party size stays 2-4. A contract board scales its harder contracts to whatever party size shows up, rather than assuming a fixed four.</li>
<li>Rook, Splicer Jax, and other named NPCs are recruitable companions for a run, not just vendors. A party of 2 can take a contract sized for 4 by bringing one or two NPC companions instead of waiting for more players.</li>
<li>Combat stays the minority activity. Of the nine contracts in the catalog below, one (Drone Decommission) is combat, and it is avoidable: a stealth or hacking path exists around the drones for a party that would rather not fight. The rest are scavenging, repair, hacking, delivery, and social/exploration work.</li>
<li>Exploration carries the session. A room’s description, its NPCs, and what a player finds there (a Rumor, a piece of station history, a side conversation) matter as much as the contract’s reward. The Session pacing table below spends more of its 120 minutes moving through rooms and talking to NPCs than fighting.</li>
</ul>
</section>
<section id="the-web-target-and-save-data" class="level2">
<h2 class="anchored" data-anchor-id="the-web-target-and-save-data">The web target and save data</h2>
<p>The Gyre’s target client is a website, not the SteamVR build <code>rfd/0045</code> targets. Save data (Debt Clock balance, Frame state, inventory) needs to persist per player across sessions, tied to a GitHub OAuth login.</p>
<p>This is new client and persistence-adapter work, not a change to the core reducers. The Progression core (<code>rfd/0043</code>) already owns profile and inventory commits through a port; a web/OAuth build needs a new adapter behind that port, not a new core. The Combat, Loot, and Budgeter cores stay unaffected: a text-based web client still issues the same loot-request and combat-tick events the SteamVR client does, just through a different front end.</p>
</section>
<section id="mapping-the-gyre-onto-the-hubfield-shell" class="level2">
<h2 class="anchored" data-anchor-id="mapping-the-gyre-onto-the-hubfield-shell">Mapping The Gyre onto the Hub/Field shell</h2>
<ul>
<li>The Hub maps to The Under-Market (Splicer’s Den, Exchange Plaza, Transit Rails) and The Commons (Cycle’s End Tavern, Chapel of the Backup, Broadcast Row). Players gather, trade, and patch up here between contracts, the same role the Hub deck plays in <code>rfd/0045</code>.</li>
<li>A Field instance maps to one contract run. The Tangle, the Sub-Net, and the Underhull each host contract-type Field rooms: a scavenge check, a hacking check, a short combat encounter. Each contract is bounded and returns the player to the Hub, matching the Hub-to-Field-to-Hub round trip.</li>
<li>Contract rewards (chits, Encrypted Bounties, salvage) resolve through the loot core’s existing first-touch contention.</li>
<li>The Debt Clock, Frame Integrity, and inventory are profile state the progression core commits, the same path the MVP slice’s inventory delta already uses.</li>
<li>Only the Drone Decommission contract type uses the combat core, reusing the existing timed-hit validation. Every other contract resolves through the Loot and Progression cores alone, keeping combat the minority path through the content, per “Party composition and tone” above.</li>
</ul>
</section>
<section id="world-map-and-zones-room-graph" class="level2">
<h2 class="anchored" data-anchor-id="world-map-and-zones-room-graph">World map and zones (room graph)</h2>
<p>Six zones, three to four rooms each.</p>
<section id="the-reclamation-wards-spawn-zone" class="level3">
<h3 class="anchored" data-anchor-id="the-reclamation-wards-spawn-zone">The Reclamation Wards (spawn zone)</h3>
<ul>
<li>The Decanting Floor is a freezing, sterile room filled with hundreds of suspended Frames. Automated robotic arms attach players to their new bodies.</li>
<li>The Decon Vents are a mandatory exit path where players are blasted with harsh chemical foam before being pushed out into the station’s general population.</li>
<li>Intake Records is a small office of humming servers where a player can review their current Debt Clock balance and Frame warranty status.</li>
</ul>
</section>
<section id="the-tangle-scavenging-and-gathering" class="level3">
<h3 class="anchored" data-anchor-id="the-tangle-scavenging-and-gathering">The Tangle (scavenging and gathering)</h3>
<ul>
<li>Collapsed Aeroponics is a humid, rusted dome where modified fungal blooms have taken over the old air scrubbers. Players can harvest bio-matter here, but risk inhaling corrosive spores.</li>
<li>The Filtration Sump is a dark, flooded sector where players can filter clean water out of the station’s waste runoff.</li>
<li>Scrap Canyon is a collapsed maintenance corridor, floor to ceiling in decades of dumped hardware, the station’s best salvage and its least stable footing.</li>
</ul>
</section>
<section id="the-under-market-safe-zone-and-hub" class="level3">
<h3 class="anchored" data-anchor-id="the-under-market-safe-zone-and-hub">The Under-Market (safe zone and hub)</h3>
<ul>
<li>The Splicer’s Den is a makeshift clinic lit by harsh LED strips. Players spend chits here to patch their Integrity or install unregulated augments.</li>
<li>Exchange Plaza is a crowded, noisy hub where players pick up bounties, trade salvaged tech, and check the current cycle’s market prices.</li>
<li>The Transit Rails are the station’s rattling internal shuttle line. Riding it skips travel time between distant zones, for a small chit fare.</li>
</ul>
</section>
<section id="the-sub-net-the-dungeon" class="level3">
<h3 class="anchored" data-anchor-id="the-sub-net-the-dungeon">The Sub-Net (the “dungeon”)</h3>
<ul>
<li>Uplink Node Alpha is a physical junction box sparking with loose wires. A player with the right augment can plug their Frame directly in.</li>
<li>The Data Sea is a surreal, text-based interpretation of the station’s intranet. Movement here is instant, but a failed hacking check causes direct neural feedback, damaging the player’s Spark.</li>
<li>The Firewall Reef is a maze of self-patching defensive subroutines rendered as jagged coral, the station’s most valuable data and its most aggressive automated defenses.</li>
</ul>
</section>
<section id="the-underhull-hazard-zone" class="level3">
<h3 class="anchored" data-anchor-id="the-underhull-hazard-zone">The Underhull (hazard zone)</h3>
<ul>
<li>The Radiation Seam is a cracked section of outer hull venting the gas giant’s ambient radiation directly into the corridor. Fast Integrity drain, fast salvage.</li>
<li>The Drone Graveyard holds dozens of decommissioned security drones, some not fully decommissioned. A slow, tense room, not a rush.</li>
<li>Airlock Seven is the only working route to the station’s exterior. A short EVA contract type launches from here.</li>
</ul>
</section>
<section id="the-commons-social-zone" class="level3">
<h3 class="anchored" data-anchor-id="the-commons-social-zone">The Commons (social zone)</h3>
<ul>
<li>Other Sparks unwind at Cycle’s End Tavern between contracts, a source of rumor-board side content and NPC banter.</li>
<li>The Chapel of the Backup is a quiet room where Sparks who fear a bad resleeve pay to store a redundant memory snapshot.</li>
<li>Broadcast Row is a row of dead advertising screens one enterprising Spark reactivated to run a pirate radio station.</li>
</ul>
</section>
</section>
<section id="npcs" class="level2">
<h2 class="anchored" data-anchor-id="npcs">NPCs</h2>
<ul>
<li>Splicer Jax runs The Splicer’s Den, gruff, transactional, quietly generous to Sparks who are clearly new.</li>
<li>Overseer Q-11 is the automated debt-collection voice heard at the start and end of every cycle, never hostile, never warm, always exact about numbers.</li>
<li>Rook is a Tangle scavenger who trades salvage tips for a cut of whatever the player finds on their first trip.</li>
<li>The Pirate DJ runs Broadcast Row and drops rumors about which zones have good salvage or heavy drone activity this cycle.</li>
</ul>
</section>
<section id="contract-catalog" class="level2">
<h2 class="anchored" data-anchor-id="contract-catalog">Contract catalog</h2>
<table class="caption-top table">
<colgroup>
<col style="width: 13%">
<col style="width: 19%">
<col style="width: 41%">
<col style="width: 6%">
<col style="width: 18%">
</colgroup>
<thead>
<tr class="header">
<th>Contract</th>
<th>Zone</th>
<th>Type</th>
<th>Est. time</th>
<th>Reward</th>
</tr>
</thead>
<tbody>
<tr class="odd">
<td>Scrub the Scrubbers</td>
<td>Collapsed Aeroponics</td>
<td>Scavenge check</td>
<td>8-10 min</td>
<td>Chits + bio-matter</td>
</tr>
<tr class="even">
<td>Sump Pump Repair</td>
<td>The Filtration Sump</td>
<td>Repair task</td>
<td>10-12 min</td>
<td>Chits + Debt Clock dent</td>
</tr>
<tr class="odd">
<td>Ghost Line</td>
<td>Uplink Node Alpha → Data Sea</td>
<td>Hacking check</td>
<td>10-15 min</td>
<td>Chits + Encrypted Bounty</td>
</tr>
<tr class="even">
<td>Reef Breach</td>
<td>The Firewall Reef</td>
<td>Hard hacking check</td>
<td>15-20 min</td>
<td>Large Debt Clock dent</td>
</tr>
<tr class="odd">
<td>Seam Walk</td>
<td>The Radiation Seam</td>
<td>Timed salvage/exploration</td>
<td>8-10 min</td>
<td>Chits, high Integrity risk</td>
</tr>
<tr class="even">
<td>Drone Decommission</td>
<td>The Drone Graveyard</td>
<td>Short combat, or a stealth/hacking bypass around the drones</td>
<td>10-12 min</td>
<td>Chits + salvage parts</td>
</tr>
<tr class="odd">
<td>Rail Courier</td>
<td>Exchange Plaza → any zone</td>
<td>Delivery</td>
<td>5-8 min</td>
<td>Small chits, low risk</td>
</tr>
<tr class="even">
<td>Signal Boost</td>
<td>Broadcast Row</td>
<td>Social/fetch</td>
<td>8-10 min</td>
<td>Rumor unlock, small chits</td>
</tr>
<tr class="odd">
<td>The Long Corridor</td>
<td>Scrap Canyon → The Underhull</td>
<td>Pure exploration, no check required</td>
<td>10-15 min</td>
<td>Station lore, a Rumor lead</td>
</tr>
</tbody>
</table>
<p>Seven of these nine contracts resolve without a fight. Drone Decommission is the one combat-typed contract, and its stealth or hacking bypass keeps even that one avoidable for a party that would rather not.</p>
</section>
<section id="entity-and-item-concepts" class="level2">
<h2 class="anchored" data-anchor-id="entity-and-item-concepts">Entity and item concepts</h2>
<table class="caption-top table">
<colgroup>
<col style="width: 9%">
<col style="width: 7%">
<col style="width: 32%">
<col style="width: 50%">
</colgroup>
<thead>
<tr class="header">
<th>Item/entity</th>
<th>Type</th>
<th>Description</th>
<th>Mechanic</th>
</tr>
</thead>
<tbody>
<tr class="odd">
<td>Memory Drive</td>
<td>Core equipment</td>
<td>A heavy, metallic cylinder housing the player’s consciousness.</td>
<td>Drops on death. Can be looted, sold, or returned by other players.</td>
</tr>
<tr class="even">
<td>Synapse Coolant</td>
<td>Consumable</td>
<td>A pressurized can of foul-smelling gray gel.</td>
<td>Restores a moderate amount of Frame Integrity.</td>
</tr>
<tr class="odd">
<td>Encrypted Bounties</td>
<td>Currency / loot</td>
<td>Datapads filled with stolen corporate secrets.</td>
<td>Sold at the Exchange Plaza to pay down the player’s debt.</td>
</tr>
<tr class="even">
<td>Pneumatic Riveter</td>
<td>Weapon / tool</td>
<td>A heavy, modified construction tool.</td>
<td>Used to pry open sealed crates, or as a slow melee weapon.</td>
</tr>
<tr class="odd">
<td>Backup Snapshot</td>
<td>Service</td>
<td>A stored memory copy, purchased at the Chapel of the Backup.</td>
<td>On Catastrophic Seizure, restores the Spark to its last snapshot state instead of a blank slate.</td>
</tr>
<tr class="even">
<td>Radiation Tab</td>
<td>Consumable</td>
<td>A chalky pill that slows Integrity drain in the Underhull.</td>
<td>One-time use, single Radiation Seam trip.</td>
</tr>
</tbody>
</table>
</section>
<section id="example-room-description" class="level2">
<h2 class="anchored" data-anchor-id="example-room-description">Example room description</h2>
<blockquote class="blockquote">
<p><strong>[The Splicer’s Den]</strong></p>
<p>The air is thick with the acrid stench of burning flux and cheap synthetic skin. Bundles of fiber-optic cables hang from the ceiling, leading to a massive, heavily modified medical chair in the center of the room. A flickering display board flashes today’s rates for Integrity patching and limb alignment. A half-dismantled Hauler Frame sits in the corner, its servos clicking in a repetitive, broken loop.</p>
<p><em>Exits: [North] to Exchange Plaza, [East] to The Transit Rails.</em></p>
<p><em>Interactables: [Splicer Jax], [Medical Vendor], [Scavenge Pile]</em></p>
</blockquote>
</section>
<section id="session-pacing-120-minutes" class="level2">
<h2 class="anchored" data-anchor-id="session-pacing-120-minutes">Session pacing (120 minutes)</h2>
<table class="caption-top table">
<colgroup>
<col style="width: 21%">
<col style="width: 4%">
<col style="width: 73%">
</colgroup>
<thead>
<tr class="header">
<th>Phase</th>
<th>Minutes</th>
<th>Content</th>
</tr>
</thead>
<tbody>
<tr class="odd">
<td>Login and orientation</td>
<td>0-10</td>
<td>Decanting Floor, Decon Vents, Intake Records, first look at the Debt Clock; recruit an NPC companion if under 4 players</td>
</tr>
<tr class="even">
<td>Loop 1-2 (exploration and scavenge)</td>
<td>10-40</td>
<td>Scrub the Scrubbers, The Long Corridor, no combat, teach the loop and the setting</td>
</tr>
<tr class="odd">
<td>Market and downtime</td>
<td>40-55</td>
<td>Exchange Plaza trades, Splicer’s Den patch-up, Cycle’s End Tavern rumor pickup, NPC banter</td>
</tr>
<tr class="even">
<td>Loop 3-4 (repair and hacking)</td>
<td>55-95</td>
<td>Sump Pump Repair, Ghost Line, real Integrity risk, still no required combat</td>
</tr>
<tr class="odd">
<td>Closing set piece</td>
<td>95-115</td>
<td>Reef Breach (a hacking contract, not combat) or Drone Decommission via its stealth bypass</td>
</tr>
<tr class="even">
<td>Wrap-up</td>
<td>115-120</td>
<td>Turn-in, Debt Clock check, Backup Snapshot purchase before logout</td>
</tr>
</tbody>
</table>
<p>A session that skips Drone Decommission entirely, or takes its bypass, sees zero required combat encounters across the full 120 minutes. A session that takes it head-on sees exactly one.</p>
</section>
<section id="smallest-loop-implementation-status" class="level2">
<h2 class="anchored" data-anchor-id="smallest-loop-implementation-status">Smallest-loop implementation status</h2>
<p><code>zone-server-h2o</code> PR #5 lands the smallest real slice of this loop: two rooms only (Decanting Floor, Splicer’s Den), pure exploration, no items or NPCs. Not the full room graph or contract catalog above; those stay design, not yet built.</p>
<p>The MUD engine (<code>mud/guest/mud_guest.cpp</code>) previously served one hardcoded setting, Middleham. The PR adds a <code>domain</code> boot field (<code>"middleham"</code> default, <code>"the_gyre"</code> new) rather than a new engine: <code>MiddlehamStateMachine</code> keeps its Middleham behavior unchanged for the default domain, and a <code>gyre_room_templates()</code> table plus a <code>DOMAIN_THE_GYRE</code> branch in the constructor, <code>clone_rooms()</code>, and <code>objective_complete()</code> cover the new one. <code>mud/web/index.html</code>/<code>mud.js</code> get a mode selector, one <code>localStorage</code> session id per mode.</p>
<p>Verified: a native (non-riscv64) link of <code>mud_guest.cpp</code> driving <code>mud_boot()</code>/<code>mud_step()</code> through the whole Gyre loop, real narration text, <code>objective_complete()</code> true after both rooms are visited (<code>mud/guest/test/gyre_smoke_test.cpp</code>). The client change (mode selector, per-domain session id, <code>domain</code> field on the wire) was driven with real Playwright, both Chromium and Camoufox, against a throwaway local stub, before the real spec (<code>mud/web/test/gyre.spec.ts</code>) was written.</p>
<p>Not verified: a <code>riscv64-musl</code> + <code>libriscv</code> build/run of the guest code (no cross toolchain available when this was written). No real FDB/H2O build or deploy. <code>gyre.spec.ts</code> needs a real <code>MUD_BASE_URL</code> deployment to go green, matching <code>mud.spec.ts</code>’s own red-first convention; it has not run against one.</p>
</section>
<section id="open-questions" class="level2">
<h2 class="anchored" data-anchor-id="open-questions">Open questions</h2>
<ul>
<li>How does GitHub OAuth login map to a player profile the Progression core already models? Whether a GitHub identity becomes the profile’s primary key, or an added claim on an existing account scheme, is undecided.</li>
<li>What does a web client need from the transport layer that the SteamVR client does not, and does it reuse the same WebTransport path <code>zone-server</code> already speaks, or a separate HTTP/WebSocket adapter for browser clients without WebTransport support?</li>
<li>How do the “dice roll” mechanics for resolving risky actions (hacking, scavenging, and similar) work, mechanically, against the Combat core’s existing timed-hit validation model?</li>
<li>Whether the Debt Clock and Effort Pips need their own core, or fit inside the existing Progression and Budgeter cores, is undecided. Default to reusing Progression until a concrete need forces a new core.</li>
<li>How an NPC companion occupies a party slot in the Presence core is undecided: a server-driven pseudo-player with its own pose/state, or a lighter-weight attachment to the recruiting player’s own state. The choice affects whether a 2-player party with two NPC companions costs the same server resources as a 4-human party.</li>
</ul>


</section>

 ]]></description>
  <guid>https://v-sekai-multiplayer-fabric.github.io/manuals/rfd/0085-the-gyre-mud-setting-on-the-loot-action-shell/</guid>
  <pubDate>Fri, 07 Aug 2026 19:00:59 GMT</pubDate>
</item>
<item>
  <title>RFD 0084: zstd compression for batched zone-state FDB values</title>
  <link>https://v-sekai-multiplayer-fabric.github.io/manuals/rfd/0084-zstd-compression-for-zone-state/</link>
  <description><![CDATA[ 





<p><em><a href="../../index.html">Manuals</a> › RFD 0084: zstd compression for batched zone-state FDB values</em></p>
<p><strong>State:</strong> published · <strong>Scope:</strong> zone-server-h2o FDB value compression</p>
<section id="problem" class="level2">
<h2 class="anchored" data-anchor-id="problem">Problem</h2>
<p>Large value blobs, such as entity batches, zone snapshots, and asset content, wrote to FDB uncompressed. Uncompressed entity batches and zone snapshots take more FDB storage and bandwidth than needed. Compressing small fixed-size structs the same way would pay compression overhead that exceeds the savings.</p>
</section>
<section id="decision" class="level2">
<h2 class="anchored" data-anchor-id="decision">Decision</h2>
<p>Compress FDB values with zstd before writing, and decompress after reading. Apply this to large value blobs: entity batches, zone snapshots, asset content. Skip it for small fixed-size structs, where compression overhead exceeds the savings. The threshold is 512 bytes. Entity batches (~8KB) and zone snapshots compress 2 to 3 times at level 3. Asset blobs (~5MB) compress 3 to 5 times at level 19.</p>
<p>Single 40-byte <code>entity_t</code> structs and packed TPC-C rows stay uncompressed. Their high-entropy fields do not compress meaningfully, and a 40-byte struct adds decompression latency for no benefit. The zstd magic number (0x28B52FFD), not a flags byte, discriminates compressed values from raw ones. Old uncompressed values therefore still read correctly once compression rolls out. zstd is orthogonal to delta compression against a baseline, and the two compose, with zstd applied after delta encoding.</p>
</section>
<section id="references" class="level2">
<h2 class="anchored" data-anchor-id="references">References</h2>
<ul>
<li>Full API design, FDB value framing, compression-level table, dependency setup, and benchmark impact: <code>DETAILS.md</code></li>
<li>Original record: <code>decisions/20260806-zstd-compression-for-zone-state.md</code></li>
<li>Source: <code>weftspun/h2o-bench-tpcc</code>, <code>rfd/0016-zstd-compression.md</code></li>
<li>Consolidation target: <code>v-sekai-multiplayer-fabric/zone-server-h2o</code></li>
</ul>
</section>
<section id="related" class="level2">
<h2 class="anchored" data-anchor-id="related">Related</h2>
<ul>
<li><code>rfd/0074-binary-value-encoding-for-fdb</code>: the packed struct format zstd compresses.</li>
<li><code>rfd/0082-zonefabric-scaling</code>: the batched <code>zf/zone_state/</code> blob this compression targets.</li>
</ul>
</section>
<section id="detail" class="level2">
<h2 class="anchored" data-anchor-id="detail">Detail</h2>
</section>
<section id="rationale" class="level2">
<h2 class="anchored" data-anchor-id="rationale">Rationale</h2>
<p>mas-bandwidth/fps assumes 10x bandwidth reduction via delta compression against a baseline (RFD 0002). zstd provides general-purpose compression that complements delta compression:</p>
<ul>
<li><p>Entity batch writes: ZoneTick writes 200 entity_t structs in one FDB transaction. Packing 200 × 40-byte structs into a single 8KB blob and compressing with zstd level 3 typically achieves 2-3x reduction on positional data (floats have redundant exponent/mantissa bits when entities are spatially clustered).</p></li>
<li><p>Zone snapshots: ghost relevance queries return entity states for replication. Compressing the snapshot reduces both FDB read amplification and network bandwidth on the return path.</p></li>
<li><p>Asset content: assetcdn stores 5MB asset blobs. zstd level 19 achieves 3-5x on typical 3D model data (GLB, USD).</p></li>
</ul>
</section>
<section id="when-to-compress" class="level2">
<h2 class="anchored" data-anchor-id="when-to-compress">When to compress</h2>
<table class="caption-top table">
<colgroup>
<col style="width: 32%">
<col style="width: 15%">
<col style="width: 11%">
<col style="width: 6%">
<col style="width: 32%">
</colgroup>
<thead>
<tr class="header">
<th>Value type</th>
<th>Size</th>
<th>Compress?</th>
<th>Level</th>
<th>Expected ratio</th>
</tr>
</thead>
<tbody>
<tr class="odd">
<td>entity_t (single)</td>
<td>~40 bytes</td>
<td>No</td>
<td>—</td>
<td>1.0x (overhead &gt; savings)</td>
</tr>
<tr class="even">
<td>entity batch (200)</td>
<td>~8KB</td>
<td>Yes</td>
<td>3</td>
<td>2-3x</td>
</tr>
<tr class="odd">
<td>zone snapshot</td>
<td>~8-16KB</td>
<td>Yes</td>
<td>3</td>
<td>2-3x</td>
</tr>
<tr class="even">
<td>asset blob</td>
<td>~5MB</td>
<td>Yes</td>
<td>19</td>
<td>3-5x</td>
</tr>
<tr class="odd">
<td>TPC-C row (packed struct)</td>
<td>50-500 bytes</td>
<td>No</td>
<td>—</td>
<td>1.0x</td>
</tr>
<tr class="even">
<td>World table row</td>
<td>~10 bytes</td>
<td>No</td>
<td>—</td>
<td>1.0x</td>
</tr>
</tbody>
</table>
<p>Threshold: compress values ≥ 512 bytes. Below that, the 4-byte zstd frame header + compression CPU cost exceeds the savings.</p>
</section>
<section id="when-not-to-compress" class="level2">
<h2 class="anchored" data-anchor-id="when-not-to-compress">When NOT to compress</h2>
<ol type="1">
<li><p>Point reads with small values: FDB point reads on 40-byte entity_t structs, where decompression adds latency for zero benefit.</p></li>
<li><p>TPC-C transaction values: packed C structs (RFD 0010) are 50-500 bytes with high entropy (IDs, counters, decimal fields). zstd cannot compress high-entropy data meaningfully.</p></li>
<li><p>Hot path latency-sensitive reads: zstd decompression at level 3 adds ~1-2µs for 8KB. For the ZoneTick range scan (200 entities), this is negligible vs FDB’s 0.1-1ms read latency. For per-entity point reads in a 100Hz tick, it would add 200 × 2µs = 400µs per tick, so avoid it there.</p></li>
</ol>
</section>
<section id="api-design" class="level2">
<h2 class="anchored" data-anchor-id="api-design">API design</h2>
<div class="code-copy-outer-scaffold"><div class="sourceCode" id="cb1" style="background: #f1f3f5;"><pre class="sourceCode c code-with-copy"><code class="sourceCode c"><span id="cb1-1"><span class="co" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">// Compress a value blob before FDB write</span></span>
<span id="cb1-2"><span class="co" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">// Returns compressed size, or original size if below threshold</span></span>
<span id="cb1-3"><span class="dt" style="color: #AD0000;
background-color: null;
font-style: inherit;">size_t</span> zf_compress<span class="op" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">(</span><span class="dt" style="color: #AD0000;
background-color: null;
font-style: inherit;">const</span> <span class="dt" style="color: #AD0000;
background-color: null;
font-style: inherit;">void</span> <span class="op" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">*</span>src<span class="op" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">,</span> <span class="dt" style="color: #AD0000;
background-color: null;
font-style: inherit;">size_t</span> src_size<span class="op" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">,</span></span>
<span id="cb1-4">                   <span class="dt" style="color: #AD0000;
background-color: null;
font-style: inherit;">void</span> <span class="op" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">*</span>dst<span class="op" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">,</span> <span class="dt" style="color: #AD0000;
background-color: null;
font-style: inherit;">size_t</span> dst_capacity<span class="op" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">,</span> <span class="dt" style="color: #AD0000;
background-color: null;
font-style: inherit;">int</span> level<span class="op" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">);</span></span>
<span id="cb1-5"></span>
<span id="cb1-6"><span class="co" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">// Decompress a value blob after FDB read</span></span>
<span id="cb1-7"><span class="co" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">// Returns decompressed size</span></span>
<span id="cb1-8"><span class="dt" style="color: #AD0000;
background-color: null;
font-style: inherit;">size_t</span> zf_decompress<span class="op" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">(</span><span class="dt" style="color: #AD0000;
background-color: null;
font-style: inherit;">const</span> <span class="dt" style="color: #AD0000;
background-color: null;
font-style: inherit;">void</span> <span class="op" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">*</span>src<span class="op" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">,</span> <span class="dt" style="color: #AD0000;
background-color: null;
font-style: inherit;">size_t</span> src_size<span class="op" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">,</span></span>
<span id="cb1-9">                     <span class="dt" style="color: #AD0000;
background-color: null;
font-style: inherit;">void</span> <span class="op" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">*</span>dst<span class="op" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">,</span> <span class="dt" style="color: #AD0000;
background-color: null;
font-style: inherit;">size_t</span> dst_capacity<span class="op" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">);</span></span>
<span id="cb1-10"></span>
<span id="cb1-11"><span class="co" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">// Check if a value is zstd-compressed (magic number 0x28 0xB5 0x2F 0xFD)</span></span>
<span id="cb1-12"><span class="dt" style="color: #AD0000;
background-color: null;
font-style: inherit;">bool</span> zf_is_compressed<span class="op" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">(</span><span class="dt" style="color: #AD0000;
background-color: null;
font-style: inherit;">const</span> <span class="dt" style="color: #AD0000;
background-color: null;
font-style: inherit;">void</span> <span class="op" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">*</span>data<span class="op" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">,</span> <span class="dt" style="color: #AD0000;
background-color: null;
font-style: inherit;">size_t</span> size<span class="op" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">);</span></span></code></pre></div></div>
<p>The magic number check lets the reader handle both compressed and uncompressed values transparently — old uncompressed values still read correctly after compression is deployed.</p>
</section>
<section id="fdb-value-framing" class="level2">
<h2 class="anchored" data-anchor-id="fdb-value-framing">FDB value framing</h2>
<pre><code>┌──────────────────────────────────────┐
│ uint8_t flags                        │  bit 0: is_compressed
│                                      │  bits 1-7: reserved
├──────────────────────────────────────┤
│ if compressed: zstd frame (variable) │
│ if not: raw value bytes              │
└──────────────────────────────────────┘</code></pre>
<p>A 1-byte flags header precedes every value. bit 0 indicates whether the remaining bytes are a zstd frame or raw data. This is backward- compatible: the flags byte is 0x00 for uncompressed, which existing readers interpret as the first byte of the struct (safe because all packed structs start with a non-zero field like an ID).</p>
<p>Wait — backward compatibility is not safe if struct’s first byte can be 0x00. Alternative: use the zstd magic number (0x28B52FFD) as the discriminator instead of a flags byte. Readers check the first 4 bytes:</p>
<div class="code-copy-outer-scaffold"><div class="sourceCode" id="cb3" style="background: #f1f3f5;"><pre class="sourceCode c code-with-copy"><code class="sourceCode c"><span id="cb3-1"><span class="dt" style="color: #AD0000;
background-color: null;
font-style: inherit;">bool</span> zf_is_compressed<span class="op" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">(</span><span class="dt" style="color: #AD0000;
background-color: null;
font-style: inherit;">const</span> <span class="dt" style="color: #AD0000;
background-color: null;
font-style: inherit;">void</span> <span class="op" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">*</span>data<span class="op" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">,</span> <span class="dt" style="color: #AD0000;
background-color: null;
font-style: inherit;">size_t</span> size<span class="op" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">)</span> <span class="op" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">{</span></span>
<span id="cb3-2">    <span class="cf" style="color: #003B4F;
background-color: null;
font-weight: bold;
font-style: inherit;">if</span> <span class="op" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">(</span>size <span class="op" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">&lt;</span> <span class="dv" style="color: #AD0000;
background-color: null;
font-style: inherit;">4</span><span class="op" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">)</span> <span class="cf" style="color: #003B4F;
background-color: null;
font-weight: bold;
font-style: inherit;">return</span> <span class="kw" style="color: #003B4F;
background-color: null;
font-weight: bold;
font-style: inherit;">false</span><span class="op" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">;</span></span>
<span id="cb3-3">    <span class="dt" style="color: #AD0000;
background-color: null;
font-style: inherit;">uint32_t</span> magic<span class="op" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">;</span></span>
<span id="cb3-4">    memcpy<span class="op" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">(&amp;</span>magic<span class="op" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">,</span> data<span class="op" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">,</span> <span class="dv" style="color: #AD0000;
background-color: null;
font-style: inherit;">4</span><span class="op" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">);</span></span>
<span id="cb3-5">    <span class="cf" style="color: #003B4F;
background-color: null;
font-weight: bold;
font-style: inherit;">return</span> magic <span class="op" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">==</span> <span class="bn" style="color: #AD0000;
background-color: null;
font-style: inherit;">0xFD2FB528</span><span class="op" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">;</span> <span class="co" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">// zstd magic, little-endian</span></span>
<span id="cb3-6"><span class="op" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">}</span></span></code></pre></div></div>
<p>This requires no framing byte and is transparent to existing code. Values that happen to start with the zstd(0x28B52FFD) magic are vanishingly unlikely for packed structs whose first field is an ID.</p>
</section>
<section id="compression-levels" class="level2">
<h2 class="anchored" data-anchor-id="compression-levels">Compression levels</h2>
<table class="caption-top table">
<thead>
<tr class="header">
<th>Level</th>
<th>Use case</th>
<th>Speed</th>
<th>Ratio</th>
</tr>
</thead>
<tbody>
<tr class="odd">
<td>1</td>
<td>Hot path (ZoneTick batch)</td>
<td>~1GB/s</td>
<td>2x</td>
</tr>
<tr class="even">
<td>3</td>
<td>Default (zone snapshot)</td>
<td>~800MB/s</td>
<td>2-3x</td>
</tr>
<tr class="odd">
<td>9</td>
<td>Warm path (asset upload)</td>
<td>~200MB/s</td>
<td>3-4x</td>
</tr>
<tr class="even">
<td>19</td>
<td>Cold path (asset CDN seed)</td>
<td>~20MB/s</td>
<td>3-5x</td>
</tr>
</tbody>
</table>
<p>zstd level 3 at ~800MB/s is not a bottleneck for 8KB values: compression takes ~10µs, negligible vs FDB’s 1.5-2.5ms commit latency.</p>
</section>
<section id="dependency" class="level2">
<h2 class="anchored" data-anchor-id="dependency">Dependency</h2>
<p>zstd is a single C library (<code>libzstd</code>), available as a system package (<code>libzstd-dev</code> on Ubuntu) or vendored as a single <code>zstd.h</code> + <code>zstd.c</code> amalgamation. No external runtime dependency, no JVM, no Python.</p>
<p>CMake:</p>
<div class="code-copy-outer-scaffold"><div class="sourceCode" id="cb4" style="background: #f1f3f5;"><pre class="sourceCode cmake code-with-copy"><code class="sourceCode cmake"><span id="cb4-1"><span class="kw" style="color: #003B4F;
background-color: null;
font-weight: bold;
font-style: inherit;">find_package</span>(zstd <span class="ot" style="color: #003B4F;
background-color: null;
font-style: inherit;">REQUIRED</span>)</span>
<span id="cb4-2"><span class="kw" style="color: #003B4F;
background-color: null;
font-weight: bold;
font-style: inherit;">target_link_libraries</span>(<span class="bn" style="color: #AD0000;
background-color: null;
font-style: inherit;">h2o-bench-tpcc</span> <span class="ot" style="color: #003B4F;
background-color: null;
font-style: inherit;">PRIVATE</span> <span class="bn" style="color: #AD0000;
background-color: null;
font-style: inherit;">zstd::zstd</span>)</span></code></pre></div></div>
<p>Dockerfile:</p>
<div class="code-copy-outer-scaffold"><div class="sourceCode" id="cb5" style="background: #f1f3f5;"><pre class="sourceCode dockerfile code-with-copy"><code class="sourceCode dockerfile"><span id="cb5-1">apt-get install -y libzstd-dev</span></code></pre></div></div>
</section>
<section id="benchmark-impact" class="level2">
<h2 class="anchored" data-anchor-id="benchmark-impact">Benchmark impact</h2>
<p>For zonefabric ZoneTick:</p>
<ul>
<li>Without compression: 200 × 40 bytes = 8KB per zone write</li>
<li>With zstd level 3: ~3-4KB per zone write (2-3x reduction)</li>
<li>FDB commit latency unchanged (dominated by fsync, not value size)</li>
<li>Network bandwidth on snapshot delivery: 2-3x more zones per Gbit/sec</li>
</ul>
<p>For assetcdn:</p>
<ul>
<li>Without compression: 5MB per asset read</li>
<li>With zstd level 19: ~1-1.5MB per asset read (3-5x reduction)</li>
<li>FDB read latency: reduced proportional to value size (less data to transfer from storage server to client)</li>
</ul>
</section>
<section id="relationship-to-mas-bandwidthfps" class="level2">
<h2 class="anchored" data-anchor-id="relationship-to-mas-bandwidthfps">Relationship to mas-bandwidth/fps</h2>
<p>Glenn Fiedler assumes delta compression (10x) for snapshot delivery. zstd is orthogonal — it compresses the current state, not the delta. The two compose:</p>
<pre><code>snapshot = zstd_compress(delta_encode(current, baseline))</code></pre>
<p>zstd on deltas achieves even higher ratios because deltas are sparse (most fields unchanged → zero bytes compress extremely well). This RFD covers zstd only; delta encoding is a future RFD.</p>
</section>
<section id="relationship-to-rfd-0010-binary-value-encoding" class="level2">
<h2 class="anchored" data-anchor-id="relationship-to-rfd-0010-binary-value-encoding">Relationship to RFD 0010 (binary value encoding)</h2>
<p>RFD 0010 mandates packed C structs with <code>#pragma pack(push, 1)</code>. zstd operates on the serialized byte stream, not the struct. The pipeline:</p>
<ol type="1">
<li>Pack struct to byte array (RFD 0010)</li>
<li>zstd compress byte array (this RFD)</li>
<li>FDB transaction set key → compressed bytes</li>
<li>FDB future get key → compressed bytes</li>
<li>zstd decompress → byte array</li>
<li>Cast byte array to struct (RFD 0010 zero-copy)</li>
</ol>
<p>Steps 2 and 5 are conditional on value size ≥ 512 bytes.</p>


</section>

 ]]></description>
  <guid>https://v-sekai-multiplayer-fabric.github.io/manuals/rfd/0084-zstd-compression-for-zone-state/</guid>
  <pubDate>Fri, 07 Aug 2026 19:00:59 GMT</pubDate>
</item>
<item>
  <title>RFD 0083: Replace FabricZone (Godot) with zone-server-h2o (libh2o + FDB + Fil-C)</title>
  <link>https://v-sekai-multiplayer-fabric.github.io/manuals/rfd/0083-zone-server-h2o-replaces-godot-fabriczone/</link>
  <description><![CDATA[ 





<p><em><a href="../../index.html">Manuals</a> › RFD 0083: Replace FabricZone (Godot) with zone-server-h2o (libh2o + FDB + Fil-C)</em></p>
<p><strong>State:</strong> published · <strong>Scope:</strong> zone server implementation</p>
<section id="problem" class="level2">
<h2 class="anchored" data-anchor-id="problem">Problem</h2>
<p>The Godot <code>FabricZone</code> engine ran the zone server side. <code>FabricZone</code>/<code>FabricZoneJournal</code> stored its journal in local SQLite, so state did not share across zone-server processes. The project also needed memory safety against untrusted client input, which the existing engine did not give.</p>
</section>
<section id="decision" class="level2">
<h2 class="anchored" data-anchor-id="decision">Decision</h2>
<p>Build <code>zone-server-h2o</code>, a native <code>libh2o</code> and FoundationDB zone server. Build it with Fil-C for memory safety against untrusted client input. It fully replaces the Godot <code>FabricZone</code> engine on the server side. The client stays Godot, unchanged. Scope carries over from <code>FabricZone</code>/<code>FabricZoneJournal</code>, not a blank-slate design: entity slots, zone-to-zone migration, ghost/AOI state, and a durable journal. The team reimplements these against FDB instead of local SQLite, so they share state across zone-server processes.</p>
<p>Physics and IK port from <code>sinew-mocap/solve</code>. Entity and ReBAC types generate from <code>lean-entity-packet</code> and <code>lean-rebac-core</code>, instead of hand-duplicating them across the retired engine, the new code, and any client-facing schema. The first working milestone is a WebTransport datagram round-trip plus a bare <code>ZoneTick</code>. The team builds everything else after that milestone works. <code>weftspun/h2o-bench-tpcc</code> gets archived once its reusable infrastructure and zonefabric design get ported. Its accepted TPC-C benchmark work stays unaffected, read-only, in place.</p>
</section>
<section id="references" class="level2">
<h2 class="anchored" data-anchor-id="references">References</h2>
<ul>
<li>Full context, consequences, and the list of ported RFDs: <code>DETAILS.md</code></li>
<li>Original record: <code>decisions/20260806-zone-server-h2o-replaces-godot-fabriczone.md</code></li>
<li><code>v-sekai-multiplayer-fabric/zone-server-h2o</code></li>
</ul>
</section>
<section id="related" class="level2">
<h2 class="anchored" data-anchor-id="related">Related</h2>
<p><code>rfd/0082-zonefabric-scaling</code>, <code>rfd/0072-actor-lite-worker-pool</code>, and the other <code>rfd/0073</code>-<code>0081</code>, <code>0084</code> records carried forward from <code>weftspun/h2o-bench-tpcc</code>.</p>
</section>
<section id="detail" class="level2">
<h2 class="anchored" data-anchor-id="detail">Detail</h2>
</section>
<section id="context" class="level2">
<h2 class="anchored" data-anchor-id="context">Context</h2>
<p>The production zone server (<code>zone-server</code>, deployed as <code>multiplayer-fabric-zone</code> on Fly.io) is a boot scaffold today — OpenTelemetry init only, no WebTransport listener, no game logic (<code>project/main.gd</code>’s own <code>TODO(cycle-5)</code> comment). The real entity/simulation engine, <code>FabricZone</code>/<code>FabricZoneJournal</code>/<code>FabricMMOGZone</code>, exists as a working Godot C++ module in <code>V-Sekai-fire/multiplayer-fabric-build</code> (<code>godot/modules/multiplayer_fabric/</code>) but has never been wired into a deployed zone server. Separately, <code>weftspun/h2o-bench-tpcc</code> designed (RFD 0002, <code>discussion</code> state, never implemented) a <code>libh2o</code> + FoundationDB “zonefabric” scenario modeling the same hub/instanced-zone shape — entity authority via a Hilbert-curve partition, ghost/AOI interest management — independently of this org’s actual zone-server work.</p>
</section>
<section id="consequences" class="level2">
<h2 class="anchored" data-anchor-id="consequences">Consequences</h2>
<ul>
<li>The Godot zone-server deployment (<code>zone-server</code> repo, Fly app <code>multiplayer-fabric-zone</code>) is retired once <code>zone-server-h2o</code> passes equivalent verification, in favor of the new implementation.</li>
<li><code>zone-backend</code> (Uro) is unaffected: it still only does identity, zone directory, asset storage, and ReBAC, and still never sees per-entity data in the live session.</li>
<li>CockroachDB’s scope stays account-level (identity, zones directory, avatars). The one exception under discussion — inventory/profile commits, previously headed toward CockroachDB via the progression hexagon’s SQLite “commit valve” — targets FDB instead, as the single write path for everything the zone server owns.</li>
<li>The related, previously-unimplemented <code>weftspun/h2o-bench-tpcc</code> RFDs this decision carries forward are filed alongside this entry, dated the same day: zonefabric scaling, actor-lite worker pool, FDB selection, verification strategy, binary value encoding, async FDB callback chain, zstd compression, slotmap entity storage, macaroon/XDP security, feature ablation, and the PERT critical path.</li>
</ul>
</section>
<section id="ported-rfds" class="level2">
<h2 class="anchored" data-anchor-id="ported-rfds">Ported RFDs</h2>
<p>The following RFDs in this repo carry forward <code>weftspun/h2o-bench-tpcc</code>’s zonefabric design, filed the same day as this decision:</p>
<ul>
<li><code>rfd/0082-zonefabric-scaling</code></li>
<li><code>rfd/0072-actor-lite-worker-pool</code></li>
<li><code>rfd/0075-fdb-over-cockroachdb-for-zone-state</code></li>
<li><code>rfd/0081-three-layer-verification-strategy</code></li>
<li><code>rfd/0074-binary-value-encoding-for-fdb</code></li>
<li><code>rfd/0073-async-fdb-callback-chain</code></li>
<li><code>rfd/0084-zstd-compression-for-zone-state</code></li>
<li><code>rfd/0080-slotmap-entity-storage</code></li>
<li><code>rfd/0076-macaroon-xdp-security</code></li>
<li><code>rfd/0078-plausible-witness-dag-feature-ablation</code></li>
<li><code>rfd/0077-pert-critical-path-zonefabric</code></li>
</ul>
</section>
<section id="related-1" class="level2">
<h2 class="anchored" data-anchor-id="related-1">Related</h2>
<ul>
<li><code>v-sekai-multiplayer-fabric/zone-server-h2o</code></li>
<li><code>V-Sekai-fire/multiplayer-fabric-build</code>, <code>godot/modules/multiplayer_fabric/</code></li>
<li><code>sinew-mocap/solve</code>, <code>lean-humanoid-rom</code>, <code>swing-twist-kusudama</code></li>
<li><code>lean-entity-packet</code>, <code>lean-rebac-core</code></li>
</ul>


</section>

 ]]></description>
  <guid>https://v-sekai-multiplayer-fabric.github.io/manuals/rfd/0083-zone-server-h2o-replaces-godot-fabriczone/</guid>
  <pubDate>Fri, 07 Aug 2026 19:00:59 GMT</pubDate>
</item>
<item>
  <title>RFD 0082: Zonefabric scaling: hub/instanced-zone game-server benchmark shape</title>
  <link>https://v-sekai-multiplayer-fabric.github.io/manuals/rfd/0082-zonefabric-scaling/</link>
  <description><![CDATA[ 





<p><em><a href="../../index.html">Manuals</a> › RFD 0082: Zonefabric scaling: hub/instanced-zone game-server benchmark shape</em></p>
<p><strong>State:</strong> published · <strong>Scope:</strong> zone-server-h2o benchmark and keyspace design</p>
<section id="problem" class="level2">
<h2 class="anchored" data-anchor-id="problem">Problem</h2>
<p>Glenn Fiedler’s mas-bandwidth/fps architecture calls for a “world database” and leaves it unbuilt. A per-entity keyspace also costs 200 times more FDB keys than a per-zone keyspace. Full-mesh O(N²) broadcast also does not scale replication across many zones.</p>
</section>
<section id="decision" class="level2">
<h2 class="anchored" data-anchor-id="decision">Decision</h2>
<p>Model weft-warp-loop’s hub/instanced-zone game server as zonefabric: a FoundationDB-backed benchmark scaled by zone count, with 200 entities per zone at fixed constants (WORLD_EXTENT=10000.0, GHOST_RANGE=150.0, AUTHORITY_CAPACITY=256, SPLIT_COST_THRESHOLD=40000.0). Persistence uses a single zstd-compressed <code>zf/zone_state/{z_id}</code> blob per zone, per 10Hz tick, holding the slotmap and effect state. This cuts FDB key count 200-fold against the per-entity keyspace, which the design retains only for migration, ghost-relevance range scans, and point lookups. Zonefabric is the “world database” that Glenn Fiedler’s mas-bandwidth/fps architecture calls for and leaves unbuilt. It builds this on FDB instead of Redis, with AOI-filtered replication (GHOST_RANGE covers 0.02 percent of world space per zone) in place of full-mesh O(N²) broadcast. Per-zone independent FDB transactions and thread-local slotmaps give near-linear core scaling, with no cross-zone conflicts.</p>
</section>
<section id="references" class="level2">
<h2 class="anchored" data-anchor-id="references">References</h2>
<ul>
<li>Full FDB keyspace design, runtime operation costs, benchmark comparison matrix, and mas-bandwidth/fps concept mapping: <code>DETAILS.md</code></li>
<li>Original record: <code>decisions/20260806-zonefabric-scaling.md</code></li>
<li>Source: <code>weftspun/h2o-bench-tpcc</code>, <code>rfd/0002-zonefabric-scaling.md</code></li>
<li>Glenn Fiedler, “Creating a first person shooter that scales to millions of players”: https://mas-bandwidth.com/creating-a-first-person-shooter-that-scales-to-millions-of-players/</li>
</ul>
</section>
<section id="related" class="level2">
<h2 class="anchored" data-anchor-id="related">Related</h2>
<p><code>rfd/0080-slotmap-entity-storage</code>, <code>rfd/0084-zstd-compression-for-zone-state</code> (the batched blob’s two layers), <code>rfd/0076-macaroon-xdp-security</code> (unmeasured transport layer), <code>rfd/0001-zonefabric-roadmap-vs-mas-bandwidth-fps</code>.</p>
</section>
<section id="detail" class="level2">
<h2 class="anchored" data-anchor-id="detail">Detail</h2>
<p><strong>Scale knob:</strong> zone count (direct, no multiplier) <strong>Source:</strong> weftspun/scenario-tpcc-bench PR #2</p>
<p>Models weft-warp-loop’s hub/instanced-zone game server. Each zone has 200 entities with uniform-random position/velocity. EFFECT_ENTITY and FANOUT_TARGET are runtime-only (produced by CastSpell, not seeded at load).</p>
</section>
<section id="tables" class="level2">
<h2 class="anchored" data-anchor-id="tables">Tables</h2>
<table class="caption-top table">
<thead>
<tr class="header">
<th>Table</th>
<th>Rows</th>
<th>Formula</th>
</tr>
</thead>
<tbody>
<tr class="odd">
<td>ZONE</td>
<td>scalefactor</td>
<td>1 per zone</td>
</tr>
<tr class="even">
<td>ENTITY</td>
<td>scalefactor × 200</td>
<td>200 per zone</td>
</tr>
<tr class="odd">
<td>EFFECT_ENTITY</td>
<td>0 at load</td>
<td>runtime-only</td>
</tr>
<tr class="even">
<td>FANOUT_TARGET</td>
<td>0 at load</td>
<td>runtime-only</td>
</tr>
</tbody>
</table>
<p>Fixed constants: ENTITIES_PER_ZONE=200, WORLD_EXTENT=10000.0, GHOST_RANGE=150.0, AUTHORITY_CAPACITY=256, INTEREST_CAPACITY=512, SPLIT_COST_THRESHOLD=40000.0. Uniform (Flat) random for entity attributes. No skewed zone selection at load — any skew comes from runtime workload.</p>
</section>
<section id="fdb-keyspace-design" class="level2">
<h2 class="anchored" data-anchor-id="fdb-keyspace-design">FDB keyspace design</h2>
<section id="per-entity-keyspace-current-granular" class="level3">
<h3 class="anchored" data-anchor-id="per-entity-keyspace-current-granular">Per-entity keyspace (current, granular)</h3>
<pre><code>zf/zone/{z_id}                         -&gt; packed zone_t (authority_cap, interest_cap, cost, population)
zf/entity/{z_id}/{e_id}                -&gt; packed entity_t (x, y, vx, vy, rtt_ms)
zf/entity_pos/{z_id}                   -&gt; range: all entities in zone (for tick scan)
zf/effect/{ee_id}                      -&gt; packed effect_entity_t (source_id, zone_id, x, y)
zf/fanout/{ee_id}/{e_id}               -&gt; empty value (presence = fanout target)</code></pre>
</section>
<section id="zone-state-keyspace-slotmap-zstd-batched" class="level3">
<h3 class="anchored" data-anchor-id="zone-state-keyspace-slotmap-zstd-batched">Zone-state keyspace (slotmap + zstd, batched)</h3>
<p>With slotmap entity storage (RFD 0017) and zstd compression (RFD 0016), the per-entity keyspace is superseded for persistence by a single batched zone-state blob per zone per persistence tick:</p>
<pre><code>zf/zone_state/{z_id}                   -&gt; zstd-compressed binary blob containing:
                                            - zone_t header (cost, population, authority_cap, interest_cap)
                                            - Slotmap&lt;EntityID, entity_t&gt;  (dense array + generation counters)
                                            - Slotmap&lt;EffectID, effect_entity_t&gt; (runtime effects, if any)
                                            - FANOUT_TARGET adjacency list (effect_id → [entity_id...])</code></pre>
<p>This reduces 200+ FDB keys per zone to 1 key per zone. At 10,000 zones, that’s 10,000 keys instead of 2,000,000 — a 200x reduction in FDB key count and transaction conflict surface.</p>
<p><strong>Generational IDs survive serialization.</strong> The slotmap’s (index, version) pairs are stored inside the blob. On load after a crash, all EFFECT_ENTITY and FANOUT_TARGET references resolve correctly because the generation counters are part of the persisted state. A fireball aimed at Entity(index=12, gen=1) will safely fizzle if Entity 12 was destroyed (gen bumped to 2) before the fireball resolved — the slotmap lookup returns NULL, no dangling reference, no hitting the wrong player.</p>
<p><strong>Per-entity keyspace retained for:</strong></p>
<ul>
<li>Entity migration between zones (point read + clear + set)</li>
<li>Ghost relevance queries (range scan by zone prefix)</li>
<li>Real-time point lookups (CastSpell source entity)</li>
</ul>
<p>Zone queries are FDB range scans over <code>zf/entity/{z_id}/</code> prefixes. At 200 entities/zone, each range read returns ~200 KV pairs packed as binary structs (RFD 0010). No SQL, no parsing — zero-copy struct cast.</p>
<p>The batched <code>zf/zone_state/</code> key is used for periodic persistence (every 100ms = 10Hz), not per-tick writes. Per-tick computation uses the in-memory slotmap only.</p>
</section>
</section>
<section id="runtime-operations" class="level2">
<h2 class="anchored" data-anchor-id="runtime-operations">Runtime operations</h2>
<section id="zonetick-read-heavy-per-zone-per-tick" class="level3">
<h3 class="anchored" data-anchor-id="zonetick-read-heavy-per-zone-per-tick">ZoneTick (read-heavy, per-zone, per-tick)</h3>
<ol type="1">
<li>Range scan <code>zf/entity/{z_id}/</code> → 200 entity_t structs</li>
<li>Update position: <code>x += vx * dt</code>, <code>y += vy * dt</code> (in C, no FDB)</li>
<li>Batch write updated entities (1 transaction, 200 sets)</li>
<li>If zone population² &gt; SPLIT_COST_THRESHOLD, emit split event</li>
</ol>
<p><strong>Cost</strong>: 1 range read (200 KV pairs) + 1 commit (200 sets) per zone per tick.</p>
</section>
<section id="castspell-write-heavy-fanout" class="level3">
<h3 class="anchored" data-anchor-id="castspell-write-heavy-fanout">CastSpell (write-heavy, fanout)</h3>
<ol type="1">
<li>Read source entity (1 point read)</li>
<li>Range scan entities within GHOST_RANGE of spell position</li>
<li>Insert effect_entity row</li>
<li>Insert fanout_target rows for each entity in range</li>
<li>Commit</li>
</ol>
<p><strong>Cost</strong>: 1 point read + 1 range read (~20 entities within 150.0 range)</p>
<ul>
<li>1 effect insert + ~20 fanout inserts + 1 commit.</li>
</ul>
</section>
<section id="ghostrelevance-read-only-interest-management" class="level3">
<h3 class="anchored" data-anchor-id="ghostrelevance-read-only-interest-management">GhostRelevance (read-only, interest management)</h3>
<ol type="1">
<li>Range scan entities within GHOST_RANGE of zone boundary</li>
<li>Return entity IDs and positions to requesting zone</li>
</ol>
<p><strong>Cost</strong>: 1 range read (~10 entities near boundary).</p>
</section>
</section>
<section id="comparison-matrix" class="level2">
<h2 class="anchored" data-anchor-id="comparison-matrix">Comparison matrix</h2>
<p>Zonefabric is a novel workload — there is no TechEmpower benchmark for game server zone scaling. Each operation is mapped to its closest existing benchmark with the specific numbers we need to beat:</p>
<table class="caption-top table">
<colgroup>
<col style="width: 22%">
<col style="width: 23%">
<col style="width: 23%">
<col style="width: 30%">
</colgroup>
<thead>
<tr class="header">
<th>Zonefabric operation</th>
<th>Closest benchmark</th>
<th>Reference</th>
<th>Our target</th>
</tr>
</thead>
<tbody>
<tr class="odd">
<td>ZoneTick (200 entities)</td>
<td>EnTT ECS 10K entities/7 systems</td>
<td>350µs total</td>
<td>&lt;10µs (200 entities, 1 system)</td>
</tr>
<tr class="even">
<td>Zone query (FDB range scan)</td>
<td>FDB range read</td>
<td>3.6M keys/sec/core</td>
<td>18,000 zone queries/sec/core</td>
</tr>
<tr class="odd">
<td>Entity position update</td>
<td>FDB batch set + commit</td>
<td>35K writes/sec/core</td>
<td>175 zone ticks/sec/core (200 writes each)</td>
</tr>
<tr class="even">
<td>CastSpell effect fanout</td>
<td>FDB read + write mix</td>
<td>90K reads + 35K writes/sec/core</td>
<td>~1,000 casts/sec/core</td>
</tr>
<tr class="odd">
<td>Ghost relevance query</td>
<td>QuickZone LBVH spatial query</td>
<td>O(N log Z), 1M zones</td>
<td>O(N) per zone (fixed 200)</td>
</tr>
<tr class="even">
<td>Entity replication (full mesh)</td>
<td>Arcane full-mesh broadcast</td>
<td>2,750 CCU @ 60Hz, 8×c6in.4xlarge</td>
<td>&gt;5,000 CCU (AOI-filtered, not full-mesh)</td>
</tr>
</tbody>
</table>
<section id="arcane-comparison-closest-game-server-benchmark" class="level3">
<h3 class="anchored" data-anchor-id="arcane-comparison-closest-game-server-benchmark">Arcane comparison (closest game-server benchmark)</h3>
<p><a href="https://github.com/brainy-bots/arcane-scaling-benchmarks">brainy-bots/arcane-scaling-benchmarks</a> measured 2,750 CCU at 60Hz with &lt;31ms latency on 8×c6in.4xlarge (16 vCPU each, 50 Gbps NIC). The ceiling was <strong>NIC saturation</strong> from O(N²) full-mesh broadcast bandwidth.</p>
<p>Zonefabric’s advantage: GHOST_RANGE=150.0 in a 10000.0 world means each zone’s interest bubble covers ~0.02% of world space. Replication scales as O(N × ghost_entities) not O(N²). With AOI filtering, we should sustain significantly higher CCU per core.</p>
</section>
<section id="fdb-baseline-our-storage-layer" class="level3">
<h3 class="anchored" data-anchor-id="fdb-baseline-our-storage-layer">FDB baseline (our storage layer)</h3>
<p>FDB 7.3 official numbers, directly comparable since we use the same C API:</p>
<table class="caption-top table">
<colgroup>
<col style="width: 40%">
<col style="width: 27%">
<col style="width: 32%">
</colgroup>
<thead>
<tr class="header">
<th>Workload</th>
<th>Single core</th>
<th>12-machine (48 cores)</th>
</tr>
</thead>
<tbody>
<tr class="odd">
<td>Reads (memory engine)</td>
<td>90,000/sec</td>
<td>5,540,000/sec</td>
</tr>
<tr class="even">
<td>Writes (memory engine)</td>
<td>35,000/sec</td>
<td>720,000/sec</td>
</tr>
<tr class="odd">
<td>Range scans</td>
<td>3,600,000 keys/sec</td>
<td>—</td>
</tr>
<tr class="even">
<td>90/10 mixed</td>
<td>—</td>
<td>2,390,000 ops/sec</td>
</tr>
<tr class="odd">
<td>Read latency (&lt;75% load)</td>
<td>0.1-1ms</td>
<td>0.1-1ms</td>
</tr>
<tr class="even">
<td>Commit latency (&lt;75% load)</td>
<td>1.5-2.5ms</td>
<td>1.5-2.5ms</td>
</tr>
</tbody>
</table>
</section>
<section id="ecs-iteration-baseline" class="level3">
<h3 class="anchored" data-anchor-id="ecs-iteration-baseline">ECS iteration baseline</h3>
<p>From <a href="https://github.com/abeimler/ecs_benchmark">abeimler/ecs_benchmark</a>, EnTT iterates 10K entities with 7 systems in 350µs. Zonefabric’s tick is 200 entities with 1 system (position+velocity update), which extrapolates to ~7µs — negligible compared to FDB commit latency.</p>
<p>The entity update is NOT the bottleneck. The <strong>network replication</strong> and <strong>FDB transaction commit</strong> are the binding constraints.</p>
</section>
</section>
<section id="benchmark-methodology" class="level2">
<h2 class="anchored" data-anchor-id="benchmark-methodology">Benchmark methodology</h2>
<section id="scaling-test-zone-count-vs-throughput" class="level3">
<h3 class="anchored" data-anchor-id="scaling-test-zone-count-vs-throughput">Scaling test: zone count vs throughput</h3>
<p>Run ZoneTick at varying zone counts (1, 10, 100, 1000, 10000) with fixed 200 entities/zone. Measure:</p>
<ol type="1">
<li>Zone ticks/sec: how many zones can be ticked per second</li>
<li>Entity updates/sec: zone ticks × 200</li>
<li>FDB ops/sec: range reads + writes</li>
<li>P50/P99 latency: per zone tick</li>
<li>Core scaling: 1, 2, 4, 8, 16 threads</li>
</ol>
<p>Expected: linear scaling until FDB commit latency becomes the bottleneck (~2ms per commit → max ~500 zone ticks/sec/core with writes).</p>
</section>
<section id="castspell-stress-test" class="level3">
<h3 class="anchored" data-anchor-id="castspell-stress-test">CastSpell stress test</h3>
<p>Sustained CastSpell at varying rates. Measure fanout latency and throughput as effect count grows.</p>
<p>Expected: CastSpell is write-heavy (1 effect + ~20 fanouts per cast). At 35K writes/sec/core, max ~1,670 casts/sec/core (21 writes per cast).</p>
</section>
<section id="ghost-relevance-test" class="level3">
<h3 class="anchored" data-anchor-id="ghost-relevance-test">Ghost relevance test</h3>
<p>Measure ghost entity query latency as zone count grows. Ghost range is fixed at 150.0, so the number of ghost entities per zone boundary is constant (~10-20), regardless of total zone count.</p>
<p>Expected: O(1) per query, independent of zone count.</p>
</section>
</section>
<section id="what-zonefabric-measures-that-existing-benchmarks-dont" class="level2">
<h2 class="anchored" data-anchor-id="what-zonefabric-measures-that-existing-benchmarks-dont">What zonefabric measures that existing benchmarks don’t</h2>
<ol type="1">
<li><p>Spatial locality: zone-scoped queries hit FDB range scans, not random point reads. This exercises FDB’s range scan path (3.6M keys/sec) rather than point reads (90K/sec).</p></li>
<li><p>Write amplification: each zone tick writes 200 KV pairs in one transaction. This measures FDB’s batch commit throughput, not single-key write throughput.</p></li>
<li><p>Fanout factor: CastSpell produces variable fanout (entities within ghost range). This is a read-amplify-write pattern not captured by TPC-C or YCSB.</p></li>
<li><p>AOI-filtered replication: ghost relevance queries are spatial range scans, not full-table scans. This is the key differentiator from Arcane’s full-mesh broadcast.</p></li>
<li><p>Core-scaling efficiency: with per-zone FDB transactions, each core processes independent zones. No cross-zone conflicts (entities belong to exactly one zone). This should give near-linear core scaling, unlike TPC-C where district-level conflicts cause retries.</p></li>
</ol>
</section>
<section id="bridge-to-mas-bandwidthfps" class="level2">
<h2 class="anchored" data-anchor-id="bridge-to-mas-bandwidthfps">Bridge to mas-bandwidth/fps</h2>
<p>Glenn Fiedler’s <a href="https://mas-bandwidth.com/creating-a-first-person-shooter-that-scales-to-millions-of-players/">“Creating a first person shooter that scales to millions of players”</a> proposes a three-layer architecture: player servers, world servers, and a “world database” (Redis-like, async, in-memory). He explicitly states: <em>“somebody just needs to make this world database”</em> and <em>“creating this world database would be a fantastic open source project.”</em></p>
<p>Zonefabric IS that world database, implemented on FDB instead of Redis.</p>
<section id="concept-mapping" class="level3">
<h3 class="anchored" data-anchor-id="concept-mapping">Concept mapping</h3>
<table class="caption-top table">
<colgroup>
<col style="width: 37%">
<col style="width: 28%">
<col style="width: 34%">
</colgroup>
<thead>
<tr class="header">
<th>mas-bandwidth/fps concept</th>
<th>Zonefabric implementation</th>
<th>Why FDB</th>
</tr>
</thead>
<tbody>
<tr class="odd">
<td><strong>World database</strong> (Redis-like, async)</td>
<td>FDB as the world database</td>
<td>ACID transactions, range scans, durability</td>
</tr>
<tr class="even">
<td><strong>World server grid</strong> (10km × 10km, 1km² cells)</td>
<td>ZONE rows with entity subspace per zone</td>
<td>FDB key prefix = spatial partition</td>
</tr>
<tr class="odd">
<td><strong>Player state history</strong> (100 bytes × 100Hz × 10K players = 100MB)</td>
<td>ENTITY rows with x, y, vx, vy, rtt_ms</td>
<td>FDB range scan returns packed structs</td>
</tr>
<tr class="even">
<td><strong>“Raycast and find first hit”</strong></td>
<td>CastSpell with GHOST_RANGE scan</td>
<td>FDB range read over entity positions</td>
</tr>
<tr class="odd">
<td><strong>“Find players in volume”</strong></td>
<td>GhostRelevance spatial range query</td>
<td>FDB range scan filtered by ghost range</td>
</tr>
<tr class="even">
<td><strong>“Apply damage to entity”</strong></td>
<td>CastSpell fanout_target insert</td>
<td>FDB transactional write</td>
</tr>
<tr class="odd">
<td><strong>Shallow state</strong> (100 bytes, interpolation)</td>
<td>entity_t packed struct (~40 bytes)</td>
<td>Binary value encoding (RFD 0010)</td>
</tr>
<tr class="even">
<td><strong>Deep state</strong> (1000 bytes, prediction/rollback)</td>
<td>Not modeled (future: add history ring)</td>
<td>FDB version stamps for MVCC history</td>
</tr>
<tr class="odd">
<td><strong>Async calls</strong> (goroutine per player, blocking on world DB)</td>
<td>H2O event loop + FDB async callbacks</td>
<td>fdb_future_set_callback (RFD 0011)</td>
</tr>
<tr class="even">
<td><strong>Input-driven simulation</strong> (no global tick)</td>
<td>ZoneTick is per-zone, on-demand</td>
<td>Each zone is an independent FDB transaction</td>
</tr>
<tr class="odd">
<td><strong>XDP kernel bypass</strong> (10G NIC, 50K players/server)</td>
<td>H2O HTTP/TCP (not UDP/XDP)</td>
<td>HTTP is the benchmark transport; XDP is production transport</td>
</tr>
<tr class="even">
<td><strong>O(N²) snapshot delivery</strong></td>
<td>AOI-filtered (GHOST_RANGE=150.0 in 10000.0 world)</td>
<td>0.02% of world space per zone — not full-mesh</td>
</tr>
<tr class="odd">
<td><strong>Static geometry, no player collision</strong></td>
<td>Entities have position+velocity only</td>
<td>No physics engine — pure data operations</td>
</tr>
<tr class="even">
<td><strong>8K-50K players per 32-CPU server</strong></td>
<td>Benchmark target: zones per core</td>
<td>FDB scales linearly with cores</td>
</tr>
</tbody>
</table>
</section>
<section id="what-mas-bandwidthfps-has-that-zonefabric-does-not-yet" class="level3">
<h3 class="anchored" data-anchor-id="what-mas-bandwidthfps-has-that-zonefabric-does-not-yet">What mas-bandwidth/fps has that zonefabric does not (yet)</h3>
<ol type="1">
<li><p>UDP/XDP transport: Glenn uses XDP kernel bypass for raw packet throughput. We use HTTP/TCP via H2O. XDP is a production transport optimization; the benchmark measures the world database layer, not the packet processing layer. Adding a UDP/XDP transport is a future deployment concern, not a benchmark concern.</p></li>
<li><p>Client-side prediction + rollback: Glenn’s deep state (1000 bytes) includes prediction history for lag compensation. Zonefabric’s entity_t is shallow (position + velocity). FDB’s MVCC version stamps could provide history, but this is not modeled in the current schema.</p></li>
<li><p>Delta compression: Glenn assumes 10x bandwidth reduction via delta compression against a baseline. Zonefabric measures raw FDB throughput; compression is a transport-layer concern.</p></li>
<li><p>Real-time multicast: Glenn identifies O(N²) snapshot delivery as the binding constraint. Zonefabric avoids this via AOI filtering (GHOST_RANGE), but does not model the snapshot delivery path.</p></li>
</ol>
</section>
<section id="what-zonefabric-adds-beyond-mas-bandwidthfps" class="level3">
<h3 class="anchored" data-anchor-id="what-zonefabric-adds-beyond-mas-bandwidthfps">What zonefabric adds beyond mas-bandwidth/fps</h3>
<ol type="1">
<li><p>Measured, not estimated: Glenn’s numbers are back-of-the-envelope (“100MB for history”, “1Gbit/sec for state”). Zonefabric produces actual measured throughput via wrk against FDB.</p></li>
<li><p>ACID guarantees: Glenn’s “world database” is conceptual (Redis- like). FDB provides strict serializable ACID transactions. CastSpell’s effect + fanout inserts are atomic, with no partial writes on failure.</p></li>
<li><p>Spatial range queries: Glenn’s world database is key-value with “raycast” primitives. FDB’s ordered key space enables native range scans, so finding all entities within GHOST_RANGE is a single range read, not a custom spatial index.</p></li>
<li><p>Zone split cost model: Zonefabric models SPLIT_COST_THRESHOLD (population² &gt; 40000 → split). mas-bandwidth/fps assumes static grid cells. Zonefabric measures the cost of dynamic zone splitting.</p></li>
<li><p>Core-scaling measurement: mas-bandwidth/fps estimates 8K-50K players per 32-CPU server. Zonefabric measures actual zone tick throughput per core, providing ground truth for capacity planning.</p></li>
</ol>
</section>
<section id="the-path-from-utopia-to-here" class="level3">
<h3 class="anchored" data-anchor-id="the-path-from-utopia-to-here">The path from utopia to here</h3>
<pre><code>mas-bandwidth/fps (concept)          zonefabric (measurement)
┌──────────────────────┐            ┌──────────────────────┐
│ Player servers       │            │ (not modeled —       │
│ (XDP, 50K players)   │            │  HTTP/wrk is the     │
│                      │            │  load generator)     │
│ World servers        │── maps to──│ ZONE + ENTITY tables │
│ (10km grid, 1km²)    │            │ (FDB range scans)     │
│                      │            │                      │
│ World database       │── maps to──│ FoundationDB         │
│ (Redis-like, async)  │            │ (ACID, range scans)  │
│                      │            │                      │
│ "Raycast, find in    │── maps to──│ CastSpell,           │
│  volume, apply dmg"  │            │ GhostRelevance       │
│                      │            │                      │
│ O(N²) snapshots      │── avoided──│ AOI (GHOST_RANGE)    │
│                      │            │ 0.02% world coverage │
└──────────────────────┘            └──────────────────────┘</code></pre>
<p>Glenn’s architecture is the target state. Zonefabric is the benchmark that proves whether FDB can serve as the world database that makes that architecture possible. The gap between “utopia” and “what we have” is:</p>
<ul>
<li>Transport: HTTP/TCP (benchmark) → UDP/XDP (production)</li>
<li>State depth: shallow only → add prediction history</li>
<li>Scale: zones per core → players per server (add player server layer)</li>
<li>Delivery: request/response → snapshot multicast (add world server layer)</li>
</ul>
<p>Each gap is a future RFD. The world database layer, the hardest part, is what zonefabric benchmarks today.</p>
</section>
</section>
<section id="architectural-completeness" class="level2">
<h2 class="anchored" data-anchor-id="architectural-completeness">Architectural completeness</h2>
<p>Three layers combine to form a complete blueprint for 25M CCU:</p>
<section id="network-fabric-xdpebpf-future-rfd" class="level3">
<h3 class="anchored" data-anchor-id="network-fabric-xdpebpf-future-rfd">1. Network fabric — XDP/eBPF (future RFD)</h3>
<p>Handles massive UDP fanout traffic at line rate (10G+), bypassing the OS kernel. mas-bandwidth/fps demonstrates 50K players per 32-CPU server with XDP. This is the transport layer — not benchmarked by zonefabric (HTTP/wrk is the benchmark transport), but the architecture is XDP-ready: zonefabric’s operations are transport-agnostic.</p>
</section>
<section id="compute-and-storage-zstd-fdb-slotmap-rfds-0016-0017" class="level3">
<h3 class="anchored" data-anchor-id="compute-and-storage-zstd-fdb-slotmap-rfds-0016-0017">2. Compute and storage — zstd + FDB + slotmap (RFDs 0016, 0017)</h3>
<p>Per-tick computation uses the in-memory slotmap (RFD 0017) — no FDB calls on the hot path. Persistence is batched at 10Hz: the entire zone state (slotmap + effects + fanout) is serialized as a contiguous buffer, zstd-compressed (RFD 0016), and written as a single FDB key.</p>
<p>At 25M CCU with 200 entities/zone, that’s 125,000 zones. At 10Hz persistence, that’s 1.25M zone-state commits/sec.&nbsp;With 100 zones per FDB transaction batch, that’s 12,500 commits/sec — well within FDB’s single-core capacity (35K writes/sec/core on memory engine). Across 48 cores: 600K commits/sec capacity, 48x headroom.</p>
<p>The zstd compression drops each zone-state blob from ~8KB to ~3KB (2-3x), reducing FDB storage and network transfer proportionally.</p>
</section>
<section id="memory-safety-slotmap-generational-ids-rfd-0017" class="level3">
<h3 class="anchored" data-anchor-id="memory-safety-slotmap-generational-ids-rfd-0017">3. Memory safety — slotmap generational IDs (RFD 0017)</h3>
<p>The slotmap’s generational handles solve the dangling reference problem inherent in dynamic game simulations:</p>
<ul>
<li><p>CastSpell fanout: Effect 10 targets Entity(index=12, gen=1). Entity 12 disconnects → slot freed, gen bumped to 2. New player takes slot 12 (gen=2). Effect 10 resolves: slotmap lookup sees gen mismatch → returns NULL → fireball fizzles. No wrong target hit.</p></li>
<li><p>Ghost entities: Ghost reference to Entity(index=47, gen=3) in zone A. Entity migrates to zone B → slot freed in zone A, gen=4. Zone A’s ghost lookup fails safely. Zone B creates new slot with its own generation.</p></li>
<li><p>Crash recovery: Zone-state blob is loaded from FDB. Slotmap generation counters are part of the persisted state. All EFFECT_ENTITY and FANOUT_TARGET references resolve correctly post-recovery, with no stale handles and no corruption.</p></li>
</ul>
</section>
<section id="why-this-is-complete" class="level3">
<h3 class="anchored" data-anchor-id="why-this-is-complete">Why this is complete</h3>
<table class="caption-top table">
<colgroup>
<col style="width: 35%">
<col style="width: 55%">
<col style="width: 9%">
</colgroup>
<thead>
<tr class="header">
<th>Problem</th>
<th>Solution</th>
<th>RFD</th>
</tr>
</thead>
<tbody>
<tr class="odd">
<td>Dangling references (use-after-free)</td>
<td>Slotmap generational IDs</td>
<td>0017</td>
</tr>
<tr class="even">
<td>FDB write amplification (200 keys/zone)</td>
<td>Batched zone-state blob (1 key/zone)</td>
<td>0002</td>
</tr>
<tr class="odd">
<td>Bandwidth / storage size</td>
<td>zstd compression (2-3x on batches)</td>
<td>0016</td>
</tr>
<tr class="even">
<td>O(N²) snapshot delivery</td>
<td>AOI filtering (GHOST_RANGE=150.0, 0.02% world)</td>
<td>0002</td>
</tr>
<tr class="odd">
<td>Core scaling contention</td>
<td>Per-zone independent FDB transactions, thread-local slotmaps</td>
<td>0005, 0017</td>
</tr>
<tr class="even">
<td>Transport bottleneck (kernel overhead)</td>
<td>XDP/eBPF kernel bypass (future)</td>
<td>—</td>
</tr>
<tr class="odd">
<td>World database (Glenn Fiedler’s gap)</td>
<td>FDB as ACID async world database</td>
<td>0006, 0011</td>
</tr>
</tbody>
</table>
<p>The three layers compose: XDP handles the packet flood, zstd + FDB + slotmap handle compute and storage, generational IDs handle memory safety. No layer requires the others to function, but together they eliminate every known bottleneck for 25M CCU.</p>


</section>
</section>

 ]]></description>
  <guid>https://v-sekai-multiplayer-fabric.github.io/manuals/rfd/0082-zonefabric-scaling/</guid>
  <pubDate>Fri, 07 Aug 2026 19:00:59 GMT</pubDate>
</item>
<item>
  <title>RFD 0081: Three-layer verification strategy (CBMC, Lean 4, plausible-witness-dag)</title>
  <link>https://v-sekai-multiplayer-fabric.github.io/manuals/rfd/0081-three-layer-verification-strategy/</link>
  <description><![CDATA[ 





<p><em><a href="../../index.html">Manuals</a> › RFD 0081: Three-layer verification strategy (CBMC, Lean 4, plausible-witness-dag)</em></p>
<p><strong>State:</strong> published · <strong>Scope:</strong> zone-server-h2o / h2o-bench-tpcc verification</p>
<section id="problem" class="level2">
<h2 class="anchored" data-anchor-id="problem">Problem</h2>
<p>CBMC proves C implementation invariants for bounded inputs, but it does not prove the specification is sound. Lean 4 proves the specification is sound, but it does not verify the C code itself. plausible-witness-dag searches for runtime invariant violations, but it does not prove their absence.</p>
</section>
<section id="decision" class="level2">
<h2 class="anchored" data-anchor-id="decision">Decision</h2>
<p>Verify h2o-bench-tpcc with three complementary tools. Each tool targets a different layer of the stack. CBMC proves C implementation invariants (SPSC ring FIFO, bounds, head-tail, NURand range) for bounded inputs. Lean 4 proves the specification is sound (SPSC linearizability, push/pop preserve bounds), but does not verify the C code itself. plausible-witness-dag searches for runtime invariant violations over HTTP (NewOrder atomicity, Delivery correctness, Stock non-negative), but does not prove their absence.</p>
<p>No single tool covers all three gaps. CBMC catches implementation bugs, Lean 4 catches specification bugs, and plausible-witness-dag catches integration bugs. Together they cover the stack from code to design to runtime behavior.</p>
</section>
<section id="references" class="level2">
<h2 class="anchored" data-anchor-id="references">References</h2>
<ul>
<li>Full layers table, CBMC harnesses, Lean 4 modules, and the plausible-witness-dag escalation ladder: <code>DETAILS.md</code></li>
<li>Original record: <code>decisions/20260806-three-layer-verification-strategy.md</code></li>
<li>Source: <code>weftspun/h2o-bench-tpcc</code>, <code>rfd/0008-verification-strategy.md</code></li>
<li>Consolidation target: <code>v-sekai-multiplayer-fabric/zone-server-h2o</code></li>
</ul>
</section>
<section id="related" class="level2">
<h2 class="anchored" data-anchor-id="related">Related</h2>
<ul>
<li><code>rfd/0072-actor-lite-worker-pool</code>: the SPSC ring these CBMC and Lean 4 proofs cover.</li>
<li><code>rfd/0078-plausible-witness-dag-feature-ablation</code>: extends plausible-witness-dag’s runtime role to pre-implementation design ablation.</li>
</ul>
</section>
<section id="detail" class="level2">
<h2 class="anchored" data-anchor-id="detail">Detail</h2>
</section>
<section id="layers" class="level2">
<h2 class="anchored" data-anchor-id="layers">Layers</h2>
<table class="caption-top table">
<colgroup>
<col style="width: 13%">
<col style="width: 19%">
<col style="width: 12%">
<col style="width: 54%">
</colgroup>
<thead>
<tr class="header">
<th>Layer</th>
<th>Tool</th>
<th>Scope</th>
<th>What it proves</th>
</tr>
</thead>
<tbody>
<tr class="odd">
<td>C invariants</td>
<td>CBMC</td>
<td>Implementation</td>
<td>SPSC ring FIFO, bounds, head-tail, NURand range</td>
</tr>
<tr class="even">
<td>Specification</td>
<td>Lean 4</td>
<td>Design</td>
<td>SPSC linearizability, push/pop preserve bounds</td>
</tr>
<tr class="odd">
<td>TPC-C semantics</td>
<td>plausible-witness-dag</td>
<td>Runtime</td>
<td>NewOrder atomicity, Delivery correctness, Stock non-negative</td>
</tr>
</tbody>
</table>
</section>
<section id="why-three-layers" class="level2">
<h2 class="anchored" data-anchor-id="why-three-layers">Why three layers</h2>
<p>CBMC proves the C code is correct for bounded inputs but cannot reason about TPC-C semantics. Lean 4 proves the specification is sound but does not verify the C implementation. plausible-witness-dag searches for runtime invariant violations via HTTP but cannot prove absence of bugs.</p>
<p>Together they cover the gap: CBMC catches implementation bugs, Lean 4 catches specification bugs, plausible-witness-dag catches integration bugs.</p>
</section>
<section id="cbmc-harnesses" class="level2">
<h2 class="anchored" data-anchor-id="cbmc-harnesses">CBMC harnesses</h2>
<ul>
<li><code>test/cbmc/spsc_harness.c</code> — FIFO ordering, overflow, underflow, head-tail invariant</li>
<li><code>test/cbmc/nurand_harness.c</code> — NURand result in [x, y]</li>
<li><code>test/cbmc/random_harness.c</code> — get_random_number in [0, max]</li>
</ul>
</section>
<section id="lean-4-modules" class="level2">
<h2 class="anchored" data-anchor-id="lean-4-modules">Lean 4 modules</h2>
<ul>
<li><code>TpccVerification/Spsc.lean</code> — SPSC ring buffer specification with proofs: <code>init_safe</code>, <code>push_safe</code>, <code>pop_safe</code></li>
<li><code>TpccVerification/Basic.lean</code> — TPC-C invariant predicates for plausible-witness-dag</li>
</ul>
</section>
<section id="plausible-witness-dag-integration" class="level2">
<h2 class="anchored" data-anchor-id="plausible-witness-dag-integration">plausible-witness-dag integration</h2>
<p>Depends on <code>fire/plausible-witness-dag</code> as a Lake dependency. The <code>resolve</code> function takes a candidate predicate and a deterministic readback, escalating through L0/L1/L2 until it finds a witness or proves none exists.</p>
<p>Escalation ladder: L0 (10 txns, W=1), L1 (100 txns, W=5), L2 (1000 txns, W=20).</p>


</section>

 ]]></description>
  <guid>https://v-sekai-multiplayer-fabric.github.io/manuals/rfd/0081-three-layer-verification-strategy/</guid>
  <pubDate>Fri, 07 Aug 2026 19:00:59 GMT</pubDate>
</item>
<item>
  <title>RFD 0080: Slotmap entity storage with generational IDs</title>
  <link>https://v-sekai-multiplayer-fabric.github.io/manuals/rfd/0080-slotmap-entity-storage/</link>
  <description><![CDATA[ 





<p><em><a href="../../index.html">Manuals</a> › RFD 0080: Slotmap entity storage with generational IDs</em></p>
<p><strong>State:</strong> published · <strong>Scope:</strong> zone-server-h2o in-memory entity storage</p>
<section id="problem" class="level2">
<h2 class="anchored" data-anchor-id="problem">Problem</h2>
<p>In-memory entity storage per zone used a hash map or a linked list approach. A hash map of 200 entities touches about 8 random cache lines per iteration. A linked list needs pointer-chasing, and both approaches cost more than a sequential scan.</p>
</section>
<section id="decision" class="level2">
<h2 class="anchored" data-anchor-id="decision">Decision</h2>
<p>Use a slotmap for in-memory entity storage per zone, replacing hash map and linked list approaches. A slotmap is a sparse set with generational indices. Each zone owns one thread-local slotmap. FDB stays the durable backing store, and the slotmap is the hot in-memory index for tick processing. Iterating 200 entities in a slotmap’s dense array touches about 8KB sequentially, roughly one L1 miss after warmup. A hash map of the same size touches about 8 random cache lines, and a linked list needs pointer-chasing.</p>
<p>Generational (index, version) handles prevent ABA problems, where a recycled slot looks like a live entity. A stale handle’s version mismatch returns null instead of the wrong entity. The team rejected a full ECS framework like EnTT as unjustified overhead for one component type and one system. The slotmap is about 100 lines of C, against EnTT’s roughly 15,000 lines of C++, at comparable iteration speed for this workload.</p>
</section>
<section id="references" class="level2">
<h2 class="anchored" data-anchor-id="references">References</h2>
<ul>
<li>Full slotmap struct layout, operations, cache-behavior table, and FDB migration sync sequence: <code>DETAILS.md</code></li>
<li>Original record: <code>decisions/20260806-slotmap-entity-storage.md</code></li>
<li>Source: <code>weftspun/h2o-bench-tpcc</code>, <code>rfd/0017-slotmap-entity-storage.md</code></li>
<li>Consolidation target: <code>v-sekai-multiplayer-fabric/zone-server-h2o</code></li>
</ul>
</section>
<section id="related" class="level2">
<h2 class="anchored" data-anchor-id="related">Related</h2>
<p><code>rfd/0074-binary-value-encoding-for-fdb</code> (the format the slotmap stores by <code>memcpy</code>), <code>rfd/0076-macaroon-xdp-security</code> (the <code>entity_id</code> caveat maps to a slotmap handle), and <code>rfd/0072-actor-lite-worker-pool</code> (each worker owns its zone’s slotmap thread-locally).</p>
</section>
<section id="detail" class="level2">
<h2 class="anchored" data-anchor-id="detail">Detail</h2>
</section>
<section id="rationale" class="level2">
<h2 class="anchored" data-anchor-id="rationale">Rationale</h2>
<p>Zonefabric’s ZoneTick iterates all 200 entities in a zone every tick. The iteration pattern is:</p>
<ol type="1">
<li>Range scan FDB <code>zf/entity/{z_id}/</code> → 200 KV pairs (cold path)</li>
<li>Deserialize into in-memory entity array (warm path)</li>
<li>Iterate: update position += velocity × dt (hot path)</li>
<li>Batch write back to FDB (cold path)</li>
</ol>
<p>Steps 1 and 4 are FDB operations (~1ms latency). Step 3 is pure compute (~7µs for 200 entities). Step 2 is the deserialization bridge — and its data structure choice determines whether step 3 is cache-friendly.</p>
<p>A hash map (open-addressing) scatters entities across memory based on hash of entity ID. Iterating 200 entities from a hash map of capacity 1024 touches ~8 cache lines randomly — 8 potential L1 misses.</p>
<p>A slotmap stores entities in a dense array indexed by a slot index. Iteration is a sequential scan of the dense array — 200 entities × 40 bytes = 8KB, which fits in 128 cache lines, accessed sequentially with hardware prefetch. Zero L1 misses after warmup.</p>
</section>
<section id="slotmap-design" class="level2">
<h2 class="anchored" data-anchor-id="slotmap-design">Slotmap design</h2>
<div class="code-copy-outer-scaffold"><div class="sourceCode" id="cb1" style="background: #f1f3f5;"><pre class="sourceCode c code-with-copy"><code class="sourceCode c"><span id="cb1-1"><span class="kw" style="color: #003B4F;
background-color: null;
font-weight: bold;
font-style: inherit;">typedef</span> <span class="kw" style="color: #003B4F;
background-color: null;
font-weight: bold;
font-style: inherit;">struct</span> <span class="op" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">{</span></span>
<span id="cb1-2">    <span class="dt" style="color: #AD0000;
background-color: null;
font-style: inherit;">uint32_t</span> index<span class="op" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">;</span>   <span class="co" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">// slot index into entries[]</span></span>
<span id="cb1-3">    <span class="dt" style="color: #AD0000;
background-color: null;
font-style: inherit;">uint32_t</span> version<span class="op" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">;</span> <span class="co" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">// generation counter for ABA safety</span></span>
<span id="cb1-4"><span class="op" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">}</span> zf_slot_t<span class="op" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">;</span></span>
<span id="cb1-5"></span>
<span id="cb1-6"><span class="kw" style="color: #003B4F;
background-color: null;
font-weight: bold;
font-style: inherit;">typedef</span> <span class="kw" style="color: #003B4F;
background-color: null;
font-weight: bold;
font-style: inherit;">struct</span> <span class="op" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">{</span></span>
<span id="cb1-7">    zf_entity_t entity<span class="op" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">;</span>    <span class="co" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">// 40 bytes: the actual data</span></span>
<span id="cb1-8">    <span class="dt" style="color: #AD0000;
background-color: null;
font-style: inherit;">uint32_t</span> version<span class="op" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">;</span>      <span class="co" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">// generation: incremented on free</span></span>
<span id="cb1-9">    <span class="dt" style="color: #AD0000;
background-color: null;
font-style: inherit;">uint32_t</span> next_free<span class="op" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">;</span>    <span class="co" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">// free list link (UINT32_MAX = end)</span></span>
<span id="cb1-10"><span class="op" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">}</span> zf_slot_entry_t<span class="op" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">;</span></span>
<span id="cb1-11"></span>
<span id="cb1-12"><span class="kw" style="color: #003B4F;
background-color: null;
font-weight: bold;
font-style: inherit;">typedef</span> <span class="kw" style="color: #003B4F;
background-color: null;
font-weight: bold;
font-style: inherit;">struct</span> <span class="op" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">{</span></span>
<span id="cb1-13">    zf_slot_entry_t <span class="op" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">*</span>entries<span class="op" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">;</span>  <span class="co" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">// dense array, capacity = power of 2</span></span>
<span id="cb1-14">    <span class="dt" style="color: #AD0000;
background-color: null;
font-style: inherit;">uint32_t</span> <span class="op" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">*</span>slots<span class="op" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">;</span>           <span class="co" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">// slot index → entry index (or free)</span></span>
<span id="cb1-15">    <span class="dt" style="color: #AD0000;
background-color: null;
font-style: inherit;">uint32_t</span> free_head<span class="op" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">;</span>        <span class="co" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">// head of free list in entries[]</span></span>
<span id="cb1-16">    <span class="dt" style="color: #AD0000;
background-color: null;
font-style: inherit;">uint32_t</span> count<span class="op" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">;</span>            <span class="co" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">// live entity count</span></span>
<span id="cb1-17">    <span class="dt" style="color: #AD0000;
background-color: null;
font-style: inherit;">uint32_t</span> capacity<span class="op" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">;</span>         <span class="co" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">// total slots</span></span>
<span id="cb1-18"><span class="op" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">}</span> zf_slotmap_t<span class="op" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">;</span></span></code></pre></div></div>
<section id="operations" class="level3">
<h3 class="anchored" data-anchor-id="operations">Operations</h3>
<p><strong>Insert</strong> (entity enters zone):</p>
<pre><code>entry_idx = free_head
free_head = entries[entry_idx].next_free
entries[entry_idx].entity = entity
entries[entry_idx].version++
slot_idx = allocate_slot()
slots[slot_idx] = entry_idx
return {slot_idx, entries[entry_idx].version}</code></pre>
<p><strong>Remove</strong> (entity leaves zone):</p>
<pre><code>entry_idx = slots[slot.version]
if entries[entry_idx].version != slot.version: return STALE
entries[entry_idx].next_free = free_head
free_head = entry_idx
entries[entry_idx].version++  // invalidate all existing handles
free_slot(slot.index)
count--</code></pre>
<p><strong>Lookup</strong> (random access by handle):</p>
<pre><code>entry_idx = slots[handle.index]
if entries[entry_idx].version != handle.version: return NULL
return &amp;entries[entry_idx].entity</code></pre>
<p><strong>Iterate</strong> (tick all entities):</p>
<pre><code>for i in 0..count:
    entity = &amp;entries[dense_order[i]].entity
    entity-&gt;x += entity-&gt;vx * dt
    entity-&gt;y += entity-&gt;vy * dt</code></pre>
<p>Iteration uses a dense order array so live entities are contiguous, even after removals (removal swaps the last entry into the freed slot).</p>
</section>
</section>
<section id="why-not-just-a-flat-array" class="level2">
<h2 class="anchored" data-anchor-id="why-not-just-a-flat-array">Why not just a flat array?</h2>
<p>A flat array works if entities never leave zones. But zonefabric has entity migration — entities cross zone boundaries. When entity #47 of 200 leaves, a flat array either:</p>
<ul>
<li>Leaves a gap (iteration must skip dead entries → branch per entity)</li>
<li>Compacts (swap-remove → O(1) but changes entity ordering)</li>
</ul>
<p>A slotmap gives O(1) insert/remove with stable handles (generational indices prevent ABA problems where a recycled slot is mistaken for a live entity). The dense iteration order is maintained by swap-remove on the slot array.</p>
</section>
<section id="why-not-entt-flecs-an-ecs-framework" class="level2">
<h2 class="anchored" data-anchor-id="why-not-entt-flecs-an-ecs-framework">Why not entt / flecs / an ECS framework?</h2>
<p>From <a href="https://github.com/abeimler/ecs_benchmark">abeimler/ecs_benchmark</a>, EnTT iterates 10K entities with 7 systems in 350µs. A slotmap with 200 entities and 1 system (position+velocity) takes ~7µs. The overhead of a full ECS framework (archetype storage, component registration, system scheduling, query matching) is not justified for a single component type and a single system.</p>
<p>The slotmap is ~100 lines of C. EnTT is ~15K lines of C++. We need the iteration speed, not the abstraction.</p>
</section>
<section id="generational-handles-and-fdb-sync" class="level2">
<h2 class="anchored" data-anchor-id="generational-handles-and-fdb-sync">Generational handles and FDB sync</h2>
<p>When an entity migrates from zone A to zone B:</p>
<ol type="1">
<li>Zone A: <code>slotmap_remove(sm_a, handle)</code> — version bumped, slot freed</li>
<li>FDB: <code>fdb_transaction_clear(zf/entity/{a_id}/{e_id})</code> — delete key</li>
<li>FDB: <code>fdb_transaction_set(zf/entity/{b_id}/{e_id}, packed_entity)</code> — insert key</li>
<li>Zone B: <code>handle_b = slotmap_insert(sm_b, entity)</code> — new slot, new version</li>
</ol>
<p>The generational handle prevents zone A from using a stale handle to access an entity that has already migrated to zone B. The version mismatch on lookup returns NULL, signaling the caller to re-read from FDB.</p>
</section>
<section id="memory-layout-and-cache-behavior" class="level2">
<h2 class="anchored" data-anchor-id="memory-layout-and-cache-behavior">Memory layout and cache behavior</h2>
<p>For 200 entities at 40 bytes each:</p>
<table class="caption-top table">
<colgroup>
<col style="width: 26%">
<col style="width: 36%">
<col style="width: 37%">
</colgroup>
<thead>
<tr class="header">
<th>Structure</th>
<th>Memory</th>
<th>Iteration cache misses</th>
</tr>
</thead>
<tbody>
<tr class="odd">
<td>Hash map (cap 1024)</td>
<td>40KB + 4KB indices</td>
<td>~8 (random access)</td>
</tr>
<tr class="even">
<td>Linked list</td>
<td>200 × (40 + 8 ptr) = 9.6KB</td>
<td>~200 (pointer chasing)</td>
</tr>
<tr class="odd">
<td>Slotmap (dense)</td>
<td>200 × 40 = 8KB</td>
<td>~1 (sequential, prefetched)</td>
</tr>
<tr class="even">
<td>Flat array</td>
<td>200 × 40 = 8KB</td>
<td>~1 (same as slotmap)</td>
</tr>
</tbody>
</table>
<p>The slotmap matches flat array iteration speed while supporting O(1) insert/remove with stable handles.</p>
</section>
<section id="capacity-and-growth" class="level2">
<h2 class="anchored" data-anchor-id="capacity-and-growth">Capacity and growth</h2>
<p>Initial capacity: 256 (AUTHORITY_CAPACITY from RFD 0002). Grows by doubling when count reaches capacity. Growth is amortized O(1) — the realloc + copy is rare (log₂(200/256) = 0 growth events at steady state).</p>
<p>Memory per zone: 256 × (40 + 8) = 12KB. For 10,000 zones: 120MB. Negligible vs FDB’s storage overhead.</p>
</section>
<section id="relationship-to-other-rfds" class="level2">
<h2 class="anchored" data-anchor-id="relationship-to-other-rfds">Relationship to other RFDs</h2>
<ul>
<li><strong>RFD 0002</strong> (zonefabric): defines ENTITIES_PER_ZONE=200 and AUTHORITY_CAPACITY=256. Slotmap capacity = AUTHORITY_CAPACITY.</li>
<li><strong>RFD 0010</strong> (binary encoding): FDB values are packed structs. The slotmap stores the same struct in memory — <code>memcpy</code> from FDB value to slotmap entry, no conversion.</li>
<li><strong>RFD 0011</strong> (async callbacks): FDB range scan callback populates the slotmap. Each KV pair in the range result is one <code>slotmap_insert</code>.</li>
<li><strong>RFD 0016</strong> (zstd): if the FDB value is compressed, decompress before <code>memcpy</code> into the slotmap entry. The slotmap always stores uncompressed data.</li>
<li><strong>RFD 0005</strong> (actor-lite): each worker thread owns its zone’s slotmap. No cross-thread access — the slotmap is thread-local. No locks, no atomics on the hot path.</li>
</ul>


</section>

 ]]></description>
  <guid>https://v-sekai-multiplayer-fabric.github.io/manuals/rfd/0080-slotmap-entity-storage/</guid>
  <pubDate>Fri, 07 Aug 2026 19:00:59 GMT</pubDate>
</item>
</channel>
</rss>
