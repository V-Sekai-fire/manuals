<?xml version="1.0" encoding="UTF-8"?>
<rss  xmlns:atom="http://www.w3.org/2005/Atom" 
      xmlns:media="http://search.yahoo.com/mrss/" 
      xmlns:content="http://purl.org/rss/1.0/modules/content/" 
      xmlns:dc="http://purl.org/dc/elements/1.1/" 
      version="2.0">
<channel>
<title>V-Sekai Fire — Manuals</title>
<link>https://v-sekai-fabric.github.io/multiplayer-fabric-manuals/pages/changelog.html</link>
<atom:link href="https://v-sekai-fabric.github.io/multiplayer-fabric-manuals/pages/changelog.xml" rel="self" type="application/rss+xml"/>
<description></description>
<generator>quarto-1.10.18</generator>
<lastBuildDate>Tue, 25 Aug 2026 19:53:17 GMT</lastBuildDate>
<item>
  <title></title>
  <link>https://v-sekai-fabric.github.io/multiplayer-fabric-manuals/changelog/2026/20260817-deck-log.html</link>
  <description><![CDATA[ 





<section id="v-sekai-multiplayer-fabric-deck-log-2026-08-17-monday" class="level1">
<h1>V-Sekai Multiplayer Fabric Deck Log — 2026-08-17 (Monday)</h1>
<p>Date: 2026-08-17</p>
<p>The WebTransport contract has a second implementation, and it contradicted one claim the fabric makes about its own wire before carrying a byte of real traffic. <code>transport-fanout</code> caps a subscriber’s slice at 64 records and calls 6400 bytes “comfortably inside one message”; two stacks that share no code both put the limit at 11 records, and <code>datasource-queen</code>’s own <code>WT_MTU_MAX</code> agrees with them rather than with the comment.</p>
<p>That is one finding rather than a pile, and the count is worth keeping honest. Building the pair also cost three library defects — an EC key <code>pywebtransport</code> would not load, an iceoryx2 subscriber buffer that drops the oldest of three sends, and an aioquic datagram queue that wedges on an oversized send. Those are real and none of them says anything about the contract.</p>
<section id="added" class="level2">
<h2 class="anchored" data-anchor-id="added">Added</h2>
<ul>
<li><a href="https://v-sekai-multiplayer-fabric.github.io/multiplayer-fabric-manuals/rfd/2123-a-second-webtransport-implementation/">RFD 2123</a>, which puts a second implementation of the WebTransport contract on a stack sharing no code with the first. RFD 2088 chose one QUIC stack on both ends, which buys interoperability by construction and gives up the check: a wire implemented once describes the program that implements it. Khronos ratifies against two independent implementations for the same reason.</li>
<li><a href="https://github.com/v-sekai-multiplayer-fabric/transport-gateway-python">transport-gateway-python</a>, the reliable half on <code>aioquic</code> 1.3.0, and <a href="https://github.com/v-sekai-multiplayer-fabric/transport-ingest-python">transport-ingest-python</a>, the datagram half. Both hand off over the iceoryx2 ring through the prebuilt wheel, and both carry no player traffic. <code>aioquic</code> implements QUIC and TLS 1.3 in Python from the RFCs, so it shares no code with the picoquic the C pair vendors. Binding picoquic from Python was rejected because both ends would then run the same QUIC and the same TLS, and h2o because its WebTransport is an unmerged pull request against an HTTP/3 stack h2o calls experimental.</li>
<li>A Python emitter in <code>contract-entity-packet</code>, so <code>lake exe packet_emit</code> writes the C header, the Python codec and the 64 golden vectors from one run. A second implementation that retyped the layout would test whether two people can copy a table. Regenerating the header and the vectors produced byte-identical output, so the change perturbs nothing.</li>
<li>A conformance gate in both new repositories: 64 vectors decoded, re-encoded and compared byte for byte, with a <code>--self-test</code> that corrupts one vector and must fail. CI runs both on every pull request and in the merge group.</li>
</ul>
</section>
<section id="changed" class="level2">
<h2 class="anchored" data-anchor-id="changed">Changed</h2>
<ul>
<li><code>transport-gateway</code> is <code>transport-gateway-c</code> and <code>transport-ingest</code> is <code>transport-ingest-c</code>. Two implementations of one contract are distinguished only by how they are built, so RFD 2111’s preference for names that say what a thing does cannot separate them. Naming the language rather than the library survives a change of library, which this day already exercised: the Python pair moved from <code>pywebtransport</code> to <code>aioquic</code> before it merged and neither name moved. Neither pair keeps the unqualified name, because the unqualified name implies the other is a variant. <code>transport-picoquic</code> is the precedent.</li>
<li>The GitHub descriptions on both C repositories. Each read “hands the result to a <strong>plane</strong> over iceoryx2”, a word RFD 2111 retired in favour of interactor. The READMEs were converted and the descriptions were missed.</li>
<li>The <code>rfd/0062</code> inventory gains a transport-layers section, which it had none of, and its counts move from 73 repositories and 49 active to 75 and 51.</li>
<li>The manifest count in <code>fabric</code>’s README moves from 45 projects to 47. That count is gated, and the gate’s own negative control hard-codes the number, so the control had to move with it or it would certify nothing.</li>
</ul>
</section>
<section id="fixed" class="level2">
<h2 class="anchored" data-anchor-id="fixed">Fixed</h2>
<ul>
<li>Eight stale repository references in six files across five repositories, each in its own pull request: <code>transport-fanout</code>, <code>transport-gateway-c</code>, <code>transport-ingest-c</code>, <code>interactor-ward</code>, <code>entities-gyre</code> and <code>datasource-queen</code>. <code>check_docs.py</code>’s moved-repository check found every one of them; GitHub’s redirects meant nothing was broken, and a name that no longer says which of two implementations it means is the problem.</li>
<li>A burst of ring publishes lost its oldest sample. <code>iceoryx2</code> defaults a subscriber’s buffer to 2 with safe overflow on, so three records published back to back arrived as two. Both repositories now set the buffer explicitly and keep safe overflow, because <code>rfd/0108</code> requires that a lagging reader never stall the writer.</li>
</ul>
</section>
<section id="what-the-second-implementation-cannot-yet-do" class="level2">
<h2 class="anchored" data-anchor-id="what-the-second-implementation-cannot-yet-do">What the second implementation cannot yet do</h2>
<p>The live cross-test against the C pair is not written, and there is nothing to write it against. <code>transport-gateway-c</code> and <code>transport-ingest-c</code> have no <code>main</code> and no <code>CMakeLists.txt</code>, and both READMEs say “State: not started”. <code>transport-ingest-c</code>’s copy of the transport code is a stale fork of <code>transport-gateway-c</code>’s, passing an <code>h3zero_callback_ctx_t</code> where <code>h3zero_callback</code> casts to <code>picohttp_server_parameters_t</code> and missing the <code>h3zero_declare_stream_prefix</code> call. That divergence is why both Python repositories consume one emitted codec instead of two copies, and it is recorded in both <code>OPEN_GAPS.md</code> files that the shared conformance gate is itself duplicated with nothing checking that the copies agree.</p>
<p>A dropped ring sample stays undetectable. <code>HeaderPublishSubscribe</code> carries a node id, a publisher id and an element count, and no sequence number. <code>rfd/0108</code> says a subscriber whose cursor falls out of the ring receives a resync signal rather than a gap; no such signal exists, and defining one is a wire decision rather than a transport one.</p>


</section>
</section>

 ]]></description>
  <guid>https://v-sekai-fabric.github.io/multiplayer-fabric-manuals/changelog/2026/20260817-deck-log.html</guid>
  <pubDate>Tue, 25 Aug 2026 19:53:17 GMT</pubDate>
</item>
<item>
  <title></title>
  <link>https://v-sekai-fabric.github.io/multiplayer-fabric-manuals/changelog/2026/20260807-deck-log.html</link>
  <description><![CDATA[ 





<section id="v-sekai-multiplayer-fabric-deck-log-2026-08-07-friday" class="level1">
<h1>V-Sekai Multiplayer Fabric Deck Log — 2026-08-07 (Friday)</h1>
<p>Date: 2026-08-07</p>
<p>Archived six repositories that the Fly.io move of <code>rfd/0089</code> left without a consumer, and resynchronized the organization inventory in <code>rfd/0062</code> to the real organization. The inventory had drifted far enough to name ten repositories by names they no longer carry, to list five repositories that left the organization, and to omit about thirty that exist.</p>
<p>The sweep also surfaced one inconsistency that no record explains: <code>zone-server-h2o</code> is archived on GitHub while <code>rfd/0094</code> names it the host of the minimum user-generated-content game loop.</p>
<section id="added" class="level2">
<h2 class="anchored" data-anchor-id="added">Added</h2>
<ul>
<li><a href="https://v-sekai-multiplayer-fabric.github.io/multiplayer-fabric-manuals/rfd/2105-archive-self-host-era-support-repos/">RFD 2105</a>, which archives the self-host-era support repositories and the empty stubs: <a href="https://github.com/v-sekai-multiplayer-fabric/fabric-container-verify">fabric-container-verify</a>, the <code>rfd/0056</code> quadlet verification queue, whose every target <code>rfd/0089</code> already archived; <a href="https://github.com/v-sekai-multiplayer-fabric/fabric-casync-central">fabric-casync-central</a>, which never carried content; <a href="https://github.com/v-sekai-multiplayer-fabric/fabric-scoop-central">fabric-scoop-central</a>, the scoop bucket for the Windows demo of the parked <code>godot-loop-slice</code>; <a href="https://github.com/v-sekai-multiplayer-fabric/fabric-platform-central">fabric-platform-central</a>, the <code>rfd/0065</code> Burrito and casync packaging path; and the two empty placeholders <a href="https://github.com/v-sekai-multiplayer-fabric/vulkan-video-godot">vulkan-video-godot</a> and <a href="https://github.com/v-sekai-multiplayer-fabric/steamdeck">steamdeck</a>. Each one is tombstoned rather than deleted, so its history stays reachable.</li>
<li>A record of the <code>fabric-platform-central</code> risk in that RFD’s <code>DETAILS.md</code>. <code>rfd/0090</code> describes a live Burrito release of <code>uro</code> on Fly.io, and <code>rfd/0065</code> puts that packaging in the archived repository. One API call reverses the archive if the deploy still reads from it.</li>
<li>A <code>Moved to V-Sekai-archive</code> section in the <code>rfd/0062</code> inventory, for the three repositories that left this organization: <code>lean-predictive-bvh</code>, <code>viser</code>, and <code>usd-converter-for-vrchat</code>.</li>
<li>Counts in the inventory: 72 repositories, 48 active and 24 archived.</li>
</ul>
</section>
<section id="changed" class="level2">
<h2 class="anchored" data-anchor-id="changed">Changed</h2>
<ul>
<li>The <code>rfd/0062</code> inventory now uses current repository names, per <code>rfd/0064</code>. GitHub keeps a redirect after a rename, so every old name still resolved and hid the drift. <code>godot</code> is <code>fabric-godot-core</code>, <code>merge</code> is <code>fabric-godot-assembly</code>, <code>observability</code> is <code>fabric-game-observability</code>, <code>manuals</code> is <code>multiplayer-fabric-manuals</code>, and the five <code>*-image</code> repositories are the <code>*-quadlet</code> repositories. <code>rfd/0105</code>’s <code>DETAILS.md</code> carries the full mapping.</li>
<li>The inventory’s repository sections follow the current stack: an engine and client area, a zone host and guests area for <code>rfd/0094</code>’s composition, a backend and data area for the FoundationDB work of <code>rfd/0103</code>, one hexagon-cores area holding both the canonical hexagons and the standalone Lean workspaces, and a content flow and authoring area.</li>
<li>The archived table now carries a Basis column naming the decision that retired each repository, and lists all 24 rather than 3.</li>
</ul>
</section>
<section id="removed" class="level2">
<h2 class="anchored" data-anchor-id="removed">Removed</h2>
<ul>
<li>The inventory’s service-images section. Every repository it listed is archived by <code>rfd/0089</code>, which supersedes <code>rfd/0061</code>’s quadlet deployment target.</li>
</ul>
</section>
<section id="split-the-archive-out" class="level2">
<h2 class="anchored" data-anchor-id="split-the-archive-out">Split the archive out</h2>
<p>Created <a href="https://github.com/v-sekai-multiplayer-fabric/multiplayer-fabric-archive">multiplayer-fabric-archive</a> and moved every retired record there, per <a href="https://v-sekai-multiplayer-fabric.github.io/multiplayer-fabric-manuals/rfd/2106-split-the-archive-into-its-own-repo/">RFD 2106</a>: the 26 rejected and superseded MADR records under <code>_archive/decisions/</code>, the image they cite, and the two superseded RFDs, <code>rfd/0011</code> on the observability stack and <code>rfd/0061</code> on the deployment target. The new repository carries the manuals history up to the split, so each moved file keeps every commit that touched it at its original path. It holds plain Markdown, with no site and no checks.</p>
<p>Both superseded RFDs used to render as current. A reader who opened either one read a dead design with no marking on the page.</p>
<p>Repairing the inbound links was the work a move forces, because a deleted file with no stub leaves a 404 on the site. Four relative links in the 2026-06-06 deck log, two pointers in <code>rfd/0071</code>, and one reference each in <code>rfd/0089</code>, <code>rfd/0060</code>, <code>rfd/0070</code>, and the <code>rfd/0062</code> inventory now point at the archive repository. <code>_quarto.yml</code> drops the <code>decisions/attachments</code> resource.</p>
<p>The move also exposed a link that never resolved. Two archived records cite the debug-orb image relative to <code>_archive/decisions/</code>, and the image sat two directories away in <code>decisions/attachments/</code>. Quarto skips <code>_archive</code>, so no rendered page ever exercised it. The archive repository puts the image where the existing links expect it.</p>
</section>
<section id="fixed" class="level2">
<h2 class="anchored" data-anchor-id="fixed">Fixed</h2>
<ul>
<li>The asset-citation guidance named <code>references.bib</code>, a file the repository no longer carries. The bibliography converted to CFF, and <code>pages/references.qmd</code> renders the 94 entries under <code>references:</code> in <code>CITATION.cff</code> through <code>cffconvert</code>. <code>AGENTS.md</code> and <code>rfd/0013</code> now name <code>CITATION.cff</code>. The changelog entries that describe <code>references.bib</code> stay as they are, because they record what was true on their own date.</li>
<li>The site config pointed at the pre-rename repository. <code>site-url</code> read <code>.../github.io/manuals</code> and <code>repo-url</code> read <code>.../v-sekai-multiplayer-fabric/manuals</code>, while the site publishes at <code>.../multiplayer-fabric-manuals/</code>, so every edit-this-page link and the feed URL resolved to the wrong place. Both now name the current repository, as do the three <code>gh api .../rulesets</code> commands in <code>rfd/0021</code>.</li>
</ul>
</section>
<section id="the-zone-tier-recorded" class="level2">
<h2 class="anchored" data-anchor-id="the-zone-tier-recorded">The zone tier, recorded</h2>
<p><code>zone-server-h2o</code> was archived and is unarchived again. It is the host repository, and <a href="https://v-sekai-multiplayer-fabric.github.io/multiplayer-fabric-manuals/rfd/2107-janet-scripting-over-a-c-taskweft-core/">RFD 2107</a> records the tier around it: h2o as HTTP ingress and reverse proxy, Janet as the dynamic layer for routing glue and configuration, <code>libtaskweft</code> as a C library holding HTN search and game state, and Godot as the client over GDExtension. Janet loads the C core as a native module. Rust is out, so the core is C.</p>
<p>FoundationDB stays the store, through <code>libfdb_c</code>. <code>rfd/0075</code> stands and <code>rfd/0006</code>’s CockroachDB does not describe this tier, so the FDB record set stays in force: <code>rfd/0002</code>, <code>rfd/0073</code>, <code>rfd/0074</code>, <code>rfd/0084</code>, <code>rfd/0097</code>, and <code>rfd/0103</code>. <code>ecto_foundationdb</code> and <code>ecto-bench-tpcc</code> keep a live consumer.</p>
<p>The record states its own gaps rather than guessing past them. The startup, memory and speed claims for this tier are not measured, and <code>data/measurements/</code> holds nothing for Janet or a C taskweft core. Four questions stay open: which part drives the <code>libfdb_c</code> callbacks, where Elixir and <code>zone-backend</code> sit, whether Janet replaces the libriscv guest runtime, and which part terminates a WebTransport session.</p>
</section>
<section id="the-guest-boundary-recorded" class="level2">
<h2 class="anchored" data-anchor-id="the-guest-boundary-recorded">The guest boundary, recorded</h2>
<p><a href="https://v-sekai-multiplayer-fabric.github.io/multiplayer-fabric-manuals/rfd/2108-local-cabi-guests-and-in-tick-fanout/">RFD 2108</a> makes a simulation guest a local shared object loaded over the plain C ABI, and h2o routes to it with no network hop. The CDN keeps one job, which is bulk asset delivery, and it no longer delivers guest programs for simulation.</p>
<p>Fan-out runs inside the tick. One publish ring per zone, one writer, and many readers holding cursors. The 100-byte packet of <code>rfd/0053</code> is serialized once and fanned out as a pointer and a refcount, so N subscribers cost one serialization rather than N. A cross-loop delivery takes one batched wakeup per tick, which caps a loop near 64 wakeups per second at 64 Hz.</p>
<p>FoundationDB stays off that path. <code>rfd/0097</code> measured a watch fire at 12115.1 us, which is most of a 15.6 ms tick, against 8.9 us for an <code>AF_UNIX</code> round trip. The watch probe also failed at exactly 10000 watches with error 1032, and a watch is edge-triggered, so it loses values between the fire and the re-read. The tick owns the <code>libfdb_c</code> callback chain of <code>rfd/0073</code> and appends the durable log off-tick.</p>
<p>The record states the cost of local mode rather than hiding it. A local <code>.so</code> shares the zone’s address space, so a guest fault takes the zone down. <code>rfd/0083</code> chose Fil-C for memory safety and <code>rfd/0095</code> built two guest classes for the same reason, and local mode has neither property. That suits first-party simulation code and does not suit user-generated code, so <code>rfd/0094</code>’s UGC loop needs its own record.</p>
<p>RFD 2108 closes two of RFD 2107’s four open questions, which are the database path owner and the guest boundary. Two stay open and one arrives: where Elixir and <code>zone-backend</code> sit, which part terminates a WebTransport session, and how a UGC guest runs.</p>
</section>
<section id="two-tiers-with-foundationdb-as-the-store" class="level2">
<h2 class="anchored" data-anchor-id="two-tiers-with-foundationdb-as-the-store">Two tiers, with FoundationDB as the store</h2>
<p><a href="https://v-sekai-multiplayer-fabric.github.io/multiplayer-fabric-manuals/rfd/2109-two-tiers-with-foundationdb-as-the-store/">RFD 2109</a> names egress as the cost that binds, and the store as the cost that does not. Bandwidth per client sets the bill, and one node serves the database load. No deployment runs, so the record carries no cost figure.</p>
<p>The tier count drops to two. Godot is the client, holding rendering, XR, IK, and local physics. Elixir and Phoenix form the server, holding identity, the room directory, and the presence relay. The server relays presence rather than simulating it, so Phoenix supplies the identity and sessions that a Godot server would need written in GDScript.</p>
<p>The store stays FoundationDB, through <code>ecto_foundationdb</code>, so <code>rfd/0075</code> stands. The record accepts <code>rfd/0103</code>’s limits rather than working around them: no joins, no <code>or</code>, no aggregates in the database, and one Between clause per query. That trade holds because the relational need is analytical, and the analytical half runs on zstd Parquet and DuckDB as <code>data/measurements/</code> already does. OLTP access is point lookups by player and room identity.</p>
<p>Twelve stores are recorded with their outcome. TiDB, YDB, and Apache Ignite are viable and not chosen, and the record says why for each. TiDB is the one that keeps relational form with no adapter work, at the cost of more processes and MySQL’s weaker constraint set. Rust was banned and then unbanned during the search, which returned TiDB to the list. Rivet 2.0 is Apache 2.0 and ships a TypeScript SDK, with no Elixir or Godot client.</p>
<p><code>taskweft</code> stays an Elixir NIF, which <code>V-Sekai-fire/multiplayer-fabric-taskweft</code> already is, so Janet and the C <code>libtaskweft</code> are dropped.</p>
<p><code>rfd/0107</code> and <code>rfd/0108</code> are marked abandoned and carry a pointer to this record. Their measurements stay valid, and the back-pressure rule of <code>rfd/0108</code> still applies to any relay.</p>
<p>Five questions stay open on record: player-hosted zones against server-hosted, interest management as the only lever on the 256 kbps figure, server authority that a relay cannot give, whether the Godot WebTransport client speaks to a Phoenix endpoint, and how many FoundationDB processes run and where.</p>
</section>
<section id="the-zone-becomes-one-pinned-godot-build-hosted-as-a-rivet-actor" class="level2">
<h2 class="anchored" data-anchor-id="the-zone-becomes-one-pinned-godot-build-hosted-as-a-rivet-actor">The zone becomes one pinned Godot build, hosted as a Rivet actor</h2>
<p><a href="https://v-sekai-multiplayer-fabric.github.io/multiplayer-fabric-manuals/rfd/2110-h2o-libriscv-as-a-rivet-actor/">RFD 2110</a> makes a zone a child process under Rivet’s <code>container-runner</code>, one process per room. The child is the engine at tag <code>v2026.06.27.1907-multiplayer-fabric</code>, which resolves to commit <code>2cecde75</code> and reports Godot 4.7.0 beta. That tag’s <code>modules/</code> already carries <code>sandbox</code> for libriscv guests, <code>http3</code> for WebTransport, <code>multiplayer_fabric</code> and <code>multiplayer_fabric_asset</code> for the zone, <code>xr_grid</code> for presence, and <code>openxr</code> with <code>webxr</code> for XR. So one build is the client, the zone server, and the guest host.</p>
<p>A C child of h2o plus libriscv stays possible and is not built. Its only advantage is memory per process, and that figure is unmeasured. No <code>resonance</code> module appears at the tag, so <code>rfd/0022</code>’s spatial audio needs checking before anyone relies on it.</p>
<p>One process per room deletes work rather than adding it. <code>rfd/0072</code>, <code>rfd/0082</code>, and <code>rfd/0086</code> become unnecessary, because each exists only to hold many zones in one process. <code>rfd/0080</code> narrows to one room’s entities. A guest fault kills one room rather than every room, which is what <code>rfd/0095</code> wanted from Bubblewrap.</p>
<p>The actor input is four optional CBOR fields, read from <code>container-runner/src/input.rs</code>: <code>command</code>, <code>args</code>, <code>env</code>, and <code>port</code>. The decoded input is also the actor’s persisted state, so a woken actor restores the same launch spec, and unknown fields are ignored rather than rejected. A room’s scene and its tuning therefore travel in the actor input, and no zone-side registry is needed.</p>
<p>The transport is settled by the runner’s source rather than by its documentation. <code>container-runner</code> is TCP-only. <code>src/proxy.rs</code> forwards to <code>http://127.0.0.1:{child_port}</code> and the <code>ws://</code> form, <code>src/child.rs</code> checks readiness with <code>TcpStream::connect((Ipv4Addr::LOCALHOST, child_port))</code>, and no <code>udp</code>, <code>quic</code>, <code>http3</code>, or <code>h2</code> appears in the runner source. <code>TCP_NODELAY</code> does not appear either, so Nagle is unmitigated there.</p>
<p>The answer is two sockets in one child. A TCP port satisfies Rivet’s contract and carries the control plane. A UDP port carries game traffic outside Rivet’s tunnel, with the engine’s <code>http3</code> picoquic module on it. Rivet never touches the pose stream, so <code>rfd/0008</code>, <code>rfd/0023</code>, <code>rfd/0049</code>, <code>rfd/0058</code>, and <code>rfd/0088</code> all stand. The cost is that Rivet’s gateway does not route the UDP port, so a zone directory must publish the host and port, which makes the Uro role required rather than optional.</p>


</section>
</section>

 ]]></description>
  <guid>https://v-sekai-fabric.github.io/multiplayer-fabric-manuals/changelog/2026/20260807-deck-log.html</guid>
  <pubDate>Tue, 25 Aug 2026 19:53:17 GMT</pubDate>
</item>
<item>
  <title></title>
  <link>https://v-sekai-fabric.github.io/multiplayer-fabric-manuals/changelog/2026/20260630-deck-log.html</link>
  <description><![CDATA[ 





<section id="v-sekai-multiplayer-fabric-deck-log-2026-06-30-tuesday" class="level1">
<h1>V-Sekai Multiplayer Fabric Deck Log — 2026-06-30 (Tuesday)</h1>
<p>Date: 2026-06-30</p>
<p>Made the loot-action slice runnable on a bare Windows 11 machine, with no Docker and no WSL. The slice now ships as a downloadable release: the authoritative server, the client, and the bots that fill the four-player party are native Windows executables, persistence is in-process SQLite, and an optional native observability stack (VictoriaMetrics + OpenTelemetry) runs alongside. One double-click on <code>run-demo.cmd</code> starts the whole thing.</p>
<p>Along the way the loop closed its last gap — the loot a player wins now travels back to the client and shows in the hub — and the Windows build and packaging were hardened across several release cuts, <code>v0.1.0-dev.3</code> through <code>v0.1.0-dev.6</code>.</p>
<section id="added" class="level2">
<h2 class="anchored" data-anchor-id="added">Added</h2>
<ul>
<li>A native Windows demo of the <a href="https://github.com/v-sekai-multiplayer-fabric/godot-loop-slice">godot-loop-slice</a> vertical slice, on the <a href="https://github.com/v-sekai-multiplayer-fabric/godot-loop-slice/releases/tag/v0.1.0-dev.6">v0.1.0-dev.6 release</a>: a plain-exe <code>…-demo-windows.zip</code> plus three unsigned MSI installers — the all-in-one demo, a server-only install (binds all interfaces and prints the host’s LAN address), and a client-only install (connects to a given server address).</li>
<li>Double-click <code>.cmd</code> launchers (<code>run-demo.cmd</code>, <code>run-server.cmd</code>, <code>run-client.cmd</code>) that run the PowerShell launcher as an in-memory scriptblock, so Windows execution policy and Mark-of-the-Web never block a downloaded script — no scoop and no policy change. The MSI Start-menu shortcuts point at the same launchers.</li>
<li>The loop’s persistence round trip now closes on the client: the granted item is synced back and shown in the hub on return, and the playable-loop smoke asserts the winning client’s inventory matches the committed profile row.</li>
</ul>
</section>
<section id="changed" class="level2">
<h2 class="anchored" data-anchor-id="changed">Changed</h2>
<ul>
<li>Windows distribution moved from a signed MSIX to an unsigned MSI. MSIX must be signed to install and the self-signed cert step failed on the runner, whereas an MSI installs through <code>msiexec</code> without a signature or admin. WiX is pinned to v5 because v6 and later require accepting the paid OSMF EULA.</li>
</ul>
</section>
<section id="fixed" class="level2">
<h2 class="anchored" data-anchor-id="fixed">Fixed</h2>
<ul>
<li>The Windows release build failed because the runner’s backslash stage path (<code>D:\a\_temp\…</code>) reached an Elixir <code>Path.wildcard</code> glob, where a backslash is a glob escape, so the exported editor was never found. The stage path is normalized to forward slashes, which also fixes the same latent bug in the MSIX build script.</li>
</ul>
</section>
<section id="removed" class="level2">
<h2 class="anchored" data-anchor-id="removed">Removed</h2>
<ul>
<li>The MSIX release workflow, superseded by the unsigned MSI path.</li>
</ul>


</section>
</section>

 ]]></description>
  <guid>https://v-sekai-fabric.github.io/multiplayer-fabric-manuals/changelog/2026/20260630-deck-log.html</guid>
  <pubDate>Tue, 25 Aug 2026 19:53:17 GMT</pubDate>
</item>
<item>
  <title></title>
  <link>https://v-sekai-fabric.github.io/multiplayer-fabric-manuals/changelog/2026/20260629-deck-log.html</link>
  <description><![CDATA[ 





<section id="v-sekai-multiplayer-fabric-deck-log-2026-06-29-monday" class="level1">
<h1>V-Sekai Multiplayer Fabric Deck Log — 2026-06-29 (Monday)</h1>
<p>Date: 2026-06-29</p>
<p>Trimmed the heavy decision records back to the lean MADR template, applying YAGNI to the docs. A decision record carries its context, decision, downsides, alternatives, and verification; the scoring ceremony around it added bulk without changing the decision, so it came out.</p>
<p>Then archived the standalone presence demo as superseded, and recorded the YAGNI principle as its own decision.</p>
<section id="added" class="level2">
<h2 class="anchored" data-anchor-id="added">Added</h2>
<ul>
<li>A decision record, <a href="../../decisions/20260629-yagni-times-structure-to-need.md">YAGNI times structure to the need</a>, adopting Beck’s framing of YAGNI as a question of timing, with Kent Beck’s essay added to <code>CITATION.cff</code>.</li>
</ul>
</section>
<section id="changed" class="level2">
<h2 class="anchored" data-anchor-id="changed">Changed</h2>
<ul>
<li>Six decision records dropped their CRIS score tables, <code>Tags</code> lists, <code>Further Reading</code> bibliographies, and the body <code>Status</code> and <code>Decision Makers</code> sections that only duplicated the YAML frontmatter, and collapsed <code>The Road Not Taken</code> to one line per alternative: <a href="../../decisions/20260506-maglev-cycle-1-gateway-handshake.md">maglev cycle 1 gateway handshake</a>, <a href="../../decisions/20260611-loot-action-core-loop-mvp-vertical-slice.md">loot-action core-loop MVP vertical slice</a>, <a href="../../decisions/20260624-fabric-platform-central-elixir-burrito-casync.md">fabric-platform-central as Elixir OTP app</a>, <a href="../../decisions/20260624-game-loop-cluster-sequence.md">game-loop-first delivery sequence</a>, <a href="../../decisions/20260624-release-tag-progression-dev-beta-rc.md">release tag progression</a>, and <a href="../../decisions/20260624-sccache-github-actions-cache-for-godot-builds.md">sccache GitHub Actions cache</a>. The maglev record kept its round-trip verification evidence under a <code>Confirmation</code> heading. The frontmatter and the decisions index are unchanged.</li>
<li>The <a href="../../decisions/madr-proposal-template.html">MADR proposal template</a> follows the same lean shape, with status and decision-makers in frontmatter, so new records keep to the load-bearing sections.</li>
<li>The <a href="../../decisions/20260611-loot-action-core-loop-mvp-vertical-slice.md">loot-action MVP vertical slice</a> gained a Confirmation section recording the verified as-built state against its decided scope: two of five cores wired (combat and loot), SQLite without a CockroachDB adapter, no performance-gate enforcement, and an OpenXR preset that duplicates the Windows build. The runtime smoke result is not yet recorded.</li>
</ul>
</section>
<section id="removed" class="level2">
<h2 class="anchored" data-anchor-id="removed">Removed</h2>
<ul>
<li>The standalone presence demo moved to <code>_archive/decisions/</code>, marked superseded by the loot-action vertical slice: the ghostly partial-body avatar, the cold-boot dependencies, the pose networking, and the orb presence markers. Their orb sibling was already archived. Three live records that linked to them were delinked so the rendered site keeps no dead links.</li>
</ul>


</section>
</section>

 ]]></description>
  <guid>https://v-sekai-fabric.github.io/multiplayer-fabric-manuals/changelog/2026/20260629-deck-log.html</guid>
  <pubDate>Tue, 25 Aug 2026 19:53:17 GMT</pubDate>
</item>
<item>
  <title></title>
  <link>https://v-sekai-fabric.github.io/multiplayer-fabric-manuals/changelog/2026/20260622-deck-log.html</link>
  <description><![CDATA[ 





<section id="v-sekai-multiplayer-fabric-deck-log-2026-06-22-monday" class="level1">
<h1>V-Sekai Multiplayer Fabric Deck Log — 2026-06-22 (Monday)</h1>
<p>Date: 2026-06-22</p>
<p>Split the <code>lean-predictive-bvh</code> proof monorepo into one repository per hexagon cluster, along the dependency seams the lakefile already drew, and archived the monorepo. Updated the vertical-slice repository map to match.</p>
<section id="added" class="level2">
<h2 class="anchored" data-anchor-id="added">Added</h2>
<ul>
<li>Six standalone Lean workspaces, one per hexagon cluster, each carrying its own <code>core/ports/adapters</code> tree, a lakefile that wires its siblings over <code>require ... from git</code>, and a CI gate that builds the production closure: <a href="https://github.com/v-sekai-multiplayer-fabric/lean-shared-core">lean-shared-core</a>, the dependency-free primitive types every other core requires; <a href="https://github.com/v-sekai-multiplayer-fabric/lean-rebac-core">lean-rebac-core</a>, the NoGod / ReBAC authorization core; <a href="https://github.com/v-sekai-multiplayer-fabric/lean-spatial-oracle">lean-spatial-oracle</a>, the predictive BVH and its R128 fixed-point with ghost expansion, SAH, and Hilbert broadphase, emitting <code>predictive_bvh.h</code> through the AmoLean codegen; <a href="https://github.com/v-sekai-multiplayer-fabric/lean-humanoid-rom">lean-humanoid-rom</a>, range-of-motion and IK constraints with the body-model and biomechanics citations; <a href="https://github.com/v-sekai-multiplayer-fabric/lean-fabric-protocol">lean-fabric-protocol</a>, the networking and SLA proofs (saturation, waypoint bounds, abyssal SLA); and <a href="https://github.com/v-sekai-multiplayer-fabric/lean-interest-mgmt">lean-interest-mgmt</a>, authority-interest and solve-order. The push order follows the dependency graph: shared-core and rebac-core first, then spatial-oracle, then fabric-protocol, then interest-mgmt, and each repository’s lake build is green.</li>
</ul>
</section>
<section id="changed" class="level2">
<h2 class="anchored" data-anchor-id="changed">Changed</h2>
<ul>
<li>The <a href="../../decisions/20260612-vertical-slice-repository-map.md">repository map</a> lists the predictive-oracle proofs as the six per-cluster repositories above, replacing the single <code>lean-predictive-bvh</code> entry under the wire, transport, and determinism specs.</li>
</ul>
</section>
<section id="removed" class="level2">
<h2 class="anchored" data-anchor-id="removed">Removed</h2>
<ul>
<li>The <a href="https://github.com/v-sekai-multiplayer-fabric/lean-predictive-bvh">lean-predictive-bvh</a> monorepo is archived and read-only; its proofs now live in the six repositories above. The CITATIONS.bib moved whole to lean-humanoid-rom, the only cluster its body-model and biomechanics references cover.</li>
</ul>


</section>
</section>

 ]]></description>
  <guid>https://v-sekai-fabric.github.io/multiplayer-fabric-manuals/changelog/2026/20260622-deck-log.html</guid>
  <pubDate>Tue, 25 Aug 2026 19:53:17 GMT</pubDate>
</item>
<item>
  <title></title>
  <link>https://v-sekai-fabric.github.io/multiplayer-fabric-manuals/changelog/2026/20260612-deck-log.html</link>
  <description><![CDATA[ 





<section id="v-sekai-multiplayer-fabric-deck-log-2026-06-12-friday" class="level1">
<h1>V-Sekai Multiplayer Fabric Deck Log — 2026-06-12 (Friday)</h1>
<p>Date: 2026-06-12</p>
<p>Recorded the instanced loot-action core-loop slice as MADRs: the product scope and the performance gate, the five hexagons, and the core architecture around Lean kernels lowered to SPIR-V over r128 fixed-point.</p>
<section id="added" class="level2">
<h2 class="anchored" data-anchor-id="added">Added</h2>
<ul>
<li>Product and process decisions: <a href="../../decisions/20260611-loot-action-core-loop-mvp-vertical-slice.md">MVP vertical slice</a>, <a href="../../decisions/20260611-first-party-curated-content-zone-baker-budgets.md">first-party content budgets</a>, <a href="../../decisions/20260611-genre-generic-naming-no-trademarks.md">genre-generic naming</a>, <a href="../../decisions/20260611-cassie-desktop-curvenet-authoring.md">CASSIE desktop authoring</a>, and <a href="../../decisions/20260611-content-build-merged-double-precision-mcp.md">a merged build via the editor MCP</a>.</li>
<li>Hexagon decisions (core, ports, adapters): <a href="../../decisions/20260611-hexagon-combat-core.md">combat</a>, <a href="../../decisions/20260611-hexagon-loot-core.md">loot</a>, <a href="../../decisions/20260611-hexagon-progression-core.md">progression</a>, <a href="../../decisions/20260611-hexagon-budgeter-core.md">budgeter</a>, and <a href="../../decisions/20260611-hexagon-presence-core.md">presence</a>, the last wrapping the existing presence decisions.</li>
<li>Core architecture decisions: <a href="../../decisions/20260611-lean4-kernel-cores-flat-c-host-adapters.md">Lean kernel cores with flat-C adapters</a>, <a href="../../decisions/20260611-core-codegen-lean-slang.md">Lean to Slang to SPIR-V codegen</a>, <a href="../../decisions/20260612-r128-fixed-point-as-lean-library.md">r128 fixed-point as a Lean library</a>, <a href="../../decisions/20260611-core-contract-pure-reducer-byte-state.md">the pure-reducer byte-state contract</a>, <a href="../../decisions/20260611-deterministic-cores-integer-seeded-rng.md">deterministic state</a>, <a href="../../decisions/20260611-server-authoritative-simulation-deferred-rollback.md">server authority with deferred rollback</a>, <a href="../../decisions/20260611-generated-behavior-sandboxed-riscv.md">sandboxed RISC-V behavior</a>, <a href="../../decisions/20260611-forward-renderer-baked-light.md">the forward renderer</a>, and <a href="../../decisions/20260611-webtransportd-core-transport-adapter.md">the webtransportd transport adapter</a>.</li>
</ul>
</section>
<section id="added-the-steel-thread-later-the-same-day" class="level2">
<h2 class="anchored" data-anchor-id="added-the-steel-thread-later-the-same-day">Added (the steel thread, later the same day)</h2>
<ul>
<li>The merged <code>multiplayer-fabric</code> assembly builds: the linux double-precision editor (23 branches, zero conflicts) and the Android arm64 double template (<code>android_release.apk</code>), per <a href="../../decisions/20260612-android-template-temurin-jdk21.md">the toolchain decision</a>.</li>
<li>Wire parity under server authority in the merged build: loot (256 rolls), combat (88 effects), and four-player contention (64 rounds, 4 concurrent clients) all match their Lean golden vectors; headless OpenXR initializes against Monado per <a href="../../decisions/20260612-headless-openxr-testing-with-monado.md">the Monado decision</a>.</li>
<li>New repos: <a href="https://github.com/v-sekai-multiplayer-fabric/loot">loot</a>, <a href="https://github.com/v-sekai-multiplayer-fabric/combat">combat</a>, <a href="https://github.com/v-sekai-multiplayer-fabric/progression">progression</a> (each a core/ports/adapters triad with Plausible-pinned golden vectors), and <a href="https://github.com/v-sekai-multiplayer-fabric/fabric-verify">fabric-verify</a> (the smokes as a quadlet queue, per <a href="../../decisions/20260612-systemd-quadlet-verification-queue.md">the queue decision</a>).</li>
<li>The r128 chain: the Lean <code>R128</code> matches the vendored <code>r128.c</code> (8,200 vectors x 7 ops), the uint32-limb <code>R128L</code> matches <code>R128</code> (50,000 sweep), and the lean-slang-emitted SPIR-V kernel matches <code>r128.c</code> on the GPU (4,096 multiplies), per <a href="../../decisions/20260612-r128-fixed-point-as-lean-library.md">the r128 Lean library decision</a>.</li>
<li>lean-slang runs on Lean 4.30 (<a href="https://github.com/V-Sekai-fire/lean-slang/pull/1">V-Sekai-fire/lean-slang#1</a>).</li>
</ul>
</section>
<section id="fixed" class="level2">
<h2 class="anchored" data-anchor-id="fixed">Fixed</h2>
<ul>
<li><code>merge</code> <code>gitassembly</code> drops the dead <code>feat/spatial-audio-server</code> ref (<a href="https://github.com/v-sekai-multiplayer-fabric/merge/pull/36">merge#36</a>), so the canonical assembly runs again.</li>
<li>The http3 listener findings are recorded as <a href="../../decisions/20260612-http3-listener-session-findings.md">a decision</a>: one listener per process, reply routing to a single session, and a teardown double free, with the loop’s interim shape until the module fixes land.</li>
</ul>
</section>
<section id="fixed-later-still" class="level2">
<h2 class="anchored" data-anchor-id="fixed-later-still">Fixed (later still)</h2>
<ul>
<li>The http3 multi-session reply routing and teardown double free, as <a href="https://github.com/v-sekai-multiplayer-fabric/godot/pull/56">godot#56</a> — the fix pattern comes from the webtransportd single-pending-slot bug (<code>fire/webtransportd@f0fc9a4</code>). Post-fix, four concurrent clients each receive their own winner announcements and the server survives teardown; the quadlet four-player smoke verifies per client.</li>
</ul>
</section>
<section id="added-replication-and-deployment" class="level2">
<h2 class="anchored" data-anchor-id="added-replication-and-deployment">Added (replication and deployment)</h2>
<ul>
<li>The entity-transform wire is integral: int64 absolute micrometers (no origin shift), velocity tied to the predictive BVH’s V_MAX, a 42-byte payload, and a Lean+Plausible source-of-truth in <a href="https://github.com/v-sekai-multiplayer-fabric/entity_packet">entity_packet</a> (<a href="../../decisions/20260612-integral-entity-transform-wire.md">decision</a>, <a href="https://github.com/v-sekai-multiplayer-fabric/godot/pull/58">godot#58</a>).</li>
<li>Fabric channels are reliability classes with control in the packet payload (<a href="../../decisions/20260612-fabric-channels-as-reliability-classes.md">decision</a>).</li>
<li>The MCP runtime bridge drives the deployed OpenXR build over adb (<a href="../../decisions/20260612-mcp-runtime-bridge-deployed-builds.md">decision</a>, <a href="https://github.com/v-sekai-multiplayer-fabric/vsekai-godot-mcp/pull/1">vsekai-godot-mcp#1</a>).</li>
<li>New repos: the standalone Lean workspaces <a href="https://github.com/v-sekai-multiplayer-fabric/loot-core">loot-core</a>, <a href="https://github.com/v-sekai-multiplayer-fabric/combat-core">combat-core</a>, <a href="https://github.com/v-sekai-multiplayer-fabric/progression-core">progression-core</a>, and the playable <a href="https://github.com/v-sekai-multiplayer-fabric/loop-slice">loop-slice</a>.</li>
</ul>
</section>
<section id="fixed-webtransport-multi-session-stability" class="level2">
<h2 class="anchored" data-anchor-id="fixed-webtransport-multi-session-stability">Fixed (WebTransport multi-session stability)</h2>
<ul>
<li>The picoquic WebTransport server survives four or more concurrent sessions and completes full rounds, on <code>feat/module-http3</code>. Reliable traffic rides one persistent framed stream per session instead of a fresh stream per message, which exhausts stream credit and starves the connect handshake (<a href="../../decisions/20260612-webtransport-persistent-framed-stream.md">decision</a>). Five supporting fixes hold the network thread up under load: a mutex on the inbound queue, an inclusive wake-loop that drains and services receive on every callback, <code>get_packet_peer</code> peeking the queue front per the MultiplayerPeer contract, a connection table sized for the fanout, and the picoquic textlog gated behind the verbose flag.</li>
<li>The queue race and the wake-loop starvation each carry a Lean+Plausible proof in the new <a href="https://github.com/v-sekai-multiplayer-fabric/http3-queue">http3-queue</a> workspace: the locked queue stays size-honest for every schedule, the inclusive loop stays starvation-free, and the models exhibit the schedules the unlocked and exclusive variants fail.</li>
</ul>
</section>
<section id="added-the-loop-on-both-transports-and-openxr" class="level2">
<h2 class="anchored" data-anchor-id="added-the-loop-on-both-transports-and-openxr">Added (the loop on both transports and OpenXR)</h2>
<ul>
<li>The loop chooses its transport at launch through <code>TRANSPORT</code>: ENet for the local slice, WebTransport for the browser and headset paths, with the text protocol unchanged across both. Control rides reliable on channel 0, the high-rate transform rides unreliable on channel 1.</li>
<li>An OpenXR client joins the four-player loop over LAN as a real player: it autoconnects to the desktop server, fights in immersive OpenXR above the frame floor, and the round commits progression. The headset controllers drive the loop through polled, edge-detected actions (left thumbstick locomotion, right trigger attack, right A grab, left Y teleport vote), reading the bool action <code>trigger_click</code> rather than a float trigger, with the XR camera held by reference so the locomotion branch runs.</li>
<li>The <a href="https://github.com/v-sekai-multiplayer-fabric/merge">merge</a> assembler returns to the base branch and drops the local assembly branch on every run, including dry runs and failures, so a half-built assembly leaves the checkout clean.</li>
</ul>


</section>
</section>

 ]]></description>
  <guid>https://v-sekai-fabric.github.io/multiplayer-fabric-manuals/changelog/2026/20260612-deck-log.html</guid>
  <pubDate>Tue, 25 Aug 2026 19:53:17 GMT</pubDate>
</item>
<item>
  <title></title>
  <link>https://v-sekai-fabric.github.io/multiplayer-fabric-manuals/changelog/2026/20260606-deck-log.html</link>
  <description><![CDATA[ 





<section id="v-sekai-multiplayer-fabric-deck-log-2026-06-06-saturday" class="level1">
<h1>V-Sekai Multiplayer Fabric Deck Log — 2026-06-06 (Saturday)</h1>
<p>Date: 2026-06-06</p>
<p>A documentation sprint on the manuals: brought the repo map onto the live org, recorded the day’s decisions as MADRs, and built the site’s metadata, navigation, and quality gates.</p>
<section id="added" class="level2">
<h2 class="anchored" data-anchor-id="added">Added</h2>
<ul>
<li>Decisions (MADR): <a href="../../decisions/20260606-compiling-godot-engine.md">compiling the engine</a>, <a href="https://github.com/v-sekai-multiplayer-fabric/multiplayer-fabric-archive/blob/main/_archive/decisions/20260606-org-split-v-sekai-multiplayer-fabric.md">two-org split</a>, <a href="../../decisions/20260606-require-pr-and-merge-queue-on-main.md">require PR + merge queue</a>, <a href="../../decisions/20260606-feature-classification-poc-baseline-stretch.md">feature classification</a>, <a href="../../decisions/20260606-pin-engine-to-frozen-godot-4-7.md">pin engine to Godot 4.7</a>, <a href="../../decisions/20260606-archival-file-naming-convention.md">archival file naming</a>, self-host on Harvester, and <a href="../../decisions/20260606-bounded-llm-steering-queue.md">bound the LLM steering queue</a>.</li>
<li>Capability decisions: <a href="../../decisions/20260606-spatial-audio-patched-resonance-audio.md">spatial audio (HRTF + probes)</a>, <a href="../../decisions/20260606-webtransport-http3-transport.md">WebTransport over HTTP/3</a>, <a href="https://github.com/v-sekai-multiplayer-fabric/multiplayer-fabric-archive/blob/main/_archive/decisions/20260606-presence-demo-networking-internals.md">presence pose networking</a>, presence demo → <a href="https://github.com/v-sekai-multiplayer-fabric/multiplayer-fabric-archive/blob/main/_archive/decisions/20260606-presence-marker-representation.md">human-readable markers</a> → <a href="https://github.com/v-sekai-multiplayer-fabric/multiplayer-fabric-archive/blob/main/_archive/decisions/20260606-ghostly-presence-body-model.md">ghostly ANNY/SOMA-X body</a>, and <a href="../../decisions/20260606-art-game-loop-steel-thread.md">art-game loop steel thread</a>.</li>
<li>A capabilities table in <code>index.md</code> mapping native video, OpenUSD baking, spatial audio, speech, pen-stroke (cassie), WebTransport, and presence to engine branches, repos, and a tier.</li>
<li>CI: a Checks workflow running <code>prek</code> (prettier) and a static, no-LLM tropes gate; a merge-queue ruleset on <code>main</code> (require PR + squash queue).</li>
<li>A Python (panflute) filter showing tier/status per page, linking superseded-by targets, with a breadcrumb trail on top and a symmetric Related section at the bottom across the site.</li>
<li>References for MaterialX, OpenUSD, Slug, Resonance Audio, sccache, MADR, tropes.fyi, Vulkan Video, Harvester, ANNY, SOMA-X, and the uncanny-valley papers.</li>
</ul>
</section>
<section id="changed" class="level2">
<h2 class="anchored" data-anchor-id="changed">Changed</h2>
<ul>
<li>Rewrote the <code>index.md</code> repository map for the live <code>v-sekai-multiplayer-fabric</code> org, grouped by role, with a cross-org “Still on V-Sekai-fire” section.</li>
<li><code>AGENTS.md</code>: adopted the MADR template and documented the tier, asset-naming, prek/tropes, merge-queue, and no-absolute-paths conventions.</li>
<li><code>_quarto.yml</code>: URLs point at the live org; the decisions index is sortable with Tier, Status, and Date columns.</li>
<li>Gave every decision a tier (proof of concept / baseline / stretch) and a frontmatter status; converted the maglev-cycle docs to MADR frontmatter.</li>
<li>Generalized the build SOP onto <code>GODOT_SRC</code> / <code>GODOT_BUILD_CACHE</code> env vars, removing hardcoded absolute paths.</li>
<li>Set decision-makers to K. S. Ernest (iFire) Lee (lyuma consulted on the classification).</li>
<li>Noted Harvester deployment on the active observability decisions.</li>
</ul>
</section>
<section id="fixed" class="level2">
<h2 class="anchored" data-anchor-id="fixed">Fixed</h2>
<ul>
<li>Corrected the presence demo (a skinned-mesh hero-asset landmark, not a VRM or a player; orbs encode head/hand pose) and spatial audio (Resonance Audio is abandoned upstream; the engine is pinned to 4.7).</li>
<li>Dead supersede links: status frontmatter held a <code>.md</code> link the listing did not rewrite, so statuses now hold a plain filename with the link in the body.</li>
<li>Breadcrumb and Related links: the filter ran with the document’s directory as its cwd, so it is now anchored to <code>QUARTO_PROJECT_DIR</code>.</li>
<li>Formatted the legacy markdown so the whole-repo prettier gate passes.</li>
</ul>
</section>
<section id="removed" class="level2">
<h2 class="anchored" data-anchor-id="removed">Removed</h2>
<ul>
<li>Closed the stale maglev-cycle proposals (cycles 2–12 and the intercept) as rejected, leaving the live direction as the only open proposals.</li>
<li>Dropped Fly.io as the deployment target, superseded by self-hosting on Harvester.</li>
</ul>


</section>
</section>

 ]]></description>
  <guid>https://v-sekai-fabric.github.io/multiplayer-fabric-manuals/changelog/2026/20260606-deck-log.html</guid>
  <pubDate>Tue, 25 Aug 2026 19:53:17 GMT</pubDate>
</item>
<item>
  <title></title>
  <link>https://v-sekai-fabric.github.io/multiplayer-fabric-manuals/changelog/2026/20260507-deck-log.html</link>
  <description><![CDATA[ 





<section id="v-sekai-fire-deck-log-2026-05-07-thursday" class="level1">
<h1>V-Sekai Fire Deck Log — 2026-05-07 (Thursday)</h1>
<p>Date: 2026-05-07</p>
<section id="added" class="level2">
<h2 class="anchored" data-anchor-id="added">Added</h2>
<ul>
<li>Maglev cycle 0 (infra) <strong>verified pass</strong> — all 4 pass criteria confirmed against the live Fly API via the <code>verify_fly_state</code> workflow on <code>infra</code>. Gateway machine <code>e7845e1ef35768</code> started + IPv4 <code>213.188.205.180</code>; CRDB volume <code>crdb_data</code> 10GB iad attached; uro deploy succeeded; mTLS secrets present</li>
<li>Maglev cycle 1 (gateway handshake) <strong>verified pass</strong> — <code>cycle-tests/cycle-1-gateway-handshake/cycle1.gd</code> completes a real-internet WebTransport/QUIC handshake against <code>gateway.chibifire.com:443</code>, sends a ping datagram, receives the gateway’s pong reply (~880 ms round-trip), exits 0</li>
<li>New repo <code>cycle-tests</code> — minimal headless Godot <code>SceneTree</code> per cycle that exercises pass criteria against live infrastructure; registered as submodule</li>
<li>New repo <code>multiplayer-fabric-godot-maglev</code> — canonical engine source for the build repo’s <code>git subrepo</code>, holding the assembled <code>multiplayer-fabric-base</code> and <code>multiplayer-fabric</code> branches</li>
<li><code>observability</code> registered as submodule (previously missing — only ADRs existed)</li>
<li>Cycle 2 producer-side OTel export wired in three apps: gateway (<code>gateway.dispatch</code> span around <code>Gateway.Router.handle/1</code>), uro (auto-instrumented Phoenix + Ecto via <code>:opentelemetry_phoenix</code> + <code>:opentelemetry_ecto</code>), zone (engine’s <code>module_open_telemetry</code> initialized in bootstrapped <code>project/main.gd</code>); all ship to <code>observability.internal:4318</code></li>
<li>Bootstrapped zone Godot project — <code>project/project.godot</code> + <code>project/main.gd</code> (the directory was an empty stub before)</li>
<li><code>master</code> branch on <code>godot</code> pinned to merge-base <code>835e2dcbba</code> so future assemblies start from a stable upstream point</li>
<li>10 single-topic feature branches replacing the kitchen-sink <code>feat/engine-patches</code>: <code>feat/engine-misc</code>, <code>feat/tapered-capsule-shape</code>, <code>feat/crypto-extensions</code>, <code>feat/predictive-bvh</code>, <code>feat/uds-test-stability</code>, <code>feat/misc-cleanup</code>, <code>feat/lint-cleanup</code>, <code>feat/ci-fixes</code>, <code>feat/agents-docs</code>, <code>feat/fork-scaffolding</code> (last is now unused)</li>
<li>Read-only verification workflows on <code>infra</code>: <code>verify_fly_state.yml</code>, <code>start_fly_apps.yml</code>, <code>verify_observability.yml</code> — query Fly + Victoria* APIs and upload state as JSON artifacts so cycle verification doesn’t depend on log scraping or local flyctl auth</li>
</ul>
</section>
<section id="changed" class="level2">
<h2 class="anchored" data-anchor-id="changed">Changed</h2>
<ul>
<li><code>build/godot/.gitrepo</code> switched source from <code>godot @ multiplayer-fabric</code> (vendored via stale pinned SHA) to <code>multiplayer-fabric-godot-maglev @ multiplayer-fabric-base</code> (canonical assembled output)</li>
<li><code>build</code> test workflows consolidated: 6 per-feature-branch <code>test-*.yaml</code> files → one <code>test-feature-branches.yaml</code> with <code>{branch, platform}</code> matrix (396 → 73 lines)</li>
<li>Baker + zone GHCR Godot binary builders now track <code>build/main</code> instead of stale pinned commit <code>b27142e94c</code> — picks up engine updates on next cron tick or <code>workflow_dispatch</code></li>
<li><code>docker</code> zone-fabric image switched from Mono Godot (with <code>GodotSharp</code> + .NET 8 runtime) to non-Mono GDScript build (<code>precision=double</code>); matches the assembled engine</li>
<li>Branch protection on <code>godot/multiplayer-fabric</code> removed so <code>update_godot_v_sekai.exs</code> doesn’t trip the bypass warning on every assembly run</li>
</ul>
</section>
<section id="fixed" class="level2">
<h2 class="anchored" data-anchor-id="fixed">Fixed</h2>
<ul>
<li>Zone deploy “Pull Godot zone binary from GHCR” failure — was pulling <code>ghcr.io/v-sekai-fire/godot-zone-double:latest</code> (no workflow ever published this name); switched to <code>multiplayer-fabric-zone-godot:latest</code> which the build workflow actually publishes</li>
<li>Duplicate <code>Control::call_gui_input</code> declaration in <code>scene/gui/control.h</code> — the OTel branch declared it in the drag-and-drop section while engine-misc declared it in the input-events section; auto-merge kept both → compile error. Aligned both to declare it in input-events</li>
<li><code>Control::call_gui_input</code> body divergence — OTel branch had <code>set_input_handled</code> clobber-restore semantics, engine-misc had <code>local_input_handled</code> OR-restore. Aligned OTel to engine-misc verbatim so add/add merges produce identical content with no conflict</li>
<li>Cross-branch file overlap in the split feature branches — <code>feat/engine-misc</code> shed <code>tests/scene/test_fabric_zone.cpp</code> (belongs in <code>feat/module-multiplayer-fabric</code>); <code>feat/fork-scaffolding</code> shed <code>AGENTS.md</code>, <code>core/math/predictive_bvh_adapter.h</code>, <code>tests/scene/test_fabric_zone.cpp</code> (belong to agents-docs / predictive-bvh / fabric module respectively); <code>feat/uds-test-stability</code> shed the <code>5a2663346c</code> re-add of test_fabric_zone; <code>feat/lint-cleanup</code> rewrote its <code>.gitignore</code> commit to add only <code>.lake/</code> (was a kitchen-sink with sandbox/uro/keychain entries); <code>feat/fix-picosocks-cmsghdr-mingw</code> reset <code>.gitignore</code> to master and dropped <code>predictive_bvh_adapter.h</code></li>
<li><code>mbedtls/crypto_mbedtls.{cpp,h}</code> was orphaned in <code>feat/fork-scaffolding</code> despite being the implementation backing the API in <code>feat/crypto-extensions</code> — moved into <code>feat/crypto-extensions</code> so the API and impl ship together</li>
</ul>
</section>
<section id="removed" class="level2">
<h2 class="anchored" data-anchor-id="removed">Removed</h2>
<ul>
<li><code>stage multiplayer-fabric remotes/v-sekai-fire/multiplayer-fabric</code> line from <code>gitassembly</code> — <code>multiplayer-fabric-base</code> is now the canonical assembled output; the redundant publish stage is gone</li>
<li><code>feat/engine-patches</code> no longer referenced in <code>gitassembly</code> — superseded by the 10 split feature branches; kept as historical ref upstream</li>
<li>6 stale per-feature-branch test workflows on <code>build</code>: <code>test-engine-patches.yaml</code>, <code>test-module-http3.yaml</code>, <code>test-module-multiplayer-fabric.yaml</code>, <code>test-module-speech.yaml</code>, <code>test-multiplayer-fabric.yaml</code>, <code>test-open-telemetry.yaml</code> — replaced by the consolidated matrix workflow</li>
<li><code>feat/fork-scaffolding</code> from the active assembly path — <code>feat/fix-picosocks-cmsghdr-mingw</code> already provides equivalent CI/Docker scaffolding (14 of 16 files overlapped)</li>
</ul>


</section>
</section>

 ]]></description>
  <guid>https://v-sekai-fabric.github.io/multiplayer-fabric-manuals/changelog/2026/20260507-deck-log.html</guid>
  <pubDate>Tue, 25 Aug 2026 19:53:17 GMT</pubDate>
</item>
<item>
  <title></title>
  <link>https://v-sekai-fabric.github.io/multiplayer-fabric-manuals/changelog/2026/20260506-deck-log.html</link>
  <description><![CDATA[ 





<section id="v-sekai-fire-deck-log-2026-05-06-wednesday" class="level1">
<h1>V-Sekai Fire Deck Log — 2026-05-06 (Wednesday)</h1>
<p>Date: 2026-05-06</p>
<section id="added" class="level2">
<h2 class="anchored" data-anchor-id="added">Added</h2>
<ul>
<li>WebTransport gateway (Elixir) deployed at <code>gateway.chibifire.com</code> and <code>gateway-cdb4.chibifire.com</code></li>
<li>CockroachDB with mTLS deployed as <code>multiplayer-fabric-crdb</code>; admin UI accessible via <code>flyctl proxy</code></li>
<li>Uro zone backend deployed at <code>hub.chibifire.com</code> and <code>bake.chibifire.com</code></li>
<li>Observability stack: VictoriaMetrics (8428), VictoriaLogs (9428), VictoriaTraces (10428), OTEL Collector (4317/4318) on single Fly Machine</li>
<li>Baker (headless Godot asset exporter) deployed as <code>multiplayer-fabric-baker</code></li>
<li>Zone server pipeline: Godot <code>template_release</code> binary build in <code>multiplayer-fabric-zone</code></li>
<li>Terraform infra management in <code>multiplayer-fabric-infra</code> with state on <code>tfstate</code> branch</li>
<li>Daily CRDB backup to Tigris via restic</li>
<li>DNS records for <code>hub</code>, <code>bake</code>, <code>zone</code> subdomains on <code>chibifire.com</code></li>
<li>AGENTS.md documentation across all repos</li>
<li>This manuals repo was created</li>
<li>Proposal template (<code>decisions/present-proposal-template.md</code>) ported from v-sekai/manuals</li>
<li><code>references.bib</code> with networking, multiplayer, Godot, CockroachDB, and infrastructure entries migrated from v-sekai/manuals</li>
<li>References page wired into Quarto (<code>_quarto.yml</code> bibliography + <code>references.qmd</code>)</li>
</ul>
</section>
<section id="fixed" class="level2">
<h2 class="anchored" data-anchor-id="fixed">Fixed</h2>
<ul>
<li>Zone binary GHCR push: renamed image to <code>multiplayer-fabric-zone-godot</code> (package ownership tied to creating repo)</li>
<li>Terraform: renamed <code>my-crdb-node</code> to <code>multiplayer-fabric-crdb</code> throughout state, configs, and scripts</li>
<li>Baker deploy: dropped <code>curl</code> package (conflicts with <code>curl-minimal</code> pre-installed in AlmaLinux 9)</li>
<li>Build CI: excluded <code>web</code> from editor merge and release jobs (web only builds <code>template_release</code>)</li>
<li>Grafana Tempo v3: removed renamed <code>ingester</code>/<code>compactor</code> config keys; added <code>-target=all</code> (subsequently replaced by Jaeger, then VictoriaTraces)</li>
<li>Replaced Grafana Tempo (AGPL-3.0) with Jaeger all-in-one (Apache 2.0), then replaced Jaeger with VictoriaTraces (Apache 2.0) to unify storage under the VictoriaMetrics vendor; trace UI moved to port 10428</li>
<li>Deslopified observability ADRs against tropes.fyi: removed vague attribution (“claims”), marketing language (“unified vendor stack”, “natural replacement”, “standard pairing”), unicode arrows, and “ecosystem”</li>
</ul>


</section>
</section>

 ]]></description>
  <guid>https://v-sekai-fabric.github.io/multiplayer-fabric-manuals/changelog/2026/20260506-deck-log.html</guid>
  <pubDate>Tue, 25 Aug 2026 19:53:17 GMT</pubDate>
</item>
<item>
  <title></title>
  <link>https://v-sekai-fabric.github.io/multiplayer-fabric-manuals/changelog/2026/20260425-deck-log.html</link>
  <description><![CDATA[ 





<section id="v-sekai-fire-deck-log-2026-04-25-saturday" class="level1">
<h1>V-Sekai Fire Deck Log — 2026-04-25 (Saturday)</h1>
<p>Date: 2026-04-25</p>
<p>Backdated from <code>multiplayer-fabric/CHANGELOG.md</code> <code>[0.2.0]</code> through <code>[0.5.0]</code> — all four releases shipped on the same day.</p>
<section id="added" class="level2">
<h2 class="anchored" data-anchor-id="added">Added</h2>
<ul>
<li>taskweft HRR property tests (67 properties) — all green after fixes</li>
<li><code>Taskweft.Test.DBHelpers</code> — auto-detects <code>multiplayer-fabric-hosting/certs/crdb/</code> TLS certs; replaces hardcoded <code>sslmode=disable</code> URL in all three prop test files</li>
<li>Module-level Postgrex pool in each prop test module (<code>setup_all</code>) — eliminates per-<code>forall</code>-iteration TLS connection overhead that caused 60 s ExUnit timeouts during PropCheck shrinking</li>
<li>Zone backend <code>/api/v1</code> prefix moved into Phoenix router (<code>scope "/api/v1"</code>); Caddy no longer strips the prefix. 6/6 QA tests green on production</li>
<li>Playwright tests:
<ul>
<li><code>wt_browser.spec.ts</code> — browser WebTransport datagram echo PASS 1.4 s</li>
<li><code>godot_web_init.spec.ts</code> — threaded wasm64 engine loads PASS 1.1 s</li>
<li><code>godot_wt_e2e.spec.ts</code> — Godot web export WebTransportPeer end-to-end datagram echo PASS 3.3 s (4 C++ bugs fixed to reach green)</li>
</ul></li>
<li><code>operator_camera.gd</code> — 2.5D top-down operator camera with twist/swing [0, 1] per axis (Survey mode: Q/E snap, scroll zoom, WASD pan; Follow mode: Blue Archive smooth entity tracking). Input actions added to <code>project.godot</code>. See <code>20260425-operator-camera-2-5d.md</code></li>
<li><code>window.__camera_state</code> exported via <code>JavaScriptBridge.eval()</code> for Playwright inspection without a full Godot web build</li>
<li>Playwright operator camera test (<code>operator_camera.spec.ts</code>) — Layer 1 JS simulation 7/7 pass; Layer 2 Godot web export test scaffolded (skipped until export built)</li>
<li><code>WasmEquiv.lean</code> — <code>vm_deterministic</code> proves compile-once libriscv VM correctness across native and Emscripten WASM hosts</li>
<li>ADRs: Three.js WebGPU observer (Stage 1), Three.js player (Stage 2), dual-client test, headless test matrix, build cache pipeline, CRIS scoring, jellyfish game, jellyfish pass condition, operator overlay, operator camera</li>
<li><code>headless_log_observer.gd</code> — corrected default port to UDP 7443; added <code>--host</code>, <code>--port</code>, <code>--dump-json</code>, <code>--frames</code> CLI flags. Exit 0 on entities received, exit 1 on timeout</li>
<li><code>fabric_client.gd</code> — <code>send_player_input(cmd, pos, extra)</code> generic CH_PLAYER write path. Default port corrected to 7443</li>
<li><code>observer.tscn</code> — <code>XROrigin3D</code> + <code>XRCamera3D</code> + left/right <code>XRController3D</code> added; port corrected to 7443</li>
<li><code>fabric_player_xr.gd</code> — OpenXR initialisation with silent desktop fallback</li>
<li><code>test_operator_camera.gd</code> — headless quaternion unit tests (4/4 pass): downward look, 90° twist, swing elevation constant, equivalence to old Euler hierarchy</li>
<li><code>headless_go.spec.ts</code> — Playwright Phase 1 GO spec; spawns <code>godot --headless</code> observer and asserts <code>entities.length &gt; 0</code> from <code>--dump-json</code> output</li>
<li><code>headless_tests.yml</code> — GitHub Actions workflow scaffolding for GO branch protection check (requires full Docker Compose stack to pass)</li>
<li>ADRs: Godot native headless observer (<code>20260425-godot-observer.md</code>), Godot native PCVR player (<code>20260425-godot-player.md</code>). Supersede Three.js ADRs</li>
<li>Lean proofs (<code>multiplayer-fabric-taskweft/lean/</code>): <code>ZoneProtocol</code> (port assignment, 100-byte packet layout), <code>ZoneObserver</code> (exit semantics, step monotonicity), <code>ZonePlayer</code> (CH_PLAYER cmd distinctness, camera swing invariant across all operator inputs)</li>
</ul>
</section>
<section id="changed" class="level2">
<h2 class="anchored" data-anchor-id="changed">Changed</h2>
<ul>
<li>Client strategy: Godot wasm32/wasm64 web export dropped. Two clients replace it — Godot native PCVR (VR + entity control) and Three.js WebGPU (browser observer + WebXR). See <code>20260425-threejs-observer.md</code></li>
<li><code>observer.tscn</code> — <code>SpectatorRig</code> replaced by <code>OperatorRig</code> with <code>operator_camera.gd</code>; <code>StatusHUD</code> re-parented to new camera path</li>
<li><code>operator_camera.gd</code> — swing-twist orientation now built as a single <code>Quaternion</code> (<code>twist_q * swing_q</code>) on <code>CameraPivot</code> instead of Euler angles split across pivot and arm. <code>SpringArm3D</code> carries no rotation</li>
<li>Client strategy updated: Three.js ADRs superseded by Godot native observer and player. Three.js browser client moved to SOMEDAY</li>
<li><code>runner.yml</code> — <code>always()</code> added to <code>docker-images</code> and <code>headless-tests</code> so sibling job failures (android, ios, etc.) do not skip them</li>
<li><code>gitassembly</code> — checkpointed <code>multiplayer-fabric</code> stage to start from remote HEAD; reassembled and pushed via <code>update_godot_v_sekai.exs</code></li>
</ul>
</section>
<section id="fixed" class="level2">
<h2 class="anchored" data-anchor-id="fixed">Fixed</h2>
<ul>
<li><code>Storage.query_all/3</code> — <code>Enum.map(nil, ...)</code> crash on DML statements (INSERT/UPDATE/DELETE return <code>rows: nil</code> in Postgrex). Fixed with <code>rows || []</code></li>
<li>Four transaction rollback tests — <code>raise "rollback"</code> inside <code>rescue _ -&gt; :ok</code> committed instead of rolling back. Fixed with <code>Postgrex.rollback(conn, :rollback)</code></li>
<li><code>references.bib</code> — non-ASCII bytes (en dashes, smart quotes, invalid Win-1252 sequences) replaced with ASCII equivalents</li>
<li><code>_server_path_callback</code> — prefer <code>p_path_app_ctx</code> over <code>p_stream_ctx-&gt;path_callback_ctx</code> (proved by Lean <code>fixed_sctx</code> theorem)</li>
<li><code>wt_server_demo.gd</code> — mirror received packet’s <code>transfer_mode</code> before <code>put_packet</code> (default RELIABLE routed echo to streams; datagrams need UNRELIABLE)</li>
<li><code>transport_peer.spec.ts</code> shape mismatch: <code>data.zones</code> → <code>data.shards</code></li>
<li>Zone registration: <code>last_put_at</code> on create, <code>created_at</code> field fix, PubSub rescue in zone controller</li>
<li>Caddyfile: added <code>handle /socket/*</code> reverse proxy; socket path was 404</li>
<li><code>gescons</code> alias updated: <code>threads=yes arch=wasm64</code> to match GHA web build</li>
<li><code>quic_picoquic_backend.cpp:589</code> — <code>SESSION_H3_SETTINGS</code> reference after the enum value was removed in the WebTransport audit (<code>6feefaf0b9</code>)</li>
<li>WebTransport audit fixes: datagram reader exclusive-lock bug (new reader per iteration → single reader reuse); <code>incoming</code> queue mutex for picoquic cross-thread safety; <code>SESSION_H3_SETTINGS</code> dead state removed from <code>SessionState</code></li>
<li><code>quic_web_glue.js</code> — Lean 4 proofs (<code>WebTransport.lean</code>) for state machine acyclicity, queue discipline, and reader exclusivity invariant</li>
<li><code>operator_camera.gd</code> — <code>delta</code> parameter bug in <code>_apply_swing_twist</code> (was using <code>get_process_delta_time()</code> instead of passed <code>delta</code>)</li>
<li><code>multiplayer-fabric-godot</code> local branches — tracking corrected from <code>origin/master</code> to their own <code>origin/feat/*</code> remotes</li>
<li><code>multiplayer-fabric-base</code> local — reset to <code>origin/multiplayer-fabric-base</code> (local was stale reassembly before <code>scu_build=yes</code> fixes were applied)</li>
<li>Stale stashes dropped from <code>multiplayer-fabric-godot</code> (libriscv Win32 files already committed; openvr mode-change noise; empty archived-branch stash)</li>
</ul>
</section>
<section id="security" class="level2">
<h2 class="anchored" data-anchor-id="security">Security</h2>
<ul>
<li>Rotated Cloudflare Turnstile keys (were plaintext in <code>.env</code>)</li>
</ul>


</section>
</section>

 ]]></description>
  <guid>https://v-sekai-fabric.github.io/multiplayer-fabric-manuals/changelog/2026/20260425-deck-log.html</guid>
  <pubDate>Tue, 25 Aug 2026 19:53:17 GMT</pubDate>
</item>
<item>
  <title></title>
  <link>https://v-sekai-fabric.github.io/multiplayer-fabric-manuals/changelog/2026/20260421-deck-log.html</link>
  <description><![CDATA[ 





<section id="v-sekai-fire-deck-log-2026-04-21-tuesday" class="level1">
<h1>V-Sekai Fire Deck Log — 2026-04-21 (Tuesday)</h1>
<p>Date: 2026-04-21</p>
<p>Backdated from <code>multiplayer-fabric/CHANGELOG.md</code> <code>[0.1.0]</code>.</p>
<section id="added" class="level2">
<h2 class="anchored" data-anchor-id="added">Added</h2>
<ul>
<li>Infinite Aquarium (Jellyfish Game) proof-of-concept: zone networking, RECTGTN jellyfish behaviour planning, UGC asset pipeline, ReBAC permissions</li>
<li><code>quic_web_glue.js</code> — browser WebTransport glue for Godot web export</li>
<li><code>web_transport_peer.cpp</code> — <code>poll_incoming_func</code> + <code>_web_poll_incoming()</code></li>
<li><code>SCsub</code> — <code>env.Append(JS_LIBS=[...])</code> fix so the JS library reaches the linker</li>
<li>manuals ADR corpus: 517 ADRs tagged with filename slug; <code>tag_adrs.exs</code>, <code>linkify_adrs.exs</code>, <code>audit_tropes.exs</code>, <code>fix_bold_bullets.exs</code> scripts; bold-first bullet trope fixed across 104 files (578 labels stripped); verbatim template heading replaced with “Design” in 309 files</li>
</ul>


</section>
</section>

 ]]></description>
  <guid>https://v-sekai-fabric.github.io/multiplayer-fabric-manuals/changelog/2026/20260421-deck-log.html</guid>
  <pubDate>Tue, 25 Aug 2026 19:53:17 GMT</pubDate>
</item>
</channel>
</rss>
