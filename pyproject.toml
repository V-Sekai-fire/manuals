[project]
name = "manuals"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "usd-core==26.8",
    "scipy",
    "pandas",
    "plotly",
    "itables",
    "cffconvert",
]

[tool.pixi.workspace]
channels = ["conda-forge"]
platforms = ["linux-64"]

[tool.pixi.pypi-dependencies]
cffconvert = "*"
usd-core = "==26.8"
scipy = "*"
pandas = "*"
plotly = "*"
itables = "*"

[tool.pixi.tasks]
render = "quarto render"
